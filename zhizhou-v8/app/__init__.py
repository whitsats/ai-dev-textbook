"""v8 交付版：**前面七棵树一直在回答「怎么把它做出来、怎么知道它还行」，
这一棵开始回答「怎么把它交出去」。**

这一棵与前面五棵的区别不在技术栈，而在**问题的位置**：前五棵的每一层都能在
你的机器上验（循环转没转、检索召回多少、账单对不对、门红没红），而这一棵管的
那几件事**在本地一键都绿的时候仍然是未知的**——镜像有多重、改一行要重打多久、
`docker compose up` 之后应用第一次请求会不会失败、这个容器跑起来之后能做什么。
它们的共同点是：**答案在一台机器的外面**（另一台机器、另一个文件系统、另一个时刻）。

| 树 | 它回答的问题 | 它不管什么 |
| --- | --- | --- |
| v3／v4 | 应用逻辑怎么写（手写与框架两条路） | 怎么装进一个能运走的盒子 |
| v5 | 检索怎么做得准 | 同上 |
| v6 | 模型怎么接、账怎么算 | 同上 |
| v7 | 怎么知道它变差了 | 怎么把这一版交给别人 |
| **v8** | **怎么把它交出去**：镜像、编排、流水线、发布 | 不重做前五棵的任何一层 |

本树与前面几棵树共用**同一份服务声明**（知舟的后端、数据库、迁移任务），
但**不 import 前五棵的任何模块**——它量的是那五棵树打出来的东西**装进容器之后**的性质，
与它们内部怎么实现无关。这不是巧合，是这个位置的必然：镜像不认识你的架构。

8.1 把这一层分成四块（**交付物本身**），四块之间没有 import 关系
（只有 `runtime` 借用了 `image.CONTEXT` 那一份上下文清单，因为「上下文里有什么」
是它们共同的前提）：

| 模块 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| `image` | **层账**：每一行一层、可见账与浪费账、瘦身四招、密钥三档 | 不算「改一行要重打多久」（那是 `cache` 的事） |
| `cache` | **构建缓存**：失效的判据、顺序的代价、`RUN` 不会自己过期 | 不管层的大小（那是 `image` 的事） |
| `compose` | **编排**：`depends_on` 的四种等法、健康检查的账 | 不管镜像怎么打出来 |
| `runtime` | **运行期权限面**：`USER`、只读根文件系统、上下文外发 | 不管服务之间的顺序 |

8.2 把这一层的另一半分成六块（**「有没有人去打它」**）。六块同样互不 import，
而它们与 8.1 那四块的分界是一条清清楚楚的线：**8.1 量「打出来的东西」，
8.2 量「谁来打、什么时候打、打完之后怎么交出去」**：

| 模块 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| `trigger` | **触发与过滤**：一次提交进不进流水线、`[skip ci]`、同仓与 fork 的 run 数 | 不管进去之后跑什么 |
| `graph` | **job 图与并发**：`needs` 的关键路径、`matrix` 的格数、`concurrency` 取消谁 | 不管缓存与产物存什么 |
| `caching` | **缓存与产物**：命中哪一条/靠哪条规则、不可改、清理、`needs` 是「拿得到」的判据 | 不管 job 怎么排队 |
| `perms` | **权限与密钥**：token 的权限面、密钥在哪种事件下可见、OIDC、action 的钉法 | 不管谁来触发（那是 `trigger` 的事） |
| `gate` | **门禁与必需检查**：七种形状报出什么、放不放行 | 不管那一格为什么没跑（前四块各答一半） |
| `release` | **发布与灰度**：钉法、审批、金丝雀的两门判据与回滚的账 | 不管镜像怎么打出来（那是 8.1） |

8.3 把这一层的第三段分成两块（**「用户眼前那一块屏幕」**）。两块互不 import，
而它们与 8.1／8.2 的分界也是一条线：**8.1 量打出来的东西、8.2 量谁来打它，
8.3 量「打出去之后，用户在等的那三十秒里看见了什么」**：

| 模块 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| `sse` | **协议那一侧**：帧的四个字段与还原、三个头与代理缓冲、心跳与空闲超时、续传的缓冲、背压与慢消费者 | 不管客户端把状态记成什么样 |
| `stream_ui` | **客户端那一侧**：六个状态与八条边、五种「卡住／显示错」的现场、取消的三层、三种重试、增量与全量 | 不管字节怎么过线（那是 `sse` 的事） |

两块的分工有一处值得单说：**它们量的是同一件事的两半，而这两半各自都能对、合起来仍然错**
——服务端把 12 段老老实实发完（`sse` 全对），而客户端在 `done` 之前就把状态冻住了（`stream_ui`
说的那个「永远等一个不会来的事件」）。这正是 8.3.5 那五种现场存在的理由。

8.4 把这一层的第四段分成三块（**「这块场地租给几个人用、用了多少、这笔账算谁的」**）。
三块互不 import，而它们与前三段的分界是**同一份用量的三个时刻**：

| 模块 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| `tenant` | **看得到什么**：三种隔离姿势、`tenant_id` 落在哪一层（四个落点的四种漏法）、向量库的四级版本 | 不管能用多少 |
| `quota` | **能用多少**：四种量（并发／速率／词元／成本）、三种限流算法、三层配额、超限的三句答复 | 不管这一笔最后花了多少 |
| `billing` | **花了多少**：三段单价与一条乘法关系、预扣与结算、账单与日志的对账 | 不管让不让这次调用进来 |

三块的分工正好是**三个时刻**：`quota` 在**调用之前**说话（让不让进），`billing` 在
**调用之后**说话（花了多少），而 `tenant` 决定**这份用量能不能按租户切开**——
切不开的用量，账单就只能按人算、不能按租户算（这正是 8.4.6 那条「对不上」的一种来源）。
另一处值得单说：**`quota` 与 `billing` 说的是同一批数字，而它们是两个问题**——
四档配额里只有「成本」那一档与账单同名，其余三档都答不了「这个月花了多少」。

8.5 把这一层的第五段分成四块（**「交出去之后，怎么知道它此刻好不好、怎么在它变坏之前先知道」**）。
四块互不 import，而它们与第四段的分界是**三个时间尺度**：预算按月算、告警按「烧得多快」响、
容量按季度看：

| 模块 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| `slo` | **好不好**：四个数与一条「怎么花」的规矩、四种 SLI 各自的盲区、一个月的四次事故与三种处置 | 不管什么时候叫人（那是 `alert` 的事） |
| `alert` | **什么时候叫人**：燃尽率与三档窗口、噪声账、状态页广播与断开清理 | 不管装不装得下（那是 `capacity` 的事） |
| `drill` | **哪里还不知道**：四种演练的发现条数与其中「文档里没写」的占比 | 不管指标本身对不对（那是 `slo` 的事） |
| `capacity` | **装得下多少**：排队放大 1/(1−ρ)、三种买法、压测拐点与翻倍季度 | 不管服务得好不好（那是 `slo` 的事） |

四块回答的是四个不同的问题：`slo` 问**这一段时间花了多少预算**，`alert` 问
**烧得快不快、要不要叫醒人**，`drill` 问**我们知不知道自己哪里不知道**，
`capacity` 问**装得下多少**。另有一处值得单说：**`slo` 与 `capacity` 的接口是同一条排队曲线**
——利用率过线之后 P95 会自己变坏，不用等错误率上升；所以「服务得好不好」与「装得下多少」
是同一条陡坡的两侧，而把 P95 的 SLO 定得很紧、却又按均值买机器，等于给自己设了一个
按计划失守的目标。

五条从 8.1 起定下来（8.2／8.3／8.4 各添过一条，8.5 添的是第五条）：

1. **离线当门、真机报数**（3.10 起的纪律）：本树**不装 Docker、不连 GitHub、不要密钥、
   不跑 runner、不开浏览器**。镜像的大小与层数是**文件系统剧本算出来的**，构建耗时是**脚本化的每层秒数**，
   编排的启动时钟是**写死的秒数**，8.2 的六组里：触发与过滤是按官方语法**建模**的
   （事件与过滤器就是那几个字符串）、`needs` 的墙钟是**脚本化的分钟数**、
   缓存的查找顺序是按官方那套顺序**逐条落成尝试记录**的、金丝雀与回滚是纯算术；
   而 8.3 的两块里：帧是按官方那四个字段**拼出来再还原回去**的（`sse.parse` 真的把
   12 段拆回 12 个事件再逐字段比）、代理缓冲与心跳是**照官方默认值建模**的
   （`proxy_buffering on`、空闲超时 60 秒）、续传的补发与背压的队列是**纯算术**；
   8.4 的三块里：隔离是按官方那几条**默认值**建模的（行策略没有策略即拒绝、
   表属主默认绕过、向量库四级各自的天花板）、用量是**一份写死的调用表**（12 条）、
   限流是纯算术（三种算法跑同一串到达）、账是**四段单价乘出来的**（其中两段由乘法关系算出，不重抄）。
   所以它能量的是**这些机制本身的性质**（层是不是只加的、被跳过的 job 与停在 Pending 的检查
   是不是同一个结论、缓存到底是靠哪条规则命中的、金丝雀在哪种判据下才救人、
   同一份代码在哪种代理配置下从「像流」变成「一吐一大片」、心跳与超时谁先到、
   哪种断线重连会丢中间一段、哪种「卡在流式中」是状态错而哪种是显示错），
   **量不了真实镜像的体积、真实流水线的时长、真实 runner 的调度、真实仓库的命中率、
   真实网络的丢包与真实浏览器的渲染**。8.5 的四块里：SLO 与预算是**按公式算的**
   （允许失败 ＝ (1 − SLO) × 请求数）、告警是按官方那套工作法的**形状建模**的（三档窗口 ＋ 燃尽率
   14.4／6／1）、演练发现是**一份写死的清单**、排队与容量是纯算术（1/(1−ρ)）；
   所以它能量的是**这些办法本身的性质**（哪种 SLI 抓不到哪一类故障、燃尽率在慢烧上强在哪、
   四种演练各自能挖出什么、利用率在哪一档之后等待开始跑得比流量快），
   **量不了真实监控的采集间隔与保留窗口、真实事故的时长与叠加、真实演练里的意外、
   真实流量的形状与突发**。这一条写进每一章的边界里。
2. **读数要么能复算，要么别写进正文**：十九块里的每一个数都能拿纸笔重算一遍
   （三十组读数、四百多条夹具都在做这件事）。
3. **两个数必须一起报**：镜像大小与可见大小、构建代价与传输代价、失败次数与成功时刻、
   写不进去几处与必须声明几处、**「进流水线几次」与「漏掉几条本该跑的」**、
   **「恢复了文件」与 `cache-hit`**、**判红的坏请求与回滚的坏请求**、
   **「补发了几条」与「中间那一段丢没丢」**、**「状态对不对」与「界面显示对不对」**、
   **「漏了几格」与「空了几格」**（多看到与少看到是两类故障）、
   **预扣与结算**、**账单与日志**、**达成率与剩余预算**（0.999568 达标，而只剩 679 条可以坏）、
   **发出的告警与其中不需要动作的**（340 条里 211 条）、**发现了多少条与其中多少条没人写过**、
   **峰值利用率与挂掉一台之后**。
   **只报其中一个，另一半就会变成一句听起来对的话。**
4. **不写「永远最新」「一定起来」「没跑就等于通过」「流式一定更快」这类话**：`RUN` 的缓存不会自己过期、
   `service_started` 只保证容器在跑、「没跑」有两种相反的后果（`if:` 放行／`paths:` 停 Pending）、
   而流式一秒也没让回答变短（它改的是**首字延迟**，不是总时长）
   ——这四条都写进了模块注释与正文的边界；8.4 又添了一条同族的话：
   **「隔离」不是一道门而是一个默认值**（不选它，它就等于「谁都看得到」），
   而**「账单对不上」不一定是有人算错**——两边的口径各是什么，先问这一句。
   8.5 又添了两条同族的话：**「有告警」不等于「该响的时候会响」**
   （按阈值响的告警对慢烧一次都不响，而它三天烧掉 20% 的预算），
   而**「演练通过」不等于「没有问题」**（一次「一切正常」的演练是没演练到位的强信号）。
"""
