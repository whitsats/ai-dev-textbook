"""v8 交付版：**前面七棵树一直在回答「怎么把它做出来、怎么知道它还行」，
这一棵开始回答「怎么把它交出去」。**

这一棵与前面五棵的区别不在技术栈，而在**问题的位置**：前五棵的每一层都能在
你的机器上验（循环转没转、检索召回多少、账单对不对、门红没红），而这一棵管的
那几件事**在本地一键都绿的时候仍然是未知的**——镜像有多重、改一行要重打多久、
`docker compose up` 之后应用第一次请求会不会失败、这个容器跑起来之后能做什么。
它们的共同点是：**答案在一台机器的外面**（另一台机器、另一个文件系统、另一个时刻）。

| 树 | 它回答的问题 | 它不管什么 |
| --- | --- | --- |
| v3／v4 | 应用逻辑怎么写（手写与框架两条路） | 怎么装进一个能运走的盒子 |
| v5 | 检索怎么做得准 | 同上 |
| v6 | 模型怎么接、账怎么算 | 同上 |
| v7 | 怎么知道它变差了 | 怎么把这一版交给别人 |
| **v8** | **怎么把它交出去**：镜像、缓存、编排、权限面 | 不重做前五棵的任何一层 |

本树与前面几棵树共用**同一份服务声明**（知舟的后端、数据库、迁移任务），
但**不 import 前五棵的任何模块**——它量的是那五棵树打出来的东西**装进容器之后**
的性质，与它们内部怎么实现无关。这不是巧合，是这个位置的必然：镜像不认识你的架构。

8.1 把这一层分成四块，四块之间**没有 import 关系**（只有 `runtime` 借用了
`image.CONTEXT` 那一份上下文清单，因为「上下文里有什么」是它们共同的前提）：

| 模块 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| `image` | **层账**：每一行一层、可见账与浪费账、瘦身四招、密钥三档 | 不算「改一行要重打多久」（那是 `cache` 的事） |
| `cache` | **构建缓存**：失效的判据、顺序的代价、`RUN` 不会自己过期 | 不管层的大小（那是 `image` 的事） |
| `compose` | **编排**：`depends_on` 的四种等法、健康检查的账 | 不管镜像怎么打出来 |
| `runtime` | **运行期权限面**：`USER`、只读根文件系统、上下文外发 | 不管服务之间的顺序 |

四条从 8.1 起定下来，后面四章都按它长：

1. **离线当门、真机报数**（3.10 起的纪律）：本树**不装 Docker、不联网、不要密钥**。
   镜像的大小与层数是**文件系统剧本算出来的**，构建耗时是**脚本化的每层秒数**，
   编排的启动时钟是**写死的秒数**——所以它能量的是**这些机制本身的性质**
   （层是不是只加的、顺序是不是真的那么贵、`depends_on` 到底等到哪一刻），
   **量不了真实镜像的体积、真实机器的构建速度、真实数据库的启动曲线**。
   这一条写进每一章的边界里。
2. **读数要么能复算，要么别写进正文**：四块里的每一个数都能拿纸笔重算一遍
   （下面的六个组、七十来条夹具都在做这件事）。
3. **两个数必须一起报**：镜像大小与可见大小、构建代价与传输代价、
   失败次数与成功时刻、写不进去几处与必须声明几处。**只报其中一个，
   另一半就会变成一句听起来对的话。**
4. **不写「永远最新」「一定起来」这类话**：`RUN` 的缓存不会自己过期、
   `service_started` 只保证容器在跑——这两条都写进了模块注释与正文的边界。
"""
