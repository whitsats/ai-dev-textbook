# 框架版（第 4 篇）的依赖。与 zhizhou-v3/requirements.txt 同一套写法：写区间，不锁死小版本。
#
# 但**下面这四个是本机实测版本**，第 4 篇正文里的 API 名称与默认值都以它们的实际行为为准：
#   langchain 1.3.2 ｜ langchain-core 1.4.0 ｜ langchain-openai 1.2.2 ｜ langgraph 1.2.2
# 框架层不写默认值时，实际生效的是下游 `openai` SDK 的常量（本机 2.36.0：读超时 600 秒、
# 重试 2 次）——这也是那一节要讲的「有默认值不等于有边界」。
langchain>=1.3,<2
langchain-openai>=1.2,<2
langgraph>=1.2,<2

# 只在跑测试时需要（本书的验收不依赖它：python tools/check_runnable.py 会用同一批测试函数自己驱动）
pytest>=8.0
