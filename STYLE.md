# 写作体例与文风规范（STYLE.md）

本文件是全书**统一文风的唯一依据**。所有章节写作、修订都以本文件为准；出现冲突时，先改规范再改正文。

---

## 一、读者与语气

| 项目 | 规定 |
| --- | --- |
| 目标读者 | 计算机相关专业在校生、转行求职者；会一点编程，但知识不成体系 |
| 人称 | 统一用 **「你」**。禁止「读者」「同学们」「我们大家」 |
| 语气 | 直接、平等、就事论事。可以断言，不要绕弯子 |
| 禁止句式 | 「众所周知」「显而易见」「不难发现」「笔者」「在教学实践中我们发现」 |
| 允许的幽默 | 少量、克制，出现在「常见坑」小节；技术正文保持严肃 |

**示例**

- ✅ 好：「`Depends` 会在每次请求时执行，这是它和普通函数调用的最大区别。」
- ❌ 差：「众所周知，众所周知 `Depends` 是一个非常好用的功能，笔者认为……」

---

## 二、每章固定结构（十段式）

每章必须包含以下小节，**顺序固定、标题固定**，便于横向对比与检索：

```markdown
# 3.6 工具调用与函数编排

> **本章导读**：（2–3 句，说明这一章解决什么问题、学完能做什么）

## 学习目标
- [ ] 能独立定义并注册一个可被模型调用的工具
- [ ] 能读懂工具调用的请求/响应报文
- [ ] 能定位工具调用失败的 4 类常见原因

## 前置知识
（列出需要先掌握的章节号；无则写「无」）

## 3.6.1 为什么需要工具调用
（正文；概念 → 示例 → 运行结果 → 解释）

## 3.6.2 工具的定义与注册
...

## 常见坑
（3–6 条，每条「现象 → 原因 → 修法」）

## 面试视角
（把 `sources/06-求职冲刺/` 的题库与本知识点挂钩，列出该考点的高频问法）

## 本章小结
（要点清单，5–8 条，每条一句话）

## 练习
- **基础**：……
- **进阶**：……
- **挑战**：……

## 延伸阅读

**官方文档**（本章结论的权威依据，必填）
- FastAPI · Dependencies — https://fastapi.tiangolo.com/tutorial/dependencies/ ｜ 本章依赖注入部分据此重写

**规范 / 论文**（有则写，无则省略）
- RFC 7519 · JSON Web Token — https://www.rfc-editor.org/rfc/rfc7519.html

**素材溯源**（选填，保留可追溯性）
- `sources/01-编程地基/FastAPI基础学习/16FastAPI的依赖注入/FastAPI的依赖注入.md`
```

> 例外：第 0 篇导论、求职冲刺篇的章可按内容需要裁剪「前置知识」「练习」，
> 但**必须保留**「本章导读」「学习目标」「本章小结」，以及「延伸阅读」中的**官方文档**。

---

## 三、代码规范

1. **可运行**：所有代码必须是能跑通的完整片段。写「片段」时用 `# ...省略...` 明确标注，不假装完整。
2. **标注路径**：多文件示例在代码块上方写清文件位置，例如：
   ```python
   # app/api/user.py
   ```
3. **语言标注**：所有代码块必须带语言标识（`python` / `bash` / `json` / `sql` / `text`）。
4. **环境统一**：Python **3.11+**，依赖管理统一用 `pip` + `requirements.txt`（首次出现时给 `uv` 作为可选加速方案）。
5. **风格统一**：遵循 PEP 8，变量 `snake_case`、类 `PascalCase`、常量 `UPPER_CASE`；类型注解尽量补全。
6. **输出示例**：命令与运行结果用 ` ```text ` 单独成块，并注明是「输出」。
   **如果这一段是树里某条命令的真输出，就把那条命令写进围栏的信息串**：
   ` ```text $ python scripts/x.py --offline `（`$` 后面就是命令，只许 ASCII——
   围栏行里的汉字会进篇幅账）。标了它，`tools/check_runnable.py` 的第八道会
   **重跑那条命令**、逐行比引用的行还在不在。不标的输出块（模型输出、模板、
   演示「把文档改坏之后的报错」）不属于这一类：它们本来就不可能靠重跑复现。
7. **错误示范**：需要展示错误写法时，用 `# ❌ 错误` / `# ✅ 正确` 注释区分，同一个坑只示范一次。

---

## 四、语言细节

| 场景 | 规定 | 示例 |
| --- | --- | --- |
| 术语首次出现 | 中文（English） | 检索增强生成（Retrieval-Augmented Generation, RAG） |
| 术语再次出现 | 统一用中文 | RAG 中文写作「检索增强生成」或直接用缩写 RAG，不混用英文全称 |
| 中英文之间 | 加一个半角空格 | 使用 FastAPI 构建 API |
| 中文标点 | 全角 | ，。：；？！「」《》 |
| 代码/命令 | 行内用反引号 | `uvicorn app:app --reload` |
| 数字 | 阿拉伯数字 | 3 个参数、第 2 章（不写「三个参数」） |
| 单位 | 半角并与数字连写 | 10ms、2GB、128K token |
| 段落长度 | 每段 ≤ 5 行；超过就拆 | — |
| 强调 | 用 **粗体**，不用下划线 | **幂等** |
| 提示框 | 统一用引用块 + 标签 | `> ⚠️ **注意**：…` |
| 交叉引用 | 用「见第 X.Y 节」，不写「见上文」 | 见第 5.4 节 |
| 列表层级 | 最多 3 级；有序列表用于步骤 | — |

**提示框标签固定为四种**：`> 💡 **提示**`、`> ⚠️ **注意**`、`> 🔥 **常见坑**`、`> 🎯 **面试**`。

---

## 五、贯穿全书的示例项目

全书所有示例尽量收敛到**同一个项目**，让读者看着它长大，而不是每章换一个新 demo：

| 阶段 | 项目形态 | 对应篇章 |
| --- | --- | --- |
| 阶段 1 | **知舟 · 博客 API**：FastAPI + SQLAlchemy + JWT 的后端服务 | 第 1 篇 |
| 阶段 2 | 用 AI 编程工具重构/扩展它，接入工程规范 | 第 2 篇 |
| 阶段 3a | **手写**智能问答与 Agent 能力：词元账、受控输出、循环与终止、计划与校验、工具、记忆、多 Agent、护栏、可观测 | 第 3 篇（3.1–3.10） |
| 阶段 3b | 用 LangChain / LangGraph 把阶段 3a **重写一遍**，与手写版逐项对照 | 第 4 篇 |
| 阶段 4 | 接入私有文档：RAG 检索 + 引用 + 评估 + 生产化 | 第 5 篇 |
| 阶段 5 | 打包为简历项目：架构图、难点、指标、演示 | 第 10 篇（10.1–10.2） |

项目名 **「知舟」** 可改，但**一旦定下就全书不改**。独立小例子允许存在，但必须服务于主线（先给主线代码，再补最小可运行示例）。

> **阶段 3 为什么拆成两半**（2026-09-13 修正）：原表把阶段 3 整体归给「第 3、4 篇」，
> 于是第 3 篇写作时只看「原理」二字，写完 3.1–3.4 后正文里「知舟」出现 **0 次**——
> 读者读完 10 万字手里没有任何交付物。整改后的写法：**第 3 篇是手写版**（不引框架，
> 每章给一个可独立验收的增量，3.10 收口成「知舟 Agent 版」），**第 4 篇用框架重写同一件事**，
> 两份实现摆在一起对照。契约（文件路径、端点、表结构、验收标准、硬约定）见
> [`PROJECT.md`](PROJECT.md)，它是第 3 篇写作时的强制依据。
>
> **硬性体例**：第 3 篇每一章（3.1–3.10）都必须有「知舟接线」小节，按固定四问回答——
> ① 这一节给知舟加了什么 ② 接口契约（文件 / 函数 / 端点 / 表，以及依赖前面哪一章的零件）
> ③ 怎么验（一条能照抄的验收命令 ＋ 是实跑还是拼接验证） ④ 不能破坏什么（既有端点形状、
> 既有测试、表结构、密钥边界）。`tools/lint_book.py` 已把这条接成检查。
> **实跑标注规则**同 1.15：不依赖服务端的部分算实跑（词元账、schema、计划校验、循环与终止、
> 工具注册表、脱敏与注入检测），依赖数据库的部分算拼接验证（建表、迁移、会话恢复、指标查询），
> 两类不许混为一谈。

---

## 六、图表与素材使用

1. 能画图就不写长段落：架构图、流程图、时序图用 **Mermaid** 代码块书写（可版本管理、可渲染）。
2. 表格用于对比（框架选型、参数说明、指标对照），不用表格承载叙述。
3. 引用素材时保留溯源：正文中引用原始资料的**经验性结论**时，在「延伸阅读」的
   「素材溯源」里写明 `sources/... .md` 与 `<!-- p.N -->` 页码。
4. **不得直接大段搬运素材原文**。素材是原料，正文要重写、重组、补例子。
5. **素材不得作为技术结论的依据**。素材是一批第三方整理的学习资料，
   审计已发现其中存在过时写法（LangChain 旧版链、已关停的 Assistants API）
   与不实内容（题库答案存在模板占位）。
6. **正文不得提到「素材」「讲义」「资料包」**。素材是写作原料，读者手里没有它。
   正文里写「素材的说法是……」「讲义这里的顺序是反的」，读者读到的是**你在跟一份
   你看过、他没看过的文档对话**——他会卡住，因为这个「素材」在他的世界里不存在。

   - ❌ 差：`### 素材的顺序说明是反的` / 「素材的说法是『中间件里的异常处理器
     完全没参与』。这句话一半对……」
   - ✅ 好：`### 一个常见的错误说法` / 「中间件里抛出的异常不会回到类型匹配
     那一档，所以在这里写 401，客户端拿到的是 500。」

   写法是**直接给结论**，不要先说「某人说错了、实际是」再给结论。
   素材与正文的**逐条对照**（哪一条被纠正、纠正成什么、依据是什么）写在章末
   「素材溯源」里——那里才是它该出现的位置。
   唯一豁免是 `0.3`（使用说明章），它本身就在讲全书的资料分工。
7. **「有没有实跑过」只标注一次，且只为读者的行动服务**。
   保留：反引号里的「未实跑」说明、「本机没有 Redis 服务端，这段需在真环境验证」
   ——读者要据此决定能不能照抄。
   删除：「已实跑核对，与素材给的顺序一致」「我跑了一遍才发现」这类过程叙述；
   结论该写「实测得到 X」，而不是「我跑过一遍」。同理，正文里不要为每一段
   都盖一次验证章——同一章反复出现的核对说明，收进章末一处即可。

---

## 七、引用与出处（硬性规定）

**技术结论必须回到官方文档。** 素材可以帮助理解与找选题，但正文里每一条
"应该这样做"的结论，都要能追到官方出处。

### 优先级

```
官方文档 ＞ 规范/RFC/标准 ＞ 同行评议论文（arXiv） ＞ 厂商官方博客 ＞ 社区文章
```

社区文章（含素材库里的 PDF）只能作为「经验与踩坑」的来源，
**不得单独支撑技术结论**；引用时必须标注其性质。

### 五条硬性要求

1. **每章必填官方文档**：「延伸阅读 → 官方文档」至少 2 条，并注明该章用到哪一部分。
2. **带版本**：涉及 API 的段落写明版本，例如 `Python 3.11+`、`LangChain v1`、
   `MCP 规范 2026-07-28`、`Pydantic v2`。API 变化频繁的章节要在小节开头标一句
   "本节基于 ⟨版本⟩，截至 ⟨访问日期⟩"。
3. **带访问日期**：链接统一从 [`REFERENCES.md`](REFERENCES.md) 取，那里记录了访问日期；
   正文不自行编造链接。
4. **链接必须可校验**：新增链接后跑 `python tools/check_refs.py --only-broken`，
   出现「失效」或「跳转」必须先修 `REFERENCES.md` 再改正文。
5. **原理要有论文**：讲机制（注意力、ReAct、RAG、LoRA 等）时给出原始论文，
   不要只引二手教程。

### 禁止事项

- 禁止引用无法追溯来源的说法（"网上都说""据说"）。
- 禁止把素材里的过时写法当现状讲——遇到与官方文档不一致的地方，**以官方文档为准**，
  并在「常见坑」里说明"为什么旧写法不适用了"（这反而是很好的教学点）。
- 禁止只贴链接不做解释；每条引用要写清"这一章用到了它的哪部分"。

---

## 八、长文一致性维护规程

全书 **10 篇 68 章、约 46 万字**。这个体量下，**不允许依赖「记得」**：
凡是需要记住的东西，一律写进仓库文件，写作前读取，写完后校验。

### 8.1 六种已知失效模式

| 失效模式 | 典型表现 | 对策 |
| --- | --- | --- |
| 上下文丢失 | 前面定过的设定（术语、例子、章节号）后面忘了 | 外置到 [`PLAN.md`](PLAN.md) / [`LEDGER.md`](LEDGER.md) / [`GLOSSARY.md`](GLOSSARY.md)，写作前只读相关片段 |
| 术语漂移 | 同一概念在不同章换写法 | [`GLOSSARY.md`](GLOSSARY.md) + `lint_book.py` |
| 体例漂移 | 有的章有「前置知识」、有的没「常见坑」 | [`STYLE.md`](STYLE.md) 十段式 + `lint_book.py` |
| 前后矛盾 | 第 3 章说 A、第 7 章说非 A | 交叉引用检查 + 篇末反向核对 |
| 内容重复 | 同一段话被复制到两章 | `lint_book.py` 跨章重复检测 |
| 事实过时 | 引用了已关停的 API | [`REFERENCES.md`](REFERENCES.md) + `check_refs.py` |

### 8.2 三本单一事实来源（Single Source of Truth）

| 文件 | 管什么 | 冲突时怎么办 |
| --- | --- | --- |
| [`GLOSSARY.md`](GLOSSARY.md) | 术语的唯一写法 | 先改术语表，再全库替换正文 |
| [`REFERENCES.md`](REFERENCES.md) | 技术结论的权威出处 | 以官方文档为准，回头改正文与素材用法 |
| [`LEDGER.md`](LEDGER.md) | 每个素材知识点的落点 | 素材说了但正文没写的，必须显式标 `⏭` 并写原因 |

这三本之外的任何「我们前面说过……」都不算数——**要写成可检索的条目**。

### 8.3 每章五步闭环（不得跳步）

```bash
# 第 1 步：落事前估——**建章的第一个动作，只存在于动笔之前**
#   ① 按 8.7 的四项各写一行：前置段 ＋ 散文 N 节 × 系数 ＋ 接线节 ＋ 固定小段 ＋ 围栏 N 行 × 15
#   ② 把那三问答完：读数长成表、散文还是输出块？围栏散在哪几节？这个数里有没有已经含了容错？
#   ③ 写下分节计划（这一章大概几节、各是什么）——最便宜的判据层
#   写到两处：tools/audit_coverage.py 的该章注释（与正文同一趟进提交门，事后补不上）
#            ＋ PLAN.md 该章下面一行「（写作前校正）」（记计划值的变化与这个数）

# 第 2 步：定位该章素材，通读并建台账（状态先标 ⬜）
python tools/audit_coverage.py --pool
# → 把知识点逐条写进 LEDGER.md，写清来源与计划落点

# 第 3 步：写作（一次只写一章；先读完本章素材，不靠回忆续写）

# 第 4 步：回填台账（✅ / 🔀 / ⏭，禁止留空）＋ 把第 1 步那个数填进逐节表的「事前估」一列

# 第 5 步：机械校验，零错误才算定稿
python tools/lint_book.py --only <章号>
```

**为什么「落事前估」是第一步，而不是回填台账那一步**：它是整套流程里唯一一个
**只能在动笔之前做**的动作——写晚了就只剩「事后补记」，而**补记能补出数，
补不出「当时怎么想的」**（6.3 与 7.1 连着两章漏掉，两次都是写完之后靠人读台账才发现）。
它同时是唯一一个**曾经没有任何检查在守**的步骤，所以现在有两道守门：
`audit_coverage.py --check` 里那一列不许空着（没落笔的章只能在**封存**的欠账登记表里，
而那张表的每一条每次跑门都出声），以及这里把它写成**第 1 步**。
`7.2` 是第一次真的按这个次序开工：动笔之前先把分项估（11,700）、三问的答案与 8 节的分节计划
写进 `tools/audit_coverage.py` 的 7.2 注释，再在 `PLAN.md` 落下「7.2（写作前校正）」那一行。
**台账里的逐节表此刻还不该存在**——那一道门本来就拒绝一张没有实测的表，
所以「台账」这一侧在动笔前后只有 PLAN 那一行；逐节表是第 4 步的事。
**而这一次真的按次序做完之后的结果**：实测 **10,671**（对计划 12,000 的 89%），
对事前估是**估高 8.9%**——它是目前四个有效样本里最准的一次；
而准的原因有名字：**唯一不是「取中位」的那一项（散文插值 800）偏差只有 +0.3%**，
详见 8.7 第二十九个实测（含一条被它推翻的前提：「围栏越散、压掉的散文越多」并没有复现）。
`7.3` 是**第二次**按这个次序开工，而它给出了方向相反的一课：分项之和 **10,200**、实测 **14,075**
（对计划 10,000 的 141%，**超出容差、计划值改为 14,000**）——**四项全都偏低，最大一笔在散文**。
两次合起来才是那条判据的完整形状：**事前估仍然值得落笔，而它准不准不取决于「估得细不细」，
取决于「用来对照的形状是不是本章的形状」**（见 8.7 第三十个实测）。
`7.4` 是**第三次**按这个次序开工，而它把这三次拉回同一句话：分项之和 **12,250**、
实测 **11,310**（对计划 12,000 的 94%）——**估高 7.7%，计划值一次都不用改**，
是第 7 篇里估得最准的一章。三次连起来看：估高 8.9% → 估低 27.5% → 估高 7.7%，
而三章的计划值只改过一次——**事前估的价值不在「准」，在于每一次偏差都能追到某一项上**
（7.4 那一次追到的仍是「取中位」的两项与前置段，见 8.7 第三十一个实测）。
`7.5` 是**第四次**按这个次序开工，也是第 7 篇的**收官章**：分项之和 **11,305**、
实测 **8,701**（对计划 11,500 的 76%，估高 23.6%）——它是本篇估得最松的一次，
而它把前面四条的判据各自推到了边界：**固定小段那条新判据验到了 −3.0%**（误差来自
「在判据之上又加了一层理由」）、**前置段第四次偏差超过 20% 而方向第二次翻转**、
**四章的合计比值 0.983**（两章 0.776 → 三章 0.955 → 四章 0.983）。
本篇至此收口：**四次事前估、五次开工、零次「写完了才想起要估」**
（6.3 与 7.1 那两笔欠账仍封在 `audit_coverage.py` 的欠账登记表里，每次跑门都出声）。
`8.1` 是**第五次**按这个次序开工，也是**第一个开篇就带着事前估的新篇**（第 8 篇的第一章）：
分项之和 **10,595**、实测 **10,434**（对计划 11,000 的 **95%**）——**估高 1.6%，全书第一次落进 ±5%**。
它准的原因与前四次都不同：**四个系数没有一个是我自己造的**——散文取四个最近实测单节的中位略下、
接线节取四章中位、固定小段照 7.4 的判据原样取七章中位（不在它之上加任何「这一章特殊」的价）、
围栏取中位偏上。7.5 的误差全部长在「判据之上再加一层价」上，本章把那层去掉，误差就落进了 2% 以内；
而它同时把「前置段」那一项钉成了**五次里唯一没准过的一项**（偏差 **+38.5%**，方向第三次翻转），
另有一条本章才显形的：**围栏那一项估的从来不只是「几块」，还有「每块里几行」**（+15.4%，
其中三行是「一致性」那一关当场逼出来的——把压成一行的字典改写回逐行摘录）——
详见 8.7 第三十三个实测；另：本章开写时**漏了术语表那一步**（写完才补登），
而它恰好暴露了术语日志对账门的边界，记在 [`GLOSSARY.md`](GLOSSARY.md) 的维护记录里。

### 8.4 每篇收尾的三件事

1. **反向核对**：从 [`LEDGER.md`](LEDGER.md) 反查正文，该篇 `⬜` 必须清零。
   `lint_book.py` 会机械回查每条 `✅` 的落点（小节是否存在、是否指向未开写的章、
   是否标了 `✅` 却没写落点），但语义仍需**按章抽 2–3 条人看**（见 8.8 第 1 条）。
2. **术语扫描**：跑 `python tools/lint_book.py`，确认无术语违规与跨章重复。
3. **交叉引用复核**：不只看「第 X.Y 节」，还要回查**小节级**引用
   （`见 1.14.6`、`（1.12.3）`）——改编号、拆章节后最容易悬空的正是这一级。
   判定必须避开版本号：`Starlette 1.6.0`、`redis 3.5.3`、`PyJWT 2.10`、
   `Python 3.11` 在字面上与章号无法区分（实测裸扫描的 8 条命中**全是版本号**）。

**这三件事跑完之后写在哪（第 6 篇这一次暴露的）**：第 5 篇的三件事有一处**正文落点**——
5.9 本来就是复盘章，于是三件事与术语日志对账门的结果直接写进了 5.9.9；
而第 6 篇**没有收口章**（4 章到 6.4 为止，四章全是增量章），同一套动作跑完只剩
「工具输出 ＋ 台账里记一行」。所以判据不能写成「每篇收尾都有一节正文」——
**承载它的那一章（复盘／收口）是可选的**；四章全是增量章时，收尾记录只能落在
[`LEDGER.md`](LEDGER.md) 的「统稿记录」里。

**第 6 篇这一次三件事的结果**：落点 **100 条**机械回查 **0 处错**（再按章抽 3 条人看，
共 12 条全对上）、小节级引用 **38 处** ＋ 章级 **1 处**、**0 悬空**、术语 **0 违规**
且 6.1–6.4 的维护记录四行齐全、`⬜` 归零。**而三件事全 0 的时候仍然错着一处**：
篇级进度那一格里 6.3 的条目被写成了「**6.4** 已完成（第 6 篇的**第三**章）」——
那一格是手写叙述，**没有哪道检查会去解析它**，所以它不落在任何一件事的射程内。
这是 8.5 那条「一道从不出声的检查与一切正常在输出上完全一样」的另一面：
**一段没有检查覆盖的手写叙述，与「核过了」在阅读上完全一样。**

### 8.5 强制机制（让检查漏不掉）

规范写在纸上会被忘，所以把它接到提交路径上：

| 层 | 文件 | 作用 |
| --- | --- | --- |
| 本机钩子 | `.githooks/pre-commit` | 每次提交依次跑：两个工具的 `--self-test` 夹具（字数口径 / 对账门）→ `lint_book.py`（正文）→ `audit_coverage.py --check`（文档数字对账：篇级小计、**逐章章表**、风险表、README 与 PROJECT 的字数、**LEDGER 的逐节校准表**（按列名取数，列名允许带注）与**每章标题上的字数**、**逐节表里「事前估」那一列有没有数**（欠账登记表每次跑都出声））→ 改动到 [`REFERENCES.md`](REFERENCES.md) 时联网校验全部链接 |
| CI | `.github/workflows/book-checks.yml` | 推送与 PR 时重跑同一套校验（含钩子自检） |
| 安装/自测 | `python tools/install_hooks.py` | 安装（本仓库 `core.hooksPath`）、查看状态、`--self-test` 验证真能拦下违规提交 |

钩子的行为约定：

- **只有「错误」拦提交**；台账 `⬜` 之类的提示属警告，不阻断。
- 本地地址（`127.0.0.1` / `localhost`）不算出处，不要求登记。
- 链接校验里**只有服务器明确返回 4xx/5xx 才算失效并拦提交**；
  连接超时（已自动重试一次）与 401/403 反爬只提示、不阻断。
  把超时当作失效会让本地网络抖一下就提交不了，而这种报警器最终会被绕过。
- 紧急绕过用 `SKIP_BOOK_CHECKS=1 git commit ...`，**并在 PR 里说明原因**；CI 仍会检查。
- **新增一条机检，必须用合成反例验证它会响**。一道从不出声的检查与「一切正常」
  在输出上完全一样；台账落点/交叉引用两道回查就是靠反例集（悬空小节、错落点、
  版本号干扰项）才确认真的生效、且不会把版本号当成章号报。
- **同时要验证它「不乱响」**。只测正向会让规则越管越宽：本轮加「正文不得指代素材」
  与「不得写核对过程」两道检查时，同一个坑踩了两次——
  `1.6.0`/`2.10` 这类版本号与章号字面一致（上一条），
  而禁用词「据说」是合法复合词「判据说」的子串。
  结论：**每条检查都要配一组「应当沉默」的反例**，至少包括
  同形干扰项（版本号）、被包含的子串（判据说）、以及被豁免的区间（素材溯源区、0.3）。
  本轮又撞到第三种形态：**干扰项不必是版本号，也可以只是一个序数词**——
  8.7 里那处「备查的第 2 个数」落在后面「实测」前面 45 字的窗口里，
  于是被当成一次对实测的引用而报了错。修法是**换一个说法**（改成「另一种估法」），
  不是放宽那条检查——放宽正好会把「引用了不存在的实测」这种真悬空一起放掉，
  而那种错**不产生任何数字**，本来就只能靠这一道拦。
  （那道检查只认中文序数，`第 2 个` 这种写法不进它的网；但**别拿它当绕检的口子**，
  书里所有「第 N 个实测」照旧写中文数字。）
- **「检查读窄了」与「检查不存在」在输出上完全一样，所以表头也要当契约来读。**
  `LEDGER` 每章那张「事前估／实测」逐节表由 `calibration_sum_lines()` 加一遍、与正文实测比，
  但第一版把这份契约写死成「第一格是 `项`、第 3 格是实测」：于是**第 6 篇五张表
  （6.1–6.4 与 7.1）第一格写的是「段」，一张都没被加过**；而 7.1 那张中间还插了一列
  「事后补算」——按列序读会把那一列当成实测。两处都不是「表写错了」，是读数的一头
  只认一种形状。改法：**按列名取数**（表头必须同时有 `事前估` 与 `实测`，而列名**允许带注**：
  `事前估（补记·8.7 式子）` 这种前缀也算——写死逐字相等，就是给「加个注」留了一个静默出口），
  并给每种形状各配一条夹具，其中一条是**应当沉默**的（改插进来的「事后补算」那一列不该响）——
  而那条夹具现在**自己造出那种形状**（在真表上插一列），不再依赖书里恰好有那么一张表。
  它一开就把四章的漂报了出来：6.1 −40、6.2 −21、6.3 −15、6.4 −20——
  四张表都是定稿后正文又长过（都长在接线节那张交付物表上），
  而**没有任何一处看起来奇怪**。
- **写在规范里的那一条，如果没有任何检查在守，它等于没写。**（`6.3`／`7.1`）
  `STYLE` 8.7 的第一条纪律是「动笔之前先把事前估落下来」，而 **6.3 与 7.1 连着两章漏掉**——
  两次都是写完之后靠人读台账才发现的，而这两章的计划值都只有一个数（篇内平摊的预算），
  没有分项，于是事后**无法区分「估错了」与「写超了」**。修法不是再把纪律写一遍，
  而是把它落成**可查的字段**：`audit_coverage.py` 量每张逐节表「事前估」那一列有没有数，
  并把那两章的欠账登记进 `_ESTIMATE_DEBT`（**封存表**，只收那两章）。
  三件事分开报：那一列一个数都没有 → 红（**登过记也一样**：登记是承认欠账、补记是把能补的补上，
  两件都要做——只有登记而没有数，等于把「没落笔」变成了一格免检）；真落了笔的章被贴上「补记」 → 红；
  登记过的章标记被删（或表没了） → 红；而登记表里的每一条 **每次跑门都打一行**
  ——一张登记过的欠条与「一切正常」在输出上必须不一样，否则这张登记表自己就成了又一个静默清单。
  两条顺带的口径：**「这一格有数」的判据是「从数开始」而不是「里面有数字」**
  ——在真文件上重演时摸出来的：那一列只剩「—（8 节 × 900）」这种注时，
  格子里有数字而其实什么也没估（被泼掉的算式）；以及 **补记 ≠ 事前估**
  （补记能补出数，补不出「当时怎么想的」：同一把尺子量第 6 篇三章，
  分项之和都是 12,450，而那三章的实测相差三千多字）；
  以及**新章不许再进这张登记表**——再漏就是一次报错，那时只有两个显式动作：补上估，
  或者把它登记进来并写下理由。
- **凡是「脚本按文件名/章节号做归集」的地方，都要配一道“漏网”守卫。**
  `audit_coverage.py` 曾以 `(\d+\.\d+)-[^\s]*\.md` 解析章文件名，
  而 `2.2-Claude Code 实战.md` 含一个空格，于是整章**被静默跳过**：
  不计入实测总数、也不算“已完成”，而所有报表数字仍然自洽。
  这正是最危险的一类错：**没有任何一处看起来奇怪**。
  所以守卫必须用**比解析器更宽**的写法（否则它只能永远沉默），
  且固定放在提交前校验里：`uncounted_chapter_files()`（见 `tools/audit_coverage.py`）。
- **正文里贴出的代码块与它旁边的输出，必须来自同一次运行。**（`2.4`）
  那章的审读清单脚本里，三条规则根本不可能出声（`http://` 里的 `//` 被当注释剥掉、
  `@ts-ignore` 本身就是注释、未使用的 import 自己匹配自己），
  而旁边的输出却是对的——那是另一版跑出来的。**读代码看不出来，读输出也看不出来。**
  所以：贴输出前先在正文的代码上重跑一次；改了代码就重跑输出，改了输出就重跑代码。
  配得上的做法是把**输入也写进正文**（三份夹具不足 60 行，但它是输出可核查的前提）。
  这条目前只能靠人执行：机器校验需要“提取片内的脚本 → 重建夹具目录 → 执行 → 比对输出”
  一连串动作，代价明显高于它目前能拦住的问题——真要做时，
  合理的最小形式是只认**带 `__main__` 驱动且前面列了夹具路径**的代码块，
  并且构不出完整沙箱时必须沉默（同 8.5 的「不乱响」）。
- **指向空处的引用要机检，不能靠回头偶遇。**（`5.3`）
  台账写着「两条修因写进了 `STYLE` 8.7 的第十七个实测」，而 8.7 里当时最后一条是第十六个——
  这个引用**不产生任何数字**，于是篇幅对账、台账计数、可运行树三关全都不响；
  而 5.1／5.2 那一轮的人工核也漏过了同一条。现在 `tools/lint_book.py` 把它收口：
  `STYLE` 里的 `- **第 N 个实测（…` 当作定义集合，再扫 `STYLE.md` / `LEDGER.md` /
  `PLAN.md` / `PROJECT.md` 里出现在「实测」前面一小段窗口中的「第 N 个」，
  不在定义集合里就是错误；「第十五个与第十六个实测」这种成对引用两个都收。
  夹具里有两条「必须拦」与三条「应沉默」，其中最关键的一条是
  **「本章实测 13,326 字」这种不带序数的正文不许被误收**——否则每章都平白多一条错。
- **每章收尾，拿素材的节标题反向对一遍台账。**（`2.2`/`2.3`）
  两章的骨架都对得上素材，但素材 §7（IDE 三种模式 / 五个动作 / 截图流程）在 **2.3 的台账里连一行都没有**：
  既没落点也没标 `⏭`，于是台账、数字对账、lint 三处都不会响——**没有任何数字与它对不上**。
  两道回查（机械解析落点标题 + 人工抽样语义）都建立在「这条已被登记」的前提上，
  **没登记的内容它们看不见**。所以加一步反向核对：

  ```bash
  # 列出素材的顶层节标题（编号 + 标题），逐条问：它在台账里是哪一行？
  python - <<'PY'
  import re, pathlib
  p = pathlib.Path("sources/02-AI编程工具链/Codex从入门到实战.md")
  for i, line in enumerate(p.read_text(encoding="utf-8").splitlines(), 1):
      s = line.strip().replace("\u200b", "")
      if re.match(r"^\d{1,2}\.\s*\S", s) and len(s) < 46 and not re.search(r"[；。，、）（)】」]$", s):
          print(i, s)
  PY
  ```

  配套两条硬要求：
  ① **每章的「素材溯源」必须写出用到的节号区间与未用节号的去向**
  （如「第 1–8、10–14、17–20 节；第 9 节归 2.5，第 15、16 节归 2.5/2.6」）——
  写出区间后缺口就是可枚举的，这比「已覆盖」这种结论句可核对；
  ② **台账里的 `✅` 必须是可核对的东西**：要么是一个落点，要么是一个区间。
  写「散落在各节」等于没写——它无法核对，而不可核对的表述在账上看起来与「已完成」一模一样。
  ③ **`⏭` 必须在接收端也留一行。**（`2.5`）向别的章「有意省略、去向已登记」只是**发出去**，
  没有任何机制确认它在目的地真的落地：2.1 台账里写着 `⏭ 属「上下文工程」`（把「根目录只做地图」推给 2.5），
  而 2.5 写完后它**既不在正文、也不在 2.5 台账里**——两边各自看上去都完整，两个 ⬜ 计数也都是 0。
  所以「去向章」开写时，反向扫描要多问一句「有哪些 `⏭` 指向我这里」：

  ```bash
  # 把 2.5 换成要开写的章号，看有哪些省略指向本章
  grep -n "⏭.*2\.5\|⏭.*归 2\.5" LEDGER.md
  ```
  （`2.2` 的「提示词库 ✅ 散落在各节」当时就是这种情况：读者其实一段都抄不到。）
  ④ **反向扫描还要问一句「标了 `⏭` 的节，正文里真的没用吗」。**（`2.6`）
  前三种错（整块无行 / 笼统行盖住 / 跱章 `⏭` 掉地上）都会丢掉内容，而这一种**不丢内容、丢账**：
  2.6 把 Harness §22（对产品经理的意义）标成 `⏭ 归第 8 篇`，而正文其实用了它
  （2.6.2 「不会写代码的人能做的那一半」）。它同时影响权重、判定与后续改动——
  「这节没用过，那下次改写时可以不管它」就是从此开始。
  所以模式要在两个方向都验：**用了的要标 ✅，没用的要能在正文里真找不到**。
  这一条只能人看，脚本无法判断「用了」是不是一种改写。

- **改稿前，先查「还有谁引用了它」：`python tools/trace_refs.py`。**（本版新增）
  改一个术语、一个数字或一句结论之前，先问一句「它在别处还出现过吗」。
  这不是 `grep` 能回答的，至少三件小事都不行：

  ```bash
  python tools/trace_refs.py "上下文工程"                # 术语：命中位置逐行列出
  python tools/trace_refs.py "26,091" --from 2.2          # 数字：26,091 / 26091 / 全角都算同一个
  python tools/trace_refs.py "真正的成本从写代码转移到了验证代码" --fuzzy   # 结论被改写的地方
  ```

  ① **正文是硬换行的，`grep` 看不见接缝。** 一个句子常常被切成两行，
  `grep "会话、第 6.2 节"` 命不中而读者看得见。实测全书有 **480 处**这类被换行切断的句子
  （另有 **1,225 次**接缝合并），工具把它们接回一行再搜、再把命中映射回原始行号；
  ② **同一个数字有多种排版**（`26,091` / `26091` / `１，４５０`），而 `45` 又不该命中 `1,450`；
  ③ **要改的不止正文**——`PLAN.md`、`COVERAGE.md`、`LEDGER.md`、`GLOSSARY.md`、
  `REFERENCES.md` 里通常也各记了一份，工具默认一并扫、单独成节列出。

  两个用法提醒：加 `--from <你正在改的章>`，输出会把该章标成【本处】，
  结尾直接给出「除本处外还要回看的章」；`--md` 的输出可贴进台账或 PR。
  命中若指向某术语，工具还会顺手核对 `GLOSSARY.md` 登记的首现章与正文是否一致
  （`上下文工程` 登记在 2.5，而 2.1 早已用中英对照引入了它——这类漂移人不会去看）。

  **近似匹配（`--fuzzy`）的门限是量出来的，不是拍的**：同义改写 0.19–0.61，
  无关段落 ≤ 0.09。标定时连着否掉两种做法——只按「命中词元 / 查询词元」算，
  查询写长一点分数就跌（0.33 → 0.21）；只按 Dice 算，本段很长时分数也跌（0.61 → 0.22）。
  两者在长度上朝**相反方向**出偏，所以取平均，并把两个分项都打印出来给人看。
  与 8.5 的一贯要求一致：**改打分方式必须重新测一组正例与反例**，
  否则「调低了阈值」看起来与「找得更全」没有区别。

> ⚠️ **注意**：钩子脚本无扩展名，必须保持 LF 换行（[`.gitattributes`](.gitattributes) 已强制）。
> CRLF 的 shebang 会让它在 Linux / macOS / CI 上直接报错。

### 8.6 提交粒度

**一章一提交**，提交信息带章号（例如 `feat: 1.1 环境搭建`）。
这样任何一次「不对劲」都能用 `git diff` 定位到具体章节，也能单独回滚。

### 8.7 篇幅口径：有效字数

技术章节的代码本身就是内容。只数汉字会出现两种错判：代码密集的章被当作偷工减料，
或者为了凑汉字把代码改写成大白话。所以统一按**有效字数**与 [`PLAN.md`](PLAN.md) 的计划比：

```text
有效字数 = 汉字数 + 15 × 有效代码行数        # 一行有效代码 ≈ 15 个汉字的信息量
```

- 数字一律取自 `python tools/lint_book.py` 的输出，**不手工估算**（[`PLAN.md`](PLAN.md)
  的「状态」列、[`LEDGER.md`](LEDGER.md) 的篇级进度表都以此为准）。
- 计数口径：**围栏行不算内容**——`有效代码行数` 只数围栏内部的非空行，
  开围栏（` ```python `）与闭围栏都不计。这条曾是「注释写一套、实现跑另一套」：
  代码按整块 `splitlines` 计，全书 671 个围栏**虚增 20,130 有效字**（2026-09-13 修正，
  见 [`PLAN.md`](PLAN.md) 的「字数口径」节）。引用块里的 `> ``` `（1.14）与
  列表项里的缩进围栏（2.5）同样按围栏处理，否则会静默少减。
- 容忍偏差 ±40%，超出即警告。
- 不要用「放宽容差」代替折算：放宽容差会把真问题一起放过。

**怎么事前估一章的字数：按**小节数与代码密度**估，不按素材量估。**
两个方向的错都犯过：

| 错向 | 例子 | 现象 |
| --- | --- | --- |
| 素材多 → 以为该多写 | 1.9 初稿 22,341（186%） | 其实是把「统一响应格式」「表单上传」也堆进来了，应当**把内容归位** |
| 素材少 → 以为该少写 | 1.10 计划 6,000，实写 11,807（197%） | 素材为零只说明「要自己写」，不说明「写得少」：Pydantic v2 的完整面貌就是十节的量 |

**代码行项不是零头，代码密集的章里它甚至占一半。** `1.11` 估 13,000、实写 20,589
（158%），就是估时只数了小节（13 节），没估代码：实际 582 行 × 15 = 8,730，占实测的 46%。

**代码行有个可以照用的先验区间**：凡是带「综合示例」的章，代码实际落在
**434–731 行**（中位约 530，15 × 530 ≈ 8,000，约占实测的四成），
而凭感觉估往往只会给 400 行——同一条错连犯三次：`1.11` 582 行、`1.12` 532 行、`1.13` 492 行，
三次都估少了。所以**估代码时先问「有没有综合示例」**，有就按 500 行起估，
不要按「几个短示例」的数法算。反过来，没有综合示例的概念性章节（如 0.x、3.x）
代码常在 100 行以内，不能搬这个区间。

**估法有效的唯一一次，正好是把代码行项算进去的那一次**：`1.14` 731 行、`1.15` 434 行。
`1.15` 事前按「14 小节 × 800 + 675 行代码」估 21,000，实写 20,599（98%）——
第一次落在 ±10% 内。区别不在于估得准，而在于**估的时候就把代码行写进了算式**
（前三次是事后才发现漏了）。所以这条估法的操作要点只有一句：
**先把代码行数估出来写进式子，再估散文**。
反过来说，**「超出容差」先看代码占比，再看是不是装了两章**——三种超标的成因完全不同：

| 成因 | 例子 | 处置 |
| --- | --- | --- |
| 把别的章的内容堆进来 | 1.9（186%） | **归位**到讲它的章，删重复示例 |
| 素材为零但本身体量大 | 1.10（197%） | 按实测**校准计划值**并记原因 |
| 代码密集，估时漏算代码行 | 1.11（158%） | 同样校准，但**原因要写成估法错**，避免当成体量错 |

**第五次低估，错法也一样，但换了个马甲：模板密集章。** `2.2` 事前按
「12 小节 × 约 800 + 固定段 2,200 + 约 150 行命令/模板 × 15」估 11,000，
实写 **20,805（189%）**。两个错向同时发生：代码行项估 150、实际 **324 行**
（工单、提示词、配置每个都是 10–30 行，且**它们就是正文内容**，不是附录）；
散文项估约 8,800、实际 **15,945 汉字**，多出来的几乎全是素材没有的原创
（权限模式全集、沙箱平台限制与两个开关、会话恢复的“不还原”清单）。
所以对**以模板/命令为主体的章**，估法要单独调参：先把模板逐个数出来估行数，
再估散文——不要用纯叙述章的「小节数 × 800」。

**第六次低估，两个错向各自加了一层。** `2.3` 事前按 8.7 式子估 13,000（11 小节：
9 长 × 900 + 2 短 × 450 + 约 205 行配置与命令 × 15 + 固定段 1,200），实写 **16,856（130%）**。
这次**不是**「装了两章」——归位在动笔前就做完了（闭环与工单归 2.2、规则文件归 2.5）。
低估集中在一处（散文），代码项反而估准了：

| 项 | 事前估 | 实测 | 评价 |
| --- | ---: | ---: | --- |
| 代码 / 配置 / 命令 | 205 行（≈3,075） | 228 行（≈3,420） | **估准了**（差 +11%）：模板密集章的代码项逐个数过之后再估，比前几次凭感觉的 150 行靠谱得多 |
| 散文（含固定段） | 约 9,925 | 13,436 | 少估约 3,500 汉字。其中两块：决策表与工具对照表**各占一整节**（不是附录）；实跑那一节从「一句带过」长成了一节 |

第二条尤其值得单独记：**实跑不是“验证已写内容”，它本身是产出。**
那一节原计划只写「用 `codex apply` 拉回本地」，而把命令真跑一遍之后，
多出来的是「非零退出码不等于没改动」「撤销要从 `HEAD` 而不是从索引」这类
**只有跑了才知道、而且读者最需要**的内容。所以估篇幅时，
对含实跑演示的章要额外留一节的量，而不是把实跑当成写作的开销。

**第七次低估，错向只剩一个：散文项。**（`2.5`）事前按「模板逐个 340 行 × 15) ≈ 5,100
＋ 散文 12 小节 × 约 850 ≈ 10,200」估 15,000，实写 **19,352（129%）**。
这次代码项反而**估高了**——逐个数模板这条自 2.2 之后已经稳定；
多出来的全部在散文，而且源头很集中：

| 项 | 事前估 | 实测 | 评价 |
| --- | ---: | ---: | --- |
| 代码 / 模板 / 配置 | 340 行（≈5,100） | 306 行（≈4,590） | **估高了 10%**：模板逐个数的估值比凭感觉稳定 |
| 散文（含固定段） | 约 9,900 | 14,762 | 少估约 4,900 汉字。多出来的是**四张对照表**（权限分层、两份规则文件、`.claude/rules` 与 Codex `.rules`、三层机制）——它们各占半节，却不在「小节数 × 850」的视野里 |

**估算式里没有「对照表」这个项。** 一张 8 行 × 4 列的对照表，写进正文顶得上 400–600 汉字，
而且它往往是那一节真正值钱的东西（读者带得走的就是这张表）。
所以对以「概念对照」为主体的章，套参数时要把**表的张数与行数**单独加一项：
`字数 ≈ 小节数 × 800~1,100 + 15 × 代码行 + 约 120 × 表格行数`。

**第八次低估，错的还是这两个项，各自又长了一层。**（`2.6`）
事前按「11 小节 × 约 900 ＋ 约 340 行代码 × 15 ＋ 固定段 1,500」估 18,000，
实写 **24,986（139%）**；其中汉字 16,226、代码 **584 行**。

| 项 | 事前估 | 实测 | 差在哪 |
| --- | ---: | ---: | --- |
| 代码 / 脚本 / 配置 | 340 行（≈5,100） | **584 行**（≈8,760） | 多出来的是 **console / JSON 围栏里贴的实跑结果**（约 100 行） |
| 散文（含固定段） | 约 10,000 | **16,226** | 27 张表、共 134 个数据行 |

第一项是这一章独有的教训：**把「跑出来的东西」当成不在正文里的东西，等于没估**。
基线失败输出、两次实跑的命令日志、`trace.json`、`md5` 校验、`verify.sh` 前后、
YAML 解析结果——它们与手写的代码一样占篇幅，而且**必须原样入书**（删了就失去可核对性）。
这与 2.4 的「输入夹具也是正文」是同一族错误：总是漏算「不是我自己写的那几个围栏」。
所以估篇幅时，围栏要分三类数：**自己写的代码、别人给的输入、自己跑出来的输出**，
只数第一类会让估值偏低三成以上。实测那一章的 578 行围栏内容是：
Python 192、Markdown 模板 136、普通 text 103、**console 71**、JSON 36、YAML 27、bash 13——
其中 console 71 行与 trace 的 JSON 17 行是**不可能事先写出来的**，只能是先跑后贴。

第二项与 2.5 同因，但这次量出了具体数字：**这一章有 27 张表、共 134 个数据行**
（不是「四张对照表」）。表头与分隔行不算，只数数据行。把这一项单独算：
134 × 120 ≈ 16,080，几乎等于实测散文 16,226——而「小节数 × 900」只给出 9,900。
也就是说，**对以「概念对照」为主体的章，表格行数比小节数更能预测篇幅**。
所以上面那条估算式里的第三项不是补丁，而是这类章的主项；
反过来，纯叙述章（如 1.x）表格寥寥，第三项自然近似为零，不会把估值抬虚。
可操作的判断：**数一下大纲里每个小节“会不会以一张表收尾”**，会的话
这一节的篇幅基本由表的行数决定，而不是由你要讲几句话决定。

**第九次低估，换了一种成分：胶水。（`3.4` 的「知舟接线」小节，2026-09-13）**
原估 ＋2,000，实测 **3,946 有效字**（汉字 1,321、代码 175 行、两份输出块）——低估一倍。
错因与前八次都不同：不是漏了围栏、也不是漏了表格行，而是**低估了“胶水”的份量**。
一段可独立验收的增量，篇幅不在那 120–180 行模块代码上，而在它周围四样东西：
接口契约表（哪些文件、什么签名、依赖前面哪一章）、验收命令与**期望输出**、
“不能破坏什么”的四条边界、以及一处反例（坏输入）。
所以新增一条与之并列的估算经验：

- **“接线类”增量按每段 ≈ 3,900–8,200 有效字估**。五个实测（量法：小节边界内的汉字
  ＋ 15 × 有效代码行）：`3.4` **3,946** ｜ `3.3` **5,990** ｜ `3.1` **5,746** ｜ `3.2` **6,648**
  ｜ `3.5` **8,103**。
  区间上界由 6,700 改为 8,200，是因为 3.5 又一次顶穿：它一段交了**三份文件**
  （注册表 ＋ 六个工具 ＋ 测试），代码 332 行 × 15 ＝ 4,980 已经是底，
  加上契约与边界 3,123 汉字就到 8,103。`3.5` 开工前的估法是 6,400，仍然按「一段一份文件」在算。
  不要按模块的代码行数估——但也不能不看代码：
  **行数是乘数**（× 15），所以估法是两步：
  ① 先数列「这一段交付哪几个文件、每个多少行」，乘 15（3.1 三份 223 行 → 3,345；3.2 三份 292 行 → 4,380）；
  ② 再加 **1,300–2,300 汉字的契约与边界正文**——四段实测 1,321 / 2,105 / 2,311 / 2,283，
  这一项才是可以照用的常数量。`3.1` 与 `3.2` 的第一版预算（2,000 / 2,500）缺的正是第①步。
- **第六个实测（`3.6`）暴露的是「两项误差互相抵消」——比单次估错更危险**。
  事前估 11,800（正文 6,000 ＋ 接线 5,800），实写 **11,537**（98%）——看起来估得很准。
  拆开看才知道两个方向都错了，而且幅度很大：

  | 项 | 事前估 | 实测 | 差 |
  | --- | ---: | ---: | --- |
  | 正文九节 | 6,000（9 × 600 ＋ 40 行代码） | **9,519**（7,944 汉字 ＋ 105 行代码） | **+59%** |
  | 接线小节（3.6.9） | 5,800（≈300 行 × 15 ＋ 2,200 汉字） | **2,018**（1,253 汉字 ＋ 51 行代码） | **-65%** |
  | 合计 | 11,800 | 11,537 | -2% |

  原因就一句话：**3.6 的代码被分放置到了各个概念节里**（三张表在 3.6.2、迁移也在 3.6.2、
  合法化在 3.6.3、写入器在 3.6.6……全章 156 行里只有 51 行在接线段），
  而 3.1–3.5 的做法是把实现集中在接线段。两种结构都对，但**估法必须跟着结构走**：
  ① 先问「这一章的代码放在哪」：集中在一段 → 用 3,946–8,103 那个区间；分散到各节 →
  按「小节数 × 1,000 ＋ 全章代码行 × 15」估，接线小节只留契约与边界（≈ 2,000）；
  ② **交付多少行 ≠ 正文贴多少行**：乘 15 的永远是后者，交付量只决定「契约与边界」有多长。
  ③ 事后必须**把两项分开对账**：只核总数会把这一次的 +59% 与 -66% 当成「估准了」。
- 一条自检：写完一段接线，问「删掉“怎么验”那一节，读者还能不能确认他做对了？」
  若答“不能”，那么它的篇幅本来就由这部分决定，估的时候就不能只算代码。

- **第七个实测（`3.7`）：错的不再是算式，是常量。** 事前估 11,300（正文 9 小节 ＝ 6,300
  ＋ 接线 ≈2,750 ＋ 分散在各节的代码），实写 **13,773**（汉字 11,701 ＋ 代码 138 行）＝ 达成 122%。
  这次第①步（先数列、再乘 15）是**对的**——代码确实分散在各概念节，接线段只贴了全章 139 行里的一小半，
  与 3.6 同类；错的是正文常量「小节数 × ≈700 汉字」。
  3.7 里有四类内容在纯概念章里不存在：**两张对照表**（离线五行、真机三行）、
  **五句读数**（每句都要给出数而不只是结论）、**四次真机记录**（含改交接单前后各一次）、
  以及**一处记账错的复盘**。它们都是「先跑、再写」逼出来的，删任何一块都会让结论悬空。
  所以新增一条与前面并列的经验：

  - **带实测表的章节，正文常量按「小节数 × 900–1,100」估，而不是 700**。
    判据是本章有没有「数字要讲」——有实测对照的章（3.1、3.6、3.7 都有）正文天然更长；
    纯概念章的 600–700 仍然适用。（本书的取向是**保留数字、改常量**，
    而不是为了对齐计划值把测量结果删掉。）

- **第八个实测（`3.8`）：两个错向再次互相抵消，而这次两边都落在区间内。** 事前估 12,350
  （正文 9 小节 × ≈950 ＝ 8,550 ＋ 分散在各节的代码 ≈60 行 × 15 ＝ 900；接线 ≈2,900——
  注意这里把接线小节**也算作一个小节**，等于把它估了两次，下面的表因此要对两次分别算），
  实写 **12,447** 有效字（汉字 10,362 ＋ 代码 139 行）＝ 达成 115%。
  （写完校对时给两行代码补了行尾中文注释，字数从 12,382 变成 12,409，正好是 +27 个汉字
  ——于是发现一条口径：**中文注释会被算两次**，既计入「汉字」，又让那一行计入「代码行 ×15」。
  这不是错（它是本书历次校准所用的同一把尺子），但要记下来：代码注释密的章会显得比实际更长。）
  按接线小节切开对账：

  | 项 | 事前估 | 实测 | 差 |
  | --- | ---: | ---: | --- |
  | 正文八节 ＋ 固定段 | 9,450（含分散在正文里的 60 行） | **8,335**（6,385 汉字 ＋ 130 行） | **-12%** |
  | 接线小节（3.8.9） | 2,900（2,000 汉字 ＋ 60 行） | **4,112**（3,977 汉字 ＋ **9** 行） | **+42%** |
  | 合计 | 12,350 | 12,447 | +0.8% |

  这一张表推翻了两个想当然的预设：
  ① **「接线小节贵在代码」是错的**（第五条实测里 3.5 就记过同源的教训）：3.8 的接线段
  只贴 9 行代码，却有 3,977 汉字——它贵在契约表、怎么验与不能破坏什么，
  而这三块的篇幅由**交付物的数量**决定（本章交了核心模块 ＋ 样例集 ＋ 测试 ＋ 一条演示路径）；
  ② **代码行不能按「接线小节里那几段」估**：本章的每段实现都对应一个概念节
  （扫描、包装、预算、脱敏、审计各一节），所以全章 139 行里 130 行落在正文。

  - **交付物本身是「规则/契约密集」的章（护栏、校验器、协议实现），
    代码行按交付物的实际行数估，不要按接线小节估**。判据是：
    它的每一段实现是否都对应一个概念节？是的话，代码就会满章分散，
    而接线段改写「这套东西的边界在哪里」。这时候接线段会很长、但不是因为它有代码。

可用的粗估式：`字数 ≈ 小节数 × 800~1,100 + 15 × 预估代码行 + “接线类”段数 × （15 × 该段代码行 + 契约常量）`。
其中契约常量按这一章交几样东西算：**两三个交付物 ≈ 2,200 汉字**（3.4–3.7），
**四样（核心模块 ＋ 样例集 ＋ 测试 ＋ 演示路径）≈ 3,500–4,000**（见 3.8 的第八个实测）。
素材为零的篇（第 3、4、6–8 篇）尤其要按这个式子估，否则会把整篇的进度判断做得偏低。

- **第九个实测（`3.10`）：组装章的篇幅由「被组装的段数」决定，不由新技术的难度决定。**
  事前估 12,000（正文九节：四个技术节各 ≈1,200 汉字 ＋ 五个方法与表达节各 ≈900
  ＋ 代码 ≈240 行 × 15），  实写 **11,311** 有效字（汉字 9,451 ＋ 代码 124 行）＝ 达成 **94%**，
  是本篇第一个落在 ±10% 以内、而且**两侧都没崩**的章。拆开看两处小偏差方向相反：
  正文估计 ≈9,000 / 实写 ≈8,200（**−9%**，方法与表达类的小节比预想短——
  它们的价值在表格而不在段落）；代码估 240 行 / 实写贴了 124 行（交付物本身 ≈390 行，
  正文只贴节选）。两条可照用的结论：

  | 章的类型 | 估法 | 因为 |
  | --- | --- | --- |
  | 组装/收口章（无新技术） | **小节数 × 900–1,100**，其中「方法/表达」类小节取 900 | 每一节都在回答「这一段为什么这样拼」，篇幅接近「一张表 300–500 汉字」 |
  | 交付物是脚本与测试的章 | 代码行按**正文会贴的行数**估（交付物的 1/3 左右），不是按交付物行数 | 与 3.6/3.8 同形：那一栏量的从来不是交付量 |

  另记下一条**组装章特有的风险**：它最容易写成「复习章」——把前九章再讲一遍。
  分界是：**组装章只重述契约与顺序，不重讲原理**（少了这一句，它会胀成两倍）。

- **第十个实测（`4.1`）：全书第一次高估，而错的是两条以前没被量过的项。**
  事前估 14,000（八节：五节 ≈950 ＋ 三节 ≈700 ＝ 7,400；代码 ≈190 行 × 15；表格 ≈26 行 × 120；
  实跑输出 ≈55 行 × 15；固定段 ≈3,000），实写 **8,242**（汉字 5,797 ＋ 代码 163 行）＝ 达成 **59%**。
  前十八次全是低估，所以这一次值得单独记两条：

  | 项 | 事前估 | 实测 | 偏差 | 错在哪 |
  | --- | ---: | ---: | ---: | --- |
  | 散文（八节 ＋ 固定段） | ≈10,400 | 5,797 | **−44%** | 「每节 950 汉字」的系数是从第 3 篇的**技术节**中位取来的；这一章八节里有三节是「读一个读数、得一条判据」，每节 500–700 就结束了 |
  | 代码 | ≈190 行（≈2,850） | 163 行（≈2,445） | −14% | **估准了**：「先把代码行写进算式」自 1.14 起就稳定 |
  | 表格 | 26 行（≈3,100） | 19 行（≈2,280） | −26% | **重复计数**（见下） |
  | 实跑输出 | 55 行（≈825） | 26 行（≈390） | −53% | 把「读数的**叙述**」与「读数的**原文**」各估了一份，而叙述已经包含在散文项里 |

  两条可照用的结论：

  - **「小节数 × X」的系数不能跨章型搬。** 本章的八节分两类：**技术节**（要交代契约、文件、边界）≈950 汉字，
    **读数节**（贴一段实测输出 ＋ 一条判据）≈600。估的时候要**按类分别计数再相加**，
    不能拿一个从别章借来的中位数乘总数。判断方法：看大纲里每一节「要不要交东西」——
    交东西的贵，只解读读数的便宜。
  - **表格行数与「小节数 × X」是同一批字的两种算法，不能相加。** 本节前面那条式子里的
    「＋ 约 120 × 表格行数」这一项，**只在散文那一项是按段落估的时候**才成立。
    若先按小节估、再把表格加一遍，同一批字就算了两遍——本章的 14,000 里约有 3,100 是这么进来的。
    可操作的做法：**先数表、再数节**，两边都算完后取较大的那个，而不是相加。

  另：本章**计划值不动**（实测是 7,000 的 118%，本就在 ±40% 容差内）——高估与低估一样，
  要改的是下一次的估法，不是已经写好的那一章。

- **第十一个实测（`4.2`）：把第十个实测的两条修正用上，事前估第一次落在 ±5% 内。**
  事前估 **8,500**（技术节四 × ≈800 ＝ 3,200 ＋ 读数节三 × ≈650 ＝ 1,950 ＋ 接线节 ≈700
  ＋ 固定段 ≈1,800 ＋ 代码 ≈125 行 × 15；**表格不另加**），实写 **8,221**，差 **−3%**。
  两条修正各自都能对上：

  | 修正 | 具体做法 | 效果 |
  | --- | --- | --- |
  | 系数按**节型**分，不跨章型搬 | 把八节分成「技术节 ≈800」与「只解读读数的节 ≈650」两类分别计数 | 4.1 那一个 950 的系数如果再用，散文项会立刻高出约 2,400 |
  | 表格**不与散文相加** | 表格行数已包含在「节 × 系数」里 | 4.1 曾因此多算约 3,100 |

  一条可操作的判断：**看这一节「要不要交东西」**——要交契约、文件、边界的贵，
  只解读一组读数的便宜。本书第 4 篇是概念密集、读数密集的篇，
  这两类节会长期共存，所以这一条比一个统一的系数更有用。

- **第十二个实测（`4.3`）：事前估第一次落在 −5%，而错的那一项是「反例型章节」。**
  事前估 **9,300**，实写 **8,871** 有效字（汉字 7,506 ＋ 代码 91 行）＝ 达成 127%（对计划 7,000）、
  偏差 **−5%**——连着两章落在小偏差里。两条修正各自对上账：

  | 项 | 事前估 | 实测 | 差 | 说明 |
  | --- | ---: | ---: | --- | --- |
  | 散文（八节 ＋ 固定段） | ≈8,250 | 7,506 | −9% | 技术节 ×850 这一档偏高了一点（本章五节里有两节是「一张表 ＋ 一条判据」） |
  | 代码 | ≈1,365（91 行） | 1,365（91 行） | **0%** | 交付物是 1,206 行，正文只贴 91 行（约 1/13） |

  - **新增一条可照用的：反例多的章，按「每条反例 ≈ 450 有效字」加项。**
    4.3 有六条反例（工具异常抛穿、`"42"` 被静默纠正、两个上限不对账、`when` 静默失效、
    剧本模型撞 `KeyError`、真机两轮差）。它们不是附带的「常见坑」，而是**每一节的主要收入**：
    每条都要「当时的读数 ＋ 为什么 ＋ 怎么防」三段，删任何一段结论都悬空。
    判据是：**这一章的多数小节是不是「先给一个默认行为，再把它推翻」**——是的话适用本条。
  - **代码项按「正文贴的行数」估，已经第三次对上了**（4.1 −14%、4.2 估准、4.3 0%）。
    与 3.6/3.8 同一回事：那一栏量的从来不是交付量。

- **第十三个实测（`4.4`）：计划值第一次由公式反推（8,500），而公式本身还需要修。**
  事前估 **11,020**、实写 **9,733** 有效字（汉字 8,113 ＋ 代码 108 行），偏差 **−12%**,
  达成 115%（对上调后的计划 8,500）。这一章与前四章不同：前四章的计划值 7,000 是
  「四舍五入的一个 7」留下的，本章是**第一次用 8.7 的公式估出一个计划值**，
  所以 −12% 说明公式还要修，而不是计划值定错了。两条修因：

  - **「技术节 ×850」对「机制类」章节偏高。** 本章八节里有五节的主体是
    **读数表与错法对照**（三种边各错一次、两种 schema 各过滤一次、
    中断的两半、帧数 4/3/6），这类小节的正文是「一条读数 ＋ 为什么 ＋ 怎么防」，
    ≈600–700 字就够，不需要展开原理。判据与第十个实测同类：**先给系数分档，再乘节数**；
    新档：**机制节 ≈680、技术节 ≈850、只解读读数的节 ≈600**。
  - **「小节数 × 系数」与「代码行数 × 15」会重复计一段字。** 机制类章节的小图
    （`MergeState`、线性三节点图、三张错法小图）既是代码、又是论述的载体：
    它们在正文里的散文部分只写「这几行说明了什么」，而 11,020 那份估算
    把同一段内容按「半节的论述」与「15 行代码」各算了一次。修法：**小图章节按
    「小图张数 ≈ 节数 / 2」从散文项里扣掉**，或干脆改用「正文贴出的代码行数 ÷ 12」反推散文。
  - 代码项本身继续估准：108 行 × 15 ＝ 1,620，实测 1,620（0%），
    **连续四章对上**（4.1 −14%、4.2 估准、4.3 0%、4.4 0%）。

- **第十四个实测（`4.5`）：高估之后又低估，而错的是单项：固定段与「技术档」的离散度。**
  事前估 **9,490**（八个编号小节：机制 3 × ≈680 ＋ 技术 3 × ≈850 ＋ 读数 1 × ≈650；
  接线节 ≈800；固定段 ≈1,800；代码 110 行 × 15），实写 **11,615** 有效字
  （汉字 9,698 ＋ 代码 127 行），偏差 **+22%**，对上调后的计划 8,500 达成 137%。
  （初稿实测是 11,024，偏差 +16%；后来读数节又跑了一次真机、把两遍并排对照，
  又增了约 590 字——**真机读数的篇幅是最不可事先估的一档**，见 8.8 第 2 项。）
  逐节实测（`## ` 切段、汉字 ＋ 15 × 围栏内非空行）后，两条修因都很具体：

  | 项 | 事前估 | 实测 | 差 | 错在哪 |
  | --- | ---: | ---: | ---: | --- |
  | 机制节（1/3/6） | 3 × 680 ＝ 2,040 | 877 / 713 / 689 ＝ 2,279 | +12% | 这一档站得住 |
  | 技术节（2/4/5） | 3 × 850 ＝ 2,550 | 1,568 / 529 / 1,536 ＝ 3,633 | **+43%** | 两节要贴**能跑的东西**（一个可运行后端、一份团队装配），第三节是纯对账、没有代码，只有 529 |
  | 读数节（7） | 650 | 1,537 | **+136%** | 真机那一节不是「贴一段读数」：它贴了**两遍运行**（各自的上场顺序 ＋ 调度记录），并做了一张「哪些是不变量」的对照表，还要逐条归因 |
  | 接线节（8） | 800 | 872 | +9% | 交付两个新模块，四问按惯例写 |
  | **固定段** | **1,800** | **3,282** | **+82%** | **本章最大的单项偏差**：八个固定小段里，「常见坑」440、「面试视角」605、「本章小结」597、「延伸阅读」463 |
  | 代码（正文贴出的行数） | 110 行（≈1,650） | 127 行（≈1,905） | +15% | 连续五章立在 ±15% 内（−14% / 0% / 0% / 0% / +15%） |

  于是两条可照用的修正：

  - **固定段不是一个常数，它随「这一章抓到的反例条数」长。** 本章的固定段是 3,282，
    而前几章按 1,800–2,000 估都对得上——差别在「常见坑」与「延伸阅读」：
    抓到的反例多，坑表就长（本章 16 条，每条约 25–30 字，再加逐条判据）；
    官方出处多，延伸阅读就长（本章 6 条，每条一句为什么）。
    可操作的做法：**估固定段时先数两件事——这一章抓到几条反例、引了几条官方出处**，
    按 `1,400 ＋ 30 × 反例条数 ＋ 250 × 官方出处条数` 估，而不是填一个固定值。
    （这一条在第 5 篇两个实测里被再修了三处：**前置段要单列**、
    坑表按「每条带不带判据」分档、出处按延伸阅读的形状分档——见第十五个与第十六个实测。）
  - **「技术节 ≈850」要按「这一节要不要贴一个能跑的东西」再分两档**：
    贴（≈1,500，要交代文件、契约、边界）与不贴（≈550，纯对账或纯对照）。
    这一条把第十三个实测的「机制节 ≈680 / 技术节 ≈850 / 读数节 ≈600」再拆了一层：
    **判据从「要不要交东西」细到「交的东西能不能被读者跑起来」**。

- **第十五个实测（`5.1`）：先错在「没单列前置段」，又错在「一张表没量就填」。**
  事前估 **12,990**（概念节 2×680 ＋ 技术节 2,400 ＋ **三段读数 3×650** ＋ 地图节 700
  ＋ 接线节 800 ＋ 固定小段 3,380 ＋ 代码 160 行 × 15），实写 **13,326** 有效字
  （汉字 10,866 ＋ 代码 164 行），偏差 **+3%**，对上调后的计划 11,000 达成 121%。
  这一章真正要记的不是偏差，是**表本身被回头重测过一遍**：初稿那张逐节表的中间三行
  是推的、不是量的（三行合计比全章实测还多 755，全部六项合计多出 8,403），
  而事前估那一栏也加错了（写成 ≈11,700，实际各项之和 12,990）。两条修正：

  - **「前置段」要从固定段里拆出来单列 ≈950**：导读 ≈550–600、学习目标 ≈250、
    前置知识 ≈200。本章 1,060、5.2 的 1,025、4.5 的 846——**三章都在这个区间，
    而前十几章的式子根本没有这一项**，它一直括在固定段里被隐式低估。
  - **「概念节 ≈680」只适用于「这一节有要展开的论证」**：本章 5.1.1 与 5.1.2
    都是「一段叙述 ＋ 一张对照表」，实际只有 ≈480/节。估之前先问这一节有没有论证链。

  另：本章的读数节是 **≈1,700/节**（不是 650，也不是 4.5 的 1,100 那一档）——
  它的判据是**这一节贴几段读数、贴完要不要互相对照**。

- **第十六个实测（`5.2`）：事前估落在 ±1%，但那是三个错向抵出来的。**
  事前估 **13,950**（散文 10 节 × 700 ＋ 固定段 3,200 ＋ 代码 250 行 × 15），
  实写 **13,802** 有效字（汉字 11,642 ＋ 代码 144 行），偏差 **−1%**，达成 115%。
  逐项看就露了：散文少 122，但**前置段 ＋ 固定小段多出 1,585、代码少了 1,590**——
  三项合起 −127，加标题块与量法残差 21，才是总账的 −148。两条修正：

  - **代码行要按「整段贴」与「节选贴」分两档。** 整段贴与树里那个文件的行数相当
    （4.4 108 行、4.5 127 行）；节选贴则要先数「本章贴几处、每处几行」——
    本章按 4.5 的密度推出来的 250 行，实际只有 144 行（−42%），因为 42 处围栏里大多是
    节选（每处 5–20 行）与输出块。**不要拿上一章的代码密度往下推。**
  - **「出处」计费要看延伸阅读的形状**：`250 × 出处条数` 在 5.1（7 条、620 字）对，
    在 5.2（20 条、911 字、≈45 字/条）就高出一倍多——后者是**一表多链接**，
    每条只写一句理由。先看它是「逐条带说明」还是「一张表」。

  这一章最值得写进清单的一条：**总账对不等于单项对**。它与第八个实测（`3.8`）同族，
  但这次错向是三个而不是两个。所以看完总偏差之后必须看逐项表——
  而逐项表自己的数也得是量的（见第十五个）。

- **第十七个实测（`5.3`）：第 5 篇第一次低估，而错的两项正是第十六个刚拆开的那两项。**
  事前估 **≈14,500**（前置段 950 ＋ 散文 8 节 ≈7,600 ＋ 固定小段 3,200 ＋ 代码 190 行 × 15），
  实写 **12,360** 有效字（汉字 10,140 ＋ 代码 148 行），偏差 **−15%**，
  对上调后的计划 12,000 达成 103%（旧计划值 7,000 是按「一个概念章」估的，而这一章装了三件事）。
  与前两章的 +3% 与 −1% 相比，这一章的偏差不是运气：
  固定小段与代码行**同时**给高了——而这两项恰好是第十六个实测刚分成两档的那两项。
  于是两条修正：

  - **「面试视角」与「本章小结」不是固定值，它随「本章有几个能与面试官对话的取舍」长。**
    本章初稿这两项合计 744（面试视角 436 ＋ 小结 308），定稿补到 1,305（771 ＋ 534）；
    而 5.1／5.2 是 1,400（805 ＋ 595）／1,523（893 ＋ 630）。
    判据不是「写得短了」，而是**还没把章节的结论压成可回答的问题**：
    本章有四个可对话的取舍（选型、维度、混用空间、体检指标），补上它们才到 3,191 的固定小段。
    写之前先数一遍：这一章能让面试官追问几个「为什么这么选」。
  - **「机制节 ≈480–680」在「要摆三组对照读数」的章上会翻倍。**
    本章 5.3.2 实写 1,154（估 800）：三个相似度 ＋ 归一化前后的两组读数 ＋ 同分次序。
    判据与第十五个实测同源——**先看这一节要摆几组读数，再乘系数**；
    读数一旦要互相对照，它就不再是「一段叙述 ＋ 一张表」。

  另：本章的动机节只有 436（估 700，−38%），因为它只干一件事（把「换检索器要钉住哪些变量」列成表）；
  而接线节 1,056（估 850，+24%）——它多贴了一次真机端点探测的**原始输出**。
  把这两项并到上一条看，就是同一个道理：**节的长短由「它要交几件东西」决定，不由名字决定。**

- **第十八个实测（`5.4`）：错向第一次「一对一错抵掉一半」，而两处都算得出来。**
  事前估 **≈15,250**（前置段 1,000 ＋ 散文 9 节 ≈7,800 ＋ 固定小段 3,600 ＋ 代码 190 行 × 15），
  实写 **13,841** 有效字（汉字 12,311 ＋ 代码 102 行），偏差 **−9%**，
  对上调后的计划 13,000 达成 **106%**（旧计划值 8,000 是按「检索优化的一个章」估的，
  而这一章装了四件事：融合、改写、重排、候选深度）。
  与前三章的 +3%／−1%／−15% 相比，这一章的 −9% 由**两处高估**与**一处低估**组成，
  而它们各自都是可判定的；另有一件要说清的事：**这 −9% 里含着一次「补内容」**——
  改写节 1,798 中有约 950 是定稿前补的真机读数小节，事前估里没有这一项，
  扣掉它偏差回到 −16%，**与第十七个实测的 −15% 同族**：

  - **代码行项要按「节选贴还是整段贴」分档，而这一章多了一个更前置的问法：核心代码落在哪。**
    本章估 190 行、实际 **102 行（−46%）**。理由不是写得少，而是
    **契约已经在前几章建好的模块里**（`corpus.py`／`split.py`／`embed.py` 一行没改），
    本章只贴「读者看不懂结论的那几段」（融合主循环、改写表、成对分）。
    所以这条修正建议写成一句可操作的问法：**这一章的引擎有几分之几是新写的？**
    新写的部分才需要贴；对照基线一行不贴。
  - **「一张表能把话说尽的小节」会稳定落到 500 上下**（动机 451、三条路对照 521、候选深度 302）。
    这与第十五个实测的「概念节 ≈680 只适用于有要展开的论证的那一节」是同一枚硬币：
    **先问这一节要交几件东西，再乘系数；只交一张表 ＋ 三条读数的，不要给 800–900。**
    反过来说，**要摆两组对照读数的那一节会翻倍**（本章融合节 1,721，估 1,100，+56%）——
    与第十七个实测里 5.3.2 那条是同一个系数。
  - **固定小段给低了 10%**（估 3,600、实际 **3,954**）。
    但比这更有价值的是它牵出的那件事：第十七个实测里写成「给高了」的 5.3（估 3,200、写 **3,191**），
    在重测之后是**估准的**——那一章当初确实偏薄，是定稿前补厚的。
    所以这一项既不是定值、也不能拿「上一章的超欠」来推；**它应当与「本章有几个可对话的取舍」成正比**
    （本章 7 个、5.3 是 4 个），而这个个数要在动笔前数出来。

  另：这一章的「没做的四件事」一节写了 619，而它的价值不在字数——
  **它是全书第一次把「哪些资料里提到的手法本章不做、什么条件下才该做」写进正文**。
  省略如果只记在台账里，读者看不到；写进正文之后，它就成了内容的一部分。

- **第十九个实测（`5.5`）：第一次由「低估」变成「高估」，而错的那一项是最高频的那一档。**
  事前估 **≈14,300**（前置段 1,000 ＋ 散文 10 节 ≈7,950 ＋ 固定小段 3,400 ＋ 代码 130 行 × 15），
  实写 **12,012** 有效字（汉字 10,332 ＋ 代码 112 行），偏差 **−16%**，
  对校正后的计划 12,000 达成 **99%**（旧计划值 6,000 是拿素材量倒推的：5,085 × 0.85，
  而这一章的体量不由素材量决定——与 5.1 那一章的 0.36 同一族错误）。
  这一章的 −16% 不像前三章那样「两处一高一低」，而是**同一个错向重复了六次**：

  - **「读数节」的篇幅上限由语料的条数决定，不由要讲的道理决定。**
    本章四组读数节实写 604／878／849／648（估 700／1,100／700／700），
    而 5.1 的读数节能到 **≈1,700/节**。差别不在写法，在**本语料只有 6 份文档、15 片、4 道题**：
    一节能摆出来的可对照读数有上限，超过就变成把同一件事说两遍。
    所以估这一档之前要先问一句：**这份语料最多能摆出几组可对照的读数？**
  - **「机制一段 ＋ 对照表一张」的小节稳定落在 390–650**（本章 387／611／604／648），
    我按 850–900 估，高了三成。这是第十五个实测那条（「概念节 ≈680 只适用于要展开论证的那一节」）的第三个变体，
    所以把它写成一条可直接套的区间：**先问这一节交几件东西——交一段机制加一张表的，取 400–650；
    要展开一条论证链的，才给 680–900；要摆两组以上对照读数的，取 1,100–1,700。**
  - **固定小段的公式仍偏低：反例系数要取 50，不是 30。**
    本章 17 条反例、5 条官方出处，按旧公式得 1,400 ＋ 30×17 ＋ 250×5 ＝ 3,160，
    实测 **3,677**（＋16%）——逐条量下来，坑表的每条实际是 **≈52 字**（888 ÷ 17），不是 30 字：
    因为本章的坑大多带一句判据或一条读数，而不是一句提醒。
    取 50 后为 3,500，仍偏低 5%；而第 5 篇四章的固定小段实测是 3,380／3,191／3,954／3,677，
    **中位在 3,500 上下，可直接按 3,500 估**（比继续修系数更不容易出错）。
  - **代码行 112 < 估 130（−14%）**：核心实现在 `app/critic.py`（234 行）与 `app/generate.py`（225 行）里，
    正文只贴「读者看不懂结论的那几段」——与第十八个实测那条「先问引擎有几分之几是新写的」一致。
  - 另有一件要单独记的：**本章 112 行围栏里有 31 行是跑出来的读数**（三档表、精炼表；A/B 表、
    幻觉名单、控制位表，另有 5 行是机制图、5 行是命令），占 28%。
    它们不可能事先写出来，**而它们与手写的代码一样占篇幅**——
    这是第八个实测（`2.6`）与第十六个实测（`5.2`）那条「围栏要分三类数」的又一次出现，
    而这次是第 5 篇里占比最高的一章。

- **第二十个实测（`5.6`）：第一次「估得准的那几项恰好是我上两章刚修过的」，而错的两项换成了新的。**
  事前估 **≈16,200**（前置段 1,050 ＋ 散文 11 节 ≈9,200 ＋ 固定小段 3,500 ＋ 代码与读数围栏 165 行 × 15），
  实写 **12,463** 有效字（汉字 10,603 ＋ 代码 124 行），偏差 **−23%**，
  对上调后的计划 13,000 达成 **96%**（旧计划值 7,000 是按素材量反推的：7,574 × 0.93，
  与 5.1／5.5 同一族错误）。  这一章的 −23% 里有一个与前四章都不同的现象：

  - **四组读数节里两组落在区间里、一组偏低很多、一组略超出上沿**：784／317／653／618（一律估 750）。
    差别不在语料（5.5 那条「读数节上限由语料决定」仍然成立），而在**这一节要交几件东西**：
    摆四张对照表的取 650–750，而**只报一两个数与一条差的只取 300–350**（本章的端到端那一节
    就是后者：两个数、一条差、一句「这一段该谁负责」）。所以第十九个实测那条还要再拆一刀：
    **先数这一节要摆几组可对照的数，再取区间**；
  - **「引擎有几分之几是新写的」这条判据本身要收窄**（第十八个实测的补充）：
    本章两个模块都是新写的（`metrics.py` 268 行、`eval_run.py` 414 行），我因此估了 165 行，
    实测 **124 行（−25%）**。所以「新写得多 → 贴得多」不成立：
    **贴多少由「有多少结论非要靠代码才能读懂」决定**——本章只有四处需要出镜
    （命中三条定义、扁平化、极性、契约清单），剩下的都在模块里；
  - **出现了一种新形态：写作中长出来的「纪律节」。** 末一节（「换尺子就要重新取基线」）
    事前估里**没有**它，实测 **505**。它不是补内容，而是一条规矩在写作过程里
    **被反复用来做决定之后自动长出来的一节**（本章用了三次：改断言、改基线、改代码，
    每一次都得先定一条判据）。可操作的问法：**写完一章回头数一下「有哪条规矩被用过三次以上」**，
    有就该单列一节，估篇幅时留 ≈500；
  - **固定小段第一次没有再偏低**（3,305 对估 3,500，−6%）。第十九个实测把反例系数
    从 30 改成 50 之后，本章 16 条坑实测 800 汉字 ＝ **≈50 字/条**，与系数一致——
    前八次都是偏低，这一条终于站住了。

  另：本章 124 行围栏按三类分——自己写的代码 **79** ＋ 用例样例 **1**、跑出来的读数与契约清单 **38**、
  命令 **6**，第三类占 **31%**（比 5.5 的 28% 还高一点）。读数本身就是内容时，
  「不是我写的那几个围栏」这一项会把偏差推向低估——估之前先把三类数摆出来。

- **第二十一个实测（`5.7`）：第一次出现「输出比代码多」的形态，也是第一次「读数节」多到六节。**
  事前估 **≈16,950**（前置段 1,000 ＋ 散文 9 节 9,350 ＋ 接线节 950 ＋ 固定小段 3,400
  ＋ 代码与读数围栏 150 行 × 15），实写 **13,806** 有效字（汉字 11,301 ＋ 代码 167 行），
  偏差 **−19%**，对上调后的计划 14,000 达成 **99%**（旧计划值 8,000 同样是拿素材量反推的：
  6,273 × 1.28，与 5.1／5.5／5.6 同一族错误）。三条修因：

  - **「一节一读数」把「哪一节会重」的判据换掉了。** 9 个散文节里有 6 节自带读数，
    于是决定体量的不再是语料，而是**这一节要不要给读者看一个数**：
    性能与门实测 1,015（估 800，**+27%**）、缓存 1,671（估 1,400，+19%），
    而并发 766（估 1,100，**−30%**）、成本 902（估 1,200，−25%）只给两组读数就收住了。
    可操作的修法：**同一节里的第二张表按 400–650 估，不按 650–750**（第二十个实测那条
    「先数要摆几组可对照的数」的第三次细化）；
  - **固定小段第一次「条数多而每条更短」**：25 条坑实测 746 汉字 ＝ **≈30 字/条**，
    低于第十九个实测那条 50 字/条的系数。两套系数并不冲突，判据是
    **这条坑要不要举例**：本章 25 条全是「一个具体做法 ＋ 它造成的具体后果」，
    一句话能说完；而 5.6 那 16 条里有 5 条要举例子，于是它 50 字/条；
  - **围栏第一次出现「输出类超过自己写的代码」**：167 行里自己写的 91 行（python）、
    跑出来的输出与清单 **72 行**（text 70 ＋ plain 2）、命令 4 行，输出类占 **43%**。
    与前两章对照：5.5 的 28%、5.6 的 31%、本章 43%——**越靠工程，读数越是内容**。
    第八个实测（`2.6`）与第十六个实测（`5.2`）已登记「不是我写的那几个围栏要一并估进去」，
    这一章把它推到了“这些围栏比代码还多”的地步：估之前先问
    **「这一章的结论有几条本身就是一段输出」**。

  另：本章也是第一次把「同一个人写的两份东西之间必须逐项相等」写进验收线
  （服务路径与直接路径的片级命中 0.8333、答案命中 0.9444、四格 1／0）——
  篇幅估价的结论与读数纪律在这一章是同一件事：**数要能对得上，才敢写下来。**

- **第二十二个实测（`5.8`）：第一次「每一节都估高了」，也是第一次「事前估那句话自己加错了」。**
  事前估 **19,900**（前置段 1,000 ＋ 散文 10 节 11,750 ＋ 固定小段 3,400
  ＋ 代码与读数围栏 250 行 × 15 ＝ 3,750），实写 **11,506**（汉字 9,526 ＋ 代码 132 行），
  偏差 **−42%**，对计划 12,000 达成 **96%**。
  （这一章的估在 `tools/audit_coverage.py` 的注释里曾记成 ≈17,400——四个加数加错了 2,500。
  已按分项改正：错的不是估法，是那句式子自己没人对账，见下第四条修因。）四条修因：

  - **每节都估高了**：10 个散文节里 9 个是负偏差，最大的两处是「不迁的决定」−59%
    （800 → 331）与「动机」−58%（900 → 376）。前几章的偏差都是**漏估代码行数**，
    这一次相反：估的时候按「这一章有几件事要讲」数小节，却把每节的体量套了 5.7 的密度
    （5.7 每节约 1,100 汉字，本章约 730）。可操作的修法：**套密度之前先问这一节是
    「论证」还是「结论」**——论证节 ≈900–1,100，结论节 ≈350–650
    （本章四个结论节：动机 376、格式 504、质量门 623、不迁 331）；
  - **接线节第一次低于 800**（950 → 662）：前几章的接线节交的是**代码**
    （3.1 的 229 行、5.7 的 167 行），本章交的是**报告与清单**。
    **交付物的形状决定接线节的体量**，不只是「交几份文件」；
  - **围栏第一次「估了一倍而实际只有一半」**（250 行 → 132 行，−47%）：
    本章的读数以**表**为主（八项清单、三套库、三档拒收都是表，不进围栏），
    围栏里只有两个模块的节选 ＋ 五段 `text` 输出。修法：**估围栏数之前先问
    「这一章的读数是表格还是输出块」**——表不算围栏（第八个实测那条的反面）；
  - **估算式自己的加法也要有一处会对账**：事前估在工具注释里是 ≈17,400，而分项之和是
    19,900，这个错静悄悄过了三关（`lint` 不读注释、对账门只比文档与正文、
    `check_runnable` 只管代码）。修法：**8.7 的估算式写成一条能相加的公式
    （一行一个加数、末尾带 `＝ 总数`）**，而不是一句话里嵌十个数字。
    它与 8.8 第 7 条是同一条判据：**这份数字是「算出来的」还是「抄过来的」？
    抄过来的那份，与它的源头之间有没有一条会响的路？**

- **第二十三个实测（`5.9`）：第一次「先量偏差、再折扣」，而折扣后的值落在实测的 0.2% 内。**
  事前估 **13,525**（前置段 800 ＋ 散文 10 节 8,400（论证 7 节 × 900 ＋ 结论 3 节 × 700）
  ＋ 固定小段 3,200 ＋ 围栏 75 行 × 15 ＝ 1,125），实写 **10,384**（汉字 9,604 ＋ 代码 52 行），
  偏差 **−23%**，对上调后的计划 11,000 达成 **94%**（旧计划值 4,000 是按「一篇总结」拍的）。
  （这一章的估在 `tools/audit_coverage.py` 里曾记成 ≈14,000：**总数是手抄的**，
  而加数之和是 13,525，差 475。5.8 那次差 2,500——**小了一个数量级，是因为这次加数分行写了**；
  但总数仍手抄，所以它仍然错。例子见下第四条修因。）四条修因：

  - **判据层仍偏高约三成，而这一章第一次看清了「哪一层稳」**：论证 7 节实测
    576／776／826／502／491／1,147／683（估 900，中位 ≈700），结论 3 节 528／302／716
    （估 700，中位 ≈500）。**分档给的是量级，给不了「这一节交几件东西」**——
    同一档里两头相差 3.8 倍（5.9.6 的 1,147 对 5.9.9 的 302）；
  - **折扣那一层站住了——这是本条真正落地的一条**：13,525 ×（1 − 23%）＝ 10,414，
    与实测 10,384 只差 **0.3%**。那个 −23% 不是一个手感，而是**已经量到的三次偏差的中位数**
    （5.6 −23%、5.7 −19%、5.8 −42%）。在判据层还偏三成的情况下，
    **「先量偏差、再折扣」比继续换判据立得住**——这是第二十二个实测那条修因的第一次复用；
  - **前置段第一次超了**（800 → 1,009，+26%）：它是**三个固定小节**
    （导读 618 ＋ 学习目标 284 ＋ 前置知识 107），按「一段」估会少算三成。
    修法：前置段按 ≈1,000 估，并把它的围栏算进去（导读那段自带一张三行流程表）；
  - **围栏类型决定折算系数的可信度**（第二十二个实测那条的第二次）：本章 52 行围栏里
    python 只有 11 行，其余是 bash 13／text 19／markdown 9，而**表 66 行不进围栏**；
    「代码行 × 15」在本章只占实测的 **7.5%**（5.7 是 18%、5.8 是 17%）。
    围栏里装的不是代码时，这一项该按「输出块」估，而不是按代码估。

  一条**事后说明**（不是修因，是口径）：上表所有数字都取自**定稿那一刻**（10,384）。
  同日又补了 **1,586 字**（第四、第五条对账与它们的演示、接线节扩写，当前 11,970）——
  这笔**不进估算样本**：估的是「写之前该估多少」，而补写发生在写完之后。
  **样本一旦开始跟着正文走，它就不再是一个样本了。**

  另：本章也是第一次把**「计划值怎么来的」写进台账**——旧值 4,000 是拍出来的，
  新值 11,000 是「事前估 × 已知偏差的中位数」算出来的，而这两步都能复算。

- **第二十四个实测（`6.1`）：第一次「四个分项同时估高」，也是第一次「折扣层没救回来、而拍出来的那个数反而准」。**
  事前估 **12,900**（前置段 800 ＋ 散文 8 节 6,600 ＋ 接线节 800 ＋ 固定小段 3,200
  ＋ 围栏 100 行 × 15 ＝ 1,500），实写 **7,706**（汉字 6,761 ＋ 63 行围栏），
  偏差 **−40%**，对计划 7,000 达成 **110%**（计划值未改：±40% 容差内）。
  这一章是第 6 篇的第一章，也是**全书第一棵不调模型的树**——它的读数全是算出来的账单，
  所以它能把「一条结论值多少字」这一件事单独量出来。四条修因：

  - **四个分项同时估高是第一次，而它把折扣那一层（第十三个实测）逼到了边界之外**：
    按中位 −23% 折，12,900 × 0.77 ＝ 9,933，仍比实测高 **30%**。前三次（5.6／5.7／5.8）
    的偏差里，「判据层偏高约三成」恰好与折扣层匹配，所以折一次就落回实测附近；
    **本章判据层高了约七成（12,900 对 7,706），折扣只是把一个错数缩了一点**。可照用的判据：
    **折扣层治的是「系统性偏高」，治不了「这一章本来就比同类小一半」**——
    所以折完之后要再问一句「这一章与上一章比，是不是少了一整类东西」；
  - **拍出来的那个数比算出来的那个数准**：计划值 7,000 对实测 7,706 只差 10%，
    而 8.7 式子算出来的 9,933 差了 30%。原因是那个 7,000 **不经过判据层**——
    它来自「第 6 篇四章平摊的篇幅预算」。可操作的一条（本章新增）：
    **估之前先问这个数要去干什么**——当**预算**用，就从总预算摊（它天然经过一次平均，
    而平均会吃掉单章的判据误差）；当**预测**用，才套判据层。两条都要写在台账里，
    因为它们给出的是两个不同的数（本章：7,000 与 9,933，实测两者中间）；
  - **「固定小段 3,200」不是常数，是「条数 × 每条多长」**（第十九／二十个实测的第四次细化）：
    本章 1,868，是全书最低——14 条坑 37 字/条（5.6 是 50、5.7 是 30）、6 条延伸阅读各 55 字、
    5 问面试各 63 字。三个数都偏小的原因是同一个：**这一章的读者不需要例子就能懂**
    （价格与分档是查得到的定义），于是坑只写「一个做法 ＋ 一个后果」一句话。
    修法：估固定小段之前先问**这一章要不要举例**；
  - **围栏与读数的关系又反了一次**：本章 63 行围栏（5.7 是 167 行），
    而两者的读数组数只差两组（6 对 8）。判据是**这几何读数要不要贴成块**——
    本章六组里**四组是表**（表不进围栏）、五个输出块各 3–6 行，代码只贴 3 段节选。
    第二十二个实测那条「估围栏数之前先问读数是表格还是输出块」在这里第二次验证。

  另：这一章也是第一次把「**拍的数与算的数并列写进台账**」——两个数差 30%，
  而它们**都对**（一个是预算、一个是预测）。让它们并列出现，比让其中一个消失安全：
  下一次要改计划值时，才有东西可对。

- **第二十五个实测（`6.2`）：折扣层第一次「几乎打中」，而估算表上出现一处新错法——围栏被算了两遍。**
  事前估 **11,250**（前置段 450 ＋ 散文 8 节 5,600 ＋ 接线节 800 ＋ 固定小段 2,600
  ＋ 围栏 120 行 × 15 ＝ 1,800），实写 **7,215**（汉字 6,435 ＋ 52 行围栏），
  分项偏差：前置段 **+20%**、散文 −25%、接线节 −14%、固定小段 −32%、围栏 −57%。
  这一章留下四条：

  - **折扣层这一次几乎打中，而它不是变好了，而是这一次的判据层恰好只高三成**：
    按 0.77 折：11,250 × 0.77 ＝ 8,663 → 还高 21%；而把**含围栏的那四个分项**
    相加得 **9,450**，× 0.77 ＝ 7,276 对实测 7,215 只差 **+0.8%**。
    把三次摆在一起，这一层的边界就清楚了：5.9 判据层高约三成 → 折后差 **0.3%**；
    6.2（本章）高 1.31 倍 → 折后差 **0.8%**；6.1 高 1.67 倍 → 折后差 **29%**。
    所以可照用的判据是：**先算「估 ÷ 实测」这个倍数，再决定折一次够不够**——
    折扣层治的是「系统性偏高约三成」，不是「无论多高都能拉回来」；
  - **围栏被算了两遍（新错法，也是最该记住的一条）**：估的时候把它单列成一个加数
    （1,800），而实测的四个分项里围栏本来就在各段内部——于是「合计」同时含两种口径，
    而**两个数都能自圆其说**（这正是它难被发现的原因）。修法不是改数，是**先定口径**：
    估的时候，围栏要么全在段内、要么全在段外。这一条与 2026-09-13 那次
    「有效代码行数把围栏也算了进去」同一族（口径写在一处、算法按另一处跑），
    而这一次它长在**估算表**上，不在工具里；
  - **固定小段两次都估高，而它的真值两次几乎相同**（6.1 估 3,200 实得 1,868；
    6.2 估 2,600 实得 1,768）。这给这一项的估法定形：**先数条数，再按「查得到的定义 → 32–37 字/条」估**
    （第十九／二十个实测的第五次细化）——价格与协议都是查得到的定义，所以坑只要写
    「一个做法 ＋ 一个后果」一句，不需要举例；
  - **前置段第一次估低（+20%）**：这一章的导读里多了一段**与旧账对读**
    （4.1 量过的手写 client 468 行 vs 框架版 78 行），那类段落没有判据可依。
    可操作的一条：**估前置段之前先问「导读要不要接旧账」**——接了就多算 250–300 字。

  另：这一章也是第一次把**「溢出的那一栏」当作结论**写进台账：0.77 这个折扣数是
  从三次实测的中位算出来的，而本章的 −2% 让它第一次有了上界（约三成）。

- **第二十六个实测（`6.3`）：第一次「事前估没有落笔」的一章，而它把一个更基本的问题顶了出来——「写超了」与「估错了」怎么分。**
  这一章的计划值是 **7,000**（与 6.1／6.2 同源：第 6 篇四章平摊的预算，未经判据层），
  实写 **11,391**（汉字 9,651 ＋ 116 行围栏），对旧值 **+63%**（超出 ±40% 容差）；
  而前三章那条路（先写事前估、再拿它和实测对）**这一章没走**：台账里只有 7,000 一个数，
  没有任何分项。于是偏差只能报出一个数，博不出原因。留下的三条：

  - **预算型的数也要在写之前落一条粗判据（本章的直接教训）**：6.1／6.2 的计划值能拿来
    对照，是因为它们的台账里另有一个**算出来的数**（判决层 × 折扣）并在；
    而本章只有「摊下来的那个数」。可照用的做法：**写之前至少写一句「这一章大概几节」。**
    那就是最便宜的判据层（本章 10 节），事后拿它一除就知道多出来的字长在哪（本章长在
    「六组读数各要一段解释 ＋ 三张表」）。没有这一句，改计划值时只能往实测上改，
    而那种改法会把「计划」慢慢变成「记录」；
  - **围栏行数是四章里最高的一章（116 行，6.1 是 63、6.2 是 52），而读数组数只多一组**：
    原因是 6.3 的六组里**四组必须贴成输出块**（退避的抖动抽样、预算切片的三次尝试、
    降级链里被排掉候选的理由、价钱阶梯），而 6.1 的四组是表（表不进围栏）。
    这是第二十二／二十四个实测那条判据的**第三次验证**：估围栏之前先问「这一章的读数是表还是输出块」；
  - **计划值取 11,000 而不是 11,391**：与 5.6–5.9 同一口径（500 的整数倍、落在实测附近而不等于实测）。
    **改计划值的理由要写进工具注释**（本章的理由是「事前估没落笔」而不是「估偏了」）——
    否则下一个看台账的人会把这一章当成「估算表失准的又一个例子」，而它其实是另一件事。
  三个数一起看才有信息：**一个折扣层需要边界，而边界只会在它跑偏的那一次里显出来。**

- **第二十七个实测（`6.4`）：事前估第一次在动笔之前落笔，而它把折扣层顶了下来——「估得对」与「折得对」是两件事，两个一起用就是双重打折。**
  这一章按 8.7 的式子先写分项（前置段 650 ＋ 散文 8 节 7,200 ＋ 接线节 850
  ＋ 固定小段 2,400 ＋ 围栏 90 行），**总分项之和 12,450**，写在
  `tools/audit_coverage.py` 的 6.4 注释里（与正文同一趟进提交门，事后补不上）；
  再按前三章的中位折扣取 `12,450 × 0.77 ≈ 9,586`，计划值取 **10,000**。实测 **11,779**
  （汉字 10,009 ＋ 118 行围栏），对分项之和 **−5.4%**，对折后 **−18.6%**。四条：

  - **对的那一半是分项估**：四段里两段落进 ±10%（散文 +4.5%、固定小段 +7.2%），
    另两段是 +10.6%（前置段——估的时候漏了「前置知识」那三条）与 +13.5%（接线节），
    而前三章的事后只能报一个总数（6.3 连事前估都没有）。
    可照用的一条：**分项估要按 8.7 式子的四项各写一行**，写不出四行时那个数就还是拍的；
  - **错的那一半是折扣层**：实测／分项之和 ＝ **0.946**，而不是 0.77。原因是两个动作
    在做同一件事——0.77 最初是拿来吸收「分项估偏大」的，而 6.4 的分项估本身就按四项
    写对了，于是它变成了**双重打折**。可照用的判据：**分项估写完自问「这个数里有没有
    已经含了容错」**；两个一起用时，偏差会以「估低了」的形式出现，而真实情况是**折多了**；
  - **围栏列为单列的一个加数，估与量都单列一行**（本章第一次先定死这个口径）：
    6.2 那次「围栏被算了两遍」就是因为两个口径混用，而**两个数都能自圆其说**。
    本章 118 行（四章最高，前三次是 63／52／116），而读数组数仍是六组——
    **五组必须贴成输出块**且两组各贴两块，是「估围栏前先问读数是表还是输出块」的第四次验证；
  - **计划值取 10,000 而不是 11,779**（与 5.6–5.9 同口径：500 的整数倍、落在实测附近
    而不等于实测）；**改它的理由写进了工具注释**（那一整段注释就是事前估的证据）。

  这一条与第二十四个（6.1：「拍出来的数比算出来的数准」）合起来才有意义：
  **6.1 那条是在说判据层要慎用，本章这条是在说折扣层要停用**——两者说的是同一件事的两面：
  **一个未经分项的常数，不该同时在两步里各打一次。**

- **第二十八个实测（`7.1`）：前一章刚把「事前估要在动笔之前落笔」写成纪律，这一章又没落笔——判据写进 `STYLE` 并不会自动生效，因为「落笔」这一步本身没有任何检查在守。**
  7.1 的计划值是第 7 篇规划阶段拍的（5 章 33,000 的均摊），**事前估一栏空着**（第二次，
  第一次是 6.3）。事后只能做两件事，两件都标明是**事后**：

  - **按 8.7 式子的分项补算**（不是事前估）：前置段 650 ＋ 散文 8 节 × 900 ＝ 7,200
    ＋ 接线节 850 ＋ 固定小段 2,400 ＋ 围栏 90 行 × 15 ＝ 1,350 → **12,450**；
    × 0.77 ≈ 9,586，而实测 **8,071**（汉字 7,051 ＋ 68 行围栏）＝ 式子估高 **18.8%**；
  - **找出式子偏在哪一段**：前置段 728（+12.0%）、**散文 4,371（−39.3%）**、
    接线节 1,062（+24.9%）、固定小段 1,910（−20.4%）、围栏 68 行（−24.4%）。

  三条可照用的：

  - **最大的那处偏差有名字**：这一章的**主体是输出块而不是散文**——六组读数里有十二个块
    直接把真输出贴出来（占了围栏 68 行的大半），而「散文每节 900」是给「讲机制」的章定的。
    所以判据从一问扩成三问：**动笔之前先问「这一章的读数会长成表、散文、还是输出块」**
    ——这是它的**第五次验证**（前四次见 6.1／6.3／6.4 与本章之前的条目）；
  - **围栏的落点也可以散着**：5.6–6.4 的围栏都集中在少数几节（输出块各贴一块），
    而本章 68 行**分散在六节里**（7.1.2 的三块、7.1.3 的两块、7.1.4 的两块、7.1.5／7.1.6／7.1.8 各一块），
    于是「散文被压到 4,371」与「围栏只有 68 行」是同一件事的两面——**同一条判据，
    围栏越散，它压掉的散文就越多**；
  - **计划值 7,000 未改，而这一次「没改」的原因与 6.2 不同**：6.2 是达成本来就准（103%），
    本章是 **8,071 / 7,000 ＝ 115%**，落在 ±40% 容差内，所以这一栏按口径不动；
    该补的不是这个数，是它旁边那一栏（事前估）。
  - **一条边界，与 8.4 那条同族**：「事前估要在动笔之前落笔」已经写在第二十六、二十七条里，
    而紧接着的一章又是空的——因为**没有任何检查在守这一步**（一篇没有检查覆盖的手写纪律，
    与「做过」在阅读上完全一样）。可照用的做法是把「事前估」也落成一个可查的字段
    （哪怕只是一个「已落笔／未落笔」的标记），否则它只会在写 `STYLE` 的那一次被想起来。
    **这一条后来两步都做了**：字段落了（逐节表的「事前估」列由 `audit_coverage.py` 守着，
    没落笔的章只能在**封存**的欠账登记表里），而「落事前估」写成了 **8.3 的第 1 步**。
    `7.2` 是第一次真的按这个次序开工：动笔之前先把分项估（**11,700**，四项取同类章的实测、
    不折）、三问的答案与 8 节的分节计划写进 `tools/audit_coverage.py` 的 7.2 注释，
    再在 `PLAN.md` 落下「7.2（写作前校正）」那一行（那一行同时记下了备查的另一种估法：
    若按本篇三章的「实测／分项」中位 0.92 折 ≈ 10,700）。

- **第二十九个实测（`7.2`）：全书第一次真的「事前估在动笔之前落笔」——而追得最准的那一项，恰恰是五项里唯一不是「取中位」的那一项；同时 7.1 那条「围栏越散、压掉的散文越多」在这一章**没有复现**，说明它缺一个前提。**

  7.2 的五项在开写前写进 `tools/audit_coverage.py` 的注释（分项之和 **11,700**，其中四项取同类章的实测中位、
  散文那一项按形状**插值** 800），与正文同一趟进提交门；实测 **10,671**（汉字 9,621 ＋ 70 行围栏）
  ＝ 对计划 12,000 的 **89%**、对事前估是**估高 8.9%**——它是目前四个有效样本里最准的一次（6.4 是 +3%、
  7.1 是估高 18.8%）：

  - 前置段 379（−41.7%）、**散文 6,417（+0.3%）**、接线节 1,096（+21.8%）、固定小段 2,779（+15.8%）、
    围栏 70 行（−22.2%）；
  - **偏差最小的那一项，是事前估里唯一不是「取中位」的那一项**：800 是在 7.1（十二个块 → 每节 521）
    与 6.3（四组块 → 每节 931）之间插出来的；另三项取的都是中位，偏差 41.7%／21.8%／15.8%。
    可照用的：**先插值，再取中位**——取中位是「不知道形状时的兜底」，而插值至少把形状对照过一遍；
  - **前置段估的 650 是在同类章的下沿上再往下取**（6.4 实测 719、7.1 实测 728），而实测只有 379：
    与 6.4 那次「分项写对之后 0.77 成了双重打折」同族——**先取中位，再看本章有没有少写一整块**
    （本章的前置知识 5 条全部只有一句话，7.1 的 5 条各带一句展开）；
  - **7.1 那条「围栏越散，压掉的散文越多」没有复现**：本章 62 行围栏散在八节里，而散文一节不短
    （6,417 对估 6,400）。两次实测方向相反，说明那条判据缺一个前提——**它压的是「该节要讲的机制叙述」
    那部分，前提是那部分本来就长**；本章每节的机制本来就短、读数多，所以散落的围栏没有可压的东西。
    前提写进本条：下一次估之前要连着问「这一节的机制叙述有多长」，而不只是问「围栏散不散」；
  - 计划值 12,000 未改（89% 落在 ±40% 容差内），而**这一章的事前估不必标「补记」**：
    `audit_coverage.py` 的 `_ESTIMATE_DEBT` 只收 6.3 与 7.1 两章，新章再漏就是一次报错。

- **第三十个实测（`7.3`）：全书第一次「事前估偏低」，而它把「插值」这条判据的边界顶了出来——插值的两个端点必须与本章**同形**；同时它证明了一件事：把一项估价压到最低，不等于把它的不确定性压掉。**

  7.3 的事前估同样落在动笔之前（写在 `tools/audit_coverage.py` 的 7.3 注释里，与正文同一趟进提交门），
  分项之和 **10,200**；实测 **14,075**（汉字 12,680 ＋ 93 行围栏）＝ 对计划 10,000 的 **141%**
  ——**超出 ±40% 容差**，所以计划值改为 **14,000**（第 7 篇 42,000 → **46,000**、全书 792,500 → **796,500**）：

  - 前置段 823（估 600，+37.2%）、**散文 7 节 8,954（估 5,250，+70.6%）**、接线节 1,405（估 900，+56.1%）、
    固定小段 2,893（估 2,400，+20.5%）、围栏 93 行（估 70 行，+32.9%）；
  - **四项全都偏低，而翻转方向的根因在插值的两个端点**：散文系数 750 是在 7.2 的单节中位 **659**
    与 6.3 的每节 **931** 之间插出来的（同样是「先插值、再取中位」），而**这两个样本都是「读数多、散文少」的形状**
    ——本章的散文主体是十来处「**只看一半会怎样**」的对照叙述（同文本换温度的 diff、同一个用户问六次的桶、
    同一 1% 偏差在五个样本量上的 p、同一组偷看在五种频率下的假阳率），单节有效字落到 **1,279**。
    可照用的：**先问「这一章有多少处只看一半就错的对照」，再决定插值的两个端点**——
    **插值只有在端点与本章同形时才比取中位准**（前半条是 7.2 的第二十九个实测、后半条是这一条）；
  - **压到最低的那一项错得最多**：前置段是本章唯一一项**故意取下沿**的（三个实测 379／719／728 的均值 609
    再往下取 600），而它偏差 +37.2%。它的体量由**开几条前置知识、导读要交代几层分工**决定，
    而这两件事在动笔前估不准：**把一项估价压低，不等于把它的不确定性压掉**——下沿只是一个更小的数，
    不是一个更准的数（与 7.2 那条「前置段估的 650 是下沿而实测只有 379」互为镜像：
    **下沿本身没有方向**，它往哪边偏取决于这一章是不是多写了一块）；
  - **一处偏差不能用系数解释**：固定小段估的是「按 7.2 的 20 条坑**减两条**」，实写是 22 条。
    「估的动作与写的动作方向相反」这一类，系数层与折扣层都吸收不了，只能记在台账里
    ——同上一条纪律：「估偏了」与「写超了」要分开；
  - **折扣层这一次把方向折反了**：事前记了两个备查的数——报 **10,200**（不折）与按第 7 篇已定稿两章的
    合计比值 0.776 折 ≈ **7,900**，而实测 14,075 比「不折」那个还高 38%。0.776 来自两章
    「读数多、散文少」的样本，本身就带着与本章不同形的偏差：**折扣层能吸收系统性的估高，
    吸收不了形状不同**——它只该用在「分项层已经按同类章取过中位、而仍需一个总体修正」的场合。

- **第三十二个实测（`7.5`）：第 7 篇估得最松的一章（估高 23.6%），而它的价值在于它把前面四条判据各自推到了边界上——它证明了两件事：一条判据给什么就用什么（「这一章特殊」不是加价项），以及一个折扣系数要到四章才稳下来（而那恰好说明它在两章时不能用）。**

  7.5 的事前估同样落在动笔之前（写在 `tools/audit_coverage.py` 的 7.5 注释里，与正文同一趟进提交门），
  分项之和 **11,305**；实测 **8,701**（汉字 7,756 ＋ 63 行围栏）＝ 对计划 11,500 的 **76%**
  ——落在 ±40% 容差内，**计划值一次都不用改**，对分项之和 ＝ **估高 23.6%**：

  - 前置段 **667**（估 550，**+21.3%**）、散文 6 节 4,459（估 5,280，−15.5%）、接线节 911（估 1,200，−24.1%）、
    固定小段 2,595（估 3,000，−13.5%）、围栏 63 行（估 85 行，−25.9%）——**五项里四项估高**，
    是全书第二次「分项同时估高」（第一次是 5.4）；
  - **固定小段那一项把 7.4 的新判据验到 −3.0%**：判据说「不按坑的条数推算，直接取实测中位」
    （1,910／2,384／2,572／2,779／2,893／3,508 的中位 **2,676**），而实测 2,595。
    **误差全部来自我在判据之上又加的那一层**（「本章带篇收尾，再上到 3,000」）。
    可照用的修正：**判据给什么就用什么**——想给某章加价时，先问「这一章与判据那些样本不同在哪里」；
    答不上来就是手感，答得上就是把那个差别写进判据；
  - **前置段第四次偏差超过 20%，而方向第二次翻转**（7.2 −41.7% ／ 7.3 +37.2% ／ 7.4 −37.9% ／ 本章 +21.3%）。
    4.4 那条「它是整套式子里唯一一项方向不定的」到此才说得清：它的体量由「导读写几段」与
    「学习目标写几条」决定，而这两件事在动笔前只是「打算」——**下沿本身没有方向**（7.3 那条）
    与「总是估高」（7.4 那条）都不对，正确的说法是**这一项估不准**；
  - **散文系数第一次「插值后再下压」，而下压过头了**（单节实测 **743**）：7.4 的实测
    （估 950、实测 864）说明低段被估高，于是本章从线性给的 980 下压到 880。
    方向对了、量又过了。可照用的：**下压的幅度取「上一章实测与插值之差」的一半**
    （7.4 那一次的差是 −86，本章照它压 −100，多压了 14 而实测又低 137）；
    更根本的一层是**这一章的主体是表**：六组读数里四组是表，而算式里的散文系数管的是叙述
    ——估这类章时，「表有多少行」应单独占一项（2.5／2.6 两条早已说过这一点，这是第三次）
  - **四章的合计比值是 0.983**（7.2–7.5 实测 44,688 ÷ 分项之和 45,455）——
    **事前估在合计上几乎是准的，而这个「准」来自四次偏差互相抵消**
    （估高 8.9% ＋ 估低 27.5% ＋ 估高 7.7% ＋ 估高 23.6%）。
    把 7.2／7.3／7.4 留下的折扣层线索连起来看：**两章 0.776 → 三章 0.955 → 四章 0.983**
    ——**它到四章才稳下来，而这恰好说明它在两章时是不能用的**（7.3 那条的端点）。

- **第三十一个实测（`7.4`）：第 7 篇估得最准的一章，而它准的方式与前三条都不同——「取中位」的那两项偏差最大，因为它们不由公式决定，由「当场写了多少条东西」决定；同时它给「插值」加了一个前提：数不出来就别插。**

  7.4 的事前估同样落在动笔之前（写在 `tools/audit_coverage.py` 的 7.4 注释里，与正文同一趟进提交门），
  分项之和 **12,250**；实测 **11,310**（汉字 10,050 ＋ 84 行围栏）＝ 对计划 12,000 的 **94%**
  ——落在 ±40% 容差内，**计划值一次都不用改**，对分项之和 ＝ **估高 7.7%**：

  - 前置段 435（估 700，**−37.9%**）、散文 7 节 6,045（估 6,650，−9.1%）、接线节 1,322（估 1,100，+20.2%）、
    固定小段 3,508（估 2,600，**+34.9%**）、围栏 84 行（估 80 行，+5.0%）；
  - **偏差最大的两项恰好是「取中位」的两项**（接线节、固定小段），而它们不由公式决定：
    接线节交了 13 条契约、固定小段写了 24 条坑——**它们的量由「当场写了多少条东西」决定**。
    可照用的修正：**固定小段不要按「坑的条数」推算**（估的动作与写的动作方向相反——估 21 条、写 24 条，
    与 7.3 同形），直接取近三章的**实测中位数**（1,910／2,779／2,893／3,508 → 中位 2,836）；
  - **前置段第三次成为估得最高的那一项**（7.2 −41.7% ／ 7.3 +37.2% ／ 本章 −37.9%：三次里两次大幅高估）。
    它的体量不由「惯性」决定，而由两件事定：**导读写几段**，以及**前置知识里有几条是「别处已经写过」**
    ——本章 6 条里有 4 条是后者（「这一条 3.8 写过，本章不重复」），所以只要 435 字。
    这与 7.2 那条（下沿 650 而实测 379）合起来指向同一句话：**前置段是整套式子里最不能靠惯性估的一项**；
  - **插值这一次是估高，它给判据加了一个前提**：散文系数 950 插在 7.2 的单节 **802** 与 7.3 的单节 **1,279**
    之间，而实测单节 **864**。差别不在「端点同不同形」而在**动笔前那个数根本数不出来**：
    7.3 留下的判据是「先问这一章有多少处『只看一半就错』的对照」——本章动笔前只是**估**了 5–6 处，
    实写集中在 7.4.3／7.4.4 两节。**数不出来就别插值，直接取下沿**（802），这是这条判据的补全；
  - 与前三条合起来看这条曲线：估高 8.9%（7.2）→ 估低 27.5%（7.3）→ 估高 7.7%（7.4），
    而三章的计划值只改过一次。**事前估的价值不在「准」，在于每一次偏差都能追到一项上**——
    四项里哪一项偏、偏在哪，是下一章的输入。

- **第三十三个实测（`8.1`）：全书第一次把分项之和估到 ±5% 以内（估高 1.6%），而它准的原因不是估得更细，是**把四条系数全部照着同类章实测的中位取**——7.5 那条「判据给什么就用什么」的第一次受益者；同时它把「前置段」那一项推到了一个新的说法上：它是整套式子里唯一一项五次没准过、且方向翻了三次的。**

  8.1 的事前估同样落在动笔之前（写在 `tools/audit_coverage.py` 的 8.1 注释里，与正文同一趟进提交门），
  分项之和 **10,595**；实测 **10,434**（汉字 9,084 ＋ 90 行围栏）＝ 对计划 11,000 的 **95%**
  ——落在 ±40% 容差内，**计划值一次都不用改**，对分项之和 ＝ **估高 1.6%**：

  - 前置段 **900**（估 650，**+38.5%**）、散文 6 节 5,930（估 4,980，+19.1%）、接线节 1,173（估 1,200，−2.3%）、
    固定小段 2,383（估 2,595，−8.2%）、围栏 90 行（估 78 行，+15.4%）——**五项里四项落在 ±20% 以内**，
    而唯一超出的那一项（前置段）恰好是这五章里唯一一次方向与上一章相反的；
  - **准的那一半可以归因**：这一章的四个系数**没有一个是我自己造的**——散文系数取四个最近实测单节
    （802／1,279／864／743）的中位略下（830），接线节取四章中位（1,209 → 1,200），**固定小段照 7.4 的判据
    原样取七章实测中位（2,595）、不在它之上加任何「这一章特殊」的价**，围栏取中位偏上。
    7.5 的结果是「判据给 2,676 而我上到 3,000，误差全部来自那一层」，本章把那层去掉，于是分项之和落进 1.6%。
    **这一条是本章最有用的东西**：事前估要准，先得有**同类章实测中位**这份存货，而它只能靠一章一章攒——
    第 7 篇五章＋第 6 篇四章攒出来的那几十个数，到本章才开始同时支撑四项；
  - **固定小段那条判据第二次被验**（7.5 −3.0%、本章 −8.2%）：两次都取实测中位，两次都估高一点，
    而两次的偏差都远小于「按坑的条数推算」那一版的 +34.9%（7.4）——**判据可以照用了**；
  - **前置段是五次里唯一没准过的一项**（7.2 −41.7% ／ 7.3 +37.2% ／ 7.4 −37.9% ／ 7.5 +21.3% ／ 本章 **+38.5%**），
    而且方向第三次翻转。五章连起来只能得到一句话：**这一项估不准**——它的体量由「导读写几段」「学习目标写几条」
    决定，而这两件事在动笔前只是打算。本章的 +38.5% 来自一个新情况：**它是新篇的开篇章**，导读里多一段
    「第 8 篇在全书里的位置」；那条差别当时**写进了估的依据里**（注释里写明了「本章是新篇的开篇章，故取 650 而不取 551」），
    但**量还是不够**——可照用的修正：**开篇章的前置段直接取同类章实测的高位一侧（本章实测 900 落在 667–1,000 区间），
    因为「新篇要交代位置」这件事的体量本身就不小**；
  - **散文系数取中位这一次落在 +19.1%**（单节实测 **988**，四个参考单节 802／1,279／864／743）：
    方向是对的（不插值、取中位），而误差来自一个本章才显形的东西——**这六节是「机制 ＋ 读数」混着写的**，
    而前四章要么偏机制（7.3 单节 1,279）要么偏表（7.5 单节 743）。可照用的：**数对照的那一步要多问一句
    「这一章每节里贴几组读数」**——它决定了单节落在 800 还是 1,200 那一头；
  - **折扣层这一次偏到另一边去了**：事前记的备查数是按第 7 篇**五章**合计比值 0.912 折出来的 ≈ **9,700**，
    而实测 10,434 比它高 **7.6%**。把本章加进去，**六章的合计比值是 0.9235**
    （63,262 ÷ 68,500）：**两章 0.776 → 三章 0.955 → 四章 0.947 → 五章 0.912 → 六章 0.923**。
    它没有收敛，而是在 0.9 上下摆，**摆的幅度与它要吸收的偏差同量级**——所以本章照旧只把它记成备查值。
    三条连起来看（7.3 的端点、7.5 的「判据之上加价」、本章的「照着中位取」）：**分项层做得越实，折扣层就越不该出手**。

  反过来，**超出并不是统一件坏事**：先问「这章是不是装了两章的内容」（→ 归位），
还是「这章本来就是这个体量」（→ 改规划值并记原因）。判据是「少掉任何一节就不完整」。

### 8.8 单章审阅清单（一章写完必须过一遍）

机械校验只能保证**形式**一致，下面四项必须人看：

1. **台账反向回查**：从 [`LEDGER.md`](LEDGER.md) 逐条看本章条目，确认落点真实存在
   （不是「填了 ✅ 但正文其实没写」）。素材覆盖的每个知识点都要能指到具体小节。
   台账里的 `⏭`（有意省略）要读一遍：**省略是内容最容易被静默丢掉的地方**。
   落点能否解析到真实小节已机检（`lint_book.py` 的台账落点回查与交叉引用回查），
   但**「落点对得上」不等于「内容真在那」**——第 1 篇统稿抽样就抓到一例
   （文件名唯一性标在 1.15.10，实际写在 1.15.13）。所以每章仍要抽 2–3 条人看。
2. **声称的输出必须实跑**：带 `# 输出` 的代码块，逐个实际跑一遍再把真实输出粘回去。
   1.5 的审阅就是这么查出两处错的：「产品公告（10 字）」实际是 8 字，
   综合示例里 `len(chunk)` 写作 17 而实际是 16、`repr` 截断位数也对不上。
   这类错误渲染出来没有任何异常，只有跑一遍才会暴露。
   做法：把章内 `` ```python `` 块逐字抽到临时目录执行，与紧随其后的 `` ```text ``
   块逐字比对（`3.2`–`3.4` 都这么做了一遍，抓出过 3.3 的一处「表里写第 8 步、
   实际第 3 步」）。
   **但要注意它的盲区**：逐字比对只验证「输出对不对」，验证不了「代码里有没有多余的东西」。
   `3.4` 的两个死变量（一段里 `base` 声明了从不使用、另一段里 `seen = set()` 从不引用）
   就是这样活过了输出比对——**输出逐字一致，代码却不像教材**。
   补法：抽出的脚本再让静态检查扫一遍未使用的名字（`ruff --select F401,F841`），
   或人读一下「每个变量都在后面被用到了吗」。
   **2026-09-17 起，这一项的大半已经没人可归了**：只要那段输出前面标了命令
   （` ```text $ python scripts/x.py --offline `），`tools/check_runnable.py` 的第八道
   就会重跑它并逐行比——当天标了 28 个块（`4.5`五、`5.3`六、`5.4`五、`5.5`三、
   `5.7`六、`5.8`二、`5.9`一），每个块的引用行都只被一条命令输出解释得了。
   剩下这一项真正要用人的地方是三处，写在这里以免被当成「已经查过了」：
   ① **贴全还是节选**：机器只判「引用的行找得到」，判不了「该给全量却只给了两行」；
   ② **不标命令的那些块**（模型输出、模板、演示「把文档改坏之后的报错」）——
   它们本来就不可能靠重跑复现，只能人跑一遍；
   ③ **输出一致而代码不像教材**（上面那两个死变量就是这一类）。
   这一道当天就响过一次：5.9 那句引用的「其中夹具 23 条是本脚本实有」，
   在脚本长到 27 条之后它还是 23——**写在围栏里的真输出，也会有“看起来永像真的”的数字**。
   **真机读数要跑两遍，且只拿「不变量」立论。** `4.5.7` 初稿写的是一次运行的读数
   （模型调度者连派五次 `researcher`、五次写出同一段 95 字），读起来像个结论；
   同一条命令再跑一遍，**绕圈的角色换成了 `writer`、五次输出从 12 字涨到 195 字**——
   凡随运行变的数都不该进正文。改后只留三个两遍都对得上的量
   （调度记录 7 条、交接 6 次、末条都是「限额用尽」），并把两遍并排列成对照表、
   明确写出「哪三件是不变量、哪两件是这一遍的偶然」。
   这与「离线当门、真机报数」是同一条纪律的另一面：**是真机读数，就得先证明它是重的**。
3. **技术结论回到官方文档**：凡是「Python 会……」「必须……」的断言，
   到 [`REFERENCES.md`](REFERENCES.md) 对应条目确认，而不是凭印象。
4. **正文里的代码是不是真的在文件里**：标注了文件路径的代码块跑一遍
   `python tools/check_runnable.py`。这一关抓的是前三关都不管的一类错——
   正文风格合规、数字对账也对，但读者照着敲报语法错（本章就抓到一行被早先的替换弄成了**两行粘连**）。
   盲区：它是「按序子集」比对，不是逐字节相同，所以**插入**不会报、**改写几个字**会报。
5. **工具的夹具必须包含「解析失败」的用例**：`check_runnable.py` 第一版把章节文件的
   **CRLF 换行**当成了正常情况，围栏正则一条也匹配不上，于是它的输出是
   「11 个块全部命中」——和「一个块都没解析至」**长得一模一样**。修法是两件事：
   夹具里加上「换行变了也要能解析」，以及一条护栏「一个块都没解析到就报错」。
   推广开：任何「遍历 + 判定」的工具，都要有一个用例问「它到底遍历到了东西吗」。
6. **偏差与判定**：有效字数偏离计划 ±40% 时，先问「是写得不够」还是「计划本就估低了」。
   若是后者，改 `tools/audit_coverage.py` 里的规划值并**记录原因**，
   再跑 `--out COVERAGE.md` 与 `--check`——不允许只改文档不改工具值。
7. **手抄的第二份数字，必须让它自己对账**。每章写完，LEDGER 里会多一张「事前估 / 实测」的
   逐节表。它有双重身份：既要**校准后面几章的估算**（`STYLE` 8.7 每条「实测」都从它来），
   又是「这一章的读数确实是在那儿量的」的证据。但它是**手抄的**——定稿阶段一动正文，
   它就停在旧测量上，而它的对账对象（正文）在别处。实测：四章四错
   （`4.5` 少 95、`5.2` 把同一行抄了两遍、`5.3` 少 1,276、`5.4` 少 768），
   而**每一张看起来都像被核过**——比没有表更坏。
   已按第二条修法接进 `--check`（按表头形状找表、逐行加起来与 `lint` 的读数比），
   同一位置还补上了 `PROJECT.md` 的「正文 N 有效字」（一加就报出六章）。
   可照用的问法：**这份数字是「算出来的」还是「抄过来的」？抄过来的那份，
   与它的源头之间有没有一条会响的路？** 没有就现在接上——本轮的教训是
   接上当天就查出十处存量错误，而它们已经静静存在了几天到两周。
   同一问法隔了一轮又抓到**第二份手写清单**：`GLOSSARY.md` 的「三、维护记录」
   是术语表的第二份账（每个新词登在哪一章、为什么这么定），此前没有任何机检——
   核了一遍，32 行日志覆盖不到 `1.12` / `2.1` / `2.6` / `5.6` 四章
   （回头查明 `1.12` 那条其实是首现章记错了），另有 `3.6` / `5.2` / `5.8`
   三处「少一个换行、一行被粘进上一行」的表格损坏（含当轮自己造的一处），
   而当时 `--self-test` 20/20、397 条禁词全过、错误 0 警告 0。
   现已接进 `lint_book.py` 的「术语日志」项（有词条的章必须有一行、每行必须三格，
   并带两个反向守）。这两个数都不参与篇幅，也不会让任何读数变难看——
   正因如此它们才需要一条会响的路。
   第三次同问出现在**正文章节里那些「文件 ＋ 行数」的表格**上（「本章加了什么」节）：
   它们既不是代码块（`check_runnable` 的第一道只比代码行），也不是汇总数字
   （`audit_coverage --check` 只比总数）——**两个门中间的一条缝**。
   接上的当天在四章查出 **14 格已漂**（`4.2` 四格、`4.3` 六格、`5.6` 两格、`5.7` 一格、
   `5.8` 一格，其中 `4.3` 的 `scripts/agent_tools.py` 已从 216 涨到 301）。
   现已在两侧各接一条：**树那一侧**由 `scripts/recap.py` 的第四条对账管（文档里标了
   `N 行` 的文件必须真的那么多行），**书这一侧**由 `check_runnable.py` 的第六道管
   （表格里的行数格逐格与盘上比，两边都带反向守与一条「改一行就红」的夹具）。
   两处的识别规则都必须**窄得写得出形状**：第二格写着「14 条用例」「6 份」「24 条」的
   那些格不算行数——规则放宽一格，假红就会让人把整条检查关掉。

   第四次同问落在**「这个脚本自己有几条夹具／那个测试文件有几条用例」**上。
   这类数字的难处与前三处不同：它有一个**唯一的主语**，所以可以写进文档的任何位置，
   而「改脚本的人」跟「改文档的人」往往不是同一个脑子里的同一件事。
   现已接进 `recap.py` 的第五条（夹具数在进程内数 `fixture_cases()`；用例数当时是数文件里的
   `def test_`，后来换成了跑起来数），接上的当天在架构文档里咬到两处。而**它咬到的第一处是我自己写的正文**：
   我在讲这条检查时引了一句历史（「抓到一处八条夹具」），脚本不知道那是在引用，当场报红。
   于是多了一条约定——**阿拉伯数字就是断言，引用旧数字写成汉字（八条夹具）**。
   这条修法值得记住的地方不在汉字，而在它的方向：
   **碰到「检查咬了合法内容」，先想的是给那类内容一个看得见的出口，而不是把检查放宽或
   把历史数字改好看**——不然写历史的人只剩两个选择：改旧数字，或把整条检查关掉。

   第五次同问是**把第四处搬回书这一侧**：那类「N 条夹具」「N 条用例」不只在树里的
   文档上，正文章节里也写了很多（三棵树合计 34 处）。接法是把两个数都变成**跑出来的**：
   夹具数逐格跑一次那个脚本的 `--self-test`；用例数不是另起一趟，而是取**自检那一趟
   真调起来几条**——与「树内测试 N 条通过」同一把尺子。**树那一侧随后也换成了同一把**
   （`recap.py` 第五条：导入那个测试模块、数 `dir()` 里的 `test_`，不再数文件里的 `def test_`），
   于是两侧数的是同一件事，不用再指望「两个恰好一致」——两侧都有同一条反向守：
   导入失败时退回数文件，两把尺子不一样时另报一行。这一改动的理由是一条实测：
   全部 22 个测试文件上，两把尺子（实跑调起来的条数、数文件里的 `def test_`）量出的数
   一模一样，但拿不到实跑值时仍留数文件那条退路，且两者不一样时会另报一行——
   **差异本身是一条信息**（那个文件里的用例不是顶层 `def test_`），不该被一个数盖掉。
   而这一轮真正抓到的东西在检查自己的肚子里，两条都是同族：
   ① 两批夹具（行数格九条、条数格九条）是 `cases += [...]` 加进去的，而那条跑夹具的
   循环**写在它们前面**——摆着、没人迭代，于是十八条断言一条都没执行过，
   而分母把它们算成了通过（`--self-test` 一直报的就是这个虚数）；
   ② 那个分母本身是手写的算式 `len(cases) + len(counts) + 4 * len(SPECS)`，
   而每棵树的固定检查其实有六处外加逐个离线入口——它从来没人核过。
   两处一起修：断言过一次记一次（分母由计数器长出来，不再算），
   打印时把拆分项也印出来，末项用减法得出，让它**必然**等于总数。
   教训不在数学，而在一句可照用的话：**「加进清单」不等于「跑过」，
   写进总数也不等于「通过了」**——一个报「N/N 通过」的检查，必须先能证明 N 是谁数的。

   第六次同问不是「某个数字漂了」，而是**尺子本身少看了一半**——而它一直是绿的。
   给输出块加的标注约定（```text $ python …）在围栏信息串里多了五个字符，
   而 `check_runnable.fences_of` 当时用的是非贪婪正则`\n```(\w*)\n(.*?)````：
   带命令标注的那类围栏**匹配不到**，于是它的开围栏被当成了上一块的闭围栏，
   从那一刻起这一篇里开与闭全部反了。错位的后果不是报错而是**少扫**：
   实测第 6 篇三章里 13 个带路径标注的代码块只扫到 5 个，而输出里照旧印着
   「5 个标注了路径的代码块，全部命中」——**「全部命中」这四个字在少扫一半时是假绿**，
   而少掉的那些块里就包括 6.3 新写的五个（它们看起来都「过了」，因为它们压根没被看过）。
   它与 2026-09-17 那次 `lint_book._FENCE` 是同一族（**改一处体例，量它的那把尺子要一起改**），
   只不过那把尺子长在另一个工具里，所以它多躲了一天。修法是把配对改成**按行扫**
   （开、闭各判一次，不再用正则去配两个 ```），并补上夹具与三条反向守。
   而修复当天就报出了真东西：**3.2 里有三块贴着一份已经被重建过的 `client.py`**。
   块 6 与块 9 只差 7 行与 2 行（缩过的文档字符串、`dataclass, field`、`RETRYABLE` 多一档、
   `from_env` 多五行），而块 10 差一半以上——它引的 `# app/api/article.py` **在 v3 树里根本不存在**
   （`get_current_user`／`ArticleService` 全库零命中）。三处的修法不同，因此值得分开记：
   前两处是**照着树重抄**（引文必须逐行真在文件里，这本来就是第一道的承诺）；
   第三处不是重抄而是**重剪**：它引的那段接线已经不在树里了，于是切成树里真有的
   `http_transport` ＋ 两个方言各自的 `_structured`（顺便把「同一件事写两遍而定级只切一层」
   这个结论落到围栏里），并把紧随其后的两句话（响应里那三个字段从哪来）
   从「那个端点」改到 `Reply` 与 3.5 的 `app/api/agent.py`；交付物表里那一行也一并换成树里真有的。
   同一天还接上两条同一族的尺子（都是第五次同问的延伸，只是主语换了）：
   **① 条数格认中文数字与「个」**——表里写着「七个用例」「十三个用例」，
   而旧正则只认阿拉伯数字 `\d+\s*条`，于是这批格**一格都没被核过**：
   当天报出四处漂（3.2 七→十三、3.3 六→七、3.4 四→五、3.5 九→十）。
   它的边界也是当天摸出来的：**中文的「一」是量词不是条数**（「里有一条用例把这件事钉死了」
   在 5.4 出现了两次，差点变成两条假红），所以 `一` 不算格，而 `1 条用例` 仍是断言。
   **② 围栏里「跑了哪些测试、通过多少」**（`# N passed` 与 `# 全树 N 条`）成为第九道：
   主语只认一个测试文件或「全树」，数可以写在下一行（3.4 就是那个写法），
   但主语只在同一个围栏内继承。接上当天在 3.1–3.8 报出**七处漂**——
   全树在 3.6／3.7／3.8 写着 89、89、122 而实测已是 **150**，
   单文件在 3.2 写 7（实测 13）、3.3 写 6（实 7）、3.4 写 4（实 5）、3.5 两处写 9（实 10）。
   这七处的共同点是：**它们都在一句命令的注释里，而那句命令从来没人真跑过就抄了下来**。
   可照用的一句话：**凡是「读者照着敲就会看到数字」的地方，那个数字都是断言**——
   表格里的要核，命令行注释里的也要核，后者更容易被当成「说明文字」。

### 8.9 素材归属的三个易错点

- **一份素材被多章共用时要按权重分记，不能整份重复计入**。`fa 04-05`（路由 /
  请求与响应）既给 1.8 的 HTTP 协议部分用，又给 1.9 的 FastAPI 用法用；两边都记 1.0
  会让两章都显得比实际充裕。写法：`("fa", "04-05", 0.45)` 与 `("fa", "04-05", 1.0)`。
- **权重的小数位必须够「反算」**：工具算素材量用的是 `int(素材总量 × 权重)`
  （截断，非四舍五入），所以权重取到「能让它正好等于逐节量出的汉字数」的那一位。
  两位小数看着干净，却会造成**同一件事在注释与表格里写成两个数**：`5.3` 的注释写
  451 ＋ 197（逐节量出），两位小数权重乘回却是 828——而读者只会看到后者。
  量出实测值后，按 `target / 总量` 向上试到能复现的一位（例：`632 / 25,444`
  在 4 位小数下无解，取 `0.02484`）。反算得了，才说明这个权重是量的不是估的；
  **反算不回去的旧值（0.6 ＋ 0.4 ＋ 0.3 ＝ 1.3 那种）本身就是错**。
- **一个素材目录里装着多章的内容时，要按文件拆开引用**。`fa 02` 的 8 篇横跨三章
  （应用配置 / 生命周期 / 中间件与依赖注入）；整目录计入 1.9，就会把 1.11、1.14 的
  素材算给 1.9。写法：`02/01-05`（该目录下按文件名排序的第 1–5 篇），其余篇另行引用。
  完整性检查已能识别子文件级引用，不会误报「未分配」。
- **题库按考点编，不按章编——不要把一个分章整块记给一章**。散文素材常常是「一份手册
  属于一章」，沿用这个默认去记题库就会错：题库自带的分章（「提示工程与链式推理（112道）」）
  会把 5 个计划章的内容装在一节里。实测 3.2：整章 38,156 汉字，真正属它的只有 15,430。
  正确做法：按题库自己的**编号重置**（`### 1.` 重新出现处）切段，必要时再按题切，
  逐组登记去向；切完后做两个校验——**各章增量之和是否恰好等于旧值**
  （只换归属，不多不少）、**同一文件各章权重之和是否 ≤ 1.0**。
- **入库判定不看权重，看「章内真实用到多少」**。同一条规则在散文与题库上都成立：
  权重是从**实测汉字数反算**出来的，不是拍出来的；反算不回去就说明它还是估的。

### 8.10 台账口径的两个易错点

- **不要把「首格以代码写法开头」的行当成图例行**：像「`*args` / `**kwargs`」
  「`global` 的使用」这类知识点，首个单元格本身就是代码写法。误删它们会让台账
  **看起来条目变少**，而这恰恰是台账要防的事。图例行只有一种：首个单元格恰好是一个状态符号。
- **章号必须锚定在行的第一个单元格**才去匹配。校验器曾被「1.8 那行的素材比值恰好是
  1.10」骗过一次，报出「第 1.10 章状态不对」这种看起来很真、实际是碰撞的误报。
  规则：凡是用章号/编号在表格里定位，都写成 `^\|\s*<编号>\s*\|`。
- **「正文已存在但状态仍是 `⬜`」必须报错**：这是「写完了没记账」的情形，
  而没记账的章在汇总里看起来就像没写。台账篇级进度表的四个数字也由脚本按表头对账。
- **汇总表不参与统计**：[`LEDGER.md`](LEDGER.md) 末尾的篇级进度表里的数字是结论，
  若把它一起计入，就会重复计数。`lint_book.py` 已按此处理。

---

## 九、交付前自检清单

- [ ] 十个固定小节齐全、顺序正确、标题用词一致
- [ ] 「学习目标」可验证（能讲出动词，不是「了解」「理解」）
- [ ] 所有代码块有语言标识，示例代码实际跑通过
- [ ] 术语首次出现给了中英文对照，之后未混用
- [ ] 中英文之间有空格，中文标点全角
- [ ] 交叉引用写的是章节号
- [ ] 「面试视角」与本篇题库挂钩，至少 3 个考点
- [ ] 「延伸阅读」含至少 2 条官方文档，且都注明"用到了哪部分"
- [ ] 所有链接跑过 `python tools/check_refs.py --only-broken`，无失效无跳转
- [ ] 涉及 API 的段落写明了版本与访问日期
- [ ] 讲机制的小节给出了原始论文，不是只引二手教程
- [ ] 「素材溯源」指向真实存在的 `sources/` 文件与页码
- [ ] **第 3 篇专属**：本章有「知舟接线」小节，四问齐全，且符合 [`PROJECT.md`](PROJECT.md) 的硬约定
      （手写不引框架、三个单点入口、密钥边界、不破坏第 1、2 篇成果）
- [ ] 跑过 `python tools/lint_book.py --only <章号>`，**零错误**
- [ ] [`LEDGER.md`](LEDGER.md) 本章条目已回填，（无 `⬜`）
- [ ] 本章有效字数达计划值 60% 以上，且与 [`PLAN.md`](PLAN.md) 的状态列一致
- [ ] 通读一遍：没有「众所周知」「笔者」这类词
