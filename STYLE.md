# 写作体例与文风规范（STYLE.md）

本文件是全书**统一文风的唯一依据**。所有章节写作、修订都以本文件为准；出现冲突时，先改规范再改正文。

---

## 一、读者与语气

| 项目 | 规定 |
| --- | --- |
| 目标读者 | 计算机相关专业在校生、转行求职者；会一点编程，但知识不成体系 |
| 人称 | 统一用 **「你」**。禁止「读者」「同学们」「我们大家」 |
| 语气 | 直接、平等、就事论事。可以断言，不要绕弯子 |
| 禁止句式 | 「众所周知」「显而易见」「不难发现」「笔者」「在教学实践中我们发现」 |
| 允许的幽默 | 少量、克制，出现在「常见坑」小节；技术正文保持严肃 |

**示例**

- ✅ 好：「`Depends` 会在每次请求时执行，这是它和普通函数调用的最大区别。」
- ❌ 差：「众所周知，众所周知 `Depends` 是一个非常好用的功能，笔者认为……」

---

## 二、每章固定结构（十段式）

每章必须包含以下小节，**顺序固定、标题固定**，便于横向对比与检索：

```markdown
# 3.6 工具调用与函数编排

> **本章导读**：（2–3 句，说明这一章解决什么问题、学完能做什么）

## 学习目标
- [ ] 能独立定义并注册一个可被模型调用的工具
- [ ] 能读懂工具调用的请求/响应报文
- [ ] 能定位工具调用失败的 4 类常见原因

## 前置知识
（列出需要先掌握的章节号；无则写「无」）

## 3.6.1 为什么需要工具调用
（正文；概念 → 示例 → 运行结果 → 解释）

## 3.6.2 工具的定义与注册
...

## 常见坑
（3–6 条，每条「现象 → 原因 → 修法」）

## 面试视角
（把 `sources/06-求职冲刺/` 的题库与本知识点挂钩，列出该考点的高频问法）

## 本章小结
（要点清单，5–8 条，每条一句话）

## 练习
- **基础**：……
- **进阶**：……
- **挑战**：……

## 延伸阅读

**官方文档**（本章结论的权威依据，必填）
- FastAPI · Dependencies — https://fastapi.tiangolo.com/tutorial/dependencies/ ｜ 本章依赖注入部分据此重写

**规范 / 论文**（有则写，无则省略）
- RFC 7519 · JSON Web Token — https://www.rfc-editor.org/rfc/rfc7519.html

**素材溯源**（选填，保留可追溯性）
- `sources/01-编程地基/FastAPI基础学习/16FastAPI的依赖注入/FastAPI的依赖注入.md`
```

> 例外：第 0 篇导论、求职冲刺篇的章可按内容需要裁剪「前置知识」「练习」，
> 但**必须保留**「本章导读」「学习目标」「本章小结」，以及「延伸阅读」中的**官方文档**。

---

## 三、代码规范

1. **可运行**：所有代码必须是能跑通的完整片段。写「片段」时用 `# ...省略...` 明确标注，不假装完整。
2. **标注路径**：多文件示例在代码块上方写清文件位置，例如：
   ```python
   # app/api/user.py
   ```
3. **语言标注**：所有代码块必须带语言标识（`python` / `bash` / `json` / `sql` / `text`）。
4. **环境统一**：Python **3.11+**，依赖管理统一用 `pip` + `requirements.txt`（首次出现时给 `uv` 作为可选加速方案）。
5. **风格统一**：遵循 PEP 8，变量 `snake_case`、类 `PascalCase`、常量 `UPPER_CASE`；类型注解尽量补全。
6. **输出示例**：命令与运行结果用 ` ```text ` 单独成块，并注明是「输出」。
7. **错误示范**：需要展示错误写法时，用 `# ❌ 错误` / `# ✅ 正确` 注释区分，同一个坑只示范一次。

---

## 四、语言细节

| 场景 | 规定 | 示例 |
| --- | --- | --- |
| 术语首次出现 | 中文（English） | 检索增强生成（Retrieval-Augmented Generation, RAG） |
| 术语再次出现 | 统一用中文 | RAG 中文写作「检索增强生成」或直接用缩写 RAG，不混用英文全称 |
| 中英文之间 | 加一个半角空格 | 使用 FastAPI 构建 API |
| 中文标点 | 全角 | ，。：；？！「」《》 |
| 代码/命令 | 行内用反引号 | `uvicorn app:app --reload` |
| 数字 | 阿拉伯数字 | 3 个参数、第 2 章（不写「三个参数」） |
| 单位 | 半角并与数字连写 | 10ms、2GB、128K token |
| 段落长度 | 每段 ≤ 5 行；超过就拆 | — |
| 强调 | 用 **粗体**，不用下划线 | **幂等** |
| 提示框 | 统一用引用块 + 标签 | `> ⚠️ **注意**：…` |
| 交叉引用 | 用「见第 X.Y 节」，不写「见上文」 | 见第 5.4 节 |
| 列表层级 | 最多 3 级；有序列表用于步骤 | — |

**提示框标签固定为四种**：`> 💡 **提示**`、`> ⚠️ **注意**`、`> 🔥 **常见坑**`、`> 🎯 **面试**`。

---

## 五、贯穿全书的示例项目

全书所有示例尽量收敛到**同一个项目**，让读者看着它长大，而不是每章换一个新 demo：

| 阶段 | 项目形态 | 对应篇章 |
| --- | --- | --- |
| 阶段 1 | **知舟 · 博客 API**：FastAPI + SQLAlchemy + JWT 的后端服务 | 第 1 篇 |
| 阶段 2 | 用 AI 编程工具重构/扩展它，接入工程规范 | 第 2 篇 |
| 阶段 3 | 为它加入「智能问答」能力：提示工程 + Agent 工具调用 | 第 3、4 篇 |
| 阶段 4 | 接入私有文档：RAG 检索 + 引用 + 评估 + 生产化 | 第 5 篇 |
| 阶段 5 | 打包为简历项目：架构图、难点、指标、演示 | 第 6 篇 |

项目名 **「知舟」** 可改，但**一旦定下就全书不改**。独立小例子允许存在，但必须服务于主线（先给主线代码，再补最小可运行示例）。

---

## 六、图表与素材使用

1. 能画图就不写长段落：架构图、流程图、时序图用 **Mermaid** 代码块书写（可版本管理、可渲染）。
2. 表格用于对比（框架选型、参数说明、指标对照），不用表格承载叙述。
3. 引用素材时保留溯源：正文中引用原始资料的**经验性结论**时，在「延伸阅读」的
   「素材溯源」里写明 `sources/... .md` 与 `<!-- p.N -->` 页码。
4. **不得直接大段搬运素材原文**。素材是原料，正文要重写、重组、补例子。
5. **素材不得作为技术结论的依据**。素材是一批第三方整理的学习资料，
   审计已发现其中存在过时写法（LangChain 旧版链、已关停的 Assistants API）
   与不实内容（题库答案存在模板占位）。
6. **正文不得提到「素材」「讲义」「资料包」**。素材是写作原料，读者手里没有它。
   正文里写「素材的说法是……」「讲义这里的顺序是反的」，读者读到的是**你在跟一份
   你看过、他没看过的文档对话**——他会卡住，因为这个「素材」在他的世界里不存在。

   - ❌ 差：`### 素材的顺序说明是反的` / 「素材的说法是『中间件里的异常处理器
     完全没参与』。这句话一半对……」
   - ✅ 好：`### 一个常见的错误说法` / 「中间件里抛出的异常不会回到类型匹配
     那一档，所以在这里写 401，客户端拿到的是 500。」

   写法是**直接给结论**，不要先说「某人说错了、实际是」再给结论。
   素材与正文的**逐条对照**（哪一条被纠正、纠正成什么、依据是什么）写在章末
   「素材溯源」里——那里才是它该出现的位置。
   唯一豁免是 `0.3`（使用说明章），它本身就在讲全书的资料分工。
7. **「有没有实跑过」只标注一次，且只为读者的行动服务**。
   保留：反引号里的「未实跑」说明、「本机没有 Redis 服务端，这段需在真环境验证」
   ——读者要据此决定能不能照抄。
   删除：「已实跑核对，与素材给的顺序一致」「我跑了一遍才发现」这类过程叙述；
   结论该写「实测得到 X」，而不是「我跑过一遍」。同理，正文里不要为每一段
   都盖一次验证章——同一章反复出现的核对说明，收进章末一处即可。

---

## 七、引用与出处（硬性规定）

**技术结论必须回到官方文档。** 素材可以帮助理解与找选题，但正文里每一条
"应该这样做"的结论，都要能追到官方出处。

### 优先级

```
官方文档 ＞ 规范/RFC/标准 ＞ 同行评议论文（arXiv） ＞ 厂商官方博客 ＞ 社区文章
```

社区文章（含素材库里的 PDF）只能作为「经验与踩坑」的来源，
**不得单独支撑技术结论**；引用时必须标注其性质。

### 五条硬性要求

1. **每章必填官方文档**：「延伸阅读 → 官方文档」至少 2 条，并注明该章用到哪一部分。
2. **带版本**：涉及 API 的段落写明版本，例如 `Python 3.11+`、`LangChain v1`、
   `MCP 规范 2026-07-28`、`Pydantic v2`。API 变化频繁的章节要在小节开头标一句
   "本节基于 ⟨版本⟩，截至 ⟨访问日期⟩"。
3. **带访问日期**：链接统一从 [`REFERENCES.md`](REFERENCES.md) 取，那里记录了访问日期；
   正文不自行编造链接。
4. **链接必须可校验**：新增链接后跑 `python tools/check_refs.py --only-broken`，
   出现「失效」或「跳转」必须先修 `REFERENCES.md` 再改正文。
5. **原理要有论文**：讲机制（注意力、ReAct、RAG、LoRA 等）时给出原始论文，
   不要只引二手教程。

### 禁止事项

- 禁止引用无法追溯来源的说法（"网上都说""据说"）。
- 禁止把素材里的过时写法当现状讲——遇到与官方文档不一致的地方，**以官方文档为准**，
  并在「常见坑」里说明"为什么旧写法不适用了"（这反而是很好的教学点）。
- 禁止只贴链接不做解释；每条引用要写清"这一章用到了它的哪部分"。

---

## 八、长文一致性维护规程

全书 **10 篇 68 章、约 46 万字**。这个体量下，**不允许依赖「记得」**：
凡是需要记住的东西，一律写进仓库文件，写作前读取，写完后校验。

### 8.1 六种已知失效模式

| 失效模式 | 典型表现 | 对策 |
| --- | --- | --- |
| 上下文丢失 | 前面定过的设定（术语、例子、章节号）后面忘了 | 外置到 [`PLAN.md`](PLAN.md) / [`LEDGER.md`](LEDGER.md) / [`GLOSSARY.md`](GLOSSARY.md)，写作前只读相关片段 |
| 术语漂移 | 同一概念在不同章换写法 | [`GLOSSARY.md`](GLOSSARY.md) + `lint_book.py` |
| 体例漂移 | 有的章有「前置知识」、有的没「常见坑」 | [`STYLE.md`](STYLE.md) 十段式 + `lint_book.py` |
| 前后矛盾 | 第 3 章说 A、第 7 章说非 A | 交叉引用检查 + 篇末反向核对 |
| 内容重复 | 同一段话被复制到两章 | `lint_book.py` 跨章重复检测 |
| 事实过时 | 引用了已关停的 API | [`REFERENCES.md`](REFERENCES.md) + `check_refs.py` |

### 8.2 三本单一事实来源（Single Source of Truth）

| 文件 | 管什么 | 冲突时怎么办 |
| --- | --- | --- |
| [`GLOSSARY.md`](GLOSSARY.md) | 术语的唯一写法 | 先改术语表，再全库替换正文 |
| [`REFERENCES.md`](REFERENCES.md) | 技术结论的权威出处 | 以官方文档为准，回头改正文与素材用法 |
| [`LEDGER.md`](LEDGER.md) | 每个素材知识点的落点 | 素材说了但正文没写的，必须显式标 `⏭` 并写原因 |

这三本之外的任何「我们前面说过……」都不算数——**要写成可检索的条目**。

### 8.3 每章四步闭环（不得跳步）

```bash
# 第 1 步：定位该章素材，通读并建台账（状态先标 ⬜）
python tools/audit_coverage.py --pool
# → 把知识点逐条写进 LEDGER.md，写清来源与计划落点

# 第 2 步：写作（一次只写一章；先读完本章素材，不靠回忆续写）

# 第 3 步：回填台账（✅ / 🔀 / ⏭，禁止留空）

# 第 4 步：机械校验，零错误才算定稿
python tools/lint_book.py --only <章号>
```

### 8.4 每篇收尾的三件事

1. **反向核对**：从 [`LEDGER.md`](LEDGER.md) 反查正文，该篇 `⬜` 必须清零。
   `lint_book.py` 会机械回查每条 `✅` 的落点（小节是否存在、是否指向未开写的章、
   是否标了 `✅` 却没写落点），但语义仍需**按章抽 2–3 条人看**（见 8.8 第 1 条）。
2. **术语扫描**：跑 `python tools/lint_book.py`，确认无术语违规与跨章重复。
3. **交叉引用复核**：不只看「第 X.Y 节」，还要回查**小节级**引用
   （`见 1.14.6`、`（1.12.3）`）——改编号、拆章节后最容易悬空的正是这一级。
   判定必须避开版本号：`Starlette 1.6.0`、`redis 3.5.3`、`PyJWT 2.10`、
   `Python 3.11` 在字面上与章号无法区分（实测裸扫描的 8 条命中**全是版本号**）。

### 8.5 强制机制（让检查漏不掉）

规范写在纸上会被忘，所以把它接到提交路径上：

| 层 | 文件 | 作用 |
| --- | --- | --- |
| 本机钩子 | `.githooks/pre-commit` | 每次提交先跑 `lint_book.py`；再跑 `audit_coverage.py --check` 对账文档数字；改动到 [`REFERENCES.md`](REFERENCES.md) 时再联网校验全部链接 |
| CI | `.github/workflows/book-checks.yml` | 推送与 PR 时重跑同一套校验（含钩子自检） |
| 安装/自测 | `python tools/install_hooks.py` | 安装（本仓库 `core.hooksPath`）、查看状态、`--self-test` 验证真能拦下违规提交 |

钩子的行为约定：

- **只有「错误」拦提交**；台账 `⬜` 之类的提示属警告，不阻断。
- 本地地址（`127.0.0.1` / `localhost`）不算出处，不要求登记。
- 链接校验里**只有服务器明确返回 4xx/5xx 才算失效并拦提交**；
  连接超时（已自动重试一次）与 401/403 反爬只提示、不阻断。
  把超时当作失效会让本地网络抖一下就提交不了，而这种报警器最终会被绕过。
- 紧急绕过用 `SKIP_BOOK_CHECKS=1 git commit ...`，**并在 PR 里说明原因**；CI 仍会检查。
- **新增一条机检，必须用合成反例验证它会响**。一道从不出声的检查与「一切正常」
  在输出上完全一样；台账落点/交叉引用两道回查就是靠反例集（悬空小节、错落点、
  版本号干扰项）才确认真的生效、且不会把版本号当成章号报。
- **同时要验证它「不乱响」**。只测正向会让规则越管越宽：本轮加「正文不得指代素材」
  与「不得写核对过程」两道检查时，同一个坑踩了两次——
  `1.6.0`/`2.10` 这类版本号与章号字面一致（上一条），
  而禁用词「据说」是合法复合词「判据说」的子串。
  结论：**每条检查都要配一组「应当沉默」的反例**，至少包括
  同形干扰项（版本号）、被包含的子串（判据说）、以及被豁免的区间（素材溯源区、0.3）。
- **凡是「脚本按文件名/章节号做归集」的地方，都要配一道“漏网”守卫。**
  `audit_coverage.py` 曾以 `(\d+\.\d+)-[^\s]*\.md` 解析章文件名，
  而 `2.2-Claude Code 实战.md` 含一个空格，于是整章**被静默跳过**：
  不计入实测总数、也不算“已完成”，而所有报表数字仍然自洽。
  这正是最危险的一类错：**没有任何一处看起来奇怪**。
  所以守卫必须用**比解析器更宽**的写法（否则它只能永远沉默），
  且固定放在提交前校验里：`uncounted_chapter_files()`（见 `tools/audit_coverage.py`）。
- **正文里贴出的代码块与它旁边的输出，必须来自同一次运行。**（`2.4`）
  那章的审读清单脚本里，三条规则根本不可能出声（`http://` 里的 `//` 被当注释剥掉、
  `@ts-ignore` 本身就是注释、未使用的 import 自己匹配自己），
  而旁边的输出却是对的——那是另一版跑出来的。**读代码看不出来，读输出也看不出来。**
  所以：贴输出前先在正文的代码上重跑一次；改了代码就重跑输出，改了输出就重跑代码。
  配得上的做法是把**输入也写进正文**（三份夹具不足 60 行，但它是输出可核查的前提）。
  这条目前只能靠人执行：机器校验需要“提取片内的脚本 → 重建夹具目录 → 执行 → 比对输出”
  一连串动作，代价明显高于它目前能拦住的问题——真要做时，
  合理的最小形式是只认**带 `__main__` 驱动且前面列了夹具路径**的代码块，
  并且构不出完整沙箱时必须沉默（同 8.5 的「不乱响」）。
- **每章收尾，拿素材的节标题反向对一遍台账。**（`2.2`/`2.3`）
  两章的骨架都对得上素材，但素材 §7（IDE 三种模式 / 五个动作 / 截图流程）在 **2.3 的台账里连一行都没有**：
  既没落点也没标 `⏭`，于是台账、数字对账、lint 三处都不会响——**没有任何数字与它对不上**。
  两道回查（机械解析落点标题 + 人工抽样语义）都建立在「这条已被登记」的前提上，
  **没登记的内容它们看不见**。所以加一步反向核对：

  ```bash
  # 列出素材的顶层节标题（编号 + 标题），逐条问：它在台账里是哪一行？
  python - <<'PY'
  import re, pathlib
  p = pathlib.Path("sources/02-AI编程工具链/Codex从入门到实战.md")
  for i, line in enumerate(p.read_text(encoding="utf-8").splitlines(), 1):
      s = line.strip().replace("\u200b", "")
      if re.match(r"^\d{1,2}\.\s*\S", s) and len(s) < 46 and not re.search(r"[；。，、）（)】」]$", s):
          print(i, s)
  PY
  ```

  配套两条硬要求：
  ① **每章的「素材溯源」必须写出用到的节号区间与未用节号的去向**
  （如「第 1–8、10–14、17–20 节；第 9 节归 2.5，第 15、16 节归 2.5/2.6」）——
  写出区间后缺口就是可枚举的，这比「已覆盖」这种结论句可核对；
  ② **台账里的 `✅` 必须是可核对的东西**：要么是一个落点，要么是一个区间。
  写「散落在各节」等于没写——它无法核对，而不可核对的表述在账上看起来与「已完成」一模一样。
  ③ **`⏭` 必须在接收端也留一行。**（`2.5`）向别的章「有意省略、去向已登记」只是**发出去**，
  没有任何机制确认它在目的地真的落地：2.1 台账里写着 `⏭ 属「上下文工程」`（把「根目录只做地图」推给 2.5），
  而 2.5 写完后它**既不在正文、也不在 2.5 台账里**——两边各自看上去都完整，两个 ⬜ 计数也都是 0。
  所以「去向章」开写时，反向扫描要多问一句「有哪些 `⏭` 指向我这里」：

  ```bash
  # 把 2.5 换成要开写的章号，看有哪些省略指向本章
  grep -n "⏭.*2\.5\|⏭.*归 2\.5" LEDGER.md
  ```
  （`2.2` 的「提示词库 ✅ 散落在各节」当时就是这种情况：读者其实一段都抄不到。）

- **改稿前，先查「还有谁引用了它」：`python tools/trace_refs.py`。**（本版新增）
  改一个术语、一个数字或一句结论之前，先问一句「它在别处还出现过吗」。
  这不是 `grep` 能回答的，至少三件小事都不行：

  ```bash
  python tools/trace_refs.py "上下文工程"                # 术语：命中位置逐行列出
  python tools/trace_refs.py "26,091" --from 2.2          # 数字：26,091 / 26091 / 全角都算同一个
  python tools/trace_refs.py "真正的成本从写代码转移到了验证代码" --fuzzy   # 结论被改写的地方
  ```

  ① **正文是硬换行的，`grep` 看不见接缝。** 一个句子常常被切成两行，
  `grep "会话、第 6.2 节"` 命不中而读者看得见。实测全书有 **480 处**这类被换行切断的句子
  （另有 **1,225 次**接缝合并），工具把它们接回一行再搜、再把命中映射回原始行号；
  ② **同一个数字有多种排版**（`26,091` / `26091` / `１，４５０`），而 `45` 又不该命中 `1,450`；
  ③ **要改的不止正文**——`PLAN.md`、`COVERAGE.md`、`LEDGER.md`、`GLOSSARY.md`、
  `REFERENCES.md` 里通常也各记了一份，工具默认一并扫、单独成节列出。

  两个用法提醒：加 `--from <你正在改的章>`，输出会把该章标成【本处】，
  结尾直接给出「除本处外还要回看的章」；`--md` 的输出可贴进台账或 PR。
  命中若指向某术语，工具还会顺手核对 `GLOSSARY.md` 登记的首现章与正文是否一致
  （`上下文工程` 登记在 2.5，而 2.1 早已用中英对照引入了它——这类漂移人不会去看）。

  **近似匹配（`--fuzzy`）的门限是量出来的，不是拍的**：同义改写 0.19–0.61，
  无关段落 ≤ 0.09。标定时连着否掉两种做法——只按「命中词元 / 查询词元」算，
  查询写长一点分数就跌（0.33 → 0.21）；只按 Dice 算，本段很长时分数也跌（0.61 → 0.22）。
  两者在长度上朝**相反方向**出偏，所以取平均，并把两个分项都打印出来给人看。
  与 8.5 的一贯要求一致：**改打分方式必须重新测一组正例与反例**，
  否则「调低了阈值」看起来与「找得更全」没有区别。

> ⚠️ **注意**：钩子脚本无扩展名，必须保持 LF 换行（[`.gitattributes`](.gitattributes) 已强制）。
> CRLF 的 shebang 会让它在 Linux / macOS / CI 上直接报错。

### 8.6 提交粒度

**一章一提交**，提交信息带章号（例如 `feat: 1.1 环境搭建`）。
这样任何一次「不对劲」都能用 `git diff` 定位到具体章节，也能单独回滚。

### 8.7 篇幅口径：有效字数

技术章节的代码本身就是内容。只数汉字会出现两种错判：代码密集的章被当作偷工减料，
或者为了凑汉字把代码改写成大白话。所以统一按**有效字数**与 [`PLAN.md`](PLAN.md) 的计划比：

```text
有效字数 = 汉字数 + 15 × 有效代码行数        # 一行有效代码 ≈ 15 个汉字的信息量
```

- 数字一律取自 `python tools/lint_book.py` 的输出，**不手工估算**（[`PLAN.md`](PLAN.md)
  的「状态」列、[`LEDGER.md`](LEDGER.md) 的篇级进度表都以此为准）。
- 容忍偏差 ±40%，超出即警告。
- 不要用「放宽容差」代替折算：放宽容差会把真问题一起放过。

**怎么事前估一章的字数：按**小节数与代码密度**估，不按素材量估。**
两个方向的错都犯过：

| 错向 | 例子 | 现象 |
| --- | --- | --- |
| 素材多 → 以为该多写 | 1.9 初稿 22,341（186%） | 其实是把「统一响应格式」「表单上传」也堆进来了，应当**把内容归位** |
| 素材少 → 以为该少写 | 1.10 计划 6,000，实写 11,807（197%） | 素材为零只说明「要自己写」，不说明「写得少」：Pydantic v2 的完整面貌就是十节的量 |

**代码行项不是零头，代码密集的章里它甚至占一半。** `1.11` 估 13,000、实写 20,589
（158%），就是估时只数了小节（13 节），没估代码：实际 732 行 × 15 = 10,980，占实测的 53%。

**代码行有个可以照用的先验区间**：凡是带「综合示例」的章，代码实际落在
**540–813 行**（中位约 640，15 × 640 ≈ 9,600，约占实测的一半），
而凭感觉估往往只会给 400 行——同一条错连犯三次：`1.11` 732 行、`1.12` 624 行、`1.13` 580 行，
三次都估少了。所以**估代码时先问「有没有综合示例」**，有就按 600 行起估，
不要按「几个短示例」的数法算。反过来，没有综合示例的概念性章节（如 0.x、3.x）
代码常在 100 行以内，不能搬这个区间。

**估法有效的唯一一次，正好是把代码行项算进去的那一次**：`1.14` 813 行、`1.15` 540 行。
`1.15` 事前按「14 小节 × 800 + 675 行代码」估 21,000，实写 20,599（98%）——
第一次落在 ±10% 内。区别不在于估得准，而在于**估的时候就把代码行写进了算式**
（前三次是事后才发现漏了）。所以这条估法的操作要点只有一句：
**先把代码行数估出来写进式子，再估散文**。
反过来说，**「超出容差」先看代码占比，再看是不是装了两章**——三种超标的成因完全不同：

| 成因 | 例子 | 处置 |
| --- | --- | --- |
| 把别的章的内容堆进来 | 1.9（186%） | **归位**到讲它的章，删重复示例 |
| 素材为零但本身体量大 | 1.10（197%） | 按实测**校准计划值**并记原因 |
| 代码密集，估时漏算代码行 | 1.11（158%） | 同样校准，但**原因要写成估法错**，避免当成体量错 |

**第五次低估，错法也一样，但换了个马甲：模板密集章。** `2.2` 事前按
「12 小节 × 约 800 + 固定段 2,200 + 约 150 行命令/模板 × 15」估 11,000，
实写 **20,805（189%）**。两个错向同时发生：代码行项估 150、实际 **324 行**
（工单、提示词、配置每个都是 10–30 行，且**它们就是正文内容**，不是附录）；
散文项估约 8,800、实际 **15,945 汉字**，多出来的几乎全是素材没有的原创
（权限模式全集、沙箱平台限制与两个开关、会话恢复的“不还原”清单）。
所以对**以模板/命令为主体的章**，估法要单独调参：先把模板逐个数出来估行数，
再估散文——不要用纯叙述章的「小节数 × 800」。

**第六次低估，两个错向各自加了一层。** `2.3` 事前按 8.7 式子估 13,000（11 小节：
9 长 × 900 + 2 短 × 450 + 约 205 行配置与命令 × 15 + 固定段 1,200），实写 **16,856（130%）**。
这次**不是**「装了两章」——归位在动笔前就做完了（闭环与工单归 2.2、规则文件归 2.5）。
低估集中在一处（散文），代码项反而估准了：

| 项 | 事前估 | 实测 | 评价 |
| --- | ---: | ---: | --- |
| 代码 / 配置 / 命令 | 205 行（≈3,075） | 228 行（≈3,420） | **估准了**（差 +11%）：模板密集章的代码项逐个数过之后再估，比前几次凭感觉的 150 行靠谱得多 |
| 散文（含固定段） | 约 9,925 | 13,436 | 少估约 3,500 汉字。其中两块：决策表与工具对照表**各占一整节**（不是附录）；实跑那一节从「一句带过」长成了一节 |

第二条尤其值得单独记：**实跑不是“验证已写内容”，它本身是产出。**
那一节原计划只写「用 `codex apply` 拉回本地」，而把命令真跑一遍之后，
多出来的是「非零退出码不等于没改动」「撤销要从 `HEAD` 而不是从索引」这类
**只有跑了才知道、而且读者最需要**的内容。所以估篇幅时，
对含实跑演示的章要额外留一节的量，而不是把实跑当成写作的开销。

**第七次低估，错向只剩一个：散文项。**（`2.5`）事前按「模板逐个 340 行 × 15) ≈ 5,100
＋ 散文 12 小节 × 约 850 ≈ 10,200」估 15,000，实写 **19,352（129%）**。
这次代码项反而**估高了**——逐个数模板这条自 2.2 之后已经稳定；
多出来的全部在散文，而且源头很集中：

| 项 | 事前估 | 实测 | 评价 |
| --- | ---: | ---: | --- |
| 代码 / 模板 / 配置 | 340 行（≈5,100） | 306 行（≈4,590） | **估高了 10%**：模板逐个数的估值比凭感觉稳定 |
| 散文（含固定段） | 约 9,900 | 14,762 | 少估约 4,900 汉字。多出来的是**四张对照表**（权限分层、两份规则文件、`.claude/rules` 与 Codex `.rules`、三层机制）——它们各占半节，却不在「小节数 × 850」的视野里 |

**估算式里没有「对照表」这个项。** 一张 8 行 × 4 列的对照表，写进正文顶得上 400–600 汉字，
而且它往往是那一节真正值钱的东西（读者带得走的就是这张表）。
所以对以「概念对照」为主体的章，套参数时要把**表的张数与行数**单独加一项：
`字数 ≈ 小节数 × 800~1,100 + 15 × 代码行 + 约 120 × 表格行数`。

可用的粗估式：`字数 ≈ 小节数 × 800~1,100 + 15 × 预估代码行`。素材为零的篇
（第 3、4、6–8 篇）尤其要按这个式子估，否则会把整篇的进度判断做得偏低。
反过来，**超出并不是统一件坏事**：先问「这章是不是装了两章的内容」（→ 归位），
还是「这章本来就是这个体量」（→ 改规划值并记原因）。判据是「少掉任何一节就不完整」。

### 8.8 单章审阅清单（一章写完必须过一遍）

机械校验只能保证**形式**一致，下面四项必须人看：

1. **台账反向回查**：从 [`LEDGER.md`](LEDGER.md) 逐条看本章条目，确认落点真实存在
   （不是「填了 ✅ 但正文其实没写」）。素材覆盖的每个知识点都要能指到具体小节。
   台账里的 `⏭`（有意省略）要读一遍：**省略是内容最容易被静默丢掉的地方**。
   落点能否解析到真实小节已机检（`lint_book.py` 的台账落点回查与交叉引用回查），
   但**「落点对得上」不等于「内容真在那」**——第 1 篇统稿抽样就抓到一例
   （文件名唯一性标在 1.15.10，实际写在 1.15.13）。所以每章仍要抽 2–3 条人看。
2. **声称的输出必须实跑**：带 `# 输出` 的代码块，逐个实际跑一遍再把真实输出粘回去。
   1.5 的审阅就是这么查出两处错的：「产品公告（10 字）」实际是 8 字，
   综合示例里 `len(chunk)` 写作 17 而实际是 16、`repr` 截断位数也对不上。
   这类错误渲染出来没有任何异常，只有跑一遍才会暴露。
3. **技术结论回到官方文档**：凡是「Python 会……」「必须……」的断言，
   到 [`REFERENCES.md`](REFERENCES.md) 对应条目确认，而不是凭印象。
4. **偏差与判定**：有效字数偏离计划 ±40% 时，先问「是写得不够」还是「计划本就估低了」。
   若是后者，改 `tools/audit_coverage.py` 里的规划值并**记录原因**，
   再跑 `--out COVERAGE.md` 与 `--check`——不允许只改文档不改工具值。

### 8.9 素材归属的两个易错点

- **一份素材被多章共用时要按权重分记，不能整份重复计入**。`fa 04-05`（路由 /
  请求与响应）既给 1.8 的 HTTP 协议部分用，又给 1.9 的 FastAPI 用法用；两边都记 1.0
  会让两章都显得比实际充裕。写法：`("fa", "04-05", 0.45)` 与 `("fa", "04-05", 1.0)`。
- **一个素材目录里装着多章的内容时，要按文件拆开引用**。`fa 02` 的 8 篇横跨三章
  （应用配置 / 生命周期 / 中间件与依赖注入）；整目录计入 1.9，就会把 1.11、1.14 的
  素材算给 1.9。写法：`02/01-05`（该目录下按文件名排序的第 1–5 篇），其余篇另行引用。
  完整性检查已能识别子文件级引用，不会误报「未分配」。

### 8.10 台账口径的两个易错点

- **不要把「首格以代码写法开头」的行当成图例行**：像「`*args` / `**kwargs`」
  「`global` 的使用」这类知识点，首个单元格本身就是代码写法。误删它们会让台账
  **看起来条目变少**，而这恰恰是台账要防的事。图例行只有一种：首个单元格恰好是一个状态符号。
- **章号必须锚定在行的第一个单元格**才去匹配。校验器曾被「1.8 那行的素材比值恰好是
  1.10」骗过一次，报出「第 1.10 章状态不对」这种看起来很真、实际是碰撞的误报。
  规则：凡是用章号/编号在表格里定位，都写成 `^\|\s*<编号>\s*\|`。
- **「正文已存在但状态仍是 `⬜`」必须报错**：这是「写完了没记账」的情形，
  而没记账的章在汇总里看起来就像没写。台账篇级进度表的四个数字也由脚本按表头对账。
- **汇总表不参与统计**：[`LEDGER.md`](LEDGER.md) 末尾的篇级进度表里的数字是结论，
  若把它一起计入，就会重复计数。`lint_book.py` 已按此处理。

---

## 九、交付前自检清单

- [ ] 十个固定小节齐全、顺序正确、标题用词一致
- [ ] 「学习目标」可验证（能讲出动词，不是「了解」「理解」）
- [ ] 所有代码块有语言标识，示例代码实际跑通过
- [ ] 术语首次出现给了中英文对照，之后未混用
- [ ] 中英文之间有空格，中文标点全角
- [ ] 交叉引用写的是章节号
- [ ] 「面试视角」与本篇题库挂钩，至少 3 个考点
- [ ] 「延伸阅读」含至少 2 条官方文档，且都注明"用到了哪部分"
- [ ] 所有链接跑过 `python tools/check_refs.py --only-broken`，无失效无跳转
- [ ] 涉及 API 的段落写明了版本与访问日期
- [ ] 讲机制的小节给出了原始论文，不是只引二手教程
- [ ] 「素材溯源」指向真实存在的 `sources/` 文件与页码
- [ ] 跑过 `python tools/lint_book.py --only <章号>`，**零错误**
- [ ] [`LEDGER.md`](LEDGER.md) 本章条目已回填，（无 `⬜`）
- [ ] 本章有效字数达计划值 60% 以上，且与 [`PLAN.md`](PLAN.md) 的状态列一致
- [ ] 通读一遍：没有「众所周知」「笔者」这类词
