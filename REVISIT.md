# 通读修订记录（跨篇一致性 · 口径 · 重复）

> 这是**横向**那一遍。`STYLE.md` 第 8.8 节管的是「单章写得对不对」，
> 第 8.4 节管的是「一篇收口时落点/引用/术语齐不齐」——两件都不是把全书摆在一起看的。
> 同一句话在两篇里各写了一遍、同一个元素被写成了两种形状、同一个东西被写成两个数，
> 只有横着读才看得见。
>
> 复算：`python tools/revisit.py --check`（下面那个记录块与盘上逐行比）。

---

## 一、三条检测与怎么读它们的输出

| 检测 | 看什么 | 命令 | 输出性质 |
| --- | --- | --- | --- |
| **体例形状普查** | 十段式里「标题固定」的四个元素实际被写成了几种形状，外加「常见坑」条数分布 | `python tools/revisit.py --shapes` | **普查**（不是判定：形状没有对错，但分裂必须有据可查） |
| **跨篇重复段** | 跨章最长的公共散文片段（≥26 汉字、汉字占比 ≥70%，排除 URL 与台账句） | `python tools/revisit.py --dupes` | **候选**（本次 17 条，需人判） |
| **同一东西两个数** | 同一个「单位 ＋ 紧跟的名词」在全书里出现过 ≥2 个值 | `python tools/revisit.py --facts` | **候选**（本次 702 条，绝大多数是量词在不同句式里的正常取值） |

形状普查这一份会被 `--check` 逐行核，所以它同时是一处「书改了、记录没跟就红」的地方：

```text
导读 ｜ H2 69
常见坑 ｜ 列表 67 ｜ 表格 2
练习 ｜ 三级标签 69
延伸阅读 ｜ 分组 39 ｜ 平铺 30
坑条数（第二节：至少 3 条、不设上限）｜ 15 条以上 33 ｜ 3–6 条 12 ｜ 7–14 条 24
```

（上面这一份是 **2026-09-22 定形之后**的普查：导读与练习各只剩一种形状。改之前它长这样——
`导读 ｜ 引用块 53 ｜ H2 16`、`练习 ｜ 三级标签 42 ｜ 编号 27`、
`坑条数（STYLE 二 写的是 3–6 条）`，三处都在第三节里说了怎么处置。）

**读普查表的规矩**：形状不同的那两组章号要能各给出一个理由。给不出理由的，就是体例漂移。
而**普查与判据必须是同一把尺子**：这一轮就吃过这个亏——普查当时问的是「`**基础**` 四个字在不在这节里」，
所以 1.9／1.10 的「分组小标题 ＋ 连续编号」被它算成「三级标签」；等形状检查（问的是「每一条自己带标签吗」）
上线，那四章当场变红。现在两处用同一个口径（`tools/revisit.py` 的 `shape_of`）。

---

## 二、逐条清单（5 条，按建议的优先级排）

### ①「延伸阅读」：30 章没写「**官方文档**」分组，而这一栏是「结论的权威依据」的落点 —— ⬜ **未动（下一件）**

- **现状**：分组 39 章 ｜ 平铺 30 章（第 7 篇 5 章全平铺、第 8 篇 5 章全平铺、10.1–10.3 等）。
- **判据**：`STYLE.md` 第二节的模板里，「**官方文档**（本章结论的权威依据，**必填**）」是固定一栏；
  第 74 行的例外条款也只允许裁剪「前置知识」「练习」，并明写延伸阅读里的**官方文档**必须保留。
  平铺的那些章**确实引了官方文档**（7.2 的 Langfuse／OpenTelemetry、8.4 的 PostgreSQL／Stripe／MDN、
  10.1 的 Amazon／UF／UT Dallas），只是没有分组小标题——所以问题不是「缺引用」，是
  **读者分不出哪一条是权威依据、哪一条是补充**。
- **建议**：统一成三栏（官方文档 / 规范·论文 / 素材溯源），成本 = 30 章各加一行小标题（纯排版，不动内容）。
  这是四条体例漂移里唯一**动到内容可读性**的一条，所以排第一。

### ②「常见坑」：2 章用三列表格，67 章用列表 —— ✅ **已改文档（2026-09-22）**

- **现状**：列表 67 ｜ 表格 **2.3、2.4**（2.3 的表格 18 行数据、2.4 同形）。
- **判据**：`STYLE.md` 第二节写的是「（3–6 条，每条『现象 → 原因 → 修法』）」——**它规定的是每条的内部形状**，
  没说容器是列表还是表格。2.3／2.4 是「工具用法」两章，坑本身是三列对照，表格反而更清楚。
- **处置**：**改文档，不改章**——第二节已写明「容器是列表或表头固定为『现象 ｜ 真实原因 ｜ 修法』
  的三列表格都行」，所以 2.3／2.4 两张表从「没有依据的例外」变成「照体例写的一种容器」。
  形状检查**不拦容器**（拦了反而会把这两章的好写法逼回列表）；需要拦的是「没有三件套的坑」。

### ③「本章导读」：16 章写成 `## 本章导读`，53 章写成引用块 —— ✅ **已定形（2026-09-22）：一律 `## 本章导读` 小节**

- **现状（改之前）**：引用块 53 ｜ H2 **16**（3.9、3.10、4.1–4.5、5.1–5.9）。
- **裁定**：认下小节形（选 **(b)**）。两条理由可核对：① 那 16 章的导读普遍长到多段 ＋ 编号列表，
  写进引用块之后标题在块内、层级消失；② **改形状不动篇幅数**——`> **本章导读**：` 与 `## 本章导读`
  的汉字数相同（那四个字从引用块挪进了标题，`：` 不是汉字），所以 53 章的 8.7 实测一个都没动。
- **动作**：53 章改成小节（正文直接跟在标题下，多段的分段）；第二节的模板与「标题固定」那条同时改掉。
- **守**：`lint_book.py` 的 `chapter_shape_issues` —— 再写成引用块就报错（有夹具）。

### ④「练习」：27 章用编号列表，42 章用「基础 / 进阶 / 挑战」三级标签 —— ✅ **已定形（2026-09-22）：一律三级标签**

- **现状（改之前）**：三级标签 42 ｜ 编号 27（3.1、3.2、3.10、5.3–5.9 等）；而编号那一组里还有**两种**写法——
  一种把难度内联在题里（7.1–7.3、8.1–8.3 的 `1. **（基础）** …`），一种完全不带（3.1、5.x、10.x）。
- **裁定**：认下三级标签。编号不携带难度，于是**「这一章的练习能不能当难度梯度用」得先看它属于哪一组**。
  级别判据写进第二节，别人能照它复核：**基础**＝照本章的树、脚本或清单跑一遍、报一个数；
  **进阶**＝新写一处东西（夹具、判据、数据行、读数项）；**挑战**＝自己定一条线或一张表（检查单、验收线、锚点），
  或跑真机、压测这类**可能推翻现有结论**的动作。
- **动作**：27 章改齐——6 章（7.1–7.3、8.1–8.3）只把内联的 `（基础）` 挪到条目上；
  21 章逐题判级别并**按 基础 → 进阶 → 挑战 重排**（同一级内保持原先后）。
  另加四章（形状检查上线后才抓出来）：1.9／1.10 的「分组小标题 ＋ 连续编号」改成逐条标签，
  2.5／2.6 的第四级名字「综合」并入「挑战」。
- **代价**：条目上每条 ＋2 汉字，共 **+238**；19 章的 8.7 实测因此各漂一次，逐条补进【事后校准 2026-09-22】。
- **守**：`lint_book.py` 的 `chapter_shape_issues` —— 有一条不带标签、顺序不是 基础→进阶→挑战、
  或缺「基础」「进阶」任一级，都报错（有夹具）。

### ⑤「常见坑条数」：`STYLE` 写的 3–6 条，全书实际是 7–29 条 —— ✅ **已改文档（2026-09-22）**

- **现状**：3–6 条 **12 章** ｜ 7–14 条 **24 章** ｜ 15 条以上 **33 章**（最多 8.2 的 29 条）。
- **判据**：这不是「写超了」，而是**那行文档过期了**——`STYLE` 8.7 的估算判据一直把坑条数当输入
  （「先数条数，再按 32–37 字/条估」），第 19／20 个实测还专门修过「要不要举例」的系数。
  也就是说：**正文按「有那么多坑就写那么多」写，而文档还停在早期的 3–6 条。**
- **处置**：改文档不改章——第二节已把「3–6 条」换成「**至少 3 条、不设上限**，每条「现象 → 原因 → 修法」」，
  而条数交给形状普查继续盯着（普查那行的小标题也从「STYLE 二 写的是 3–6 条」改成现在的口径）。
  这本书的坑大多带判据或读数，越工程越多，这是它该有的样子。

---

## 三、查过、判定「没问题」的（写明判据，免得下次重查）

| 查什么 | 怎么查 | 结论 |
| --- | --- | --- |
| **跨篇同一结论的复述** | `trace_refs.py "<一句话>" --fuzzy --top 8` 两次：「离线入口不许读 `.env`、不许联网」（3.10 起）与「估算值不许当账用」（3.1 起） | **一致**：各篇都以「这是『离线当门、真机报数』在本篇的形态」收尾，再各加一句本篇的额外约束（8.2 多「不许调 GitHub API」、8.3 多「不许起浏览器」）——是**同一条纪律的分篇落地**，不是两种说法 |
| **17 条跨篇重复段**（`--dupes`） | 逐条看 | **全部有意**：相邻章的纪律句（「不改前四棵树」「注意别改断言」）、练习模板（10.5–10.7 的「各找出本书对应的那一节」）、章末「素材处置」句。**没有一条是「同一结论在两篇里各写一遍」** |
| **「N 棵树」「不改前 N 棵树」**（`--facts` 里最大的那一族） | 该族候选 + 逐句看 | **一致**：树随篇建，所以 6.x 说「前三棵／第四棵」、7.x 说「前四棵／第五棵」、8.x 说「前五棵／第六棵」、10.x 说「六棵树」——**相对指称随篇号变**，正是体例 |
| **术语漂移** | `lint_book.py`（697 条禁止写法，0 违规）＋ `INDEX.md` 的 14 条零命中裁定 | **已守**：这一层不需要通读 |
| **章节与计划 1:1** | `load_plan_chapters()` 与盘上文件求两个方向的差集 | **69 / 69，两向都空** |
| **汇总数字** | `totals.py --check`（69 章 / 852,539 字 / 20 类站点 100 处；定形那一轮把练习改成三级标签后 **+238 字**，重生成过） | **已守** |

---

## 四、这一轮暴露的共同形状

四条体例漂移（①②③④）是**同一个形状**：**门只认关键字，不认形状。**
`lint_book.py` 的 `REQUIRED_ALWAYS` 里有「本章导读」，于是「引用块」与「`## 本章导读`」都算通过；
「延伸阅读」同理——只要这四个字在，里面怎么排都行。

所以下一层要做的是**形状检查**，而顺序不能反：
**先定形（①～④各选一种）→ 再改章 → 最后把选定的形状写成 `lint_book` 的一条错误路。**
现在就加门的话它立刻变红（现状本身就是分裂的）；而在书稿上「先改章、后定形」，又会换一种形状。

`tools/revisit.py` 与形状检查是两层，现在**两层都进门了**：

- **普查那一层**（`revisit.py --check`）：抢「改了某一章的形状而这份记录没跟」
  （钩子 4g/5 与 CI 各跑一次，与 `index_book.py --check` 同一个形状）；
- **判据那一层**（`lint_book.py` 的 `chapter_shape_issues`）：导读与练习的**对错**由它判
  （钩子第一关；十一个夹具）。③④定形当天就搬进去了，而它一上线又抓出四章第三种写法——
  **在普查与判据用同一把尺子之前，「已统一」与「还有第三种」确实可以同时为真**。

---

## 五、下一步（按成本排）

1. ✅ ③④ 两道选择题已定形，并已改齐（含形状检查上线后抓出的另外四章）；
2. ✅ ②⑤ 已改 `STYLE`（不改章）；
3. ⬜ ① 30 章补「**官方文档**」分组（机械，可批量做）——它是四条里唯一动到可读性的一条；
4. ⬜ 沿着同一句追问再查一遍：还有哪些「标题固定」的元素其实只被关键字守着
   （导读与练习现在有形状检查了，而「延伸阅读」那三栏还是关键字）。

> 通读的结论不是「书里有几处错」，而是**「同一件事在这本书里有几种写法」**——
> 这一遍看下来，答案是有四种，而且每一种都能追到一句没有写下来的当时的理由。
> 定形之后只剩一种，而**「只剩一种」这件事本身得由一道检查说出来**，不能靠读着像。
