"""v7 工程化版：**前四棵树一直在回答「怎么把它做成」，这一棵开始回答「怎么知道它还行」。**

前四棵树共 45 章做的事，全都可以用一句话检查「跑起来了没有」：循环有没有转、
检索有没有召回、账单对不对得上。而**上线之后真正会出事的那一类**，是「跑起来了，
但悄悄变差了」——换了一版提示词、升了一个模型、扩了一段语料，四棵树里没有任何一层
能自己回答「比上周好还是差」。这一层（评测流水线、可观测、版本化、安全、合规）
就是本树的增量，它与前四棵树的边界如下：

| 树 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| v3／v4／v5 | 应用逻辑（循环、编排、检索、生成） | 怎么判断它变没变差 |
| v6 | 模型那一层（选型、协议、网关、计费） | 同上 |
| **v7** | **「怎么知道它还行」**：数据集、指标、门、报告 | 不重写应用逻辑，也不重复模型那一层 |

三条从 7.1 起就定下来，后面四章都按它长：

1. **离线当门、真机报数**（3.10 起的纪律，6.1 起在本篇之外也成立）：
   本树第一版的读数**不调模型、不要密钥、不联网**——评测流水线量的是
   「数据、指标、门」这三样东西自己的性质，它们全都是算得出来的；
2. **读数要么能复算，要么别写进正文**：离线跑的那份「系统记录」是**造出来的**，
   所以它能量指标体系与门，**量不了模型质量**——这一条写在正文的边界里；
3. **不下结论的那几档要有名字**：`empty` / `underpowered` / `too_coarse`
   （`app/gate.py`）——它们与「通过」长得不一样，是这个模块存在的理由。

7.1 把这一层分成三块，边界写出来它们才不会长到一起：

| 模块 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| `suite` | **数据集**：唯一性、污染、指纹、抽样 | 不知道指标是什么 |
| `metrics` | **指标注册表**：方向、需要的输入、测不了返回 `None` | 不知道门在哪 |
| `gate` | **门**：基线、抖动带、分辨率、三档不下结论、条目级 diff | 不做聚合（那是 `metrics` 的事） |

7.2 加的是**另一条腿**：7.1 的三块量的是**离线判据**（这一批题上好不好），而
出了事要看的是**在线现场**（刚才那一次请求里谁慢、谁贵、谁错了）。两者共用的东西
只有一样——**同一条 trace_id 与同一套属性名**，所以那一根线本身也要写成模块：

| 模块 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| `trace` | **现场**：span 树、自耗时与缺口、语义约定缺口、关联、采样、分位 | 不算钱（钱要价表，那是 `board` 的事） |
| `board` | **看板与告警**：四栏账、覆盖率、认哪一层、阈值与窗口 | 不决定「这条 trace 长什么样」（那是埋点的事） |

7.3 加的是**第三块和第四块**。前两块回答「它现在怎么样」，而真正会出事的那一类是
「**刚才改了点什么**」——同一版提示词可以今天好明天坏，于是「比上周好还是差」这个
问题里先得能回答「这是哪一版」（7.2 的看板按维度分组时，缺的正是「版本」这一维）：

| 模块 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| `registry` | **版本与配置**：内容戳、三条线的 diff、标签与发布、回滚、实验台账 | 不知道谁看到了哪一版（那是分桶的事） |
| `rollout` | **分桶与放量**：哈希分桶、留缝、命名空间互斥、比例失衡、阶梯、偷看 | 不存文本（版本住在注册表里） |

这一对与前四块同样**没有 import 关系**：注册表可以在没有任何流量的项目里用
（它的第一批用户是「把提示词存进 Git」的团队），而分桶可以在没有版本表的项目里用
（它的第一批用户是灰度开关）。两者只在一件事上合起来：**“产线上跑的是哪一版”**
这个句子需要一个内容戳（注册表给）与一个分桶结论（分桶给）才能填完。

7.4 加的是**第五块和第六块**，而它们与前四块的关系是**反向的**：前三章都在问
「它还行吗」，这一对回答的是「**有人想让它不行的时候，谁会先拦住**」。
3.8 已经给过原理（威胁模型、四条处理纪律、权限预算、红队样例集），
这两块接的是它的落地——**且共用一份带标签的样例集**：没有它，
「我把规则调严了」与「我把规则调坏了」在报表上一模一样。

| 模块 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| `guard` | **输入与输出侧的判定**：四档规则、两笔账（拦截率与误杀率）、来源分级、每条规则自己的收益 | 不决定谁有权限做写操作（那是 `policy` 的事） |
| `policy` | **工具权限**：令牌与 scope、数据面、写操作审批、审计链、供应链检查 | 不判断一句话是不是攻击（那是 `guard` 的事） |

这两块与前三对的分工可以用一句话说清：**`guard` 报的是「像不像坏话」，`policy` 报的是「他到底被允许做什么」**。所以本章的读数里那三条穿得过四档的越狱（`A08`／`A09`）
不靠更严的规则收尾，而是靠输出侧复检与权限边界——**规则拦不住意图，但边界拦得住后果**。

7.5 加的是**最后一块**，它回答的问题与前四对都不同：前四对问「它还行吗」与
「谁会拦住」，这一块问「**它留下的东西，我们能不能说清楚**」。它与 1.14（脱敏的
最小实现）、7.2（内容捕获默认关）、7.4（审计链）、5.8（语料戳）的分工是：
那四处各给了一条原理或一个开关，这一块给的是**三张能对账的表**（留存、删除、审计自查）。

| 模块 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| `privacy` | **留存与脱敏**：六类数据的期限与「谁来执行它」、四种脱敏各自除掉了什么、识别码的两笔账、删除的传播范围、内容捕获的开／关、审计记录自查 | 不重做 7.4 的判定（那一层管「该不该拦」）与 5.3 的索引过滤（那一层管「该不该召回」） |

三块（`suite` / `metrics` / `gate`）、两块（`trace` / `board`）、两块（`registry` / `rollout`）、
两块（`guard` / `policy`）、一块（`privacy`）之间**没有 import 关系**，这不是巧合：离线判据在没有流量的
日子也要能跑，看板在没有评测集的项目里也得能用，安全判定在没有版本表的项目里
照样要能拦，而留存表要在前面八块全部跑过的项目里才对得上账
——它们各自量的是**同一件事的不同侧面**，合起来才是一句话的完整性。
"""
