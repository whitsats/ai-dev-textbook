"""v6 网关版：**前三棵树一直在用一个模型，这一棵开始把模型当成可选的东西**。

前三棵树共 41 章都在回答「怎么把一件事做成」，而它们从头到尾只调用同一个模型：
`.env` 里的那一个。真的上线时没人这么干——同一个应用里往往同时挂着三四个模型，
问一句简单的话走便宜的那个，写一段草稿走贵的那个，批量任务走半价的通道。
**这一层（选型、接入、网关、计费）是前三篇一句都没讲过的**，也正是本树的增量。

与前三棵树的边界（`PROJECT.md` 的硬约定在这里同样成立）：

| 树 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| v3／v4／v5 | 应用逻辑（循环、编排、检索、生成） | 用哪个模型、花多少钱 |
| **v6** | **模型那一层的四个问题**：选哪个、怎么接、怎么路由、怎么算钱 | 不重写应用逻辑；**故意不引框架**（前三棵树已经把「框架替你做什么」讲透了） |

三件事从 6.1 起就定下来，后面三章都按它长：

1. **离线当门、真机报数**（3.10 起的纪律）：本树第一版的所有读数都是**算出来的**，
   不需要密钥、不需要网络，所以它在提交门里能一直跑；
2. **口径必须进表**：每条价格都带 `as_of` 与 `source`，**没有出处的数字不进表**；
3. **能断言的关系都要断言**：供应商自己写下的乘法关系（写 1.25×、读 0.1×、批量 0.5×）
   是这张表的第一道防线——抄错一个数，`violations()` 会说话。

6.2 又把这一层分成两半，**边界要写出来否则它们会长到一起**：

| 模块 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| `registry` / `select`（6.1） | 选谁、这一次多少钱 | 不碰线上的形状 |
| **`wire`**（6.2） | **中立形状与三家的翻译表**（哪一栏落在哪） | 不发请求、不知道路由 |
| **`dialects`**（6.2） | **三个适配器**：载荷怎么长、答复怎么解、流怎么拼 | 不做重试与降级（那是 6.3） |

所以 6.2 的两个模块里**没有一行 `requests`**：它们只把「一次调用」变成三份字典、
把三份答复变回一个对象。这样「重试与降级」（6.3）接在适配器外面，而不是长在三个适配器里面。

6.3 又把这一层分成两半，而分界线是**判据**：

| 模块 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| `wire` / `dialects`（6.2） | 中立形状、翻译表、三个适配器 | 不发请求、不知道路由 |
| **`gateway`**（6.3） | **出错之后怎么办**：分档、退避、预算、熔断 | 不知道名单上有谁、不排序 |
| **`router`**（6.3） | **名单怎么配、按什么顺序、哪一个能走** | 不知道错误码、不重试 |

`gateway` 的判据是**错误码**，`router` 的判据是**这一次请求的形状**。
把它们合成一个模块也能跑，而那样做的第一个后果是「哪些错值得重试」与
「名单上谁能接这一次请求」变成同一张表里的两种行——它们迟早会互相污染：
一次「超窗」的修法恰好是「换一个窗口更大的候选」，于是路由规则会从错误码长出来，
而错误码会开始带上候选的名字。

6.4 又把这一层分成两半，而分界线是**钱**：

| 模块 | 它管的那一层 | 它不管什么 |
| --- | --- | --- |
| `registry` / `select`（6.1） | **这一单多少钱**（一笔账） | 不管钱花在哪一栏 |
| `wire` / `dialects`（6.2） | 形状与翻译表 | 不发请求、不算钱 |
| `gateway` / `router`（6.3） | 出错之后怎么办、名单怎么配 | 不记账、不看总量 |
| **`meter`**（6.4） | **钱花在哪一栏、怎么降下来、怎么盯住** | 不选型、不发请求、不重试 |

6.1 与 6.4 的分界线要写清：前者算的是**一笔账**（`call_cost()` 返回一个总数），
后者算的是**一页账**（`bill()` 返回四栏）与**一批账**（`board()` 与三道闸）。
两个都在算钱，而那正是它们必须分开的理由：**「这一单多少钱」与「钱花在哪一栏」
是两个问题，合在一起时后一个会被前一个吃掉**——只会算总数的人看得到 42.8 美元，
看得到「哪一栏该动手」的人才知道下周能把它变成 30。
"""
