# 术语表（GLOSSARY.md）

> **本文件是全书术语的唯一事实来源。** 同一概念在全书只有一种首选写法；
> 新术语必须**先登记在这里、再写进正文**。校验由 `python tools/lint_book.py` 完成。

---

## 一、用法

1. 术语**首次出现**时给「中文（English, 缩写）」形式，之后统用首选写法或缩写。
2. 「避免的写法」是硬约束：正文出现即报错，需改回首选写法。
3. **同词不同义要分开登记。** `Token` 在大模型语境是「词元」，在认证语境是「令牌」——
   两者都是正确写法，所以拆成两行，并各自限定适用范围。拆分而不是粗暴禁用，
   是因为一刀切会把正确用法也拦下来（参见 1.8 的 Token 认证）。
4. 若确实需要引入新写法（例如某厂商官方中文文档用了别的词），改这里，再全库替换。

<!-- LINT:TERMS:BEGIN 本段是 lint_book.py 的解析区间，增删行请保持表格结构 -->
| 首选写法 | 英文 | 避免的写法 | 首次出现 |
| --- | --- | --- | --- |
| 大语言模型 | Large Language Model, LLM | 大型语言模型、语言大模型 | 0.3 |
| 词元 | Token | 切词单元（**限大模型语境**，见下方「认证令牌」） | 0.3 |
| 认证令牌 | Access Token / Refresh Token | 认证词元、登录票据 | 1.8 |
| 认证 | Authentication | 身份验证 | 1.13 |
| 授权 | Authorization | 鉴权、权限管理 | 1.13 |
| 访问令牌 | Access Token | 登录令牌 | 1.13 |
| 刷新令牌 | Refresh Token | 更新令牌 | 1.13 |
| 作用域 | Scope | 权限范围、作用范围 | 1.13 |
| 会话 | Session | 会话状态 | 1.13 |
| 口令 | Password | 密码（**限登录凭证语境**；加密算法仍是「密钥」） | 1.13 |
| 口令哈希 | Password Hashing | 密码加密（哈希不可逆，说「加密」是错的） | 1.13 |
| 签名 | Signature | 数字签章 | 1.13 |
| 越权访问 | Broken Object Level Authorization, BOLA | 越权操作、水平越权 | 1.13 |
| 上下文窗口 | Context Window | 上下文长度、窗口长度 | 0.3 |
| 上下文工程 | Context Engineering | 上下文工程学 | 2.1 |
| 提示工程 | Prompt Engineering | 提示词工程、提示工程学 | 0.3 |
| 幻觉 | Hallucination | 臆造、胡编 | 0.3 |
| 工具调用 | Tool Use | 工具使用（作动词时用「调用工具」） | 0.3 |
| 智能体 | Agent | 智能代理 | 0.3 |
| 检索增强生成 | Retrieval-Augmented Generation, RAG | 检索增强型生成 | 0.3 |
| 嵌入 | Embedding | 内嵌向量、嵌入编码 | 0.3 |
| 向量数据库 | Vector Database | 矢量数据库 | 0.1（展开于 5.3） |
| 重排 | Rerank | 二次排序 | 5.4 |
| 评测 | Evaluation | 模型评估 | 0.3 |
| 可观测性 | Observability | 观测性、可观察性 | 0.3 |
| 服务等级目标 | Service Level Objective, SLO | 服务级别目标 | 0.2 |
| 数据校验 | Data Validation | 数据验证 | 1.9 |
| 参数校验 | Parameter Validation | 参数验证 | 1.9 |
| 校验器 | Validator | 验证器 | 1.9 |
| 序列化 | Serialization | 串行化 | 1.9 |
| 状态码 | Status Code | 状态代码、返回码 | 1.9 |
| 请求体 | Request Body | 请求主体 | 1.9 |
| 响应模型 | Response Model | 响应模式 | 1.9 |
| 依赖注入 | Dependency Injection, DI | 依赖插入 | 1.9 |
| 中间件 | Middleware | 中间层 | 1.9 |
| 跨域资源共享 | Cross-Origin Resource Sharing, CORS | 跨来源资源共享 | 1.8 |
| 限流 | Rate Limiting | 流量限制、速率限制 | 1.11 |
| 洋葱模型 | Onion Model | 洋葱式模型、同心圆模型 | 1.11 |
| 依赖覆盖 | Dependency Override | 依赖替换 | 1.11 |
| 主机头 | Host Header | 主机报头 | 1.11 |
| 统一响应格式 | Unified Response Envelope | 统一返回格式、响应封装 | 1.14 |
| 业务码 | Business Code | 业务状态码 | 1.14 |
| 请求 ID | Request ID | 请求标识、链路 ID | 1.14 |
| 异常处理器 | Exception Handler | 异常拦截器、错误处理器 | 1.14 |
| 兜底处理器 | Catch-all Exception Handler | 全局兜底、最后一道处理器 | 1.14 |
| 脱敏 | Data Masking | 打码、遮蔽 | 1.14 |
| 结构化日志 | Structured Logging | 结构化打印 | 1.14 |
| 日志轮转 | Log Rotation | 日志滚动、日志切分 | 1.14 |
| 三层架构 | Layered Architecture | 三层分层、三层结构 | 1.14 |
| 微调 | Fine-tuning | 精调、细调 | 0.1（展开于 3.1） |
| 模型网关 | Model Gateway | 模型代理层 | 6.3 |
| 熔断器 | Circuit Breaker | 断路器、熔断开关 | 6.3 |
| 成本看板 | Cost Dashboard | 账单看板、费用看板、成本面板 | 3.9（展开于 6.4） |
| 批量通道 | Batch Channel | 批量接口、离线通道、批处理通道 | 6.1（展开于 6.4） |
| 前缀命中率 | Prefix Hit Rate | 缓存命中比、前缀命中比 | 6.4 |
| 评测流水线 | Evaluation Pipeline | 评估流水线、评测流程 | 0.1（展开于 7.1） |
| 指标注册表 | Metric Registry | 指标登记表、度量注册表 | 7.1 |
| 抖动带 | Noise Band | 噪声带、波动带、抖动阈值 | 7.1 |
| 分辨率 | Resolution | 解析度、分辨力 | 5.6（展开于 7.1） |
| 语义约定 | Semantic Conventions | 属性约定 | 3.9（展开于 7.2） |
| 头部采样 | Head Sampling | 前端采样、预采样 | 3.9（展开于 7.2） |
| 尾部采样 | Tail Sampling | 后端采样、事后采样 | 3.9（展开于 7.2） |
| 自耗时 | Self Time | 独占耗时、自身耗时、自己的耗时 | 7.2 |
| Harness | Agent Harness | 工装、外挂框架、工作底座 | 2.1 |
| Vibe Coding | Vibe Coding | 氛围编程、感觉式编程 | 2.1、2.4 |
| 提示注入 | Prompt Injection | 提示词注入、注入攻击（需与其他注入区分时写全称） | 2.1 |
| 包幻觉 | Package Hallucination | 依赖幻觉、包臆造 | 2.1 |
| 过度代理 | Excessive Agency | 代理过度、权力过大 | 2.1 |
| 上下文腐烂 | Context Rot | 上下文腐化、上下文失效 | 2.1 |
| 完成标准 | Done Criteria | 完成条件、完结标准 | 2.1 |
| 代理循环 | Agentic Loop | 智能体循环、Agent 循环 | 2.2 |
| 权限模式 | Permission Mode | 许可模式、授权模式 | 2.2 |
| 沙箱 | Sandbox | 沙盒 | 2.2 |
| 工作树 | Git Worktree | 工作目录树 | 2.2 |
| 沙箱模式 | Sandbox Mode | 沙盒模式、隔离模式 | 2.3 |
| 审批策略 | Approval Policy | 审批模式、认可策略 | 2.3 |
| 工作区 | Workspace | 工作空间 | 2.3 |
| 规则文件 | AGENTS.md / CLAUDE.md | 提示文件、项目说明书 | 2.3 |
| 自动复核 | Auto Review | 自动审核、自动审批 | 2.3 |
| 任务规格 | Task Spec | 任务说明书、需求单 | 2.1 |
| 追踪记录 | trace | 执行轨迹、运行轨迹（**限任务过程语境**；栈追踪仍是「栈追踪」） | 3.2 |
| 必需状态检查 | Required Status Check | 强制检查、必须通过的检查 | 2.6 |
| 停机点 | Stop Point | 人工卡点、卡点 | 2.6 |
| 评测用例 | Eval Case | 评估样例、评估用例 | 2.6 |
| 知舟 | Zhizhou | 智舟 | 0.2 |
| 素材库 | — | 参考资料库 | 0.3 |
| 官方文档 | — | 官网资料、权威文献 | 0.3 |
| 契约优先 | Contract First | 契约先行、接口先行 | 2.4 |
| 多根工作区 | Multi-root Workspace | 多工作区、多目录窗口 | 2.4 |
| 夹具 | Fixture | 样例文件、测试样例文件 | 2.4 |
| 技能 | Skill | 技能包（**限 Agent 语境**；岗位与能力语境里的「技能」是普通用词，不属本表） | 2.5 |
| 钩子 | Hook | 挂钩 | 2.2 |
| 自动记忆 | Auto memory | 自动 Memory、记忆库 | 2.5 |
| 渐进式披露 | Progressive Disclosure | 渐进披露 | 2.5 |
| 模型上下文协议 | Model Context Protocol, MCP | MCP 协议（缩写本身已含「协议」） | 2.5 |
| 自回归 | Autoregressive, AR | 自回归式生成 | 3.1 |
| 自注意力 | Self-Attention | 自身注意力 | 3.1 |
| 缩放点积注意力 | Scaled Dot-Product Attention | — | 3.1 |
| 注意力头 | Attention Head | 注意力模块 | 3.1 |
| 因果掩码 | Causal Mask | 因果遮挡、下三角掩码 | 3.1 |
| 分词器 | Tokenizer | 切词器 | 3.1 |
| 词表 | Vocabulary | 词典表 | 3.1 |
|  KV 缓存 | KV Cache | 键值缓存 | 3.1 |
| 前缀缓存 | Prompt Caching | 提示缓存 | 3.1 |
| 涌现能力 | Emergent Ability | 突现能力 | 3.1 |
| 对齐税 | Alignment Tax | — | 3.1 |
| 零样本提示 | Zero-Shot Prompting | 无示例提示 | 3.2 |
| 少样本提示 | Few-Shot Prompting | 多示例提示（英文 multishot 时两词同义，中文统一用「少样本」） | 3.2 |
| 思维链 | Chain-of-Thought, CoT | 思考链、链式思维（与本章标题的「链式推理」同义，但与其他「链」区分时用全称） | 3.2 |
| 自一致性 | Self-Consistency | 自洽性 | 3.2 |
| 思维树 | Tree of Thoughts, ToT | 思考树 | 3.2 |
| 提示链 | Prompt Chaining | 链式提示、流水线提示 | 3.2 |
| 自我纠错链 | Self-Correction Chain | 自检链 | 3.2 |
| 结构化输出 | Structured Outputs | 结构输出、JSON 模式（后者只是其一种形态） | 3.2 |
| 受约束解码 | Constrained Decoding | 约束解码 | 3.2 |
| 严格工具调用 | Strict Tool Use | 严格模式（说全称，否则与数据库的严格模式混） | 3.2 |
| 预填响应 | Prefilled Response | 预填充、前缀续写 | 3.2 |
| 思考块 | Thinking Block | 思考段落、reasoning 块 | 3.2 |
| 金律 | Golden Rule | — | 3.2 |
| 增强型大语言模型 | Augmented LLM | 增强大模型、扩充式大语言模型 | 3.3 |
| 护栏 | Guardrail | 防护栏、守卫栏 | 3.3 |
| 人类在环 | Human in the Loop, HITL | 人在回路、人工回环 | 3.3 |
| 编排者–工作者 | Orchestrator–Workers | 主管–工人、协调者–工作者 | 3.3 |
| 评估者–优化者 | Evaluator–Optimizer | 评价者–优化器、生成–评审循环 | 3.3 |
| 工具集 | Toolset | 工具集合 | 3.3 |
| 终止条件 | Stop Condition | 退出条件 | 3.3 |
| 任务分解 | Task Decomposition | 任务拆解、任务拆分 | 2.4 |
| 依赖图 | Dependency Graph | 依赖关系图 | 3.4 |
| 计划校验 | Plan Validation | 计划检查 | 3.4 |
| 重规划 | Replanning | 重新规划 | 3.4 |
| 上下文压缩 | Compaction | 历史压缩、上下文摘要化 | 3.4 |
| 结构化笔记 | Structured Note-taking | 结构化记录 | 3.4 |
| 子代理 | Sub-agent | 子智能体、子 Agent | 2.2 |
| 直接注入 | Direct Prompt Injection | 直接提示注入 | 3.8 |
| 间接注入 | Indirect Prompt Injection | 间接提示注入、内容注入 | 3.8 |
| 越狱 | Jailbreak | 破解、越权提示 | 3.8 |
| 不可见字符 | Invisible Character | 隐形字符 | 3.8 |
| 权限预算 | Agency Budget | 能力预算、权限额度 | 3.8 |
| 误杀率 | False Positive Rate, FPR | 误报率、误拦率 | 3.8 |
| 审计日志 | Audit Log | 审计记录表 | 3.8 |
| 样例集 | Sample Set | 样本集 | 3.8 |
| 有限状态机 | Finite State Machine, FSM | 有限自动机 | 3.4 |
| 行为树 | Behavior Tree | 行为树图 | 3.4 |
| 水位线 | Water mark | 警戒水位、阈线 | 3.1 |
| 提示版本号 | Prompt version | 提示词版本、prompt 版本 | 3.2 |
| 工具定义 | Tool Definition | 工具声明、工具规格 | 3.5 |
| 工具描述 | Tool Description | 工具说明、工具注解 | 3.5 |
| 工具结果 | Tool Result | 工具返回内容 | 3.5 |
| 工具注册表 | Tool Registry | 工具注册中心 | 3.5 |
| 并行工具调用 | Parallel Tool Use | 批量工具调用、并发工具调用 | 3.5 |
| 幂等键 | Idempotency Key | 幂等标识、去重键 | 3.5 |
| 短期记忆 | Short-term Memory | 临时记忆、会话记忆 | 3.6 |
| 长期记忆 | Long-term Memory | 持久记忆、永久记忆 | 3.6 |
| 工具结果清理 | Tool-result Clearing | 工具结果裁剪、清理工具输出 | 3.6 |
| 记忆装配 | Memory Assembly | 记忆拼装、上下文拼装 | 3.6 |
| 交接单 | Handoff | 派单、子任务卡 | 3.7 |
| 交接契约 | Handoff Contract | 交接协议、交接文档 | 3.7 |
| 编排器 | Orchestrator | 代理调度器、主管程序 | 3.7 |
| 主管 | Lead Agent | 主智能体、领导代理 | 3.7 |
| 缺口 | Gap | 缺失项、遗漏组 | 3.7 |
| 打折 | Degraded | 降级结论、余额不足收口 | 3.7 |
| 可观测性 | Observability | 可观察性 | 3.9 |
| 黄金信号 | Golden Signals | 黄金指标 | 3.9 |
| span | Span | 跨度 | 3.9 |
| 判分器 | Grader | 评分器、打分器 | 3.9 |
| 失败分布 | Failure Distribution | 错误分布 | 3.9 |
| 运行中断 | Run Abort | 执行中断 | 3.9 |
| 终态 | Outcome | 最终状态 | 3.9 |
| 回归门 | Regression Gate | 回归闸门、回退门 | 3.9 |
| 基线 | Baseline | 基准线 | 3.9 |
| 内容捕获 | Content Capture | 内容采集、内容记录 | 3.9 |
| 装配层 | Assembly Layer | 组装层、拼装层 | 3.10 |
| 端到端验收 | End-to-End Acceptance | 端对端验收、E2E 测试 | 3.10 |
| 接线状态 | Wiring Status | 接入状态、连接状态 | 3.10 |
| 反向守 | Reverse Guard | 反向用例、对照组（**限断言的反向用例语境**；A/B 实验里的「对照组」是正确用语，7.3 用了 10 处） | 3.10 |
| 手写版 | Hand-written Version | 旧版实现、重构版 | 4.1 |
| 框架版 | Framework Version | 框架实现 | 4.1 |
| 检索版 | Retrieval Version | 搜索版 | 5.1 |
| 检索器 | Retriever | 召回器、查询器 | 5.1 |
| 召回 | Retrieval (of chunks) | 查回、取回（**限检索语境**） | 5.1 |
| 零召回 | Zero Recall | 空召回（已占用，见下方说明） | 5.1 |
| 噪声 | Noise | 脏片、无关片 | 5.1 |
| 引用核对 | Citation Check | 引用校验、引用检查 | 5.1 |
| 答案片 | Answer Chunk | 答案块、命中片 | 5.1 |
| 拒答 | Refusal | 弃答、拒绝回答 | 5.1 |
| 阈值裕度 | Threshold Margin | 安全间隔、容错带 | 5.1 |
| 分界线 | The Offline/Online Line | 分界点、楚河汉界 | 5.1 |
| 剧本模型 | Scripted Model | mock 模型、模拟模型 | 4.1 |
| 有界性 | Boundedness | 限制性、超时兜底 | 4.1 |
| 兜底值 | Fallback Default | 隐含默认、隐性默认 | 4.1 |
| 消息占位 | Message Placeholder | 历史变量、消息槽 | 4.2 |
| 必填变量 | Required Variable | 必需变量 | 4.2 |
| 选填变量 | Optional Variable | 可选变量、可缺省变量 | 4.2 |
| 输出解析器 | Output Parser | 解析插件、解析器组件 | 4.2 |
| 链 | Chain（LCEL） | 链式对象、管道编排 | 4.2 |
| 超级步 | Super-step | 超步、步进 | 4.3 |
| 图递归上限 | Graph Recursion Limit | 递归次数上限 | 4.3 |
| 归约器 | Reducer | 合并器、聚合函数 | 4.4 |
| 状态 schema | State Schema | 状态定义、状态模板 | 4.4 |
| 子图 | Subgraph | 嵌套图、子流程图 | 4.4 |
| 检查点 | Checkpoint | 存档点、落盘点 | 4.4 |
| 线程 ID | Thread ID | 线程号 | 4.4 |
| 私有通道 | Private Channel | 内部通道、隐藏通道 | 4.4 |
| 调度者 | Supervisor | 监管者、调度节点 | 4.5 |
| 检查点后端 | Checkpoint Backend | 存储后端、存档后端 | 4.5 |
| 时间旅行 | Time Travel | 时空旅行、历史回溯 | 4.5 |
| 交接限额 | Handoff Limit | 交接上限、派发上限 | 4.5 |
| 魔数 | Magic Bytes | 文件签名、幻数 | 5.2 |
| 切分 | Chunking | 切块 | 5.2 |
| 结构感知切分 | Structure-aware Chunking | 结构化切分 | 5.2 |
| 标题路径 | Heading Path | 标题链 | 5.2 |
| 父子片 | Parent-child Chunks | 父子块 | 5.2 |
| 乱码比 | Garbled Ratio | 乱码率、乱码比例 | 5.2 |
| 归一化 | Normalization | 标准化 | 5.3 |
| 空向量率 | Empty-vector Ratio | 零向量率 | 5.3 |
| 低分召回 | Low-score Recall | 弱分召回、低分命中 | 5.3 |
| 召回标尺 | Recall Ruler | 基准线、参照基线 | 5.3 |
| 字面代理 | Literal Proxy | 字面近似、表面语义 | 5.3 |
| 嵌入空间 | Embedding Space | 向量空间 | 5.3 |
| 混合检索 | Hybrid Retrieval | 混检 | 0.1（展开于 5.4） |
| 查询改写 | Query Rewriting | 查询重写、问题改写 | 5.1（展开于 5.4） |
| 倒数排名融合 | Reciprocal Rank Fusion, RRF | 倒数排序融合、排名倒数融合 | 5.4 |
| 候选集 | Candidate Set | 候选池、候选列表 | 5.3（展开于 5.4） |
| 候选深度 | Candidate Depth | 召回深度、检索深度 | 5.4 |
| 成对打分 | Pair Scoring | 两两打分、配对打分 | 5.4 |
| 交叉编码器 | Cross-Encoder | 交叉编码模型、交互编码器 | 5.4 |
| 置信度 | Confidence | 确信度、置信分值 | 5.5 |
| 分档 | Binning | 分等级、划档 | 5.5 |
| 控制位 | Control Field | 控制符、控制标记 | 5.5 |
| 知识精炼 | Knowledge Refinement | 知识蒸馏、精炼化 | 5.5 |
| 按句绑定 | Sentence-level Binding | 逐句绑定、句级绑定 | 5.5 |
| 支撑档位 | Support Level | 支撑度、支持度 | 5.5 |
| 无依据句 | Unsupported Sentence | 幻觉句、无支撑句 | 5.5 |
| 评测集 | Evaluation Set | 测试集 | 5.6 |
| 文档级判据 | Document-level Criterion | 文档层判据 | 5.6 |
| 片级判据 | Span-level Criterion | 片段级判据 | 5.6 |
| 可检测最小差异 | Minimum Detectable Difference | 最小可检测差异 | 5.6 |
| 极性 | Polarity | 方向性 | 5.6 |
| 阈值扫描 | Threshold Sweep | 参数扫描 | 5.6 |
| 四格表 | Confusion Table | 混淆矩阵 | 5.6 |
| 扁平表 | Flat Table | 平坦表 | 5.6 |
| 单飞 | Single Flight | 请求合并、并发去重 | 5.7 |
| 版本戳 | Version Stamp | 版本标记、版本指纹 | 5.7 |
| 事件帧 | Event Frame | 消息帧、事件包 | 5.7 |
| 按号补发 | Resume by Last-Event-ID | 事件续传、补推 | 5.7 |
| 心跳 | Heartbeat | 保活消息、探活帧 | 5.7 |
| 背压 | Backpressure | 反压、压力回传 | 5.7 |
| 冷跑 | Cold Run | 冷启动（**限缓存语境**；服务冷启动是另一件事） | 5.7 |
| 热跑 | Warm Run | 热启动 | 5.7 |
| 首字时长 | Time to First Token, TTFT | 首屏时长、首次响应时长、**首字延迟（8.3 补：同一个东西在两个篇里被叫了两个名字）** | 5.7 |
| 兜底文案 | Fallback Text | 兜底话术、默认回复 | 5.7 |
| 高基数 | High Cardinality | 高维基数、无限基数 | 5.7 |
| 拒收 | Rejection (of a document) | 拒绝入库、不予入库、剔除 | 5.8 |
| 语料戳 | Corpus Stamp | 语料版本、语料指纹、语料哈希、库戳 | 5.8 |
| 内容戳 | Content Stamp | 内容指纹、文件戳 | 5.8 |
| 质量门 | Quality Gate | 质检门、质量闸门、质量关卡、质量门控 | 5.8 |
| 差量 | Delta | 差分、变更集、差异量 | 5.8 |
| 决策记录 | Architecture Decision Record, ADR | 架构决策记录、决定记录、决策条目 | 5.9 |
| 失效线 | Invalidation Line | 失效条件、推翻条件、过期条件 | 5.9 |
| 读数引用 | Reading Reference | 数字引用、指标引用、数据引用 | 5.9 |
| 文档对账 | Document Reconciliation | 文档校验、文档一致性核对 | 5.9 |
| 已知不足 | Known Limitations | 不足清单、未验证清单 | 5.9 |
| 长档 | Long-context Tier | 超长档、长文本档、长上下文档 | 6.1 |
| 缓存写价 | Cache Write Price | 缓存写入价、写缓存价 | 6.1 |
| 缓存读价 | Cache Read Price | 缓存读取价、缓存命中价 | 6.1 |
| 命中率底线 | Break-even Hit Rate | 回本命中率、临界命中率、命中率阈值 | 6.1 |
| 任务画像 | Task Profile | 负载画像、调用画像、用例画像 | 6.1 |
| 方言 | Dialect | 协议族、厂商方言 | 6.2 |
| 规范形状 | Canonical Shape | 中立形状、标准化形状 | 6.2 |
| 翻译表 | Translation Table | 转换表、翻译清单 | 6.2 |
| 适配器 | Adapter | 转接器、适配器层 | 6.2 |
| 兼容层 | Compatibility Layer | 兼容接口、兼容模式 | 6.2 |
| 分桶 | Bucketing | 桶划分、分流桶 | 5.7（展开于 7.3） |
| 灰度发布 | Gradual Rollout | 灰度上线、渐进发布 | 2.6（展开于 7.3） |
| 发布标签 | Deployment Label | 部署标签、环境标签 | 7.3 |
| 受保护标签 | Protected Label | 受保护标记、锁定标签 | 7.3 |
| 放量阶梯 | Rollout Ladder | 放量台阶、扩量阶梯、金丝雀阶梯（8.2 补：同一个东西在两个篇里被叫了两个名字） | 7.3 |
| 比例失衡 | Sample Ratio Mismatch | 样本比例失衡、分流失衡 | 7.3 |
| 偷看 | Peeking | 提前偷看、多次查看 | 7.3 |
| 静默漂移 | Silent Drift | 隐性漂移、悄悄漂移 | 7.3 |
| 审计链 | Audit Chain | 审计链条、日志链（**与「审计日志」是两个东西**：链要能自证没被改过） | 7.4 |
| 最小化 | Data Minimization | 最少化、尽可能少收集 | 7.5 |
| 目的限定 | Purpose Limitation | 用途限制、目的限制 | 7.5 |
| 留存期限 | Retention Period | 保存期、数据保存期限 | 7.5 |
| 可关联性 | Linkability | 可连接性、关联能力 | 7.5 |
| 重新识别 | Re-identification | 再识别、重识别 | 7.5 |
| 准标识符 | Quasi-identifier | 间接标识符、半标识符 | 7.5 |
| 校验位 | Check Digit | 检验码、校验码 | 7.5 |
| 形状规则 | Shape Rule | 形态规则、外观规则 | 7.5 |
| 删除范围 | Deletion Scope | 删除传播、删除足迹 | 7.5 |
| 层账 | Layer Ledger | 层总量、镜像层之和（**7.2 的「三层账」是「三层的账」，与本词条不是一回事**） | 8.1 |
| 可见账 | Visible Size | 可见体积、解包后体积 | 8.1 |
| 浪费账 | Wasted Size | 浪费体积、冗余体积 | 8.1 |
| 白障 | Whiteout | 白障层、空白层 | 8.1 |
| 必需检查 | Required Check | 必需状态检查、必过检查、必选检查 | 8.2 |
| 爆炸半径 | Blast Radius | 危害半径、波及面、作恶范围 | 2.1（展开于 8.2） |
| 绿而没验 | Green Without Verification | 空绿、绿灯没验、绿而没查 | 8.2 |
| 静态 key | Static Cache Key | 静态键、固定 key、死 key | 8.2 |
| 客户端状态机 | Client State Machine | 前端状态机、UI 状态机、界面状态机 | 8.3 |
| 慢消费者 | Slow Consumer | 慢客户端、慢读端、滞后的消费者 | 8.3 |
| 队列上限 | Queue Cap | 队列长度上限、积压上限、待发上限 | 8.3 |
| 卡在流式中 | Stuck Streaming | 流式卡死、挂在流式、流式僵住 | 8.3 |
| 显示错 | Display-Only Error | 界面错、渲染错、显示漂移 | 8.3 |
| 缓冲长度 | Buffer Depth | 缓冲大小、缓冲条数、留存条数 | 8.3 |
| 增量帧 | Delta Frame | 差分帧、分片帧、活量帧（**8.3 的「全量帧」是它的对偶，两个词成对使用**） | 8.3 |
| 一吐一大片 | Flush All At Once | 整段糊上来、一下全出来、攒完再发 | 8.3 |
| 租户 | Tenant | 客户方、租用方（**与「用户」是两个层次：一个租户里有很多用户**） | 8.4 |
| 行级安全 | Row-Level Security, RLS | 行级权限、行过滤 | 8.4 |
| 分区键 | Partition Key | 分区字段、分片键 | 8.4 |
| 配额 | Quota | 用量上限、限量（**与「预算」分开：那是 6.3 里一次调用内部的账**） | 8.4 |
| 限流 | Rate Limiting | 速率限制、流量控制 | 8.4 |
| 负载卸载 | Load Shedding | 过载保护、弃流（**与「降级」不是一回事：那是超限答复里的第三种**） | 8.4 |
| 预扣 | Reservation | 预留扣费、预冻结 | 8.4 |
| 搁浅成本 | Stranded Cost | 沉没成本、损耗（**两个词在会计里都有别的意思**） | 8.4 |
| 服务级指标 | Service Level Indicator, SLI | 服务水平指标、服务指标 | 8.5 |
| 服务级目标 | Service Level Objective, SLO | 服务水平目标、服务目标 | 8.5 |
| 错误预算 | Error Budget | 错误余量、容错额度（**与 8.4 的「配额」分开：那是租户级的闸机**） | 8.5 |
| 燃尽率 | Burn Rate | 燃烧速率、消耗速率 | 8.5 |
| 多窗口多燃尽率 | Multi-window, Multi-burn-rate | 多窗口燃尽、多级燃尽 | 8.5 |
| 症状告警 | Symptom-based Alerting | 症状报警、按症状报警（**与「原因告警」相对：后者进面板与工单**） | 8.5 |
| 演练 | Drill | 演习、彩排 | 8.5 |
| 复盘 | Postmortem | 事后分析、事后复盘、回顾会（**它的产物是行动项，不是一份文档**） | 8.5 |
| 行动项 | Action Item | 待办项、整改项 | 8.5 |
| 事故时间线 | Incident Timeline | 故障时间线、事件时间线 | 8.5 |
| 利用率 | Utilization | 使用率、负载率 | 8.5 |
| 预留余量 | Headroom | 冗余度、余量水位 | 8.5 |
| 拐点 | The Knee | 转折点、临界点 | 8.5 |
| 元监控 | Meta-monitoring | 监控监控、次级监控（**测的是告警链路自己能不能把消息送到人手里**） | 8.5 |
| 应聘者跟踪系统 | Applicant Tracking System, ATS | 简历筛选系统、简历库、招聘系统（**它筛的是「字段解没解析出来」，不是「你行不行」**） | 10.1 |
| 关键词覆盖率 | Keyword Coverage | 关键词命中率、匹配度、关键词得分 | 10.1 |
| 投递漏斗 | Application Funnel | 转化漏斗、投递转化率 | 10.1 |
| 证据链 | Evidence Chain | 论证链、证明链（**它的判据是「每一句都能被追问三轮」，不是「写得多」**） | 10.1 |
| 版面预算 | Layout Budget | 篇幅预算、页面配额 | 10.1 |
| 量化 | Quantification | 数字化、数据化（**限简历与项目描述语境**） | 10.1 |
| 段落预算 | Segment Budget | 时间分配、时长分配 | 10.2 |
| 讲述骨架 | Narrative Skeleton | 讲故事模板、表达框架 | 10.2 |
| 主线 | Storyline | 重点案例、主要案例 | 10.2 |
| 追问防御 | Probe Defense | 防守话术、圆场 | 10.2 |
| 反事实追问 | Counterfactual Probe | 假设性追问、如果式提问 | 10.2 |
| 失败预案 | Demo Fallback | 备案、Plan B | 10.2 |
| 澄清五问 | Clarifying Five | 需求五问、五个问题（**限系统设计面语境；它固定是五条**） | 10.3 |
| 范围表 | Scope Table | 范围清单、假设表、三栏表（**限系统设计面语境；1.5 那个「需求表」是另一件事，未被拦**） | 10.3 |
| 非目标 | Non-goal | 不做清单、反向清单、排除项 | 10.3 |
| 分档路由 | Tiered Routing | 分级路由、模型分流（**与 5.5 的「分档」分开：那个分的是答案，这个分的是请求**） | 10.3 |
| 语义缓存 | Semantic Cache | 相似缓存、智能缓存（**与 3.1 的「前缀缓存」分开：命中判据与代价都不同**） | 10.3 |
| 容量估算 | Capacity Sizing | 规模估算、容量预算、资源估算 | 10.3 |
| 降级链 | Degradation Chain | 兜底链、降级路径（**它是一张有顺序的链，不是「降级」这个动作**） | 10.3 |
| 考点索引 | Topic Index | 题库索引、考点清单（**限第 10 篇的题库语境；它只到「专题 → 章」这一层**） | 10.4 |
| 三层可用性 | Three-layer Usability | 可用性分层、三层可用度（**限题库语境：题面／考点／答案**） | 10.4 |
| 归属表 | Chapter Mapping | 章节映射表、归属清单（**与 0.1／4.4 的「映射表」分开：那张是关系的映射，这张是专题到章的对照**） | 10.4 |
| 题库盲区 | Bank Blind Spot | 覆盖缺口、题库缺口 | 10.4 |
| 速查表 | Cheat Sheet | 速记表、速查卡 | 10.4 |
| 专题漂移 | Topic Drift | 主题偏移、分类漂移（**限题库语境：题库自己的题跑到了另一个专题里**） | 10.4 |
<!-- LINT:TERMS:END -->

---

## 二、固定指代

> 本节与「三、维护记录」是说明性表格，**不参与术语校验**（校验只读上方标记区间）：
> 第二节只有两格，第三节另有一道对账门（有词条的章必须有一行、每行必须三格），
> 由 `python tools/lint_book.py` 的「术语日志」检查项负责。

| 写法 | 含义 |
| --- | --- |
| 第 X 篇 / 第 X.Y 节 | 本教材的篇与章，编号以 [`PLAN.md`](PLAN.md) 为准 |
| 素材库 / `sources/` | 从原始资料抽出的纯文本素材，只提供选题与经验 |
| 官方文档 | [`REFERENCES.md`](REFERENCES.md) 中登记的权威出处，技术结论以它为准 |
| 台账 / [`LEDGER.md`](LEDGER.md) | 知识点落点记录，用于确认「素材里的知识点没有默默消失」 |

---

## 三、维护记录

| 日期 | 变更 | 原因 |
| --- | --- | --- |
| 2026-09-11 | 建立本表，登记 20 个术语 | 第 0 篇完稿，第 1 篇开写前统一口径 |
| 2026-09-11 | 补登 9 个术语（校验 / 序列化 / 状态码 / 请求体 / 响应模型 / 依赖注入 / 中间件） | 1.9 引入 FastAPI 术语，需先定唯一口径 |
| 2026-09-11 | 补登 5 个术语（CORS / 限流 / 洋葱模型 / 依赖覆盖 / 主机头） | 1.11 写中间件前定口径；CORS 早在 1.8 出现却漏登，一并补上 |
| 2026-09-12 | 补登 11 个术语（认证 / 授权 / 访问令牌 / 刷新令牌 / 作用域 / 会话 / 口令 / 口令哈希 / 签名 / 越权访问） | 1.13 写认证前定口径。「鉴权」列入避免写法：它同时指认证与授权，是歧义的根源；「口令」与「密钥」分开登记，因为报错文案与术语表要一致 |
| 2026-09-12 | 补登 9 个术语（统一响应格式 / 业务码 / 请求 ID / 异常处理器 / 兜底处理器 / 脱敏 / 结构化日志 / 日志轮转 / 三层架构） | 1.14 的可维护性主题引入一批新口径。这批是**正文写完后补登**的（违反了本表「先登记再写」的规矩），因为该章是六个主题的合集、开写前没把术语清单过一遍——下一章起恢复先登记 |
| 2026-09-12 | 补登 5 个术语（沙箱模式 / 审批策略 / 工作区 / 规则文件 / 自动复核） | 2.3 写 Codex 前定口径。这与 2.2 的「权限模式 / 沙箱」是**两套工具各自的官方词**，不互相替代：「沙箱」是概念，「沙箱模式」是它的取值（`read-only` / `workspace-write` / `danger-full-access`）。首版把「工作目录」列进「工作区」的避免写法，一跑就报 5 处——那两词指的不是一回事（前者是 cwd，后者是本工具的授权范围），与 8.5 的「不乱响」原则一并记下 |
| 2026-09-12 | 补登 3 个术语（契约优先 / 多根工作区 / 夹具） | 2.4 写全栈实操前定的口径。**「契约先行」反向抓出了我自己**：同章的小节标题写的是「为什么契约先行」，而学习目标与小结里用的是「契约优先」——一个词在一章里两种写法，靠登记「避免的写法」才被机器抓住 |
| 2026-09-13 | 补登 5 个术语（技能 / 钩子 / 自动记忆 / 渐进式披露 / 模型上下文协议）；并修正「上下文工程」的首现章 2.5 → **2.1** | 2.5 写上下文工程前定口径。「技能」与岗位语境里的普通用词同形，所以按 `Token` 的先例**加限定范围**而不是禁用；「上下文工程」的漂移是写这个工具时抓到的：`GLOSSARY` 登记 2.5，而 2.1 早就用它（中英对照引入并整节讲解）——登记与正文漂移人不会去翻，但下一章会以错误的「首次出现」为前提 |
| 2026-09-13 | 补登 13 个术语（零样本提示 / 少样本提示 / 思维链 / 自一致性 / 思维树 / 提示链 / 自我纠错链 / 结构化输出 / 受约束解码 / 严格工具调用 / 预填响应 / 思考块 / 金律） | 3.2 写提示工程前定口径。两类要留意：**「少样本提示」与英文's multishot 是同一个东西**（中文不另造「多示例提示」，否则同一概念两个词）；**「严格工具调用」不简化成「严格模式」**（数据库、解释器都在用这个词，短形式会歧义） |
| 2026-09-12 | 补登 4 个术语（代理循环 / 权限模式 / 沙箱 / 工作树） | 2.2 写 Claude Code 前定口径。这批在动笔**之前**登记，已恢复本表规矩。两个「最像」的译名都是有意的：**沙箱**取官方中文文档与素材一致的那一个，「沙盒」列入避免写法；`worktree` 统一译「工作树」，不用「工作目录树」（易与「工作目录」混淆） |
| 2026-09-14 | 补记 6 个术语（交接单 / 交接契约 / 编排器 / 主管 / 缺口 / 打折）——**当时登进了表，忘了写这一行** | 3.7 写多 Agent 前定口径。两处是**成对**登记而不是各登各的：**「交接单」与「交接契约」指两件不同的东西**（前者是一次派发的具体内容，后者是它必须包含哪些字段的约定），3.8 的四档表与 3.9 的按角色切分都要引用后者；**「主管」与「编排器」也不合并**——前者是那一个角色的名字，后者是它做的那件事，正文里两处都会出现（「主管那一步」与「编排器」的回退）。另：「缺口」与「打折」是两个不同的缺陷类型（前者是少交付了元素，后者是结论降级），合并会让 3.7.4 的交接单对照表读不出结论 |
| 2026-09-14 | 补登 10 个术语（可观测性 / 黄金信号 / span / 判分器 / 失败分布 / 运行中断 / 终态 / 回归门 / 基线 / 内容捕获） | 3.9 写可观测性与评估前定口径，**先登记再写**。四处值得记：**「可观测性」的避免写法不能用「观测性」**——它是首选写法的子串，校验工具会直接跳过这一行（工具按「子串不算独立禁词」判定），所以真正的常见误写是「可观察性」；**`span` 不译成中文**——它已是通用叫法，译成「跨度」会在同一段里与「跨度」的日常义混起来；**「终态」不写作「最终状态」**——后者在长句里会被读成「最后的状态怎样」这种普通描述，而它要指「试验结束时环境里的状态」这个专有概念（与 transcript 相对）；**「失败分布」不写作「错误分布」**——本章的失败含「预算收口」这一非错误档，叫「错误分布」会把设计好的终止方式漏掉 |
| 2026-09-14 | 补登 4 个术语（短期记忆 / 长期记忆 / 工具结果清理 / 记忆装配） | 3.6 写记忆前定口径。两处判断：**「短期/长期」必须成对登记**——只登一个，另一个就会在正文里漂成「临时记忆」「永久记忆」；**「工具结果清理」不写作「清理工具结果」以外的任何形式**（官方叫 tool-result clearing），而它在 3.6.4 的表头与正文里会反复出现，不登记就会一份稿里三种写法。另：「装配」这个词 3.6 用得很重（记忆装配、上下文装配），登记为**记忆装配**而不是「上下文装配」——因为 3.1 已把「上下文」锁定为「送进模型的词元」，加限定词会把它读成另一个东西 |
| 2026-09-14 | 补登 4 个术语（装配层 / 端到端验收 / 接线状态 / 反向守） | 3.10 写组装章前定口径。四处都是**为避免同词两义而定**：「装配层」限定的是**把零件拼起来的那一层**（3.6 的“记忆装配”是它内部的一个函数，两者不同层，所以不合并）；「端到端验收」不写作「E2E 测试」——后者在本书里已经指「跑得快、跑得勤的那层测试」，而本章指的是**起服务后走真实 HTTP 面**的那一次；「接线状态」是本章新增的**表名**，避免写成「接入状态」（与 1.8 的“接入”同形但无关）；「反向守」不写作「对照组」——对照组是实验术语，它指的是**为一条断言配的反向用例**（「放行时真的会执行」那一类） |
| 2026-09-14 | 补登 8 个术语（直接注入 / 间接注入 / 越狱 / 不可见字符 / 权限预算 / 误杀率 / 审计日志 / 样例集） | 3.8 写安全与防护前定口径。三处都是**拆开而不是合并**：**直接注入与间接注入必须成对登记**——官方文档的威胁模型就是按「谁是对手」分的（用户是对手 / 用户可信而内容不可信），合成一个「提示注入」会把本章最重要的一条判据藏起来；**「越狱」与「提示注入」不是同义词**（前者改的是模型的策略，后者改的是任务的目标）；**「权限预算」不写作「能力预算」**——它算的是 [A]/[B]/[C] 三条属性，不是模型的能力清单，用「能力」会把读者的联想引到别处。另：「审计日志」把「审计记录表」列入避免写法，因为本章的审计是**每一条都带判定与依据**的流，而不只是一张表 |
| 2026-09-13 | 补登 9 个术语（任务分解 / 依赖图 / 计划校验 / 重规划 / 上下文压缩 / 结构化笔记 / 子代理 / 有限状态机 / 行为树） | 3.4 写规划与决策前定口径。三处值得记：**「任务分解」排除了「任务拆解」**——2.4 用过一次，改动是零代价的（同字数），但同一概念两种写法会让读者以为是两个东西；**「重规划」不写作「重新规划」**，因为后者在长篇里极易被当成普通描述而反复漂移；**「子代理」不写作「子 Agent」**——2.2 起已在用前者，本节只是把它固定下来。另：**「关键路径」这次故意不登记**——它在 2.1/2.2 里指的是「关键用户链路」（人工抽查那条路径），而 3.4 里指任务图的最长依赖链，是**同词两义**，硬登记会重演「结束条件」那类误伤；正文里写成「关键路径（最长依赖链）」就地区分 |
| 2026-09-13 | 补登 2 个术语（水位线 / 提示版本号）——**又一批「写完才登」** | 3.1.10 与 3.2.11 的接线小节引入了两个词，开写前没过一遍术语清单。同上一次一样，先把规矩再说一遍：**先登记再写**——这一批没出事是因为两个词都是新造词，不会与旧写法撞车；若是它们撞上了别处的既有写法，改的就不是两行表，而是二十处正文。另：「知舟接线」是小节名而非术语，不登记 |
| 2026-09-13 | 补登 6 个术语（工具定义 / 工具描述 / 工具结果 / 工具注册表 / 并行工具调用 / 幂等键） | 3.5 写工具调用前定口径，**先登记再写**已恢复。两处刻意的窄口径：**「工具结果」不把「观察」列入避免写法**——3.2/3.3 的 ReAct 语境里「观察」是循环四步的一步，与「工具返回的内容」不是同一个层次（`Observation` 同时包含工具结果与模型自己的推断）；**「幂等键」不写作「去重键」**，因为幂等说的是「重跑结果一致」，去重只是它的一种实现 |
| 2026-09-15 | 补登 7 个术语（混合检索 / 查询改写 / 倒数排名融合 / 候选集 / 候选深度 / 成对打分 / 交叉编码器） | 5.4 写混合检索与重排前定口径，**先登记再写**。四处值得记：**「混合检索」与「查询改写」是补登**——它们分别在 0.1 与 5.1 就出现在正文里（当时的写法是「5.4 会讲」），一直没登记，而这一章正是它们的展开处，不登记就会在展开时漂成「混合搜索」「问题改写」；**「倒数排名融合」不写作「倒数排序融合」**——后者是素材里的译法，而「排序」在本书里已指「排一次序」这个动词（5.3 的 `search()` 就干这个），用它会让「融合」读成一种排序；**「候选深度」不写作「召回深度」**——它数的是**进了候选池的条数**（每一路各出多少），不是「召回了多少」，两者在本章会同时出现且不相等（融合候选数 ≠ depth）；**「交叉编码器」不简化成「编码器」**（5.3 的嵌入器也是编码器的一族，短形式会把两章混起来） |
| 2026-09-13 | 补登 7 个术语（增强型大语言模型 / 护栏 / 人类在环 / 编排者–工作者 / 评估者–优化者 / 工具集 / 终止条件） | 3.3 写「Agent 是什么」前定口径。**「护栏」不写作「防护栏」**（它要与 3.8 的安全语境共用同一个词）；**「终止条件」不写作「退出条件」**——「退出」在本节里会被误读成「进程退出」，而它指的是「循环停止」。首版把「结束条件」也列进了避免写法，一跑就报 1.2 一处：那里讲的是 `while` 循环的终止，**「结束条件」是那一章的正确用语**——与 2.3「工作目录 vs 工作区」同一类错：同名易混的词要按语境拆分，而不是一并禁用 |
| 2026-09-14 | 补登 5 个术语（手写版 / 框架版 / 剧本模型 / 有界性 / 兜底值） | 4.1 写 LangChain 前定口径，**先登记再写**。三处值得记：**「手写版」与「框架版」必须成对登记**——第 4 篇通篇在做对照，只登一个的话另一个会漂成「原版」「新版」（而这两个词已在 1.13、2.1、2.5、3.1 当普通描述用过，不能反过来当禁词）；**「剧本模型」不写作「假模型」**——后者已在 3.2、3.3 里当普通描述用过，不宜追改，而 4.1 指的是**离线跑测试时替代真实服务商的那个有确定输出的模型对象**（与 3.9 的「剧本驱动」是同一套做法的两个粒度）；**「有界性」限定的是「超时与重试有没有显式值」这一件事**，不写作「限制性」，也不与 1.11 的「限流」混用（后者限的是流量，前者限的是单次等待）。另：「兜底值」是名词（下游 SDK 在你没设时替你以为的那个值），与 1.14 的「兜底处理器」同族但不同物 |
| 2026-09-14 | 补登 5 个术语（消息占位 / 必填变量 / 选填变量 / 输出解析器 / 链） | 4.2 写提示与编排前定口径，**先登记再写**。四处值得记：**「必填变量」与「选填变量」必须成对登记**——4.2 的整节结论就建立在「历史落在选填那一类、所以忘了传不会报错」上，只登一个的话另一个会在正文里漂成「必需变量」「可选变量」（两个词太像口语，最容易顺手写出来）；**「消息占位」不写作「历史变量」**——它占的是一个位置，装什么由调用方决定（历史是最常见的一种，不是唯一一种）；**「输出解析器」不简单叫「解析器」（而用全称）**，因为第八章的风险分析里「解析器」指的是语法解析组件；**「链」的避免写法里没有「管道」**——4.1 已经用「通用管道」描述过框架收走的那一层（那是另一个意思，所以不能一并禁） |
| 2026-09-14 | 补登 6 个术语（归约器 / 状态 schema / 子图 / 检查点 / 线程 ID / 私有通道） | 4.4 写状态编排前定口径，**先登记再写**。四处值得记：**「归约器」不写作「合并器」或「聚合函数」**——它们描述的是「把两份合成一份」这个动作，而本章要指「每个键各自挂的那一个函数」这个**槽位**（它也可以是「覆盖」）；**「状态 schema」保留英文 `schema`**——全书的 `schema` 不译（1.10 与 4.2 已如此），译成「模式」会与设计模式的「模式」撞；**「检查点」不写作「快照点」**——`stream_mode="values"` 的每一帧在 4.4 里叫「快照」，两个词必须分开，否则「流吐了几帧」与「存在哪一步」会被读成同一件事；**「线程 ID」保留全称**——单说「线程」已是 1.7 的并发设施（该书用了 110 处），而这里的 `thread_id` 是**检查点的游标**，两者不是同一个东西；而「会话 ID」**不入避免写法**——1.13 的登录会话与 2.2 的自由会话都在用这个词，一并禁写会误伤两处（同「结束条件」「关键路径」那几次的判断：同名易混的词按语境拆分，不登记） |
| 2026-09-15 | 补登 4 个术语（调度者 / 检查点后端 / 时间旅行 / 交接限额），**另有两个候选词登记失败并撤回** | 4.5 写持久化与多智能体前定口径，**先登记再写**。这一批最值得记的恰恰是**没登成的那两个**：「分支」（候选避免写法：分叉、支线）与「交接限额」的第二个避免写法（轮数上限）——**一跑校验就各自打回 2–3 章**（2.2/2.3/3.3/3.10 用「分叉」讲工作流的分支、3.4 用「支线」讲计划的两条路，而 4.4 的「轮数上限」指的是 `max_rounds` 那一件事）。两个词都是**同名易混**：它们在别章是**正确用语**，硬统一就会像首版的「结束条件」那样误伤。所以按既有先例撤回登记，只留三个能立住的：**「调度者」（Supervisor）与 3.7 的「主管」（Lead Agent）不合并**——前者是图上的一个节点与它的路由决策，后者是三角色结构里的一个角色身份，两者的英文与出处本就不同（避免写法里也不放「主管」：它在 3.7、3.8 共用了 43 处）；**「检查点后端」不写作「存储后端」**——本章的后端只管检查点，而「存储」在本篇里已被 3.6 的消息表占用；**「时间旅行」不写作「历史回溯」**（也不把「回放」列入避免写法：它在 4.4 已指 replay，是另一件事）。另：「交接限额」的避免写法最终定为「交接上限」与「派发上限」两个（都不会与既有章撞） |
| 2026-09-15 | 补登 11 个术语（检索版 / 检索器 / 召回 / 零召回 / 噪声 / 引用核对 / 答案片 / 拒答 / 阈值裕度 / 分界线），**另有两个候选词登记失败并撤回** | 5.1 写 RAG 全景前定口径，**先登记再写**。三处判断最费事：**「检索版」必须与「手写版」「框架版」并列登记**——第 5 篇又把一棵树加进那张对照表，只登两个的话第三个会漂成「RAG 版」「知识层版」；**「零召回」不能只写成「空召回」**——素材《生产级 RAG》里就有「大量空召回」这个说法（描述现象），而本章的「零召回」是一个**判定档**（该召回的召回了没有），两个词并存、不互相替；**「召回」要加限定范围而不是禁用**——它在 3.5、3.9 里当过普通动词（「把工具结果召回上下文」），与本章的「从检索库里取回片」不是一回事。另：「分界线」指的是**离线索引与在线检索／生成之间那一条**，不写「分界点」（那是几何里的点，本章讲的是一条线两侧的工程差异）。**两个没登成的**：「召回率」（候选避免写法「查全率」）一跑就打到 5.6 与 6.x 的正文——它们那里的「查全率」是评测指标的正式叫法；「引用」作为独立词条也被回下——它在 1.15、3.2 里已是普通用语（引用文献、引用变量），**只能登「引用核对」这个动作，不能登「引用」本身**，与「结束条件」「关键路径」那几次同一条判断 |
| 2026-09-14 | 补登 4 个术语（超级步 / 中断 / 图递归上限 / 调用层护栏） | 4.3 写工具与 Agent 前定口径，**先登记再写**。四处判断：**「超级步」不写作「步」**——本章有两条结论（「`recursion_limit` 数的是 super-step 而不是模型调用」「一次往返占 2 步」）靠一个不与日常「步」混的词才立得住；**「中断」与 3.9 的「运行中断」是两个东西**（前者是等人审批的**暂停点**，后者是一次试验被打断），两个词都在表里、且不能互写；**「图递归上限」不写作「最大步数」**——后者会立刻让人按「步」去数，而正确的心算是「除以 2」（这正是本章实测抓到的那条陷阱）；**「中间件」这个词 1.9 已登记**（FastAPI 的中间件），4.3 用的是它同一含义的框架形态，**因此不新增行**（一行一个词，重复登记会让术语表自己变成两套口径）。另：**「中断」与「调用层护栏」这两个词本章用得很重却没有登记**，原因同上一条规矩：它们的候选避免写法（「打断」「挂起」「包装层」）在 3.4、3.9、1.14 里分别是**正确用语**（3.4 的「打断模型」、1.14 的「包装层」），一并禁写会误伤八处——这与「结束条件」「关键路径」那两次的判断一致：**同名易混的词按语境拆分，不登记；只有真会漂的才登记** |
| 2026-09-15 | 补登 6 个术语（归一化 / 空向量率 / 低分召回 / 召回标尺 / 字面代理 / 嵌入空间），**另有三个候选词被自己拦下** | 5.3 写嵌入与索引前定口径，**先登记再写**。登之前先跑了一遍全库，于是三处判断落到了更小的避免表上：**「嵌入」不登**——它在本章既是章名的一半（向量化）又是动词（把文本嵌入），候选避免写法（嵌入向量、文本嵌入）都在本章自己的正文里出现过，登了就要改回去，而那两个说法本身没有歧义——与 4.3 的「中断」、3.7 的「结束条件」同一条判断；**「全零占比」不入避免写法**——它是运维原文里的指标名（5.3.6 引它来说明「这个数在报警什么」），而本章自己的首选写法是「空向量率」，两个词并存不互替；**「基线」不入避免写法**——它已被 5.1／5.2 占用（基线切法）共 44 处，而本章那把标尺叫「召回标尺」（避免写法只留「基准线」「参照基线」，都是零命中）。另：「归一化」与 3.1 的「层归一化」不冲突——它们指的是同一个动作在不同尺度上的两件事，因此只禁「标准化」（那个词在全库零命中，禁它是因为它与归一化在统计语境里是两件事）；「嵌入空间」不写作「向量空间」（后者是线性代数的那个空间，不是嵌入模型学出来的那个） |
| 2026-09-16 | 补登 7 个术语（置信度 / 分档 / 控制位 / 知识精炼 / 按句绑定 / 支撑档位 / 无依据句），**另有两个候选词在登之前就被自己拦下** | 5.5 写生成侧前定口径，**先登记再写**。登之前先跑全库，两处判断因此变了形状：**「置信度」的避免写法里不能有「可信度」**——它在 0.2、2.4、3.10 里当普通描述用过（「本节的可信度边界」「可信度取决于…」），登了就要改回三章，而它本身没歧义；**「分档」也不能把「分级」当禁词**——1.14 的「日志分级」、3.9 的「告警要分级」是已定口径，所以避免写法落到真正会漂的那两个（分等级、划档）。三处值得记：**「控制位」不写作「控制符」**——后者在编程语境里已被当普通说法用过（且本章要指「`Retrieve`／`IsREL`／`IsSUP`／`IsUSE` 这四个显式化的取值」，不是字面符号）；**「知识精炼」的避免写法里必须有「知识蒸馏」**——两者英文都像 refine／distill 这一类，而蒸馏是训练侧的另一件事（第 9 篇），不登的话本章的「精炼」会被读成它；**「按句绑定」与「支撑档位」必须成对登记**——本章的核心结论（「幻觉是一份名单」）同时靠这两个词：前者定「引用从哪来」，后者定「每句落在哪一档」，只登一个的话另一个会漂成「逐句挂引用」「支撑度」。另：**「兜底」不登**——全库已用 78 次（1.6–3.10 都有），是普通描述，而本章只是把 CRAG 那一档动作也叫兜底 |
| 2026-09-16 | 补记 6 个术语（魔数 / 切分 / 结构感知切分 / 标题路径 / 父子片 / 乱码比）——**当时登进了表，忘了写这一行**（说明文字此前被挂在 5.5 那一行的末尾） | 5.2 写解析与切分前定口径，**先登记再写**。这一批最值钱的不是登成的六个，是**登之前先跑了一遍全库**——5.1 那次是「召回率 / 引用」两个词登完才发现打到别章，这次改成先查后用，于是四处判断各自定下了更小的避免表：**「魔数」不写作「文件签名」**——1.13 已把「签名」登记为密码学签名（JWT 那段），同一本书里两个「签名」会让「校验签名」变成两件事；**「切分」的避免写法只放「切块」**——「分块」一跑就打到 1.15（那里是**分块流式**与**分块写盘**，是 I/O 的切，不是文档的切）与本章面试题里学生的那句问话，「拆分」在 2.2、2.3、3.2、3.7、4.5 与 5.1 里还都是通用动词（拆分子任务、拆分调用），两个都不能禁；**「父子片」不写作「父子块」**，而这一条是**在本章自己的正文里抓到 2 处的**（面试视角那一问一答）——当场改回，与 5.1 的「召回」同一条判断：同名易混的词要么加限定范围、要么只禁真正会漂的那一个；**「标题层级」不入避免写法**——它指 markdown 的 `#` 级数，与「标题路径」（那串「父 › 子」）是两件事，本章两处都在讲前者。另：「结构感知切分」保留全称，因为「结构化」在本篇已用于结构化输出（3.2、4.2）；「乱码比」**原稿自己漂了两处**（正文一处写成「乱码率」、`split.py` 注释一处写成「乱码比例」），改回之后它的重量才立得住——它只算三类字符（控制字符 ＋ 替换字符 ＋ 私用区），写成「乱码率」会被顺手扩展到别的字符类别上 |
| 2026-09-16 | 补登 5 个术语（拒收 / 语料戳 / 内容戳 / 质量门 / 差量），**另有三个候选词被自己拦下** | 5.8 写入库管线前定口径，**先登记再写**——这一批是「先跑全库再用」的第三次，五行的避免写法因此都能取到「零命中」的那一组。三处值得记：**「拒收」必须与 5.1 的「拒答」分开**——前者是**文件没进库**（内容太少／乱码超线／缺解析器三档），后者是**模型不回答**；两词的避免写法也不能互串（把「拒答」写进「拒收」的避免表，5.1／5.5 的八处正确用法会一起判红，同「召回收回」那次的教训）。**「语料戳」与「内容戳」成对登记**——前者是**报告级的那一个值**（由进了库的内容算出来，可直接喂 5.7 的版本戳），后者是**单个文件级的那一个值**；只登一个的话另一个会漂成「语料版本」「文件指纹」，而这两件事在 5.8 里必须能分开说（「改一份文档 → 语料戳变，而其余十一份的片照旧复用」）。**「质量门」保留全称**——本章的三档拒收是它的取值，简成「质检」会丢掉「门」这个「过与不过的分界」义，也不与 1.11 的中间件洋葱模型混。**三个没登成的**：「锚」是 5.6 的「基线」在验收里的**角色名**（A 组读数必须逐项相等），不是另一个东西，而 3.3／4.5 的「锚点」（观测锚点）与它同名不同物、全库「基准」另有 18 处正确用法——登了就要改十几处，与 4.3「中断」、5.7「限流」同一条判断；「内容哈希」保留为**机制名**（3.2 的提示版本号、3.9 的指纹都用它，全库 9 处都是正确用法），**不入避免写法**——而这一查当场抓出 5.8.8 有一处把它当「要看的那个值」用（与全章 7 处「内容戳」不一致），**已改回**：这正是先查后用的意义，不查就会写成一行禁词，然后把另外两章的四处正确用法一起判红；「入库」（57 处）与「复用」（98 处）是全库通用用语，登了会把正确用语变成禁词（同 5.5「兜底」）。另：**带括号的说明是不参与校验的**——工具按 `、`／`，` 切开后逐个逐字匹配，所以「（**限登录凭证语境**）」这类带范围说明的单元格是**惰性的**（本表里已有 21 处这样写，其中「零召回／空召回」那个是**故意**的：两个词并存、不互相替）。本批因此把口径说明写在这一列（日志）里，避免单元格只放真禁词，五行都能真响。另：「全量报告」与「验收清单」是本章的**交付物名**而非术语，不登——它们只在正文里写死，不构成会漂的一族 |
| 2026-09-16 | 补登 11 个术语（单飞 / 版本戳 / 事件帧 / 按号补发 / 心跳 / 背压 / 冷跑 / 热跑 / 首字时长 / 兜底文案 / 高基数），**另有两处判断在登之前就分开了** | 5.7 写在线侧前定口径，**先登记再写**。这一批最值得记的是**不登什么**：**「限流」不登**——它已在 1.11 是普通用语（83 处），而本章做的两件事（并发闸门与排队上限）与「按时间窗限流」不是同一件事，登了会把一个正确用语变成禁词（同 4.3「中断」、5.1「召回」那两次判断）；**「降级」不登**——全库已用 37 处（3.7 的「打折」与「结论降级」同族），本章只是把它落成三档具体动作，因此只登**兜底文案**这一个会漂的名词（避免写法落「兜底话术」「默认回复」，两个都零命中）。三处必须成对的：**「冷跑」与「热跑」成对登记**（只登一个，另一个会漂成「冷启动」——而服务冷启动是另一件事）；**「按号补发」不叫「事件续传」**——1.15 的「断点续传」是 HTTP Range 的文件续传（9 处正确用法），两者同名不同物，术语表里给本章这个动作另起一个不与它混的名字；**「事件帧」不写作「消息帧」**——本章按段发、每段一帧，与 WebSocket 的「消息」不是同一层。另：「心跳」虽在 1.7 出现过一次（后台任务举例），但指的是同一件事，因此登记为术语而不是拆词条（与 4.3「中间件」同一处置）；「高基数」的避免写法只留两个（高维基数、无限基数），**不把「无界标签」列入**——它在本章说明里是自然的描述。 |
| 2026-09-17 | 修正 1 个术语的首现章（**向量数据库：1.12 → 0.1**） | 1.12（数据库与缓存）一直记着「向量数据库」的首现章，而**本章正文一次都没出现过这个词**（按字面数 0 处）——真正最早的出处是 0.1 的岗位地图（技能表与岗位要求各一处），与「混合检索」「查询改写」记作 0.1（展开于 5.4）是同一种情况，故改为 **0.1（展开于 5.3）**。抓出它的是 2026-09-17 新增的**术语日志对账门**：它先报「1.12 有词条却没有登记行」，回头一查才发现是**词条与章压根对不上**——这又是一类只有机器问得出、人不会去翻的错（同 2026-09-13 那次「上下文工程」的首现章）。改对之后，第 0 篇那 20 个词仍由「建立本表」那一行按篇登记，不另开行 |
| 2026-09-17 | 补记 9 个术语（上下文工程 / Harness / Vibe Coding / 提示注入 / 包幻觉 / 过度代理 / 上下文腐烂 / 完成标准 / 任务规格）——**当时登进了表，忘了写这一行**（2026-09-17 由术语日志对账门抓出） | 2.1 是全书唯一按「能力边界与风险」展开的章，而这两条主线在素材里没有对应内容（台账记：素材只覆盖「思维模型」一条线，另两条按官方出处原创），要造一批新词，就得先把写法定死。四处判断值得记：**风险族一次定名**——「包幻觉」（不写「依赖幻觉」）、「过度代理」（不写「代理过度」）、「上下文腐烂」（不写「上下文腐化」）、「提示注入」（不写「提示词注入」）都出自 OWASP 那一族风险清单在编码场景里的落点，四种顺手的译法一旦散进本篇各处就收不回来（其中「提示注入」是总名，3.8 再按「谁是对手」拆成直接注入与间接注入，两者并存而不互替）；**`Harness` 不译**——2.1 讲它的词源与三层演进、2.6 整章用它，译成「工装」「外挂框架」会把同一个东西读成两个；**「Vibe Coding」也保留英文**（避免「氛围编程」「感觉式编程」），首现记 2.1、2.4 两处，因为 2.4 是全栈实操那一章；**「完成标准」与「任务规格」**是本章讲「怎么把任务交出去」时的两个关键词，后者在 2.6 的 CI 语境里还会再用一次。另：「上下文工程」的首现章**正是这一章**——它后来被记成 2.5，2026-09-13 那一行已改回 2.1，从现在起两处登记对得上。这一批早于「先查后用」（那条规矩是 5.1 那轮才立的），避免写法是事后补核的：九条现在全库零命中 |
| 2026-09-17 | 补记 3 个术语（必需状态检查 / 停机点 / 评测用例）——**当时登进了表，忘了写这一行**（2026-09-17 由术语日志对账门抓出）；并修正「追踪记录」的首现章（**2.6 → 3.2**） | 2.6 把 AI 编程接进工程流水线，三个词都是这一章才立起来的，而且都长在**别人的界面（GitHub）**上：**「必需状态检查」保留 GitHub 的原词**（不写「强制检查」「必须通过的检查」——后者会与正文里「必须通过这一关」那类句子混，而它是 GitHub 上的一个具体开关）；**「停机点」不写「卡点」**（「卡点」全库零命中）——它指的是流水线里**AI 的产物必须停下来等人看**的那一处；**「评测用例」是单条**，与 5.6 的「评测集」（集合）分开命名——前者是 CI 里每条一跑的原子单位，后者是评测资产，混用会让「跑了几条」与「有几套集」读成同一件事。另修正一处：**「追踪记录」的首现章记错了**（记 2.6，而 2.6 正文零命中，那里只出现过目录结构图里的模块名 `trace 记录器`）；它真正最早出现在 3.2 的安全一节，3.3 起才当成一个构件用起来（`Trace` 那张表）。它与 3.9 的「内容捕获」是一对容易混的词（前者记过程、后者落盘内容），所以首现章必须指对 |
| 2026-09-17 | 补记 8 个术语（评测集 / 文档级判据 / 片级判据 / 可检测最小差异 / 极性 / 阈值扫描 / 四格表 / 扁平表）——**当时登进了表，忘了写这一行**（2026-09-17 由术语日志对账门抓出） | 5.6 是「把命中定义一次」的章——八个词里核心的那几个，都是为了让**同一条结论**写得出来：**文档级判据与片级判据必须分开命名**（本章最重的一条读数是「文档级命中率是个钝读数」：四个配置全 1.0000，换片级才出分辨率 0.8333／0.7222／0.8333／0.8333——两个判据合成一个词，这条结论就写不出来）；**「可检测最小差异」（取 1/n）与「极性」（哪个方向算坏）是同一张门的两半**——前者定「差多少才算变了」、后者定「变了以后哪边算差」，而本章正是踩过「方向没定义的键」那个坑；**「阈值扫描」不写「参数扫描」**（扫的是上下两个阈值，不是所有参数）；**「四格表」不写「混淆矩阵」**（本章只用到 2×2 那一种，且它是拒答判据的形状，不是通用指标）；**「扁平表」不写「平坦表」**——它是 5.6.9 那张**把读数变成门的表**的名字（一行一个键，带极性与阈值），与排版意义上的「扁平」无关；**「评测集」不写「测试集」**——它与 2.6 的「评测用例」是两个粒度。另：本章的「基线」与验收里那个角色名「锚」是两个词，别合并（见 5.8 那一行） |
| 2026-09-17 | 登记 5 个术语（长档 / 缓存写价 / 缓存读价 / 命中率底线 / 任务画像），**另有三处候选词被自己拦下**；并修正「微调」的首现章（**6.1 → 0.1（展开于 3.1）**） | 6.1 写模型选型前定口径，**先登记再写**（第 6 篇的第一批词）。这一批的判据与前几批不同：**五个词都是「这一章要反复算的那个量」的名字**——「长档」是价格表里的那一栏（它一旦漂成「长上下文档」，6.1.3 那条「多一个词元整单 +92.5%」的读数就读不出它自己的意思）；「缓存写价」与「缓存读价」必须**成对登记**（本章两个回本点全靠它们配对，而且它们与 1.12 里那个数据库缓存不是同一件事）；「命中率底线」区别于「第几次回本」——前者从不假设「连着调」（同 5.9 把「失效线」与「极性」分开的那条判断）；「任务画像」是 `PROFILES` 的中文名，它带着 `source` 字段，所以要有一个词能指「这个数是从哪量来的」。**三处没登成的**：「登记表」不单登——它在 5.3／5.4 的「模型清单」与 5.7 的「价目表」里都是**正确用语**，登了要改三章（同 4.3「中断」、5.1「召回」那条判断），所以本章口径叫「模型登记表」，避免写法只留两个零命中的（模型登记册、登记清单）；「回本点」不登——它是本章推导出来的口语名，正文里说得清「第几次回本」；「口径栏」不登——它指表里那两栏具体格子，是一个位置名而不是概念。另：**「微调」的首现章一直是错的**（记为 6.1，而它最早出现在 0.1 的岗位地图，3.1 展开），抓出它的是**术语日志对账门**：它先报「术语表里有 6.1 的词条却没有 6.1 的登记行」，回头一查才发现这一章压根不用这个词——与 2026-09-17 那次「向量数据库 1.12 → 0.1」同一族错：**词条与章对不上，而两处各自看起来都完整**。改对之后，「微调」仍由 0.1 那一行按篇登记，不另开行 |
| 2026-09-18 | 登记 5 个术语（方言 / 规范形状 / 翻译表 / 适配器 / 兼容层） | 6.2 写多厂商接入前定口径，**先登记再写**（第 6 篇的第二批词）。这一批与前几批最大的不同是：**其中一个词是「补登」的**——「方言」早在 3.6（真机走 OpenAI 兼容方言）、3.7、4.1（`model_provider` 描述的是协议方言）就已经在用，只是从来没登记过，而 6.2 正是它的展开处；此时不登，它就会在展开时长出「协议族」「厂商方言」两个同义词。四处判断：**「规范形状」不写作「中立形状」**——正文里会同时出现「中立答复」这种自然描述，而术语要指那个**进得了表、能被逐格核的形状**；**「翻译表」不写作「映射表」或「对照表」**（两者在全书各有 16 处与 27 处正确用法：前者是数据库与字典的映射，后者是章节对照），避免写法只留零命中的两个；**「适配器」不把「适配层」列入避免写法**——它在 4.5 是「存储适配层」（13 处背后的同一层意思），同「结束条件」「关键路径」那条判断：同名易混的词按语境拆分，不登记；**「兼容层」必须与「规范形状」分开命名**——前者是**一家的形状**（它兼容的是那一家的客户端），后者是中立的那一份，这章最重的一条结论（8 个模型里有一半上不了兼容层）就长在两者的区分上。另：「撞车」与「哨兵值」这两个词本章用得很重（停止原因撞车、`data: [DONE]` 是哨兵值），但**都不登记**：前者是全库通用口语（1.12、3.9 各有一处，且都是「两件事撞在一起」这个意思），后者在 1.4 已是正式用法（`None` 哨兵）且指的是同一件事，登了会把两处正确用法一起判红 |
| 2026-09-17 | 登记 5 个术语（决策记录 / 失效线 / 读数引用 / 文档对账 / 已知不足），**另有三个候选词被自己拦下** | 5.9 写复盘章前定口径，**先登记再写**。这一批与前几批不同：**五个词里四个是「名字」而不是「概念」**——它们分别是一份文件夹、一栏、一条命令与一节的名字，所以判据换成了「**它在树里有没有一个同名或同指的东西**」：**「决策记录」对应 `docs/decisions/` 里那六份文件**（保留 ADR 缩写，全库 17 处，其中 5.3 引过一次素材的 ADR-002）；**「失效线」对应记录第六栏**，它必须与 5.6 的「极性」分开——后者是**读数往哪个方向算坏**，前者是**这个决定什么条件下不成立**（因此本章把正文里原有的三处「失效条件」全改回了「失效线」：两个词同时漂，这一栏就读不出它自己的意思）；**「文档对账」对应 `scripts/recap.py` 与提交门里那一条**；**「已知不足」对应架构文档的末一节**，它装着两条**没验证过**的东西（真实并发、多租户）——**没验证过的事不能写成失效线**：失效线的前提是「这个决定在什么条件下不成立」，而它们连「成立」都还没验过。**三个没登成的**：「架构文档」不登——它在本章指 `docs/architecture.md` 那一份具体文件，而 2.1 已用同一个词泛指「项目的架构说明」（两文件 12 处均正确），登了会一起判红；「对账」单写不登——它在本章是「两份手写清单互相对」的通用动作（同 5.8 的「复用」、5.7 的「降级」）；「基线」已有 3.9 的登记（3.9／5.6 共几十处正确用法），本章只是引用它。另：「未验证项」不列入「已知不足」的避免写法——它是 2.2／2.5 的**交付格式字段**（13 处正确用法）；「一致性检查」同此（5.6 的代码-正文比对在用，3 处正确用法）。两个词各属一族，不互相替 |
| 2026-09-19 | 登记 2 个术语（模型网关 / 熔断器），**另有四组候选词被自己拦下** | 6.3 写网关前定口径，**先登记再写**（第 6 篇的第三批词）。这一批的入口与 6.1／6.2 不同：本章的主语是一个**新模块**（`app/gateway.py`），所以判据先问「这个词在树里有没有同名或同指的东西」：**「模型网关」对应那个模块与它做的那件事**，避免写法只留「模型代理层」（零命中）——它与 6.2 的**「兼容层」不合并**：后者是**一家的形状**（它兼容的是那一家的客户端），前者是「谁去发、发几次、发不出去怎么办」，两者在 6.3.1 那张表上分别占左右两半；**「熔断器」与 1.11 的「限流」不合并**——限流限的是**流量**（按时间窗），熔断限的是**调用**（按失败次数），而且它治的是「修不好的那一种错就不要再试了」，与 5.7 的**「降级」**（把服务换成另一个姿势继续提供）也各是一件事；避免写法「断路器」「熔断开关」均零命中（前者是它在工业控制里的另一个常见叫法，登了才能挡住它）。**四组没登成的**：「退避」不登——全书已用 25 处（1.14／3.2／4.2／5.7 都在说「指数退避」），而本章只是把它落成三行参数（起手／倍数／一次等待的上限），登了会把四个篇章的正确用法一起判红（同 5.7「限流」、5.5「兜底」、4.3「中断」同一条判断）；「降级链」不登——它是 6.3.5 的**节名与机制名**，而「降级」已在 5.7 定为普通用语、「链」只是那一段的形状，避免写法（兜底链、备用链）实测零命中，所以这一格是**登了也不会漂**的那一类（同 5.8「全量报告」、5.9「验收清单」：交付物名不入表）；「指纹」不登——3.9 的「指纹」指**轨迹摘要**、本章指**载荷指纹**，同名不同物，处置同 4.5「分支」：**不统一，而在正文里把语境界定清楚**（本章 8 处全部写作「载荷指纹」，且「版本指纹」「语料指纹」已在 5.7／5.8 的避免表里占位）；「冷却」与「半开」不登——它们是熔断器的**状态名与参数名**（机制内部名，没有第二个会漂的写法），与 5.7「心跳」那次相反（那一个已在 1.7 见过，所以登为术语；这两个全库只出现在本章）。另：本章是 9 月 18 日开始写的，而术语表的口径在**动笔之前**先写死（比 1.14 那次「正文写完再补登」的错法好一档），这一行是随章节交付一起补上的 |
| 2026-09-19 | 登记 3 个术语（成本看板 / 批量通道 / 前缀命中率），**另有四组候选词被自己拦下**，其中一个是**补登** | 6.4 写成本工程前定口径，**先登记再写**（第 6 篇的第四批词）。这一批的入口与 6.3 不同：本章的主语不是一个模块，而是**一页账**，所以判据先问「这个词在别处有没有同名或同指的东西」——**「成本看板」是这一批唯一的补登**：它早在 3.9（可观测性）里就已经在用，6.2／6.3 也都提到过它，只是从来没登记过；此时不登，它会在本章长成「账单看板」「费用看板」两个同义词（避免写法实测全库零命中），而它与 3.9 的「监控面板」不是一件事：后者看的是**可用性与延迟**，它看的是**钱**；**「批量通道」必须与「兼容层」「降级链」分开命名**——前两者是**形状**与**失败**，它是一条**分开的路**（0.5× 与最多 24 小时），三者在 6.4.5 的叠加表里各占一行；**「前缀命中率」是本章唯一的新名词**，而它必须与 5.7 的「缓存命中率」分开：后者不指定分母，本章最重的一条读数（按调用 95.0% 对按词元 36.3%）就是靠这一对词才写得出来——避免写法取「缓存命中比」「前缀命中比」（两个都零命中），**不把「缓存命中率」列入**：它在 5.7／5.6 等 7 处都是正确写法，登了会把那几章一起判红（同 4.3「中断」、5.7「限流」）。**四组没登成的**：「看板」不登——它已是 0.1／3.9 等九章的通用词（32 处），本章只是给它加了定语；「四栏」不登——它是本章对账页形状的口语名（`bill()` 的四个键），属「交付物名／表名不入表」那一类（同 5.6「扁平表」、5.8「台账」）；「分流」不登——5.7 已有「A/B 分流」，而本章的「分流」是同一动作的规模版（按画像选候选），两处指同一个意思，登了要回头改 5.7 的口径；「命中率」不登——它是 5.6／5.7 已在用的通用指标名，本章做的事是**把它拆成三个口径**，拆法是节内的事，不是另一个术语。另：本章是 9 月 19 日开始写的，这三行与正文同一趟交付，而**「补登」这一类在本表里已出现过三次**（「方言」3.6→6.2、「微调」0.1、「成本看板」3.9→6.4）——它们的共同点是「词早就在用、口径从没写死」，而三处的首现章都不在本章，所以行里都带「（展开于 …）」 |
| 2026-09-19 | 登记 4 个术语（评测流水线 / 指标注册表 / 抖动带 / 分辨率），**另有四组候选词被自己拦下**，其中两个是**补登** | 7.1 写评测流水线前定口径，**先登记再写**（第 7 篇的第一批词）。这一批的入口与第 6 篇不同：本章的主语既不是一个模块、也不是一页账，而是**一条判定链**（数据集 → 指标 → 门 → 报告），所以判据先问「这个词在别处有没有同指的东西」——**「评测流水线」是这一篇的第一个词，而它早在 0.1（岗位地图）里就已经在用**；此时不登，它会在这一篇里长成「评估流水线」「评测流程」两个同义词（两个避免写法实测全库零命中）；**「分辨率」也是补登**：它从 5.6 起就在用（那条「可检测最小差异 1/24」），5.7／5.8／5.9 三章各自都在用它；而本章第一次把它写成一个**式子**（`1/n`，n 是「这一次真跑了几条」）——口径一旦定死，它就不再是「精度」「误差」的同义写法；**「指标注册表」与「抖动带」是本章的两个新名词**，各自挡住一组近义写法：前者挡「指标登记表」「度量注册表」（它不只是一张表，是**带校验的注册**：方向与需要的输入在注册时校验、重名要报错），后者挡「噪声带」「波动带」「抖动阈值」（它量的是**极差**，而「阈值」是拿它去做门的那一步——**容差 ＝ 抖动带，但两者不是同一个东西**）。**四组没登成的**：「条目级 diff」不登——它是报告里一栏的名字（`gate.diff()` 的产物），属「交付物名／表名不入表」那一类（同 5.8「台账」、6.4「四栏」），而「聚合级 vs 条目级」这一对口径写在同一节里；「门禁／留出集／冒烟」不登——它们是**切分的名字**（`suite.py` 里就是三个字符串），登了等于给一个配置值立词条；「空集」不登——它是五档里 `empty` 那一档的口语名，而档名在契约与代码里就是 `empty`（英文档名不入表，同 6.3 那四档处置）；「覆盖率」不登——它从 1.14 起就是测试覆盖率的常用词（7 章在用），本章做的是**给它一个分母**（`measured`／`unmeasured` 两个数一起报），那是节内的事，不是另一个术语。另：本章是 9 月 19 日开始写的，这四行与正文同一趟交付；**补登这一类在本表里已出现过四次**（「方言」3.6→6.2、「微调」0.1、「成本看板」3.9→6.4、「批量通道」6.1→6.4），而本章一次补了两个——共同点仍是「词早就在用、口径从没写死」 |
| 2026-09-19 | 登记 4 个术语（语义约定 / 头部采样 / 尾部采样 / 自耗时），**其中三个是补登**，另有四组候选词被自己拦下 | 7.2 写可观测性前定口径，**先登记再写**（第 7 篇的第二批词）。这一批与前几批最不同的一点是：**四个词里有三个的首现章不在本章**——3.9（可观测性与评估）早就把「语义约定」「头部采样」「尾部采样」讲过，只是从没登记过；不登的话，它们会在本章展开时长成同义词（「属性约定」、「前端采样」、「后端采样」，三个避免写法实测全库零命中）。**「自耗时」是本章唯一的新名词**，它与「缺口」必须分开：前者是「这一层自己花了多少」（自己的时长 − 所有子 span），后者是「有多少时间不在任何 span 里」（根时长 − 子 span 覆盖的**并集**）——两个数在串行树上相等、在并行树上分叉（读数里 0ms 对 300ms），合成一个词这条结论就写不出来；避免写法取「独占耗时」「自身耗时」「自己的耗时」（三个都零命中）。**四组没登成的**：「缺口」不单登——它是 7.2.2 那句判据里的口语名（代码里是 `gap_ms()`），与「自耗时」成对出现、不成一族；「成本看板」在 6.4 已登记（3.9 展开于 6.4），本章只是把它挂到 trace 上；「覆盖率」是 1.14 起的通用词（7 章在用，7.1 那一行已写过处置）；「告警疲劳」不登——它在 3.9 的告警一节已是通用说法，而本章做的是**给它一个能算的读数**（同一串窗口下 4／1／1 次），那是节内的事。另：本章是 9 月 19 日开始写的，而**它是全书第一份「事前估在动笔之前就落笔」的章**——口径与篇幅一起在开写前写死，这一行与正文同一趟交付 |
| 2026-09-19 | 登记 8 个术语（分桶 / 灰度发布 / 发布标签 / 受保护标签 / 放量阶梯 / 比例失衡 / 偷看 / 静默漂移），**其中两个是补登，另有五组候选词被自己拦下** | 7.3 写版本化与灰度前定口径，**先登记再写**（第 7 篇的第三批词）。这一批的入口与前两批不同：本章的主语既不是一条判定链、也不是一块现场，而是**一次改动**（改了哪一版、谁看到了它），所以判据先问「这个词在别处有没有同指的东西」——**「分桶」与「灰度发布」是两个补登**：「分桶」早在 5.7 就是那句「A/B 分流要先回答按什么分桶」（「按用户哈希分桶」），6.4.7 把它展开成「同一个人只能进一边」并量出跨池用户（按用户 0/1000 对按请求 964/1000），而本章补的是**它的机制那一层**（哈希版本、半开区间、留缝、命名空间）；「灰度发布」在 2.6 与 4.5 已用过，本章补的是**它的时间账**（挪标签 0 秒 ＋ 一个缓存 TTL）。**「发布标签」与「受保护标签」必须分开命名**：前者是「哪一版在线上」（挪它就是一次发布），后者是**谁能挪它**（权限）；只登一个的话另一个会漂成「部署标签」「锁定标签」，而本章最重的一条结论（受保护标签把「谁能改生产」从约定变成权限）就长在两者的区分上。**「比例失衡」与「偷看」是同一张门的两半**：前者量的是**分桶坏没坏**（与效果好不好不是一件事），后者量的是**多看几次要付多少假阳率**（名义 5% → 看 20 次 24.05%），合成一个词这条结论就写不出来。**「静默漂移」不写作「隐性漂移」**——它指的是一件很具体的事：现场跑的那一份与标签指的那一版**内容戳对不上**（有人改了提示词而没换版本），而它必须与 8.x 的「漂移」（文档与盘面的漂移）分开说。**五组没登成的**：「规范化」不登——它是 `canonical()` 那个动作的通用名（GLOSSARY 里已有「归一化」，两者不是一件事），全库多处当普通描述用；「快照」不登——它已是 4.4 的一般用语（流里的每一帧）；「实验」不登——它是本章与 5.7／6.4 共用的通用词，本章只给它补一个必填字段（失效线）；「副本」不登——日志副本是运维的常规说法；「看板」在 6.4 已登记（成本看板），本章只是把「版本」这一维加进去。另：本章是 9 月 19 日开始写的，这八行与正文同一趟交付 |
| 2026-09-19 | 登记 1 个术语（审计链），**另有六组候选词被自己拦下** | 7.4 写安全落地前定口径。**这一批只有一行，是本篇最短的一批**，原因在章的定位：它接在 3.8（安全对齐与防护）后面，是一章「落地」——本章绝大多数词（提示注入 / 越狱 / 直接注入 / 间接注入 / 误杀率 / 权限预算 / 审计日志 / 样例集 / 包幻觉 / 过度代理）都在 3.8 或 2.1 登记过了，本章的义务是**用它们**而不是新造。唯一要登的是**审计链**：3.8 只给了「审计日志」，而本章最长的一组对照就长在两者之间（同一个篡改在链上 3/3 检出、在只有日志的对照上 0/3），不分开命名的话它会漂成「日志链」或与「审计日志」混用。避免写法取「审计链条」「日志链」（两个在 `book/` 下实测 **0** 命中）。**六组没登成的**：「一档／二档／三档／四档」不登——它们是**同一套规则的开关**（`tier` 参数的四个取值），不是四个概念，也不各自展开一节（本章用的是「四档判定」这个算法名，而这一个是 7.4.3 那一节的主语）；「拦截率」不登——3.8 已把「拦截率 ／ 误杀率」成对立过，只登一半会把成对关系抄成两半（本章的两笔账必须一起报，这正是这一对不能拆的理由）；「来源分级」不登——它指的是「同一句文本放在四个位置」，而 3.8 已给过直接／间接注入这一对，本章做的是**在四个位置上各判一次**（读数里的 4 个位置）；「自主性档」不登——`book/` 实测 **0** 命中（这个词是本章构思时自己造的一个候选名，实写时用的是已登记的「权限预算」）；「钉版」不登——它是普通说法而不是术语（2.1 写过「锁版本」、2.6 写过「锁文件」，都不是词条），本章只用它的**行为**（不钉版发现 0 次、钉版发现 1 次），不需要为它立名。另：这一行与正文同一趟交付 |

| 2026-09-19 | 登记 9 个术语（最小化 / 目的限定 / 留存期限 / 可关联性 / 重新识别 / 准标识符 / 校验位 / 形状规则 / 删除范围），**另有五组候选词被自己拦下** | 7.5 写数据合规前定口径，**先登记再写**（第 7 篇的第五批词，也是本篇最后一批）。这一批是**全书最多「先登记再写」的一章而它自己只有一篇词**：三件套里有两个词是**新造的中文口径**（最小化、目的限定）——它们在政策文本里天天出现，而在技术书里没有公认的中文写法，不定的话会漂成「最少化」「用途限制」（四个避免写法在 `book/` 下实测均 0 命中）；**「留存期限」不写作「保存期」**，因为「保留期」在 3.6（记忆）已经是另一个语境的口语写法，故只把「保存期」列为避免写法，不去动 3.6 的用法。**「可关联性」与「重新识别」必须成对登记**：前者是手法的性质（两份数据能不能 join），后者是攻击的成功面（能不能把一个人认出来）——本章第三节那两组读数（四种手法里两种可关联、尾号加生日唯一率 80%）就是这两个词各自的读数，合成一个词这两组数就写不出来。**「准标识符」是本章唯一一个生词**（1 命中，就在第三节那一段）：它指的就是那两个字段（尾号与生日）本身——单独看不敏感、合起来能定位。**「校验位」与「形状规则」是一对**：前者查「是不是」（Luhn、身份证第 18 位），后者查「像不像」（四条正则），两个词不分开的话，本章那组「12 处命中／4 处确认／2 处假阳／6 处无校验位可用」的读数就说不清——它是本章最长的一组对照；避免写法取「检验码」「校验码」（0 命中）与「形态规则」「外观规则」（0 命中）。**「删除范围」是给一句已经存在的话补的名字**：3.6 与 3.8 都说过「能删、有保留期」，而「删到哪几个存储」这件事一直没有名字，本章把它定成「删除范围」。**五组没登成的**：「脱敏」已在 1.14 登记（本章只是把它放到盘上的语境里）；「匿名化」不单登——它是脱敏要达到的那个状态，而本章最重的一句判据是「脱敏 ≠ 匿名」，登成两个词会让那句话读起来像在比较两个名词；「内容捕获」已在 3.9 登记（本章只量了它两个位置）；「识别码」不登——它是这一类对象的统称，而 1.14 用过「PII」、3.8 用过「个人信息」，三个说法都不需要统一成一个词（它不是需要统一口径的概念，是一类对象的集合名词）；「删除传播」不登——它与「删除范围」同指，登两个会让同一件事在一章里有两个名字。另：这一行与正文同一趟交付 |
| 2026-09-20 | 登记 4 个术语（层账 / 可见账 / 浪费账 / 白障），**另有五组候选词被自己拦下**——**这是全书第一次「补登」，而它是补的一章压根没登的那一次** | 8.1 写容器化部署。**这一批与前七篇的每一批都不同：它不是开写前定口径，是写完之后回头补的**——本章开写时漏了术语这一步，录完台账才回头补上（四个避免写法在 `book/` 下实测仍是 **0 命中**，因为这一章自己也没用过别的叫法，但这是运气，不是纪律）。四个词的判据与前几批相反：**它们全长在同一个量上**——「镜像有多大」。**「层账」与「可见账」必须分开命名**：前者是每行指令相加（1.60 GB），后者是联合文件系统真报出来的那一个数（1.49 GB），而本章最长的一组读数就长在两个数之间（浪费账 107 MB）；不分开命名，本章开头那句反直觉的话（「删了不会变小」）就写不出来。**「浪费账」是给「层账 − 可见账」这个差立的名**（正文 16 处全用这个写法）。**「白障」保留 Docker 的原词 `whiteout`**，而且不写作「白障层」：它是**那一层里的一个文件**，加上「层」会让它与承载它的那一层混。**五组没登成的**：「构建上下文」不登——官方术语的直译，中文写法已经固定；「多阶段构建」同此；「只读根文件系统」不登——它是 `read_only: true` 的中文直译，属「参数名不入表」那一类（同 6.3「冷却」「半开」）；「健康检查／就绪探针」不登——8.1.5 讲的四个参数就是它本人，登它等于给一组参数立词条；「基础镜像」不登——它从 1.1 起就是通用说法。另：**这一次漏登暴露了术语日志对账门的一个边界**——它查的是「有词条却没有登记行」，而**「本该有词条却一个都没登」这一类它看不见**（0 词条的章在它眼里与「不需要词条的章」长得一样）；4.1 那次「新树建起来而 `SPECS` 里没它」是同一族：**对账只能查「两边都有而其中一边错」，查不出「两边都没写」。** |
| 2026-09-20 | 登记 4 个术语（必需检查 / 爆炸半径 / 绿而没验 / 静态 key），**另有六组候选词被自己拦下**，其中**一处是跨篇漂**（把 7.3 的「放量阶梯」写成了「金丝雀阶梯」——已改回，并把那个写法补进 7.3 那一行的禁止列） | 8.2 写 CI/CD 前定口径（第 8 篇的第二批词）。**这一批与前七篇都不同的一点：它里面没有一个是「新概念」——四个词都是书里之前已经绕不过去的说法，而登记的理由是「它是会被反复引用的那一格」**：必需检查（官方 `required status checks`，而它的合格状态只有三种；**这一条是「跨篇统一」而不是新词**——2.6 全篇写的是「必需状态检查」（7 处），本次一并改成「必需检查」，因为 8.2 要用它指那一整类结论：报 Success 的、停在 Pending 的、被取消的）、爆炸半径（**补登**——它在 2.1 已经是四条判据之一，8.2 把它扩到流水线的身份层：一个被投毒的三方 action 能做什么；三种写法都试过：危害半径／波及面／作恶范围）、绿而没验（`gate` 四个分组里的第二组，与「停在 Pending」是一对相反的结论）、静态 key（不含指纹的缓存键，而它看起来一直命中）。拦下的六组：钉法（与 2.1「锁版本」同指，8.2 只当普通说法用；**与 7.4 那次拦下的「钉版」是同一条理由**）、金丝雀阶梯（与 7.3「放量阶梯」同指，已纠正）、固定样本数／固定窗口（它们是「判据」的取值，不是词条）、矩阵格／格数（口语）、Pending（官方状态名直引）、跳过标记（五个官方字符串直引）。**另记下一条对账门看不见的事**：「跨篇同指的两个名字」只能靠人抓——「金丝雀阶梯」与「放量阶梯」都不是错的，而它们指的是同一件事（8.1 那次记的是另一类：本该有词条却一个都没登）。**同一批里还做了一次跨篇归一**：2.6 里四处「必需状态检查」（学习目标、正文与两处树形图）都改成了「必需检查」——这是上面那条的正例修法；而它有一个连带后果：**2.6 的实测从 23,885 变成 23,871**（差的就是被换掉的那几个字），它只记在 `PLAN` 章表那一行。**改一个已完工章的用词，就动了它的篇幅数**——这一条与 8.2 落地时 8.1 的那次漂同族，已写进 `STYLE` 8.11 的第三条边界 |
| 2026-09-20 | 登记 8 个术语（租户 / 行级安全 / 分区键 / 配额 / 限流 / 负载卸载 / 预扣 / 搁浅成本），**另有六组候选词被自己拦下**，其中**一处是「限流」把源文里的「速率限制器」翻了回来** | 8.4 写多租户、配额与计费前定口径。**先登记再写**（这一章三块模块里有两块的名字直接来自这一批：`tenant` 与 `quota`）。拦下的六组里最值得记的一处：官方那篇讲的是 rate limiter，直译成「速率限制器」会让它与「限流器」并存——**一个概念两种写法比用错一个词更坏**（读者会以为是两个东西）；另，「预算」（6.3 已登记）与本章的「配额」是两层不同的账（一次调用内部 vs 租户级），这一行也把它写清 |
| 2026-09-20 | 登记 8 个术语（客户端状态机 / 慢消费者 / 队列上限 / 卡在流式中 / 显示错 / 缓冲长度 / 增量帧 / 一吐一大片），**另有六组候选词被自己拦下**（`EventSource`／`Last-Event-ID`／`retry` 是 API 名与字段名、SSE 与事件流是协议官译、心跳与背压已登记、状态机与缓冲区是通用词），其中**一处是跨章漂**：把 5.7 已经定下的「首字时长」写成了「首字延迟」——已全篇改回（13 处），并把「首字延迟」记进那一行的避免写法 | 8.3 写前端流式交互。**这一批与前几批的不同在于它一次拦下了两件事**：一是同一概念两个名字（首字延迟／首字时长），二是「同一件事的对偶词必须成对登记」（状态错与显示错、增量帧与全量帧）——把对偶拆成两个互不相干的词条，读的人就看不出它们是成对的 |
| 2026-09-20 | 登记 14 个术语（服务级指标 / 服务级目标 / 错误预算 / 燃尽率 / 多窗口多燃尽率 / 症状告警 / 演练 / 复盘 / 行动项 / 事故时间线 / 利用率 / 预留余量 / 拐点 / 元监控），**另有七组候选词被自己拦下** | 8.5 写线上运维前定口径，**先登记再写**（第 8 篇的第五批词，本篇收口）。两处值得记：**「错误预算」与 8.4 的「配额」必须分开**（一个量「这一段时间能坏多少」、一个量「这个租户能用多少」）；**「演练」与「复盘」也分开**（前者是主动去坏、后者是坏了之后），这一批还拦下了「演习」「事后分析」这类更顺口的写法 |
| 2026-09-21 | 登记 6 个术语（应聘者跟踪系统 / 关键词覆盖率 / 投递漏斗 / 证据链 / 版面预算 / 量化），**另有五组候选词被自己拦下** | 10.1 写简历前定口径，**先登记再写**（第 10 篇的第一批词）。三处值得记：**「应聘者跟踪系统」不是「筛选系统」**（它只把字段解析出来，判你行不行的还是人）；**「量化」不是「数字化」**（后者在这个语境里会被读成「把简历做成数据表」）；**「投递漏斗」与 5.9／8.4 的「账」分开**（它数的是投递的去向，不是调用或钱） |
| 2026-09-21 | 登记 7 个术语（澄清五问 / 范围表 / 非目标 / 分档路由 / 语义缓存 / 容量估算 / 降级链），**另有五组候选词被自己拦下**（「鉴权」→ 用「授权」、「会话状态」→ 用「会话」，以及「模型分流」「智能缓存」「兜底链」这三组更顺口的写法） | 10.3 写系统设计面试前定口径，**先登记再写**（第 10 篇的第三批词）。两处值得记：**「分档路由」与 5.5 的「分档」必须分开**（一个分请求、一个分答案）；**「语义缓存」与 3.1 的「前缀缓存」必须分开**（命中判据不同、代价也不同）；而这一批拦下的两个词当场就在正文里被 lint 报了出来；第三处是反向的：候选词「需求表」差点把 1.5 那个合法的旧用法拦下（一登记就报错），于是它没进拦下名单 |
| 2026-09-21 | 登记 6 个术语（段落预算 / 讲述骨架 / 主线 / 追问防御 / 反事实追问 / 失败预案），**另有四组候选词被自己拦下** | 10.2 写项目讲述前定口径（第 10 篇的第二批词）。三处值得记：**「段落预算」与 10.1 的「版面预算」必须分开**（一个按秒、一个按行，而两者都是「一页／三分钟里装什么」）；**「追问防御」不是「话术」**（前者是三层追问各自的处置，后者会把「承认边界」那一档当成失败）；**「反事实追问」单独成条**（它是第三层，而三层里只有它考的是机制理解而不是记忆） |
| 2026-09-21 | 登记 6 个术语（考点索引 / 三层可用性 / 归属表 / 题库盲区 / 速查表 / 专题漂移），**另有四组候选词被自己拦下**（「映射表」在 0.1／4.4 里已指另一件事、不改指；「速记表」「速查卡」「覆盖缺口」是更顺口但更含糊的写法） | 10.4 写题库索引前定口径（第 10 篇的第四批词）。两处值得记：**「归属表」与 0.1／4.4 的「映射表」必须分开**（后者是关系的映射、前者是专题到章的对照，而这一批一开始真把「映射表」列进了拦下名单，一登记就会把 4.4 的合法用法报成违规，于是撤回）；**「考点索引」是一次性的词**——它只在本篇的题库语境里成立，所以英文栏里也写了「限第 10 篇」 |
