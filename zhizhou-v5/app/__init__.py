"""知舟 v5：检索版（第 5 篇）。

树的关系先说清楚，这是 `PROJECT.md` 第一节的硬约定：

- `zhizhou-v3/` 是**手写版**（第 3 篇），不出现任何 Agent 框架；
- `zhizhou-v4/` 是**框架版**（第 4 篇），同一件事用 LangChain / LangGraph 再写一遍；
- 本树是**检索版**（第 5 篇）：在 v4 那一层（模型出口、提示、链、Agent、
  状态编排、持久化）之上长**知识层**——把外部资料变成能检索、能引用、能评估的东西。

三棵树共用同一份 `.env`（`LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL`）与同一套工具契约，
所以正文里能逐段对照，而差异里不会混进无关变量。
本树**不依赖 v3 或 v4 的内部实现**：需要的那点东西（模型出口、剧本模型）在这里重写一份，
写短一点、只留第 5 篇用得上的部分。
"""
