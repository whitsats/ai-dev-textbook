# 检索版（第 5 篇）的依赖。**按章加，不预装。**
#
# 5.1 的新东西全部在标准库里（倒排索引、BM25 打分、切分、引用核对），
# 所以这一份与 `zhizhou-v4/requirements.txt` 开头一致——本树唯一用框架的地方是真机出口。
# 后面几章会各自加一行，写在对应的那一章里（这是本书的一贯做法：
# 依赖出现在正文讲过它之后，而不是提前装好等读者去猜）：
#   5.3 向量化与向量数据库：sentence-transformers 或服务商嵌入接口 ＋ pymilvus
#   5.4 混合检索与重排：rank-bm25 /（可选的）reranker
#   5.6 RAG 评估：fiftyone？不——RAGAS
#
# 5.2 实际用到的四个解析库写在下面（当初那行计划里的 `pypdf` 最后没用上：
# 正文选了 PyMuPDF，理由是它给得出**页级**文本与分栏顺序，pypdf 给不了）。
# 正文按「能力探测」写——装了就多认一种格式，没装就走 Unsupported 路径；
# 但**那四条 Unsupported 路径的取值本身也是断言**（例如「图片缺 OCR 引擎」），
# 所以它们必须能被真跑到：CI 上不装，就等于那几条用例永远只走同一支。
#
# 本机实测版本（第 5 篇正文里的 API 名称以它为准）：langchain 1.3.2 ｜ langchain-openai 1.2.2
# ——与 zhizhou-v4 同理，这两行锁死：区间写法在 CI 上装到了 1.4.2／1.6.3，
#    而正文的 API 名称与行为是照 1.3.2／1.2.2 写的（同一个坑在一次 CI 里踩了两遍）。
langchain==1.3.2
langchain-openai==1.2.2

# 5.2 的解析器（括号里是本机实测版本）：
pymupdf>=1.24            # PDF（1.28.0）：`import fitz`——按魔数路由、页级抽文本
pillow>=10.0             # 图片与 OCR 的前置（10.4.0）
python-docx>=1.1         # docx（1.1.2）：样式里那层标题层级
python-pptx>=1.0         # pptx（1.0.2）：备注页
# OCR 这一行**本机没装**，而 CI 会装——于是它同时把两种「没有引擎」的形态都跑到：
# 本机是「壳不在」（import 就失败），CI 是「壳在、引擎不在」（pytesseract 只是壳，
# 真正的引擎是系统里的 `tesseract` 二进制，CI 上没有）。两条都归同一档 Unsupported，
# 且抛出的文案相同——因为对读者来说修法是同一条（两样都要装上）。
pytesseract>=0.3

# 只在跑测试时需要（本书的验收不依赖它：python tools/check_runnable.py 会用同一批测试函数自己驱动）
pytest>=8.0
