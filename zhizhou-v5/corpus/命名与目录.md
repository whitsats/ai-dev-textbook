# 知舟命名与目录约定

## 目录分层

后端代码按九个目录分层：`api`、`services`、`dao`、`models`、`schemas`、
`config`、`dependencies`、`core`、`utils`。跨层调用只允许自上而下，
`dao` 不得反向依赖 `services`。

## 命名

- 表名用**单数**（`article` 而不是 `articles`）；
- 文件名**禁止**出现 `_v2`、`_new`、`_old` 这类后缀——版本由 git 记，不由文件名记；
- 检索相关的模块统一放在 `rag` 包下，不散在 `utils` 里。
