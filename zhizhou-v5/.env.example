[TEMPLATE]
# 与 v3／v4 完全同一套变量名：三棵树共用一份配置，
# 「加了知识层之后差在哪」里就不会混进无关变量。
#
# 注意：**检索这一侧不需要密钥**。`scripts/why_rag.py --offline` 从头到尾不读这几行，
# 所以「知识层能不能被验证」不取决于你有没有服务商。
LLM_API_KEY=sk-xxx
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0
