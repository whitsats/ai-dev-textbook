知舟上线检查单（发布前逐条打勾）

一、变更面
- 本次改动的提示正文版本号已更新，并同步进了缓存版本戳。
- 语料清单的哈希变了（新增或修改过文档），已重新入库。
- 索引参数（维度、n-gram、切分策略）与上一次发布一致，或已写明为什么变。

二、质量面
- 回归门跑过：片级命中与答案命中不得低于上一次发布的基线。
- 引用核对通过：发出去的答复里每一句的编号都由代码绑定。
- 拒答四档没有退步：漏拒从 1 降到 0 才算改善，误拒不许增加。

三、容量面
- 单次提问的四笔账与上一次发布会话的可比口径一致。
- 缓存的命中层分布没有突变（答案层命中率突然翻倍通常意味着键算错了）。
