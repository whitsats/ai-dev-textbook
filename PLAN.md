# 编写计划（PLAN.md）

> **本版为素材审计校准 + 体系缺口审计后的定稿**：已采纳扩展**方案 A**（见第四节），
> 全书为 **10 篇 69 章 / 约 855,000 字**。第 0–1 篇正文**已完成**（18 章，实测 180,110 字），
> 第 2 篇正文**已完成**（6 章，实测 111,636 字），第 3 篇正文**已完成**（10 章，实测 163,223 字）
> ——即 3.1–3.10 已定稿（3.5–3.10 均含接线小节与真机联调记录，台账已回填）；
> 第 4 篇正文**已完成**（5 章，实测 46,682 字，含框架版可运行树 `zhizhou-v4/`）；
> 第 5 篇正文**已完成**（9 章，实测 115,217 字）；
> 第 6 篇正文**已完成**（4 章，实测 38,091 字）——这一篇含**第四棵**可运行树 `zhizhou-v6/` 的四段增量（6.1 选型与算账、6.2 多厂商接入、6.3 网关与降级、6.4 计量与看板）。
> 第 7 篇正文**已完成**（5 章，实测 52,848 字）——这一篇至此收口，含**第五棵**可运行树 `zhizhou-v7/`（工程化版：7.1 的评测流水线（数据集、指标与门三层）＋ 7.2 的可观测两块（追踪与成本看板）＋ 7.3 的版本与分桶两块 ＋ 7.4 的护栏与策略两块 ＋ 7.5 的留存与脱敏一块）。
> 第 8 篇正文**已完成**（5 章，实测 67,495 字）——**本篇至此收口**，含**第六棵**可运行树 `zhizhou-v8/` 的**五段增量**（8.1 的容器化四块：`image`／`cache`／`compose`／`runtime`；8.2 的交付流程六块：`trigger`／`graph`／`caching`／`perms`／`gate`／`release`；8.3 的用户眼前那一格两块：`sse`／`stream_ui`；8.4 的租户与账三块：`tenant`／`quota`／`billing`；8.5 的线上运维四块：`slo`／`alert`／`drill`／`capacity`）。
> 第 10 篇正文**已完成**（7 章，实测 77,282 字，**10.7 是全书最后一章**）——**本篇不建第七棵树**（动笔之前定的形态：它交的不是服务，是**题与账**；选树之前问过用户，与第 5、6 篇同例），所以它是全书第一篇章内**没有「接线」小节**、篇内**没有 `SPECS` 行**的篇。
> 上面这些「已完成」里，第 5–7 篇指**篇内已定稿的章数**（5.1 含检索版可运行树 `zhizhou-v5/`，
> 5.2 在它里加上多格式解析与四代切分，5.3 加上嵌入与三种向量索引，
> 5.4 加上两条路融合、查询改写与重排，5.5 在生成侧加上分档、绑定与拒答，
> 5.6 把五章读数统一成一条判据并接成回归门，
> 5.7 把管线装成常驻服务：三层缓存 ＋ 单飞 ＋ 限流 ＋ 事件流 ＋ 版本戳 ＋ 四笔账，
> 5.8 把它装成一台能验的设备：入库管线 ＋ 多格式语料 ＋ 三套库并排对照 ＋ 八项验收清单，
> 5.9 把八章的零件与读数装成两份**能被机器核对**的文档：两张架构图 ＋ 六条决策记录 ＋ 一条对账命令），
> 第 5 篇与第 6 篇至此先后收口（第 6 篇 6.1–6.4 全部定稿，其「篇收尾三件事」跑完的结果落 [`LEDGER.md`](LEDGER.md) 的第 6 篇统稿记录——这一篇**没有收口章**，四章全是增量章；`zhizhou-v6/` 是**第四棵树**，与前三棵最大的不同是**它不调模型**——
> 读数是算出来的账单，不要密钥、不要网络，所以它能在提交门里一直跑）；
> 第 7 篇开工（7.1–7.5 全部定稿；`zhizhou-v7/` 是**第五棵树**，它换的是问题：前四棵回答「怎么把它做成」，这一棵开始回答「**怎么知道它还行**」——7.1 答「变差了没有」（离线判据），7.2 答「刚才那一次发生了什么」（在线现场），而两者只共用一样东西：同一个 `trace_id`）；
> 全书进度 **69 / 69 章**，实测 852,584 字。
> 第 3 篇自 2026-09-13 起改为**「原理 ＋ 逐章接线 ＋ 组装」**：3.1–3.10 每章给知舟加一段
> 可独立验收的增量，并以 3.10 收口；契约（文件路径、端点、验收标准、硬约定）见 [`PROJECT.md`](PROJECT.md)。
> 本文档里的汇总数字（文首进度句与篇级小计、篇级覆盖度表、分篇标题、批次表、第八节看板、
> `README.md` 的进度与结构表、`COVERAGE.md` 全表）**一律由 `python tools/totals.py --sync`
> 从单一来源写回**——手改这些格会被 `--check` 报出来；口径与边界见 [`STYLE.md`](STYLE.md) 8.11。
> 素材覆盖度数据来自 `python tools/audit_coverage.py`，报表见 [`COVERAGE.md`](COVERAGE.md)；
> 体系缺口分析见 [`GAPS.md`](GAPS.md)；官方文档出处见 [`REFERENCES.md`](REFERENCES.md)。
> 状态标记：⬜ 未开始 ｜ 🟡 进行中 ｜ ✅ 已完成

---

## 一、总体设计

### 1.1 组织原则

1. **从浅到深**：地基（会写代码）→ 工具（会用 AI 写代码）→ 原理（懂为什么）→ 框架（会搭）→ 系统（能做）→ 求职（能讲）。
2. **一条主线**：全书围绕同一个项目「知舟」成长（见 [`STYLE.md`](STYLE.md) 第五节），不换 demo。
3. **知识点闭环**：每个技术点都回答三件事——*是什么 → 怎么用 → 面试怎么问*。
4. **题库不单独堆放**：题库按考点打散，嵌回各章「面试视角」，第 10 篇只做综合冲刺。
5. **素材是原料不是成品**：所有章节重写，保留溯源，不整段搬运。

### 1.2 规模

| 项目 | 数值 |
| --- | --- |
| 篇数 / 章数 | **10 篇 / 69 章**（含扩展篇 14 章与第 3 篇收口章 3.10；第 9 篇留作「进阶方向」，暂不启用） |
| 计划总字数 | **约 855,000 字** |
| 素材总量 | **1,245,172 汉字**（散文 249,610 ＋ 题库 995,562） |
| 需原创补写的章 | **62 / 69 章**（全书口径，含方案 A 扩展篇、第 3 篇接线与题库驱动章），其中 28 章散文素材为零 |

> 与初版计划的差异：素材审计把第 4 篇由 8 章压到 5 章（56 → 53 章）；
> 体系缺口审计采纳方案 A，新增第 6–8 篇与「AI 系统设计」1 章（53 → 68 章）。
> 扩展篇编号后，**求职冲刺由第 6 篇改为第 10 篇**，章号相应变为 10.1–10.7。

### 1.3 难度曲线

```
浅 ────────────────────────────────────────────────────────────► 深
第1篇        第2篇        第3篇        第4篇        第5篇
Python     AI 工具链    原理与概念   框架与编排   系统工程
FastAPI    （生产力）    （为什么）   （怎么做）   （做多大）
素材 0.61   素材 0.36    素材 0.04    素材 0.12    素材 0.47

第6篇        第7篇        第8篇        第10篇
模型接入     应用工程化   交付与产品化  求职输出
（怎么调）    （怎么控）   （怎么上线）  （怎么讲）
新增·纯原创  新增·纯原创  新增·纯原创   题库驱动
```

---

## 二、素材审计结论（本版新增）

审计按**汉字量**而非页数计量，并区分两类素材。这一步改变了原计划的多处判断。

### 2.1 三个关键发现

**发现一：素材总量很大，但分布极度失衡。**

| 篇 | 章数 | 计划字数 | 散文素材池 | 题库池 | 散文比值 | 判定 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 0 导论 | 3 | 12,000 | 0 | 0 | 0.00 | 无素材 |
| 1 编程地基 | 15 | 184,500 | 112,192 | 0 | **0.61** | 偏薄 |
| 2 AI 时代的开发方式 | 6 | 115,500 | 41,855 | 0 | **0.36** | 严重不足 |
| 3 大模型与 Agent 原理 | 10 | 155,500 | 5,700 | 271,204 | **0.04** | 严重不足 |
| 4 AI 应用开发框架 | 5 | 38,000 | 4,529 | 0 | **0.12** | 严重不足 |
| 5 RAG 与生产级系统 | 9 | 110,000 | 52,171 | 0 | **0.47** | 偏薄 |
| 6 模型接入与成本工程（扩展） | 4 | 35,000 | 0 | 0 | **0.00** | 无素材 |
| 7 AI 应用工程化（扩展） | 5 | 56,500 | 0 | 0 | **0.00** | 无素材 |
| 8 交付与产品化（扩展） | 5 | 63,000 | 0 | 0 | **0.00** | 无素材 |
| 10 求职冲刺 | 7 | 85,000 | 0 | 201,627 | — | 题库驱动 |
| **合计** | **69** | **855,000** | | | | |

> 散文比值 = 散文素材汉字 ÷ 计划汉字。教材正文不是素材的压缩，一章 6,000 字
> 需要 1.2 倍以上素材才算充裕。分篇用**素材池**（各章计权相加，单份素材在一篇里最多算 1.0），因为同一段素材
> 会被多章共用，各章直接相加必然虚高。

**发现二：页数会骗人，关键词检索也会骗人。**

| 素材 | 页数 | 汉字 | 真相 |
| --- | ---: | ---: | --- |
| LangChain 从入门到实战 | 23 | **2,279** | 页数看着不少，实际只有 2 千多字，且全文**没有一个规范的代码块** |
| LangGraph 智能体实战 | 28 | **2,401** | 同上 |
| 大模型 AI Agent 从 0-1 笔记 | 113 | **12,774** | 113 页只有 1.3 万字，是稀疏的演示稿 |
| Python 核心语法手册 | 238 | **54,421** | 其中 **30,078 字（55%）藏在无编号标题的"进阶块"里**，覆盖异常/日志/装饰器/OOP 进阶/网络编程/并发编程；按关键词检索会误判为"没素材" |
| 生产级 RAG 系统构建实战 | 229 | **32,068** | 结构极佳：Milvus、RAGAS、流式通信、可观测性、企业级项目，几乎逐条对上第 5 篇 |

**发现三：题库质量分层严重，不能一律当作"真题答案"。**

| 题库 | 汉字 | 真实题量 | 质量 |
| --- | ---: | ---: | --- |
| AI-Agent 面试题合集 | 352,616 | 1038 道 | **低**：结构完整（考点/解答过程/答案），但答案是通用套话，存在专题漂移——"规划与决策"专题里混入强化学习的状态空间/动作空间题，"检索增强"专题抽到的是灰度发布流量分配 |
| Android 面试题合集 | 344,074 | 1136 道 | **中**：结构完整，抽样内容有实质（如 Intent 五要素讲得清楚），但仍需逐题筛选 |
| Flutter-Dart 面试题合集 | 214,864 | 714 道 | **偏低**：至少 20 题答案是模板占位（"先明确题目中的核心对象和使用场景……"），零实质内容 |
| 后端高频面试题大集合 | 84,008 | 165 道 | **高**：含考点分析与难度标注，但**体系是 Java**（JVM 107 次、Spring 57 次、垃圾回收 83 次，而网络 0 次、Python 2 次），须换视角 |

> 这条发现直接推翻了两处原计划：10.4 原本要"从 1038 道中精讲 120 道真题"——
> 这些题不是真题，答案也不可信，只能当**考点索引**用；10.5 原本假定是通用后端题库，
> 实际是 Java 体系，须整体换成 Python / AI 岗位视角重写。

### 2.2 逐章覆盖度

完整 69 行逐章报表见 [`COVERAGE.md`](COVERAGE.md) 第三节。判定为**偏薄及以下**的章节汇总：

| 判定 | 章节 | 章数 | 计划字数 |
| --- | --- | ---: | ---: |
| 严重不足 | 1.10、1.11、1.12、1.13、1.14、1.15、2.1、2.4、2.5、2.6、3.2、3.3、3.4、3.5、3.6、3.7、4.1、4.2、4.3、4.4、4.5、5.1、5.3、5.5、5.7、5.8、5.9 | 27 | 393,600 |
| 偏薄 | 1.1、1.5、1.6、2.2、2.3、5.2、5.6 | 7 | 89,000 |
| 无素材 | 0.1、0.2、0.3、3.10 | 4 | 24,000 |
| 题库驱动（散文为零） | 3.1、3.8、3.9 | 3 | 46,900 |
| **合计** | | **41** | **553,500** |

**原定第 1–5 篇中素材为零的 8 章**：`0.1` `0.2` `0.3`（导论，本就是综述性写作）、
`3.1` 大模型基础（Transformer/Token）、`3.7` `3.8` `3.9`（多 Agent／安全／
可观测性，只有不可信的题库）、`5.9` 项目复盘。
> 后来 5.9 也量出了素材（**299 汉字**：素材第 1 章的「需求分析与架构文档模板」一节，
> 2026-09-17 按 `STYLE` 8.9 从 5.1 名下拆分，5.1 因此由 3,962 降到 3,663）——
> 它现在在「严重不足」那一栏里，而不是「无素材」。
> 按节/按题实测后，这份名单已不准确：**3.1 改判「题库驱动」**（题库有 9,539 汉字
> 就是它的地盘），**3.7 实有笔记素材 966**，而 `3.8`/`3.9` 仍散文为零。
> 现在的口径是上表，不再是「素材为零 8 章」。

采纳方案 A 后，零素材章增至 **25 章**：再加上第 6–8 篇的 14 章与第 10 篇的 7 章
（求职篇属题库驱动，正文同样须重写）。全书口径的完整表见 [`COVERAGE.md`](COVERAGE.md) 第三节。

> **防丢检查（本轮新增）**：`audit_coverage.py` 现在会核对「每个素材单元是否都被分配」——
> 本轮就是靠它发现**手册第 12–17 章（if/for/while 控制流）没有被任何章用到**，
> 已补进 1.2 的素材池（该类比值从 1.39 升到 2.20）。

> `3.7`–`3.9` 虽然题库里有 300 多道同名题目，但审计抽样确认答案是通用套话
> （详见下文发现三），因此实质上仍需原创撰写，不能计入“有素材”。

### 2.3 由审计得出的三处计划修订

1. **第 4 篇由 8 章压到 5 章**（55,000 → 33,000 字）。LangChain + LangGraph 合计只有
   4,680 个汉字且无结构化代码，写 8 章等于 100% 原创；压到 5 章并把细节交给官方文档，
   反而是更诚实的做法。
2. **第 5 篇综合项目换主干**。原计划用《实战智能出行 Agent 助手》（仅 1,944 汉字）撑
   10,000 字的实战章，不可行；改用《生产级 RAG》自带的「企业级多格式知识库问答系统」
   （素材 10,169 汉字，含项目结构、评估、运维），智能出行项目降为补充案例。
3. **第 3 篇明确标注"以原创为主"**。9 章里 6 章判定严重不足、3 章为「题库驱动（散文为零）」，AI Agent 题库
   降级为考点索引，答案须依权威资料重写。
4. **采纳扩展方案 A**（第 6–8 篇 + 系统设计章）。工程化与交付这一整块在素材中
   **覆盖度为 0**，只能依官方文档原创；它们依次排在批次四，不与素材改写混在一起。

---

## 三、分篇章节规划

每章列出**计划字数**与**散文比值**（素材充裕度）。

> **字数口径（重要）**：表中的「计划」是**有效字数**，第 1 篇及以后各篇的
> 「状态」列一律填**有效字数**（口径由 `lint_book.py` 统一计算）。
> 技术章节的代码本身就是内容，只数汉字会把「代码密集、讲得清楚」的章误判成偷工减料：
> 1.4 的汉字只有 2,735，但它还有 205 行有效代码，换算后是 5,810。
> 折算公式：
>
> ```text
> 有效字数 = 汉字数 + 15 × 有效代码行数     # 一行有效代码 ≈ 15 个汉字的信息量
> ```
>
> 为什么不直接放宽容差：放宽容差会把**真问题**（某章确实没写够）一起放过；
> 折算成同一口径再比，才能既不冤枉代码密集的章、也不放过写得单薄的章。
> 偏差容忍 ±40%，由 `python tools/lint_book.py` 自动校验。
>
> 🔧 **本节口径修正（2026-09-13）**：上式里的「有效代码行数」原本把整块 `splitlines`
> 全数计入，于是开围栏（` ```python `）与闭围栏也各占去 15 个汉字的位置——
> 与上面那句「有内容、**不含空行与围栏**」自相矛盾。
> **全书 671 个围栏、虚增 20,130 有效字**（实测 311,870 → **291,740**）。
> 已修 `lint_book.py`，并重算本表每章的「✅ 字数」「达成%」与全部汇总；
> 换算关系很简单：**每章减去 30 × 代码块数**（2.6 减 1,230、1.11 减 2,250）。
>
> 修正时踩到的三处细节（都已进 `lint_book.py` 的注释）：围栏并不总是裸 ` ``` `——
> 引用块里是 `> ``` `（1.14）、列表项里是缩进后的 `   ``` `（2.5）；少了这三态判定会
> 静默少减一行（第一轮就漏了 2.5，靠「每章差值应恰为 30 × 块数」这一比对才揪出来）。
> **本文件里 2026-09-13 之前的叙述性快照**（各章「实写 X 字、达成 Y%」）记的是旧口径，
> 不逐条回改——它们记录的是当时的实测，与新表不一致时以新表为准。
> 教训与 8.5 同族：**口径写在一行注释里、实现却按另一种算法跑，两边从来没对过**；
> 而它同时是每章状态、达成% 与全书总分的单一输入，一旦漂了就全漂。
>
> **已完稿 8 章的实测规律**（用于后续章节估算，而不是事后调数）：
>
> | 章的形态 | 实测达成 | 实例 |
> | --- | --- | --- |
> | 单一主题 | 80%–105% | 1.2 语法 96%、1.3 数据结构 80%、1.4 装饰器 97% |
> | 概念密集 / 一章装三块以上内容 | 140%–157% | 1.5 面向对象 157%、1.6 异常+文件+模块 152%、1.7 并发 140% |
>
> 处理方式：**按实测校准计划值（改计划，不删已覆盖的内容），并在修订说明里写清原因**；
> 校准后的计划值必须同时改 `tools/audit_coverage.py` 与本文档，再用 `--out`/`--check` 对齐；
> 改完顺手跑一次 `python tools/totals.py --sync`（篇级小计、分篇标题、批次表、看板的数字
> 都从规划值算出来，不必手改）。

### 第 0 篇 · 导论（3 章 / 12,000 字，全部原创）

| 章 | 标题 | 计划 | 素材 | 状态 |
| --- | --- | ---: | --- | --- |
| 0.1 | 岗位地图：AI 应用开发岗到底要什么 | 4,000 | 无素材 | ✅ 3,945 字（达成 99%） |
| 0.2 | 能力模型与 180 天学习路线 | 5,000 | 无素材 | ✅ 3,597 字（达成 72%） |
| 0.3 | 本教材使用说明与素材索引 | 3,000 | 无素材 | ✅ 2,933 字（达成 98%） |

### 第 1 篇 · 编程地基：Python 与 FastAPI 后端工程（15 章 / 184,500 字，素材 0.61）

| 章 | 标题 | 计划 | 散文素材 | 比值 | 判定 | 状态 |
| --- | --- | ---: | ---: | ---: | --- | --- |
| 1.1 | 环境搭建：Python、依赖管理、IDE 与 Git | 4,000 | 2,115 | 0.53 | 偏薄 | ✅ 3,764 字（达成 94%） |
| 1.2 | Python 语法速通：变量、类型、控制流 | 6,000 | 13,177 | 2.20 | 充裕 | ✅ 5,120 字（达成 85%） |
| 1.3 | 常用数据结构与推导式 | 6,000 | 7,484 | 1.25 | 充裕 | ✅ 4,393 字（达成 73%） |
| 1.4 | 函数、装饰器与高阶用法 | 6,000 | 7,605 | 1.27 | 充裕 | ✅ 5,240 字（达成 87%） |
| 1.5 | 面向对象：类、继承、魔术方法 | 9,000 | 4,606 | 0.51 | 偏薄 | ✅ 8,729 字（达成 97%） |
| 1.6 | 异常处理、文件操作与模块化 | 7,500 | 3,599 | 0.48 | 偏薄 | ✅ 6,979 字（达成 93%） |
| 1.7 | 并发与异步：线程、进程与 asyncio | 8,000 | 6,688 | 0.84 | 够用 | ✅ 7,846 字（达成 98%） |
| 1.8 | Web 与 HTTP 基础（面试必考） | 8,000 | 8,827 | 1.10 | 够用 | ✅ 5,815 字（达成 73%） |
| 1.9 | FastAPI 入门：路由、请求与响应 | 15,000 | 20,619 | 1.37 | 充裕 | ✅ 14,285 字（达成 95%） |
| 1.10 | 数据校验：Pydantic 模型 | 11,500 | 1,356 | 0.12 | **严重不足** | ✅ 10,854 字（达成 94%） |
| 1.11 | 依赖注入与中间件 | 20,500 | 5,436 | 0.27 | **严重不足** | ✅ 18,831 字（达成 92%） |
| 1.12 | 数据库与缓存：SQLAlchemy + Redis | 17,500 | 7,407 | 0.42 | **严重不足** | ✅ 16,153 字（达成 92%） |
| 1.13 | 认证与授权：JWT、OAuth2 与权限设计 | 20,500 | 6,876 | 0.34 | **严重不足** | ✅ 19,428 字（达成 95%） |
| 1.14 | 三层架构、日志、异常与单元测试 | 24,000 | 10,535 | 0.44 | **严重不足** | ✅ 23,060 字（达成 96%） |
| 1.15 | 项目实战：知舟博客 API 从零到一 | 21,000 | 7,394 | 0.35 | **严重不足** | ✅ 19,138 字（达成 91%） |

> 本轮修正（素材归属）：`1.9` 原本整目录引用 `fa 02-13`，但 `02` 目录的 8 篇横跨三章
> （应用配置属 1.9，生命周期与生产实践属 1.14，中间件与依赖注入属 1.11），
> 且 `fa 04-05` 被 1.8 与 1.9 完整重复计入。审计已支持子文件级引用
> （写法 `02/01-05`）与共用素材按权重分记（1.8 记 0.45 / 1.9 记 1.0）。
> 修正后：`1.9` 26,625 → 25,346，`1.8` 10,702 → 8,827，`1.11` 6,752 → 7,245，
> `1.14` 7,777 → 8,563。判定档位变化：`1.8` 充裕 → 够用、第 1 篇 充裕 → 够用。
>
> 本轮修正（`1.11` 再次归位）：`1.11` 原本整目录引用 `fa 20`（十篇中间件），
> 等于把认证 / 日志 / 连接池也塞进一章——这正是 1.9 踩过的坑。已归位：
> `20/06`（JWT 认证中间件）→ 1.13、`20/08`（日志中间件）→ 1.14、
> `20/09`（连接池中间件）→ 1.12；`1.11` 只留 `20/01-05+07+10`。
> 归位后 1.11 素材 7,245 → 5,436（判定回落到「严重不足」，这是实情：
> 依赖注入与中间件必须按官方文档原创），1.12/1.13/1.14 分别升到
> 7,407 / 6,876 / 10,535，第 1 篇素材池总量不变（素材只是换了归属）。
>
> 本轮修正（`1.13` 的计划值本身就是错的）：原计划 6,000 是按「素材 6,876 汉字 × 0.9」
> 倒推出来的，这正是 [`STYLE.md`](STYLE.md) 8.7 明确禁止的估法——**按素材量估篇幅**。
> 1.10 已经演示过一次：素材为零不说明写得少。认证章的完整面貌（口令存储 → JWT 结构 →
> 签发与校验 → OAuth2 角色与授权方式 → 依赖注入认证 → scope 与 RBAC → 刷新与撤销 →
> Cookie/Session → 中间件 vs 依赖 → 综合示例）本身就是十几节的量，
> 且代码密集（认证的关键都在代码里）。已按 8.7 的公式（小节数 × 800–1,100 + 15 × 代码行）
> **写作前**改为 17,000；第 1 篇 147,000 → 158,000，全书 513,000 → 524,000。
>
> 本轮修正（`1.13` 事前估算再次偏低）：按 8.7 公式估 17,000，实写 20,739
> （汉字 11,953 + 代码 580 行），达成 121%。**错因与 1.11 同一条：代码行估少。**
> 这是第三次同因偏差（1.11 为 732 行、1.12 为 624 行），已把「带综合示例的章，
> 代码实际落在 550–750 行」这个先验区间写进 [`STYLE.md`](STYLE.md) 8.7。
> 按实测校准为 20,500；第 1 篇 158,000 → 161,500，全书 524,000 → 527,500。
>
> 本轮修正（`1.14` 同样是按素材量定的篇幅）：原始计划 10,000 是「素材 10,535 × 0.95」
> 倒推的，所以显示为「够用」。但这一章装了**六个主题**（统一响应格式、异常处理、日志、
> 三层架构、单元测试、生命周期与生产实践），其中后两个还是从 1.9 与 1.11 归位过来的。
> 已按 8.7 公式**事前**估 21,500（16 小节 × 800 + 代码按 600 行起估，用了本轮新增的
> 「带综合示例的章代码落 550–750 行」先验），实写 24,132（112%）——**第四次低估，
> 偏差仍在代码那头**：实得 813 行 × 15 = 12,195，高于预估的 9,000，
> 且实跑审阅后正文又长了一截（补了中间件异常边界的四组对照、`AsyncMock`、
> 脱敏深度上限等实测结论）。已按实测校准为 24,000，不留容差。
> 判定降为**严重不足**（10,535 / 24,000 = 0.44）——这是实情：
> 日志工程化、测试组织与统一响应格式的取舍均需按官方文档原创。
> 第 1 篇 161,500 → 175,500，全书 527,500 → 541,500。
>
> 另一次修正（`1.15` 的素材归属）：原计划 12,000 是按「素材 10,991 × 0.92」倒推的，
> 但 10,991 里有 **3,597 汉字的前端讲解**——第 1 篇是后端工程，这部分不属本篇。
> 修正后后端可用散文只有 **7,394**，比值降到 **0.35（严重不足）**。若不动手会把
> 前端素材算成后端的「充裕」底子，那一章的写作策略（以素材改写为主 vs 以官方文档原创为主）
> 就会选错。同时按 8.7 公式事前估 21,000（14 小节 × 800 + 表格/示例密集按 675 行代码）——
> 表单、上传、下载与断点续传、静态挂载、评论树这些技术点**只在这一章讲**，
> 篇幅本来就压不下去。第 1 篇 175,500 → 184,500，全书 541,500 → 550,500。
> `1.9` 的经历：写作前按「10 小节 × 约 900」估 12,000，实写 22,341（达成 186%）——
> 原因是把「统一响应格式」与「表单 / 文件上传」也塞进了同一章。处理不是放宽容差，
> 而是**把两摇内容归位**（fa 10 → 1.14，fa 11-12 → 1.15，Cookie 写入 → 1.13，
> 计 1.14 由 7,000 提到 10,000、1.15 由 10,000 提到 12,000），再删掉重复的平行示例，
> 实写 15,665（含实跑后的四处素材纠错），按实测校准为 15,000。
> 教训：**素材充裕不等于该堆在一章里**。

> 本轮校准（`1.11`）：估 13,000、实写 20,589（达成 158%）。原因与 1.9、1.10 都不同：
> **漏算了代码行项**。本章 732 行代码 × 15 = 10,980，占实测的一半；
> 而 `STYLE.md` 8.7 的公式本就含这一项，是估算时只数了小节、没看代码密度。
> 已校准为 20,500。教训：**代码密集的章（中间件、ORM、模板）必须先把代码行估出来**，
> 否则会系统性地把这类章判成「严重超标」。

> 本轮校准（`1.10`）：按素材量估的 6,000 与实际相差近一倍（实写 11,861）。
> 这不是内容超载——十节里没有一节是多余的，是**估法本身错了**：素材为零只说明
> 「要自己写」，不说明「写得少」。已按实测校准为 11,500，教训写进 [`STYLE.md`](STYLE.md) 8.7。

> 本轮修正：`1.2` 的素材池原本漏了手册第 12–17 章（if/for/while），补上后从 8,343 升到 13,177。

> - 本章投入最重（占全书 26%），因为后面所有篇章的代码质量都取决于这里。
> - **1.10 是全篇素材最薄的一章**（素材仅 1,356 字），但薄的是素材不是章：
>   它按 Pydantic v2 的完整面貌写成十节（数据契约 → 模型 → Field → 类型 → 配置 →
>   校验器 → 嵌套继承联合 → 别名序列化 → FastAPI 集成 → 综合示例），实写 11,861 字。
> - `1.1` 的 venv、依赖锁、Git 工作流在素材中零覆盖，须原创。
> - 意外的收获：素材手册的"进阶块"含**网络编程 8,580 字**（TCP/IP 五层协议）与
>   **并发编程 5,693 字**，让 `1.8` 与 `1.7` 从"缺素材"变成"充裕/够用"。
> - 注意 `1.11` 的判定一路回落：初判"充裕" → "够用" → 归位后"严重不足"。
>   两次回落是两个不同的原因：先是把每个讲义文件的溯源头部也算成了素材，
>   后是把不属于它的三篇（认证 / 日志 / 连接池）还给了对应的章。
>   它必须靠官方文档原创补齐，这与 `1.10` 同属"素材薄、章不薄"的一类。

> 本轮修正（`2.1` 同样按素材量定了篇幅）：原计划 5,000 是「素材 6,180 × 1.24」的
> 正向读法——素材充裕就该少写。但 6,180 只覆盖本章**三条主线里的一条**：
> Vibe Coding → Harness 的思维模型（外加五阶段成熟度）。另外两条——**能力边界**与
> **风险**——素材零覆盖，必须回到 OWASP GenAI LLM Top 10、METR 的对照实验、
> DORA 2025 与两篇安全论文原创。已按 8.7 公式事前估 8,000
> （9 小节 × 约 600 + 固定段约 2,000 + 实跑演示约 80 行代码），
> 判定由「充裕」改为**偏薄**（6,180 / 8,000 = 0.77）——这是实情，不是降级。
> 第 2 篇 38,000 → 41,000，全书 550,500 → 553,500；需原创补写的章 49 → 50。

> 本章事后校准（`2.1`）：事前估 8,000，实写 11,159（达成 139%）。原因不是内容超载，
> 而是**估法只数了小节、没估表格**：本章的主体是纯文字与技术结论，而 8.7 的公式
> 是按「小节约几段散文」推的，没给对比表、风险清单与出处表留位置
> ——而它们恰恰是本章最有价值的部分。已按实测校准为 **11,000**；
> 第 2 篇 41,000 → 44,000，全书 553,500 → 556,500；
> 第 2 篇散文比值 1.26 → 1.18（充盈 → 够用）。教训写进 [`STYLE.md`](STYLE.md) 8.7。

> **本轮修正（第 2 篇素材重复计入）：** 三份手册（Claude Code / Codex / Harness）
> 每份都被多章按「整份文件的百分比」记了一次，权重之和最高到 2.0：
>
> | 文件 | 原记 | 合计 | 改为 |
> | --- | --- | ---: | --- |
> | `Claude-Code从入门到实战.md` | 2.2（0.9）+ 2.5（0.5） | 1.40 | 2.2（0.84）+ 2.5（0.11）+ 2.6（0.05） |
> | `Codex从入门到实战.md` | 2.3（0.9）+ 2.5（0.4） | 1.30 | 2.3（0.81）+ 2.5（0.13）+ 2.6（0.05） |
> | `AI-Harness入门与实践.md` | 2.1（0.7）+ 2.5（0.5）+ 2.6（0.8） | 2.00 | 2.1（0.7）+ 2.5（0.15）+ 2.6（0.15） |
>
> 改为按**各章实际用到的节**实测拆分（例如 2.5 用两份手册里的
> 规则文件 / MCP / Skills / Hooks 节，而不是「切一半」）。后果不小：
> **`2.5` 由充裕（2.11）落到偏薄（0.57）、`2.6` 由够用（1.18）落到严重不足（0.45）**
> ——它们当初的「素材充裕」是重复计数造成的错觉。两章的篇幅**不减**
> （素材薄≠章该薄，参见 1.10 的教训），但原创量要重新排。
> 审计已新增这道检查（权重之和 > 1.0 就报）。
> 第 4、5 篇的三份素材（`LangChain` 1.30、`RAG技术` 1.05、`生产级RAG` 1.35）
> 同样超额，需逐份量出节级拆分后再改，已列入下方待定项。

> 本章事前校准（`2.2`）：原定 7,000 是「素材量 × 1.24」倒推的，而本章的代码块
> 不是算法而是**命令、工单模板与配置**，代码行项占比很高（8.7 公式里的
> `15 × 代码行` 在这一章是主导项）。按 11 节 × 约 800 + 固定段 2,200 + 约 150 行 × 15
> 事前估 **11,000**。第 2 篇 44,000 → 48,000，全书 556,500 → 560,500；
> `2.2` 判定由充裕（1.77）回落为够用（1.13）。

> 本章事前重估与事后校准（`2.3`）：原定 6,000 是「素材 10,325 汉字 × 比例」推的，方向错了
> （素材量不决定篇幅）。按 8.7 式子事前改为 **13,000**，实写 **16,856（达成 130%）**，
> 按实测校准为 **16,500**。归因：不是「装了两章」（归位已完成：闭环与工单归 2.2、
> 规则文件归 2.5、进阶能力归 2.5/2.6），而是两项低估——
> ① **固定段的规模**：决策表、工具对照表、五步综合示例各占一节，不是附录；
> ② **实跑带出的新内容**：`git apply` 与 `--3way` 的原子性差别、
> 「索引也被改写」、以及两种恢复来源的差别，这一整块照文档写不出来。
> 第 2 篇 58,000 → 68,500，全书 570,500 → 581,000；
> `2.3` 判定：充裕 → **偏薄**（0.62）。教训写进 [`STYLE.md`](STYLE.md) 8.7。

> 本章事前重估（`2.4`）：6,000 → 14,000，同时把素材权重从 0.8 改为 **0.67**。
> 原值同样是「素材量 × 比例」倒推的；而这份素材是**视频课件大纲**，当时的构成记录是
> 课程旁白 1,799 汉字（「简介：讲解…」「掌握 X」，不可作散文素材）、
> 文末团队规范 4,076、其余操作步骤与概念 9,694。按实际用到的部分量出 0.67 后，
> 散文素材 10,431，比值 2.07 → **0.75（偏薄）**。
> ⚠️ **这三行数字里的 4,076 已被证伪**（实测 739），0.67 / 10,431 / 0.75 随之作废——
> 详见本节的第三次校准。
> 第 2 篇 68,500 → 76,500，全书 581,000 → 589,000；
> 需原创补写的章 54 → 55（该行原记 52，已按实测校正；并新加一道对账：
> 这一行现在也由 `tools/audit_coverage.py --check` 按 `needs_original()` 口径核，
> 漂了会拦提交——此前它无人核对，实测写着 52 而实际已是 54）。

> 本章事后校准（`2.4`）：事前估 **14,000**，实写 **15,301（达成 109%）**，按实测校准为 **15,000**。
> 归因不是「写多了」，而是两项没算进去：
> ① **可执行清单的输入夹具也是正文**——不贴出跑的那三份文件，下面的输出就无法核对；
> ② **一手报告要给出处**：把 Veracode / CodeRabbit / GitClear 从「Wikipedia 转述」换成自家报告，
> 多出的是一条原则，不是一段文字。
> 第 2 篇 76,500 → 77,500，全书 589,000 → 590,000；判定与章数不变。

> **回补与二次校准（`2.2`、`2.3`，2026-09-12）**：读者指出这两章与 PDF 素材不符。
> 逐节对齐后确认：骨架对得上，但**「能照着做」的部分被压掉了**——
> IDE 五个动作的提示词、JetBrains 与桌面端实操、截图五轮的视觉分析提示词、
> §19 的七组提示词库、§20 的三段检查清单（2.2）；以及素材 §7 的编辑器三种模式与五个动作（2.3，
> 它在台账里**连一行都没有**）。已全部回补：2.2 新增 2.2.13「可直接复制的模板与检查清单」
> 与安装登录小节，2.3.4 新增编辑器小节。
> 篇幅按实测二次校准：`2.2` 21,000 → **26,000**（实写 26,091），`2.3` 16,500 → **17,500**（实写 17,657）。
> 第 2 篇 77,500 → 83,500，全书 590,000 → 596,000；素材比值 0.67 → 0.62（分母变大）。
> 一条教训已写进 [`STYLE.md`](STYLE.md) 8.5：**素材里的可复制段落（提示词、模板、清单）是正文资产，
> 压成一句概括就是丢内容**；台账的 `✅` 必须可核对，写「散落在各节」等于没写。

> **反向核对与三次校准（`2.4`，2026-09-12）**：按 `STYLE.md` 8.5 的新规矩，拿素材的 9 章 25 节反向扫一遍台账，
> 又发现 8 节没有对应行（合计约 4,457 汉字），已把其中五节补进正文
> （2.4.2 预设命令、2.4.1 插件形态、2.4.8 联调分工、2.4.10 交付前的仓库文档）。
> 同时**修掉一个从没被复算过的数字**：旧记「文末团队规范 4,076 汉字」，实测只有 **739**——
> 它在源文件里对不上任何一段连续区间（最接近的 4,123 是把第八、九章一起算了进去），
> 而它曾是权重计算的实际输入（0.4 × 4,076 ≈ 1,630）。改成逐节实测后：
> 素材权重 **0.67 → 0.40**、散文素材 10,431 → **6,195**、比值 0.75 → **0.36（严重不足）**。
> 判定变差不是坏消息，而是承认这一章的内容主要来自官方文档；
> 篇幅 15,000 → **17,000**（实写 17,646）。
> 第 2 篇 83,500 → 85,500，全书 596,000 → 598,000；素材比值 0.62 → 0.61。

> **`2.5` 篇幅事前估（2026-09-13）**：原计划 8,000 是按素材量给的，而这一章的正文主要来自官方文档
> （素材比值 0.57 只说明「素材少」，不说明「该写得少」）。按 [`STYLE.md`](STYLE.md) 8.7 先把模板
> **逐个**数出来：CLAUDE.md / AGENTS.md / SKILL.md / hooks `settings.json` / Codex `.rules` /
> `.mcp.json` 加综合示例，合计约 **340 行**（×15 ≈ 5,100），散文按 12 小节 × 约 850 估。
> 篇幅 8,000 → **15,000**，第 2 篇 85,500 → 92,500，全书 598,000 → **605,000**。
> 分母变大，两个数字跟着变：第 2 篇素材比值 **0.61 → 0.56**，
> 而 2.5 自己的判定由「偏薄」落到**严重不足**（4,594 / 15,000 = 0.31）。
> 这不是坏消息：判定说的是「素材够不够」，不是「这一章该不该写」——
> 规则文件、Skills、hooks 与 .rules 的写法本来就只能在官方文档里找。
> 这是 2.2 之后第二次「配置模板体」估幅（上次估 150 行、实 324 行），所以这次先数模板再估散文。

> **`2.5` 篇幅事后校准（2026-09-13，写后）**：事前估 15,000、实写 **19,352**（129%）。
> 两处低估：一是官方文档在这批机制上比素材走得更远（这一章的价值正在这里），
> 二是四张对照表（`.claude/rules` 与 Codex `.rules`、两份规则文件、三层机制、三级权限）各占掉半节。
> 按实测校准为 **19,500**；同时 Harness 的素材权重由「切一半」的 0.15 改成**按节实测的 0.07**
> （本章素材 4,594 → 3,888）。两个数字一升一降，2.5 的比值由 0.31 → **0.20**。
> 第 2 篇 92,500 → **97,000**，全书 605,000 → **609,500**；第 2 篇素材比值 0.56 → **0.53**。

### 第 2 篇 · AI 时代的开发方式（6 章 / 115,500 字，素材 0.36）

| 章 | 标题 | 计划 | 散文素材 | 比值 | 判定 | 状态 |
| --- | --- | ---: | ---: | ---: | --- | --- |
| 2.1 | 与 AI 协作编程：思维模型、能力边界与风险 | 11,000 | 3,090 | 0.28 | **严重不足** | ✅ 10,769 字（达成 98%） |
| 2.2 | Claude Code 实战：从对话到工程化 | 26,000 | 12,272 | 0.47 | 偏薄 | ✅ 24,621 字（达成 95%，含回补） |
| 2.3 | Codex 实战：云端与本地的工作流 | 17,500 | 10,248 | 0.59 | 偏薄 | ✅ 16,607 字（达成 95%，含回补） |
| 2.4 | AI 编辑器与 Vibe Coding 全栈开发 | 17,000 | 6,195 | 0.36 | **严重不足** | ✅ 17,106 字（达成 101%，含回补） |
| 2.5 | 上下文工程：规则文件、Skills、Hooks 与 MCP | 19,500 | 3,888 | 0.20 | **严重不足** | ✅ 18,662 字（达成 96%） |
| 2.6 | AI Harness：把 AI 编程接入工程流水线 | 24,500 | 6,160 | 0.25 | **严重不足** | ✅ 23,871 字（达成 97%） |

> **本轮修正（2026-09-13，开写 2.6 前量权重）**：给 2.6 量素材权重时，工具的
> 「同一份素材被多章重复计入」检查报出 Harness 三章合计 **1.28 > 1.0**，倒推出
> **2.1 的 0.7 从未与用到的节对过**——它说的是「整份文件的三分之二」，而 2.1 实际只用到
> §1 前言与词源、§2 HarnessAgent、§3 三层演进、§4 五个硬问题、§5 五阶段、§6 组件概览
> ＝ **3,051 汉字 → 0.35**。改后 2.1 由「偏薄」落到**严重不足（0.28）**，
> 与台账自己的说法一致：「素材只覆盖思维模型一条主线，能力边界与风险两条必须按官方出处原创」。
> 2.6 的三份权重也一并按节实测：Harness **0.54**（§6.1/6.4–6.9、§7–9、§12–25＝4,776）、
> Claude **0.06**（§15 ＋ §16.1–16.2＝878）、Codex **0.04**（§15.1–15.3＝460），合计散文素材 **6,160**。
> （Harness 那一项在反向核对时由 0.51 提到 0.54：§22「对产品经理的意义」起初被标成
> 「不在本章范围」，但 2.6.2 确实用了它。）
> 顺带修掉 0.7% 的口径不一致：三章切片相加曾到 1.007，因为 2.2 的余数按含文件头的分母算
> （14,882），而报表分母是 14,786；按同一口径重算 2.2 = **0.83**。
> 篇幅：2.6 由 6,000 提到 **18,000**（事前估，按 8.7 式子：11 小节 × 约 900
> ＋ 脚本/模板/配置约 340 行 × 15 ＋ 固定段约 1,500 ≈ 16,600，取 18,000）。
> 第 2 篇 97,000 → **109,000**，全书 609,500 → **621,500**；第 2 篇素材比值 0.53 → **0.47**。
> 这是同一类错的**第三次**（前两次：2.4 的 4,076、2.5 的 0.15），所以量法已写进 [`STYLE.md`](STYLE.md) 8.5。

> **`2.6` 篇幅事后校准（2026-09-13，写后）**：事前估 18,000、实写 **25,115**（140%）。
> 这是**第八次低估**，两个错向各又长了一层：
> ① 代码行项估 340、实际 **584 行**（占实测 35%）。多出来的这一章特有的东西是
> **不是散文的那些行**：console 围栏 82 行、JSON 42 行、YAML 29 行（实跑输出与配置），
> 规则与模板文件（`text`/`markdown`）262 行，四项合计 415 行＝**71%**；
> 真正自己写的 Python 只有 157 行。这与 2.4 的「输入夹具也是正文」同族：
> 只算了「自己写的代码」，漏了「跑出来的东西」和「贴在正文里的配置」。
> ② 散文项估约 10,000、实际 **15,361 汉字**（剔代码块）。这一章有 **27 张表、134 个数据行**，
> 表格行本身占 3,158 汉字，其余 12,203 汉字是 11 小节的讲解——平均每小节 **1,110 汉字**、
> 平均每张表带 **450 汉字**的配套说明，而「小节数 × 900」只给出 9,900。
> 对以概念对照为主体的章，**表格（及其讲解）比小节数更能预测篇幅**（估算式第三项是主项而非补丁）。
> 按实测校准为 **24,500**，2.6 比值 0.33 → **0.25**。
> 第 2 篇 109,000 → **115,500**，全书 621,500 → **628,000**；
> 分母变大后，第 2 篇的素材比值由 0.47 → **0.45**，判定由「偏薄」落到**严重不足**。
> 这不是坏消息，而是把话说清：这一篇的正文比素材多得多，因为真正难的部分
> （权限机制、钩子契约、CI 门禁、评测）官方文档才是唯一可靠的来源。

> 这是素材质量最好的一篇：Claude Code 素材有 468 个结构化条目，密度高、可塑性好。
> `2.4` 已写完：那份原档以截图为主，示例确实全部重写；它的技术结论主要来自各家官方文档，
> 2.4.5 的七个数字换成了**一手报告**（Veracode / CodeRabbit / GitClear，出处见 [`REFERENCES.md`](REFERENCES.md)）。

### 第 3 篇 · 大模型与 Agent 原理（10 章 / 155,500 字，素材 0.04 — **原理 ＋ 逐章接线 ＋ 组装**）

| 章 | 标题 | 计划 | 散文素材 | 比值 | 判定 | 状态 |
| --- | --- | ---: | ---: | ---: | --- | --- |
| 3.1 | 大模型基础：从 Transformer 到 Token | 15,000 | 0 | 0.00 | 题库驱动 | ✅ 15,463 字（达成 103%；含题库 9,539 字考点索引与 3.1.10 知舟接线） |
| 3.2 | 提示工程与链式推理 | 21,500 | 306 | 0.01 | 严重不足 | ✅ 22,496 字（达成 105%；含题库 20,069 字考点索引与 3.2.11 知舟接线） |
| 3.3 | Agent 是什么：LLM + 规划 + 工具 + 记忆 | 15,000 | 1,906 | 0.13 | 严重不足 | ✅ 18,057 字（达成 120%；含题库 603 字考点索引与 3.3.10 知舟接线；表结构节选已按 3.6 的真相对齐） |
| 3.4 | 规划与决策策略 | 15,000 | 386 | 0.03 | 严重不足 | ✅ 15,151 字（达成 101%，含题库 15,735 字考点索引；接线小节已回填） |
| 3.5 | 工具调用与函数编排 | 19,500 | 1,293 | 0.07 | 严重不足 | ✅ 23,420 字（达成 120%；含题库 36,712 字考点索引；固定段已补齐） |
| 3.6 | 记忆与上下文管理机制 | 11,800 | 842 | 0.07 | 严重不足 | ✅ 11,537 字（达成 98%；含题库 34,746 字考点索引；三张表＋迁移＋装配层＋真机跨会话实测） |
| 3.7 | 多 Agent 协作框架设计 | 13,800 | 966 | 0.07 | 严重不足 | ✅ 13,788 字（达成 122%；含题库 40,864 字考点索引；交接契约＋编排器＋预算分摊＋离线/真机两组对照；第十次修正） |
| 3.8 | 安全、对齐与防护 | 12,400 | 0 | 0.00 | 题库驱动 | ✅ 12,447 字（达成 115%；含题库 51,466 字考点索引；四档升级对照＋样例集四个比率＋33 条测试；第十一次修正） |
| 3.9 | 可观测性与评估 | 19,500 | 0 | 0.00 | 题库驱动 | ✅ 19,543 字（达成 151%；含题库 58,383 字考点索引；span 树＋四类指标＋评测集与回归门＋142 条测试；第十二次修正） |
| 3.10 | 项目实战：知舟 Agent 版从零到一 | 12,000 | 0 | 0.00 | 无素材 | ✅ 11,321 字（达成 94%；收口章；架构图＋端到端验收 8 条用例＋一键脚本；计划值 12,000 **不调**，见第九次修正） |

> **第五次修正（2026-09-13：这一篇少了交付物）**：学生在第 1 篇手里有一个能跑的知舟，
> 第 2 篇学会了怎么和 AI 一起改它，而第 3 篇读完 75,500 字后**手里什么也没多**——
> 正文里「知舟」出现 **0 次**，而 [`STYLE.md`](STYLE.md) 第五节的阶段 3 明确把
> 「加入智能问答：提示工程 ＋ Agent 工具调用」归给第 3、4 篇。这不是章节质量问题，
> 而是**篇级设计漏了一件事**：原理篇也需要交付物。已采纳方案 B（选项见第七节决策项 9）：
> 3.1–3.10 每章给知舟加一段**可独立验收的增量**（新增小节「知舟接线」，四问固定：加了什么 /
> 接口契约 / 怎么验 / 不能破坏什么），并以**新增章 3.10** 收口成「知舟 Agent 版」：
> 架构图、端到端验收测试、演示脚本、5 分钟讲法与追问防御。
> 契约（文件路径、端点、表结构、验收标准、八条硬约定）写在 [`PROJECT.md`](PROJECT.md)；
> 3.1–3.4 正文已定稿，接线小节按 PROJECT.md 第六节的进度表**逐章回填**（lint 已盯住）：
> 2026-09-13 四个小节**全部回填**，实测 5,746（3.1）｜6,648（3.2）｜5,990（3.3）｜3,946（3.4）——
> 量法：**小节边界内的汉字 ＋ 15 × 有效代码行**（四问两端的标题也算在内），可照抄复现；
> 四个数都高于事前估，第 3 篇与全书据此重设（121,500 → **131,500**、690,500 → **700,500**），
> 算法与错因记在本节下方与 [`PROJECT.md`](PROJECT.md) 二节的四份实测。
> 计划值按 STYLE 8.7 重设（增量部分也在算式里：模块与测试的代码行 ＋ 讲解），
> 第 3 篇 75,500 → **121,500**、全书 644,500 → **690,500**、章数 68 → **69**。
> 素材量一字未变（增量是代码与工程判断，题库与笔记里没有项目素材），
> 所以本篇比值 0.08 → **0.05**——分母变大就要如实说它更缺素材。
>
> **接线小节的第一份实测（3.4 写完当天）**：原估 ＋2,000 字，实测 **3,946**——低估一倍。
> 错因不是“没数围栏”（那个错已经犯过三次），而是**胶水比零件长**：一段可独立验收的增量，
> 占篇幅的不是那 120–180 行模块代码，而是接口契约表、验收命令与期望输出、
> 以及“不能破坏什么”那四条边界。3.4 的实测构成：汉字 ＋1,372、代码 ＋175 行、两份输出块。
> 若不修正，后面五章会自带同一个偏差（按 ＋4,000 写、按 ＋3,000 计），
> 所以按实测重设全篇预算（3.1–3.9 依次 ＋2,000 / ＋2,500 / ＋5,000 / ＋4,000 /
> ＋4,500 / ＋4,500 / ＋4,000 / ＋4,000 / ＋4,000），并把它写进 STYLE 8.7：
> **“接线类”增量按“每段 ≈ 4,000 有效字”估，而不是按模块的代码行数估。**
>
> **第七次修正（2026-09-13：四段接线全部回填后，那套预算仍然偏低）**：
> 3.3 ｜ 3.1 ｜ 3.2 三段回填后的实测是 **5,990 ｜ 5,746 ｜ 6,648**，
> 而事前给的是 5,000 ｜ 2,000 ｜ 2,500——后两段低估到近三倍。原因不在“每段估多少”这个数，
> 而在**没把代码行当乘数看**：3.1.10 带三份代码 223 行（×15 ＝ 3,345）占六成，
> 3.2.11 的 292 行（×15 ＝ 4,380）占三分之二；而四段的“胶水正文”其实相当稳定
> （汉字 1,321 / 2,105 / 2,311 / 2,283；代码 175 / 259 / 229 / 291 行）。所以估法改成两步：**先数列（要交几个文件、各多少行）
> × 15，再加 2,000–2,300 汉字的契约与边界**；3.1 12,000 → **15,000**、3.2 14,500 → **21,500**，
> 其余各章按此重估后再开写。STYLE 8.7 的经验区间同步改为 **4,000–6,700**。

> **第八次修正（2026-09-13：3.5 开写前按 STYLE 8.7 重设，同时在题库里再抓到一处归属错）**：
> 3.5 的旧计划值 11,500 是「3.1 素材零覆盖」时期拍的。按 8.7 的两步算式重估：
> 正文 9 小节 × ≈1,000 ＝ 9,100 汉字 ＋ 代码 ≈270 行 × 15 ＝ 4,050；
> 接线一段（`registry.py` ＋ 6 个工具 ＋ 测试 ≈280 行 → 4,200，加契约与边界 2,200）≈ 6,400；
> 合计 ≈19,550 → 取 **19,500**。第 3 篇 131,500 → **139,500**、全书 700,500 → **708,500**。
> 同一次核对又在题库里抓到**第六处「按考点编」性质的错**，也是最深的一层：
> 「工具调用与函数编排（114道）」整章 37,923 汉字原先整章记给 3.5，而它自带 16 个编号分组，
> 逐组量完有 **9,033 汉字不属它**——权限组 8 题 2,330 → 3.8、可观测组 2,443 与编排评测组 2,519 → 3.9、
> 自适应编排半组 1,741 → 3.4。3.5 由 45,745 降到 **36,712**（题库列，不参与散文比值），
> 3.4 题库 13,994 → **15,735**、3.8 49,136 → **51,466**、3.9 53,421 → **58,383**。
> 前五次错在分章之间、第六次错在**一个分组内部**，这一次错在**组是对的、但整章不该只归一个主章**——
> 章名里的「编排」把调度的、评测的、权限的都收进来了。归属的判据只能是题目自己的谓语
> （问的是「怎么调」，还是「怎么知道它调对了」），**不能是章名的字面**。

> **第九次修正（2026-09-13：写完 3.5 后重估 3.6–3.9 的接线预算）**：
> 起因是一个可量的错——`3.5` 的接线小节实测 **8,103 有效字**（3,123 汉字 ＋ 332 行代码），
> 而它的预算当时定的是 6,400；3.5 之前那四段实测是 3,946 / 5,746 / 5,990 / 6,648。
> 五个数排起来看得清规律：**接线小节的代价随「这一段交付几个文件」跳级**，
> 因为代码行在折算里是乘数（×15）：一段交一份文件 ≈ 3,900–4,000，交两份 ≈ 5,700–6,650，
> 交三份 ≈ 8,100。而 3.6–3.9 的预算（4,500 / 4,000 / 4,000 / 4,000）是照
> 「一段一份文件」的 3.4 拍的，而它们的交付物分别是：
>
> | 章 | 交付物（按 `PROJECT.md` 契约） | 估计代码行 | 接线预算（旧 → 新） | 章计划（旧 → 新） |
> | --- | --- | ---: | --- | --- |
> | 3.6 | 三张表 ＋ 迁移 ＋ `memory.py` ＋ 测试 | ≈240 | 4,500 → **5,800** | 10,500 → **11,800** |
> | 3.7 | `orchestrator.py` ＋ 交接契约 ＋ 测试 | ≈200 | 4,000 → **5,300** | 10,000 → **11,300** |
> | 3.8 | `guardrails.py` ＋ 注入样例集 ＋ 测试 | ≈170 | 4,000 → **4,800** | 10,000 → **10,800** |
> | 3.9 | `trace.py` ＋ `eval_cases.jsonl` ＋ 回归脚本 ＋ 测试 | ≈240 | 4,000 → **5,900** | 11,000 → **12,900** |
>
> 第 3 篇 139,500 → **144,800**、全书 708,500 → **713,800**（章数不变）。
> `STYLE.md` 8.7 的经验区间同步改为 **3,900–8,200**，并把「先数列、再乘 15」两步算式里
> 的第①步写成「**这一段交几个文件、每个多少行**」——本次错的仍是第①步，错法与前三次同类：
> 把「一段」默认成了一个文件。
> 顺带记一条与字数无关但同源的教训：**接线小节的篇幅不是代码撑起来的，是「怎么验」与
> 「不能破坏什么」撑起来的**（3.5 那一段 332 行代码只占 4,980，另 3,123 汉字全是契约与边界）。
> 所以 3.10 的 12,000 不调：它是组装章，本来就要把十段增量的契约重述一遍，这个体量是它的内容决定的。

> **第十次修正（2026-09-14：3.7 按实测定稿）**：3.7 计划 11,300，**实写 13,773**（汉字 11,701 ＋ 代码 138 行），
> 达成 **122%**。这次错的不在算式，在**常量**：前面九次修正一直在修「先数列、再乘 15」的第①步，
> 而这一章的第①步是对的（实现分散在各概念节，接线段只贴 139 行里的 50 行左右）——
> 错的是正文常量「小节数 × ≈700 汉字」：**有实测数字要讲的章，正文会长过纯概念章**。
> 3.7 要塞进两张对照表（离线五行、真机三行）、五句读数、四次真机记录（含修交接单前后各一次）
> 与一处记账错的复盘，这些内容每一块都是「先跑、再写」逼出来的，删任何一块都会让结论悬空。
> 所以计划值改为 **13,800**（第 3 篇 144,800 → **147,300**、全书 713,800 → **716,300**，章数不变），
> 并在 `STYLE.md` 8.7 的经验区间里补一条：**带实测表的章节**按小节数 × ≈900–1,100 估，
> 而不是 700。

> **第十一次修正（2026-09-14：3.8 按实测定稿）**：3.8 计划 10,800，**实写 12,382**（汉字 10,297 ＋ 代码 139 行），
> 达成 **115%**。开写前的事前估是 ≈12,350，当时就定下规矩「先按计划开工、写完按实测校准
> （若超 10% 则改计划值并记修正）」，所以这次是**按规矩执行**而不是事后措辞。
> 错的**不是正文那一项**（估算 ≈8,550、实写 10,297，偏 +20%，落在第十次修正给出的
> 900–1,100/节区间内），而是**代码行那一项**：估 60 行、实贴 139 行（+132%）。
> 原因是本章的代码大量来自 `guardrails.py` 与样例集这两个交付物本身
> ——与 3.6 同一种形态（代码分散在各概念节，而它每一节的方法都贴得住）。
> 这条已补进 `STYLE.md` 8.7 的第八个实测：**交付物本身是「规则/契约密集」的章，
> 代码行要按交付物的实际行数估，不能按「接线小节里的行数」估**。
> 计划值改为 **12,400**（第 3 篇 147,300 → **148,900**、全书 716,300 → **717,900**，章数不变）。
>
> **第十二次修正（2026-09-14：3.9 按实测定稿）**：3.9 计划 12,900，**实写 19,543**（汉字 15,568 ＋ 代码 265 行），
> 达成 **151%**。开写前的事前估是 ≈15,400，超 10% 即改计划值，所以这次仍是按规矩执行；
> 但偏差大过前两次，错因值得写清：**两处都低估，而且是本书第一次「正文估错」超过代码那一项**。
> ① 正文估 8 小节 × ≈1,000 ＝ 8,000，实写 15,568÷19,543 里约 11,000（+38%）——
> 3.9 与 3.7 同属「带实测表」的那一类，但它的表更多也更长（三支柱表、四类黄金信号映射表、
> 三类 grader 对照表、能力/回归对照表、pass@k 与 pass^k 对照表、统计五条建议表、离线/真机
> 跑法对照表），**其中四张是「同一个概念在 Agent 上的映射」，不是可直接采信的经验值**，
> 每一张都要先把传统口径写出来再逐行翻译——这类表格的汉字成本约为普通说明表的两倍；
> ② 代码估 ≈240 行、实贴 265 行（+10%，这一项反而准；STYLE 8.7 第八个实测的修法在起作用）。
> 所以 `STYLE.md` 8.7 再补一条：**含「口径翻译表」的章，表格按普通表 2 倍估**，
> 并给出可复算的判据——数一遍表里有多少行属于「传统做法 → Agent 上的对应」。
> 计划值改为 **19,500**（第 3 篇 148,900 → **155,500**、全书 717,900 → **724,500**，章数不变）。

> **再再修正（2026-09-13：散文笔记的权重原本也是估的）**：那份笔记全文 12,852 汉字，
> 旧值按 0.15/0.45/0.10/0.20/0.10 分给 3.2–3.6（和恰好 1.00，看上去很「干净」），
> 但**每一个值都对不上**。开写 3.3 前按它自己的节标题逐节量，结果如上表：
> 3.3 由 5,748 降到 **1,906**、3.2 由 1,916 降到 **306**，而 `3.7` 原本被当成纯题库章，
> 实际上它的笔记素材有 **966**。第 3 篇散文池 12,773 → **5,700**，比值 0.19 → **0.09**；
> 题库驱动章由 4 章减为 **3 章**（3.1/3.8/3.9），需原创补写的章 57 → **58**。
> 逐节之和与文件实测逐个相等，已分配 5,699 与未分配的 7,153 都逐项记在
> `tools/audit_coverage.py` 的注释里。（分母随 3.3 计划值 6,000 → **10,000** 变大，
> 本篇比值再降至 **0.08**，第 3 篇合计 67,000 → **71,000**、全书 636,000 → **640,000**。）
> 顺带一句口径提醒：工具量的是**剥掉提取脚本写入的溯源头之后的正文**（这份笔记 12,774 汉字），
> 不是文件的 12,852——照 12,852 去乘权重会多出两个字，而 3.4 的 `0.03022` 正是照 12,774 反算的。
>
> **这一篇没有哪一章算「素材够用」**：最高的是 `3.3`（0.32），因为那份笔记是**代码与
> 提纲为主**（汉字只 1,906，另有大量代码不参与散文计量）；`3.4` 最低（0.06）。
> 九章都须原创，且**不能引用题库答案**——1038 道题的答案是通用套话。
>
> **本轮修正（2026-09-13：题库权重改为按题库自己的分章实测）**：旧值给每章一律
> `0.05`，而 1038 道题自带九个分章，体量差到 1.7 倍（最小 28,778、最大 50,032 汉字），
> 一律 0.05 等于把「题多题少」抹平。实测口径：权重 ＝ 分章汉字 ÷ 全题库 352,616。
> 同时发现 **`3.1` 并非「素材零覆盖」**——题库的「大语言模型原理与应用（151道）」
> 就是它的地盘，其中注意力与结构 3,399 ＋ 训练范式 2,136 ＋ Scaling Law 2,647
> ＋ 分词四题 1,357 ＝ **9,539**，已计入（判定由「无素材」→「题库驱动」）。
> 第 3 篇题库池 123,415 → 288,276。
>
> **再修正（同日傍晚：分章整块 → 按考点分组）**：上一步仍把「一个分章整块归一章」
> 当作默认，而**题库没有哪个分章是整章只归一章的**。最突出的是
> 「提示工程与链式推理（112道）」：整章 38,156 汉字原先全记在 3.2 名下，实测只有
> 15,430 属它，其余按题拆给 3.1（3,083）｜3.4（2,170）｜3.5（2,519）｜3.6（2,873）
> ｜3.7（1,088）｜3.8（2,463）｜3.9（3,121），另有 5,409 有意不分配（RAG 架构待第 5 篇、
> Token 成本与路由待第 6 篇）。**增量之和 42,795 恰好等于旧值**——只换归属，没多没少。
> 第 3 篇题库池 288,276 → **282,867**（五章归还给别的篇的那部分不再计在本篇）；
> 有意不分配的合计 24,983 已逐项登记在 `tools/audit_coverage.py` 的注释里，
> 不让它静静消失。
>
> **第三次同类修正（2026-09-13 开写 3.3 前：连「组」也不只属一章）**：题库里
> 「大语言模型原理与应用」分章的**组13「单Agent 循环」**（10 题、3,186 汉字）
> 原先整组记给 3.3，而组名里的「单 Agent 循环」让人以为整组都是概念，
> 实际上只有两题是——其余七题分属规划、工具、记忆、多 Agent。按题实测后归位：
> 3.3 ← **603**、3.4 ← 1,161、3.5 ← 700、3.6 ← 389、3.7 ← 333（十题之和 3,186
> 与整组实测逐个相等）。于是 **3.3 的题库素材由 3,173 降到 603**，
> 比值 0.32 → **0.19**，「笔记 1,906 ＋ 题库 603」才是它真正的底子；
> 另**按 STYLE 8.7 事前估把计划值 6,000 重设为 10,000**（9 小节 ≈ 6,300–7,650
> ＋ 对照表 4 张 ≈ 1,400 ＋ 固定段 ≈ 1,400 ＋ 最小 Agent 循环代码 ≈ 160 行 ≈ 2,400，
> 取 10,000；不叠二次下调，理由见 3.2 的教训）。
> 三个选项见第六节决策项 2。
>
> **第四次同类修正（2026-09-13 开写 3.4 前：题库里有一整块不属本书）**：
> 「Agent 规划与决策策略（86道）」整章 **31,355** 汉字原先全记给 3.4，而它内部自带
> 12 个考点分组，横跨四个计划章与一大块**超出本书范围**的内容。逐组实测：
> 3.4 ← 任务分解 2,372 ＋ LLM 规划 2,576 ＋ 反应式决策 2,256 ＋ 多步推理与回溯 1,867
> ＋ 形式化四题 1,592 ＝ **10,663**；3.5 ← 工具语义表示 1,957；3.7 ← 多 Agent 角色分工 3,855；
> 3.9 ← 监控指标与异常检测 3,202；**不采用 11,678**（经典搜索与 A*/MCTS 3,727、
> MDP 与价值函数 2,902、部分可观测与状态估计 2,803、约束传播 1,859、复杂度类别 387），
> 逐项登记原因。五部分之和 31,355 与整章实测逐个相等；第 3 篇题库池 282,881 → **271,202**
> （2026-09-13 「工具调用与函数编排（114道）」逐组归位后为 **271,204**，见下）。
> 这一处多了一类新错：前三次是「归错了章」，这次是**题库里有整块内容不属于本书的任一章**。
> 把「每个素材单元都必须有归属」理解成「每道题都要分配给某一章」，就会为了数字闭合
> 把 RL 题塞进 3.4；正确的做法是：「有归属」里包含一个合法的归属——**不采用，并登记原因**。
> 同时按 3.1–3.3 同例重设计划值：3.4 由 7,000 → **11,500**（9 小节 ≈ 7,200–8,100
> ＋ 对照表 4 张 ≈ 1,400 ＋ 固定段 ≈ 1,400 ＋ 代码 ≈ 90 行 ≈ 1,350）。
> 四轮重设（3.1 → 10,000、3.2 → 12,000、3.3 → 10,000、3.4 → 11,500）之后的合计：
> 第 3 篇 71,000 → **75,500**、全书 640,000 → **644,500**，与第一、八节的数字一致。

### 第 4 篇 · AI 应用开发框架（5 章 / 38,000 字，素材 0.12 — **缺口最严重**）

| 章 | 标题 | 计划 | 散文素材 | 比值 | 判定 | 状态 |
| --- | --- | ---: | ---: | ---: | --- | --- |
| 4.1 | LangChain 核心抽象与第一个应用 | 7,000 | 230 | 0.03 | 严重不足 | ✅ 8,242 字（达成 118%；官方文档＋本机实测；新建框架版可运行树 `zhizhou-v4/` 的模型出口与消息层；计划值 7,000 **不调**） |
| 4.2 | 提示模板、输出解析与 LCEL 编排 | 7,000 | 330 | 0.05 | 严重不足 | ✅ 8,221 字（达成 117%；官方文档＋本机实测；提示模板 / 契约 / LCEL 三层；含 `json_mode` 失败与并行账两组读数；**事前估首次落在 ±5% 内**，见 `STYLE` 8.7 与台账） |
| 4.3 | 工具与 Agent：让模型动手干活 | 7,000 | 1,891 | 0.27 | 严重不足 | ✅ 8,871 字（达成 127%；官方文档＋本机实测；`@tool` 工具层 ＋ `create_agent` 装配 ＋ 调用层护栏中间件；六种错误参数的两种处置、`recursion_limit` 的 super-step 折算、审批两法、真机控制实验四组读数；计划值 7,000 **不调**。素材量于 2026-09-14 由 2,287 **改为 1,891**：旧值把 LangGraph 那份文件的 0.3 整块算作本章所用，实测只有第九、十章共 300 汉字） |
| 4.4 | LangGraph：StateGraph 与状态编排 | 8,500 | 1,553 | 0.18 | 严重不足 | ✅ 9,733 字（达成 115%；官方文档＋本机实测；状态与归约器、三种边错法、超级步折算、并行与 `InvalidUpdateError`、帧数、输入/输出 schema 与私有通道、中断的两半；计划值由 7,000 **上调为 8,500**，事前估 11,020、偏差 −12%，见 `STYLE` 8.7） |
| 4.5 | 持久化、多智能体编排与实战 | 8,500 | 523 | 0.06 | 严重不足 | ✅ 11,615 字（达成 137%；官方文档＋本机实测；跨进程的 `SqliteCheckpointer`、快照表与时间旅行、检查点与 3.6 三张表划界、Supervisor ＋ 三角色、交接限额与两种载体；五组读数含真机**两遍**（两遍都是「模型调度者连派五次同一角色」，而绕圈的角与「谁按下末条」两遍都不一样——正文只拿「七条记录／六次交接／末条 `done`」三个不变量立论）；计划值由 5,000 **上调为 8,500**，事前估 9,490、偏差 +22%，见 `STYLE` 8.7 第十四个实测） |

> 第 4 篇正文**已完成**（5 章，实测 46,682 字）。
>
> 两本素材合计 4,680 汉字，且**没有一个规范的代码块**（原 PDF 把代码排成了图片）。
> 这一篇实质上是原创写作，必须以官方文档为准。
> `4.1`/`4.2` 的比重看似比旧值更低，是因为旧值把整份文件切三块估的（三块之和 1.3），
> 而实测下来：§1–3 核心概念＝ 230 汉字、§4 Prompt 模板＝ 330、§5–8 工具与 Agent
> 与完整项目＝ 1,567。**LCEL 与输出解析素材里根本没有**，只能原创。

### 第 5 篇 · RAG 与生产级系统（9 章 / 110,000 字，素材 0.47）

| 章 | 标题 | 计划 | 散文素材 | 比值 | 判定 |
| --- | --- | ---: | ---: | ---: | --- |
| 5.1 | RAG 全景：为什么需要检索增强 | 11,000 | 3,663 | 0.33 | **严重不足** | ✅ 13,326 字（达成 121%；新建**第三棵树** `zhizhou-v5/`（检索版）；自建 6 份私有语料 ＋ 字符 2-gram 倒排索引 ＋ BM25；三条路径（不检索／检索到资料／检索到空）与引用核对；四组读数——召回三形态（命中 19.79／零召回 0／噪声 6.03）、阈值裕度 (6.03, 8.07) 不到 2 分、两条路径直答 0 对带资料 1、真机 4 条里 2 条硬编（含一条**编造出处**）；计划值由 6,000 **上调为 11,000**，见 `STYLE` 8.7 第十五个实测） |
| 5.2 | 文档解析与切分策略 | 12,000 | 6,620 | 0.55 | **偏薄** | ✅ 13,827 字（达成 115%；`zhizhou-v5/` 第二段增量：魔数格式检测 ＋ 八条解析路 ＋ 四类「读不出」分开报；四代切法 ＋ 父子片（含 5.1 的空行切做基线，五代同语料对照）；三组读数——答案片**由第 5 位回到第 1 位**（而这条读数当场被判为「钝的」：四种切法都是第 1）、重叠的账 100% → 291.5%、片必须是原文子串的不变式逼出**三处静默丢字符**；计划值由 7,000 **上调为 12,000**，见 `STYLE` 8.7 第十六个实测） |
| 5.3 | 向量化与向量数据库 | 12,000 | 648 | 0.05 | **严重不足** | ✅ 12,374 字（达成 103%；`zhizhou-v5/` 第三段增量：嵌入出口 ＋ 离线哈希 n-gram ＋ 精确／IVF／PQ 三种手写索引；四组离线读数：归一化前后的三个相似度、两类硬错与「不在同一空间」、`nlist × nprobe` 两张格子、向量 vs BM25；**一条与预设相反的读数**：它没有多召回一条，但把 Q3 的「零召回」变成了「3 片噪声」；18 条离线用例；**换嵌入模型只改配置会静默错**） |
| 5.4 | 检索增强：混合检索、重排与查询改写 | 13,000 | 18,695 | 1.44 | 充裕 | ✅ 13,855 字（达成 106%；`zhizhou-v5/` 第四段增量：两条路出口 ＋ 倒数排名融合 ＋ 改写表与两个改写实现 ＋ 成对打分与重排 ＋ 深度扫描；七组读数里**两条与预设相反**——融合与字面路的 top3 命中数完全一样（2/3），而它把向量单独做对的第一名压到了第三；真正救回两条缺口的是改写（Q3 从零召回到第 2，Q5 从第 3 到第 1）；25 条离线用例；计划值由 8,000 **上调为 13,000**，见下文与 `STYLE` 8.7） |
| 5.5 | 生成、引用与幻觉抑制 | 12,000 | 5,085 | 0.42 | **严重不足** | ✅ 12,026 字（达成 100%；`zhizhou-v5/` 第五段增量：置信度与三档 ＋ 句级精炼 ＋ 按句绑定 ＋ 拒答判据 ＋ 四个控制位；四组离线读数里**两条与预设相反**——库里没有的题落在 **Ambiguous（0.3016）**而不是最低档，精炼四题合计丢掉 **90.1%** 的字且有 1 条从答得出变答不出；夹具三条坏形态里「编号合法、指错了片」**只有代码绑定抓得到**；真机跑两遍，不变量是「编号合法 ＋ 绑定地址相同」，而 Q1 的档位在 Fully 与 Partially 之间漂——档位是措辞的函数；20 条离线用例；**计划值由倒推的 6,000 校正为 12,000**，见 `tools/audit_coverage.py` 的 5.5 注释） |
| 5.6 | RAG 评估：指标、实验与迭代 | 13,000 | 7,574 | 0.58 | **偏薄** | ✅ 12,477 字（达成 96%；`zhizhou-v5/` 第六段增量：命中定义 ＋ 四项指标 ＋ 24 条评测集 ＋ 五组读数 ＋ 21 项基线与回归门；**四条与预设相反**——文档级命中率四配置全 **1.0000**（钝读数，片级才分辨：0.8333／0.7222／0.8333／0.8333）、改写没有片级收益、「先改写再判」一次都没触发、阈值扫描 24 个窗口里 **0 个为零**；评测集的反向守当场抓出一条不是原文子串的断言，改后**重取了基线**；门的极性缺陷（`sweep.best_errors` 被判成越大越好）以反向守修掉；15 条离线用例；**计划值由倒推的 7,000 校正为 13,000**，见 `tools/audit_coverage.py` 的 5.6 注释） |
| 5.7 | 生产级工程：缓存、并发、版本与成本 | 14,000 | 6,273 | 0.45 | **严重不足** | ✅ 13,828 字（达成 99%；`zhizhou-v5/` 第七段增量：三层缓存与版本戳 ＋ 单飞与限流 ＋ 事件流与按号补发 ＋ 四笔账与容量 ＋ 日志/指标/追踪 ＋ 有界重试与三档降级；**三条与预设相反**——热跑省下的账全部记在答案层（那张命中表的分母是三遍合起来的 48 次，**不是线上分布**；检索层 0.0000 是被答案层挡住了，而嵌入那 24 次命中发生在填缓存那一遍：CRAG 判档要再走两条原始路）、**判档不是免费的**（一次提问的「检索 3 笔」＝ 检索 1 ＋ 判档 2）、**把 5.5 的按需检索接成线上默认值：省下 15/24 次检索而答案命中从 0.9444 掉到 0.3333**；验收线是「与 5.6 逐项相同」（片级 0.8333、答案命中 0.9444、四格 1／0、两条路径候选 24/24）；29 条离线用例（缓存 10 ＋ 流式 8 ＋ 服务 11）；**计划值由倒推的 8,000 校正为 14,000**，见 `tools/audit_coverage.py` 的 5.7 注释） |
| 5.7（写作前校正） | 计划值 8,000 → **14,000**；比值 0.78 → **0.45**（判定从「偏薄」变「严重不足」） | — | — | — | 事前估 ≈16,950（见 `tools/audit_coverage.py` 的 5.7 注释） |
| 5.8 | 综合项目：企业级多格式知识库问答系统 | 12,000 | 3,312 | 0.28 | **严重不足** | ✅ 11,520 字（达成 96%；`zhizhou-v5/` 第八段增量：入库管线（发现／解析路由／三档质量门／内容戳／复用与差量）＋ 多格式语料 `knowledge/`（csv ／ txt ／ html ＋ **三份坏样本**）＋ 一条命令的验收（四组读数 ＋ 八项清单 ＋ 10 条夹具）＋ 21 条离线用例；**四条读数**——A 组与 5.6 基线**逐项相同**（片级 0.8333、答案命中 0.9444、四格 1／0，差一格就 `assert` 停）、扩库**候选变了 13/24 条而四项读数一格没动**（新片挤掉的是排名靠后、本来就不装答案的位置；差值 0.0000 在分辨率 1/24 之下，**不报**）、迁移**答案命中 0.9444 → 1.0000、片级 0.8333 → 0.8889（+0.0556 ＞ 0.0417，报得出）**、同一份 `corpus/` 段落切 15 片而结构切 12 片（**片更少读数更好**）；验收清单在**第一次运行里抓到两条错**——进库行的 `fmt` 是空的（真缺陷，已修）与清单自己把「模型自称」读成「答复不合格」（同一族错误的第三次露面，改成两把尺子分开报）；**计划值由倒推的 10,000 校正为 12,000（第一次把已知的系统偏差直接折进计划值）**，实测偏差 **−42%**（事前估按分项加出来是 **19,900**；原先记的 ≈17,400 是算式自己加错了 2,500，已改正），见 `tools/audit_coverage.py` 的 5.8 注释与 `STYLE` 8.7 的第二十二个实测） |
| 5.8（写作前校正） | 计划值 10,000 → **12,000**；比值 0.33 → **0.28**（判定仍是「严重不足」） | — | — | — | 事前估 **19,900** 再打七折（前两次实测分别偏低 23% 与 19%）取下整；实测 11,506，偏差 **−42%**（这一格的 17,400 与 −34% 是原记值：分项之和其实是 19,900，定稿时已改正） |
| 5.9 | 项目复盘：从能跑到能讲 | 11,000 | 299 | 0.03 | **严重不足** | ✅ 11,984 字（达成 109%；`zhizhou-v5/` 第九段增量：`docs/architecture.md`（两张 Mermaid 图 ＋ 节点→模块对照表 ＋ 图外三模块表 ＋ 已知不足）＋ `docs/decisions/0001–0006`（六字段：编号与标题／状态／**背景＝当时手上的那个数**／决定／**被否掉的选项**／**失效线**）＋ `scripts/recap.py`（五条对账：模块双向点名 20/20、六字段齐全、30 条读数引用逐条与 `eval_baseline.json` 相等、8 处行数标注逐处与盘上相等、3 处条数标注逐处与实得相等）＋ `tests/test_recap.py` 6 条；计划值由按「一篇总结」拍的 4,000 校正为 **11,000**（2026-09-17），而这一章第一次拿**已经量到的三次偏差的中位数（−23%）**去折事前估：13,525 × 0.77 ＝ 10,414 对**定稿那一刻**的实测 10,384 只差 0.3%（同日又补 1,586 字、当前 11,970——**补写不计入估算样本**），见 `STYLE` 8.7 第二十三个实测） |
| 5.9（写作前校正） | 计划值 4,000 → **11,000**；素材 0 → **299**（从 5.1 名下拆入）；比值 0.00 → **0.03**（判定由「无素材」变「严重不足」） | — | — | — | 事前估 **13,525**（前置段 800 ＋ 散文 10 节 8,400 ＋ 固定小段 3,200 ＋ 围栏 75 行 × 15）；实写 10,384（**定稿那一刻**；同日补 1,586 字后为 11,970，**不计入样本**），偏差 **−23%**，折扣后只差 0.3% |

> **按节实测后的真实分布是「极度集中在检索」，不是「整篇充裕」**：两份 RAG 素材的散文
> 有一多半落在一个地方——`5.4` 拿 **18,695**（查询改写、融合、重排、路由、多表示索引 /
> RAPTOR / ColBERT）；而基础环节几乎没素材：**《生产级 RAG》第 3 章（Milvus 实战）
> 只有 197 汉字**（正文以代码与表格为主），`5.3` 因此落到 **0.09**——全篇最低。
>
> 所以这一篇的写法要分开定：
>
> | 章 | 素材状况 | 写法 |
> | --- | --- | --- |
> | 5.4 | 充裕（1.44，旧值 2.34 是按 8,000 计划算的） | 以素材整理改写为主，但示例迁到官方文档的当前 API；**有一处把素材当反例**（不归一化的加权和） |
> | 5.2 / 5.6 | 偏薄–够用（0.55–0.58） | 素材打底＋按官方文档补齐缺口（5.6 的计划值由 7,000 上调为 13,000，见 `tools/audit_coverage.py` 的 5.6 注释） |
> | 5.7 | **严重不足（0.45）** | 素材只给了「流式」与「运维」两块骨架（且两者都以代码为主），缓存、并发、版本与成本四块的**口径与读数一条都没有**——必须回到官方文档（MDN / sse-starlette / Prometheus / Redis / asyncio）并自测；计划值由倒推的 8,000 校正为 **14,000** |
> | **5.1** | **严重不足（0.36）** | **已写成**（13,326 有效字）；素材只给了动机与四阶段的骨架，检索器、语料、四组读数、后八章地图全部原创——计划值因此由 6,000 上调为 11,000（见 `STYLE` 8.7 第十五个实测） |
> | **5.5** | **严重不足（0.42）** | **已写成**（11,896 有效字）；已定稿的 11 节里，素材只给了 CRAG／Self-RAG 的机制与骨架，四组读数、夹具、拒答判据与控制位全部原创。**这个 0.42 与 5.1 的 0.36 同一性质**：旧计划值 6,000 是拿素材量倒推的（5,085 × 0.85），而一章的体量由小节数与代码密度决定——校正后为 12,000 |
> | 5.3 / 5.8 | **严重不足（0.09 / 0.33）** | 散文原创：Milvus / BGE / RAGAS 官方文档；自带项目（第 10 章 2,763 汉字）以代码为主体，须重写并补讲 |
>
> `5.6` 的素材里有 **2,265 汉字是文末重复的副本**（评估 / 实战 / 进阶三块各出现两次，
> 逐字比对 40 字窗口 22/22 重合），已剔除，只算首次出现。

> **本轮修正（2026-09-13：第 4、5 篇的素材权重全部改为按节实测）**：
> `audit_coverage.py --check` 一直报着三处「同一份素材被多章重复计入」（各章权重之和 > 1.0），
> 它们与 `2.4`/`2.5`/`2.6` 是同一个错——权重是「把整份文件切一块」估的，没人拿它和
> 实际用到的节对过。现在三份都逐节量过了（逐节汉字量写进 `tools/audit_coverage.py` 的注释，可复核）：
>
> | 素材 | 汉字 | 旧权重之和 | 实测定下的分布 |
> | --- | ---: | ---: | --- |
> | `LangChain从入门到实战.md` | 2,279 | 1.30（4.1–4.3） | 4.1 §1–3 ＝ 230 ｜ 4.2 §4 ＝ 330 ｜ 4.3 §5–8 ＝ 1,567 |
> | `RAG技术从入门到深入.md` | 25,444 | 1.05（5.1–5.5） | 主体在 5.4（14,053）；5.1/5.2/5.3 各仅数百字 |
> | `生产级RAG系统构建实战.md` | 32,068 | 1.35（5.2–5.8） | 解析 5.2 ＝ 5,988 ｜ 评估 5.6 ＝ 5,309 ｜ 运维 5.7 ＝ 6,273；第 3 章 Milvus 仅 197 |
>
> **权重的小数位是反算出来的**：取到「能让 `int(素材总量 × 权重)`（工具用 `int()`
> 截断）正好等于实测值」的那一位。于是**本表里的素材量与上表的实测数逐个相等**，
> 可以相乘反查；旧值 0.10/0.14/0.69 会把 230 算成 227、330 算成 319，同一件事在
> 注释与表格里写成两个数。反算后的权重（含 5 位小数者）记在
> `tools/audit_coverage.py` 的注释里。
>
> 顺带修掉一处**篇级口径**的毛病：分篇的「素材池」原是把引用到的**整份文件**相加，
> 于是归给别章的部分也算进了本篇（第 5 篇因此显示 0.94）。改为「各章计权相加，
> 每份素材在一篇里最多算 1.0」后，第 1/2/5 篇由 0.64 / 0.45 / 0.94 落到
> **0.61 / 0.36 / 0.83**，题库池由 995,562 落到 **201,627**（后者原是把整份题库充数）。
> 两个变化都让数字变小——**不是坏消息，而是把话说清**：第 4、5 篇要以官方文档为主来写。
> 需原创补写的章 55 → **57**，风险章节 34 → **36**（多出的一章是 `5.7`：6,273/8,000
> ＝ 0.78，从「够用」落到「偏薄」——旧值 0.80 正是权重估高带来的假边界；
> 写 5.7 前又按 8.7 重估了一次体量，它的计划值由 8,000 校正为 **14,000**，
> 比值因此落到 **0.45**——与 5.5 同一条：**素材少不等于这一章小**）。
> 这是同一类错的**第四、五次**（前三次：2.4 的 4,076、2.5 的 0.15、2.6 的 0.15），
> 所以量法已写进 [`STYLE.md`](STYLE.md) 8.5，且工具的重复计入检查会在闸门里一直看着。
>
> 闸门也跟着补了一层：`--check` 原先只核**篇级小计**，章表里的逐章数字没人管，
> 而这两处是各自手写的——本轮 4.1 就在表里写 227、注释里写 230，两边都「看着对」。
> 现在逐章核「计划 / 素材 / 比值」三格（带夹具），所以上面这张表里的每个数字
> 都被锁住了。

### 第 6 篇 · 模型接入与成本工程（4 章 / 35,000 字，**素材为零，纯原创**）

| 章 | 标题 | 计划 | 官方出处 | 状态 |
| --- | --- | ---: | --- | --- |
| 6.1 | 模型选型：能力、价格与上下文窗口的三角权衡 | 7,000 | Claude Models · OpenAI API docs | ✅ 7,706 字（达成 110%；定稿那一刻是 7,666，写 6.2 时这一表的「`__init__.py` 行数」格涨了一行；第四棵树 `zhizhou-v6/` 第一段增量：登记表 8 个模型 × 2 家 ＋ 四笔账 ＋ 缓存两个回本点 ＋ 长档悬崖。六组读数**全部是算出来的**（不要密钥、不要网络）——同一张画像最贵与最便宜差 **48.5 倍**；多一个词元整单 **+92.5%**（长档按**整单**作用于跨过 272K 的那一单，过线后唯一的省法是切两单）；缓存回本点 **第 2 次／第 3 次**、命中率底线 **21.74%／52.63%**；「名次翻转」证明「哪个模型便宜」必须带画像问。**建树当天入 `SPECS` 表**，两处下限第一版写成「安全的小数」而自检当场各报一次；计划值 7,000 **未改**——拍出来的数（四章平摊预算）比算出来的数（判据层 × 折扣）准，见 `STYLE` 8.7 第二十四个实测） |
| 6.2 | 多厂商接入：Messages、Responses 与 OpenAI 兼容层 | 7,000 | platform.claude.com · developers.openai.com | ✅ 7,215 字（达成 103%；定稿那一刻是 7,194 ——本章此后只多了一行正文；第四棵树第二段增量：**规范形状 ＋ 翻译表 ＋ 三个适配器**。三个协议被当成**同一件事的三种方言**——八栏里**没有一栏同名**，形状层分三堆 4／4／0；六组读数全部能用手复核：同一请求翻成三份载荷是 **27／27／28** 个 key 路径、**806／812／862** 字节，而**共有的 key 路径只有 1 个**（`model`）；工具参数**一家给对象、两家给 JSON 字符串**（坏 JSON 要报错，不许静默成 `{}`）；「为什么停」是 5 档 × 3 方言＝**15 格**，只有一种方言能原样表达全部五档——而最重的一格是「**要调工具**」在 Responses 上读成「**答完了**」（撞车：`completed` 同时表示两种结束，因此判工具要看 output 里有没有那个 item）；同一段答复在三种帧形下是 **12／9／7 帧**（只有一家发 `event:` 名与 `ping`，只有一家以 `[DONE]` 收尾——**它不是 JSON**）；**24 个「模型 × 方言」格里只有 11 格能调到工具**，其中 8 格的原因只有一个（另一家的四个模型不在兼容层的图纸上）；计划值 7,000 **未改**——折扣层这一次几乎打中（折后 7,276 对实测定稿值 7,194＝+1.1%），而估算里有一处**新错法：围栏被算了两遍**，见 `STYLE` 8.7 第二十五个实测） |
| 6.3 | 模型网关：统一接口、路由、重试与降级 | 11,000 | LiteLLM · vLLM · Ollama | ✅ 11,391 字（达成 103%；计划值由 7,000 **改为 11,000**——不是估算表算偏了，而是这一章**事前估没有落笔**（旧值只是「四章平摊的预算」），所以事后无法区分「估错了」与「写超了」；第三段增量：`app/gateway.py` 的错误分档表、退避与截止时间预算、熔断器 ＋ `app/router.py` 的候选排序与降级链，六组读数全部可手算（分档表 12 档 × 3 方言＝**36 格**，只看状态码会判错 **13 格（36.1%）**；退避五次合计 15.5s 而调用方只给 12s；预算切片第一片只有 4.00s（最坏 90s 从不出现）；同一模型**不带工具能走、带工具不能**；价钱阶梯末跳／首跳 **48.55**），见 `STYLE` 8.7 第二十六个实测） |
| 6.4 | 成本与性能工程：Token 计费、缓存与模型分流 | 10,000 | 官方定价页 · OpenAI 数据控制 · 两家批量页 | ✅ 11,779 字（达成 118%；第四棵树第四段增量：`app/meter.py` 的四栏账与口径断言 ＋ 长档倍数 ＋ 断点顺序 ＋ 批量与叠加 ＋ 分流 ＋ 分桶 ＋ 看板与三道闸。六组读数全部是算出来的（连 6.3 那条脚本化的传输都不需要）：那一笔的 **89.0% 在输出**而输入三栏只占 11.0%；归因把「贵了 1.751 倍」拆成 63.0% 输入塌了 ＋ 37.0% 输出涨了；**300,000 输入命中 96.7% 仍然是长档**，裁掉 10.0% 的词元 → 账降 **44.0%**；同一批提示词只改排列可缓存 **3,400 → 800 → 0**（一个 20 词元的时间戳排第一行就让 3,400 全落空）；批量 **0.5×／24 小时**：能挪 10.0% 的调用却带 **94.9%** 的钱，叠加不是相加（0.530 对 0.559，而这一笔上缓存的天花板就是输入那一栏的 11.0%）；分流在价目表上退化成整体降档（四个画像全部落在同一个候选，**96.2%** 是上界）而它真正的实体是「按画像发现谁装不下」；分桶键用「请求」会让 1,000 人里 **964** 个在一次实验里跨到两边；看板要报**三个命中率**（95.0%／36.3%／24.6%）；三道闸的判据是「已花 ＋ 在飞」（95.0 ＋ 8.0 ＝ 103.0%，而只看已结算会判成告警）。**计划值 7,000 → 10,000**——这一章第一次把事前估写进了 `tools/audit_coverage.py` 的注释（按 8.7 式子估 12,450、按 0.77 折取 10,000），实测落在 11,764，所以**折扣层的上界第一次被证伪**：本章的实测／分项和是 0.80，不是 0.77，见 `STYLE` 8.7 第二十七个实测） |

> 第 6 篇正文**已完成**（4 章，实测 38,091 字，四段增量全部落进 `zhizhou-v6/`），并已收口——
> 篇收尾三件事（落点回查 / 术语扫描 / 交叉引用复核）的结果见 [`LEDGER.md`](LEDGER.md) 的「第 6 篇统稿记录」。
>
> 这一篇回答「AI 应用到底怎么把模型接进自己的服务」。素材里没有**成篇**的相关内容，
> 每章必须先立好 [`REFERENCES.md`](REFERENCES.md) 的出处再动笔。
>
> **一处口径修正（2026-09-17 写 6.1 时）**：「素材为零」这句话在第 6 篇上要收窄成
> 「**散文素材为零，但有两条改派进来的账**」。台账里早就登记过两处指向本篇的 `⏭`：
> `AI-Agent面试题合集-1038道.md` 的「Token 成本与路由」（2,307 汉字，记在 3.2 那个 20,069 里）
> 与 `大模型与AI-Agent从0到1笔记.md` §5.5「成本控制」（273）。按 8.4 那一条「去向章开写时
> 反向扫描」，6.1 逐块核过之后**领走 799 汉字**（题1 234 全取、题6 299 全取、题7 半块 130、
> 笔记那节的算式形状 136），其余一条不领并写明归 6.3／6.4／第 8 篇。
> **这些汉字不重记权重**（权重仍挂在 3.2 名下，重记会让同一份素材进两次账），
> 所以本表里的素材比值仍是 0.00——但读者能看到这 799 是从哪来的。

### 第 7 篇 · AI 应用工程化（5 章 / 56,500 字，**素材为零，纯原创**）

| 章 | 标题 | 计划 | 官方出处 | 状态 |
| --- | --- | ---: | --- | --- |
| 7.1 | 评测流水线：数据集、指标与 CI 回归 | 7,000 | Ragas 官方文档 · pytest | ✅ 8,071 字（达成 115%；新建**第五棵树** `zhizhou-v7/` 的第一段增量：评测流水线（数据集、指标、门三层）。六组读数全部可手算（不调模型、不联网）：24 条题的整集指纹**按编号排序**（换顺序不变、改一字就变）、重复 0 组而「把 q21 的输入改成 q02 那一句」当场报出 1 组（再换切分就是污染）；**5 条指标 × 24 条 ＝ 120 格里有 4 格测不了**（拒答题没有片段），而「测不了记成 0」让同一条指标从 **1.0000 掉到 0.8333**（分子没变、分母从 20 变 24）；**分辨率是 1/n** —— 5.6 那条 1/24 ＝ 0.0417 在门禁的 18 条上变成 1/18 ＝ 0.0556，一次 0.0417 的改动从「看得见」变成「看不见」；门有五档而**三档不是通过**（`empty`／`underpowered`／`too_coarse`，pytest 的退出码 5 是同一件事的另一处写法）；**容差取量出来的抖动带**（0.1111 ＝ 分辨率的 2 倍）：取 0 时噪声被当成退步，取抖动带时「差 1 条」判 pass、「差 3 条」才判 regress；**总分没变不等于没变**（三好三坏抵消），而「数据集少两条」会把总分从 0.6111 拉到 0.5625 —— 系统一个字没改；**按条数抽** 8 条的 20 份样本里 13 份丢掉「表格」那一层（只有 1 条），按层抽是 0 份。**本章事前估没有落笔**（第二次，第一次见 6.3），见 `STYLE` 8.7 第二十八个实测） |
| 7.2 | 可观测性：链路追踪、指标与成本看板 | 12,000 | Langfuse · OpenTelemetry GenAI | ✅ 10,671 字（达成 89%；**第五棵树** `zhizhou-v7/` 的第二段增量：两块模块 `trace`／`board`（span 树与自耗时、语义约定缺口、两档采样、分位、四栏账与覆盖率、告警事件），六组读数 ＋ **50 条夹具** ＋ **28 条用例**（trace 15 ＋ board 13）＋七个输出块；它是全书**第一份「事前估在动笔之前就落笔」的章**） |
| 7.2（写作前校正） | 计划值 7,000 → **12,000**（本篇无素材可拆，判定不变）；**这是全书第一次在动笔之前把事前估写下来** | — | — | 事前估 **11,700**（前置段 650 ＋ 散文 8 节 × 800 ＝ 6,400 ＋ 接线节 900 ＋ 固定小段 2,400 ＋ 围栏 90 行 × 15；**不折**——四项都取了同类章实测的中位。若按本篇三章实测／分项的中位 0.92 折 ≈ **10,700**）；算式、三问的答案与分节计划见 `tools/audit_coverage.py` 的 7.2 注释 |
| 7.3 | 提示与配置版本化：灰度发布与 A/B 实验 | 14,000 | Langfuse · GrowthBook · 十二要素 · Git | ✅ 14,075 字（达成 101%；**计划值 10,000 → 14,000**——全书第一次「事前估偏低」，分项之和 10,200 估低 27.5%；**第五棵树** `zhizhou-v7/` 的第三段增量：两块模块 `registry`／`rollout`（内容戳与三条线的 diff、标签发布与回滚、静默漂移、哈希分桶与留缝、命名空间互斥、比例失衡、放量阶梯、偷看模拟）。六组读数全部可手算（不调模型、不联网），另有一组是固定种子的模拟（带蒙特卡洛误差）） |
| 7.3（写作前校正） | 计划值 6,000 → **10,000**（本篇无素材可拆，判定不变）；**这是全书第二次在动笔之前把事前估写下来** | — | — | 事前估 **10,200**（前置段 600 ＋ 散文 7 节 × 750 ＝ 5,250 ＋ 接线节 900 ＋ 固定小段 2,400 ＋ 围栏 70 行 × 15 ＝ 1,050）；四项都取同类章实测的**下沿**，故不再乘 0.77；备查的第二个数：按第 7 篇已定稿两章的合计比值 0.776 折 ≈ **7,900**。三问（读数长成什么 / 围栏散不散 / 含不含容错）已逐条答完，分节计划 7 散文节 ＋ 1 接线节，均见 `tools/audit_coverage.py` 的 7.3 注释 |
| 7.4 | 安全落地：提示注入、越狱与工具权限治理 | 12,000 | OWASP LLM Top 10 · MCP 授权 · NIST AI RMF | ✅ 11,320 字（达成 94%；**第五棵树** `zhizhou-v7/` 的第四段增量：两块模块 `guard`／`policy`；**事前估在动笔前落笔**：分项之和 12,250、**估高 7.7%**） |
| 7.4（写作前校正） | 计划值 7,000 → **12,000**（本篇无素材可拆，判定不变）；**这是全书第三次在动笔之前把事前估写下来** | — | — | 事前估 **12,250**（前置段 700 ＋ 散文 7 节 × 950 ＝ 6,650 ＋ 接线节 1,100 ＋ 固定小段 2,600 ＋ 围栏 80 行 × 15 ＝ 1,200）；前置段取四章实测的中位下沿、接线节取中位、散文那一项是**插值**（7.2 单节 802 ／ 7.3 单节 1,279，本章预计 5–6 处「只看一半就错」的对照）→ 故不再乘 0.77。备查的第二个数：按第 7 篇**三章**的合计比值 0.955 折 ≈ **11,700**——而同一个比值在两章时是 0.776，**一个折扣系数在小样本上晃得比它要吸收的偏差还大**。三问（读数长成什么／围栏散不散／含不含容错）已逐条答完，分节计划 7 散文节 ＋ 1 接线节，均见 `tools/audit_coverage.py` 的 7.4 注释 |
| 7.5 | 数据合规与脱敏：留存、隐私与审计 | 11,500 | OpenAI 数据控制 · NIST AI RMF | ✅ 8,711 字（达成 76%；**第五棵树** `zhizhou-v7/` 的第五段增量：一块模块 `privacy`；**事前估在动笔前落笔**：分项之和 11,305、**估高 23.6%**——本篇估得最松的一次，见 `STYLE` 8.7 第三十二个实测） |
| 7.5（写作前校正） | 计划值 6,000 → **11,500**（本篇无素材可拆，判定不变）；**这是全书第四次在动笔之前把事前估写下来** | — | — | 事前估 **11,305**（前置段 550 ＋ 散文 6 节 × 880 ＝ 5,280 ＋ 接线节 1,200 ＋ 固定小段 3,000 ＋ 围栏 85 行 × 15 ＝ 1,275）；前置段取下沿一侧（按 7.4 判据：由「导读写几段」与「前置知识里有几条是『别处已经写过』」定）；散文那一项是**插值后下压**（7.2 单节 802 ／ 7.3 单节 1,279，本章的对照**在动笔前就数得出 6 处**，线性给 ≈980，再照 7.4 的实测修正下压到 880）；接线节取中位；固定小段**改用 7.4 留下的新判据**（不按坑的条数推算，直接取实测中位）→ 故不再乘任何折扣。备查的第二个数：按第 7 篇**四章**的合计比值 0.947 折 ≈ **10,700**——同一个系数在两章时 0.776、三章 0.955、四章 0.947，**它到四章才稳下来，而这恰好说明它在两章时不能用**。三问（读数长成什么／围栏散不散／含不含容错）已逐条答完，分节计划 6 散文节 ＋ 1 接线节，均见 `tools/audit_coverage.py` 的 7.5 注释 |

> 与第 3 篇的分工：**第 3 篇讲原理（为什么危险、为什么要评估），本篇讲落地（怎么做）**。

### 第 8 篇 · 交付与产品化（5 章 / 63,000 字，**素材为零，纯原创**）

| 章 | 标题 | 计划 | 官方出处 |
| --- | --- | ---: | --- |
| 8.1 | 容器化部署：Docker、镜像瘦身与 Compose | 11,000 | Docker 官方文档 | ✅ 10,480 字（达成 95%；**第六棵树** `zhizhou-v8/` 的第一段增量：四块模块 `image`／`cache`／`compose`／`runtime`（层账与可见账、构建缓存的位置代价、缩身四招、上下文外发与传输、四种等法、运行期权限面与密钥三档。六组读数全部手算得出，不装 Docker、不联网）；**事前估在动笔前落笔**：分项之和 10,595、**估高 1.6%**——全书第一次落进 ±5%，见 `STYLE` 8.7 第三十三个实测） |
| 8.1（写作前校正） | 计划值 7,000 → **11,000**（本篇无素材可拆，判定不变）；**这是全书第五次在动笔之前把事前估写下来**，也是**第一个开篇就带着事前估的新篇** | — | 事前估 **10,595**（前置段 650 ＋ 散文 6 节 × 830 ＝ 4,980 ＋ 接线节 1,200 ＋ 固定小段 2,595 ＋ 围栏 78 行 × 15 ＝ 1,170）；前置段按 7.4 的判据定（「导读写几段」与「前置知识里有几条是「别处已经写过」」）——本篇开篇、导读多一段，故取 650；散文系数取四个最近实测单节（802／1,279／864／743）的**中位 833 略下**（既不插值——7.4 的补全「数不出来就别插值」，也不在判据之上加价）；接线节取四章中位；固定小段**照 7.4 判据原样取七章实测中位 2,595，不加「这一章特殊」那一层**（那一层正是 7.5 的全部误差）；围栏取中位偏上。五项全部取「同类章实测的中位或中位一侧」→ **不再乘任何折扣**；备查的第二个数：按第 7 篇**五章**合计比值（52,828／57,905 ＝ 0.912）折 ≈ **9,700**——同一个系数在五章之间走 0.776 → 0.955 → 0.947 → 0.912，**它没有继续收敛，而是摆在 0.9 上下，摆动幅度与它要吸收的偏差同量级**（见 8.7 第三十三个实测）。三问（读数长成什么／围栏散不散／含不含容错）已逐条答完，分节计划 6 散文节 ＋ 1 接线节，均见 `tools/audit_coverage.py` 的 8.1 注释 |
| 8.2 | CI/CD 与发布：自动化测试、构建与灰度发布 | 11,500 | GitHub Actions 文档 | ✅ 14,429 字（达成 125%；**第六棵树** `zhizhou-v8/` 的第二段增量：六块模块 `trigger`／`graph`／`caching`／`perms`／`gate`／`release`（触发与过滤、job 图与并发、缓存与产物、流水线权限面、门禁的七种形状、发布与灰度）；`scripts/ci_reader.py` 六组读数 ＋ 138 条夹具；六个测试模块 95 条用例；**事前估在动笔前落笔**：分项之和 11,696 → 实测 14,414，**估低 23.2%**——三处偏差里两处大：散文六节实测 9,488（估 5,556，+70.7%）与围栏 138 行（估 90 行，+53.3%） |
| 8.2（写作前校正） | 计划值 7,000 → **11,500**（本篇无素材可拆，判定不变）；**这是全书第六次在动笔之前把事前估写下来**，也是第 8 篇的第二次 | — | — | 事前估 **11,696**（前置段 900 ＋ 散文 6 节 × 926 ＝ 5,556 ＋ 接线节 1,200 ＋ 固定小段 2,690 ＋ 围栏 90 行 × 15 ＝ 1,350）；**四个系数全部换成已定稿章的实测中位**：前置段取 8.1 的实测 900（它当初按同一条判据拍的是 650，实测 +38.5%）、散文系数取最近四个单节实测（1,279／864／743／988）的**中位 926**（8.1 用的是它前面四个的 833——**这是判据第一次随实测向上移动，而不是在它之上加价**）、接线节照判据 1,200、固定小段取最近六章实测中位 2,690、围栏 90 行照 8.1 实测；分项之和 11,696 → 计划值 11,500（第 8 篇 37,000 → **41,500**、全书 811,000 → **815,500**），不再乘任何折扣系数 |
| 8.3 | 前端流式交互：SSE 与 AI SDK 对话式 UI | 12,500 | Vercel AI SDK · Next.js | ✅ 13,820 字（达成 110%；**第六棵树** `zhizhou-v8/` 的第三段增量：两块模块 `sse`／`stream_ui`（帧的四个字段与还原、三个头与四种部署、心跳与空闲超时、续传的缓冲、背压与慢消费者、六个状态与八条边、五种「卡住／显示错」的现场、取消的三层、两种分片模式）；`scripts/stream_reader.py` 六组读数 ＋ **62 条夹具**、`tests/test_sse.py` **29 条用例** ＋ `tests/test_stream_ui.py` **23 条用例**（全树 224 条）。**事前估 12,560；实测 13,806 ＝ 估低 9.9%**（估得最准的一项是围栏：估 110 行、实测 116 行，+5.5%） |
| 8.3（写作前校正） | 计划值 7,000 → **12,500**（本章无素材可拆，判定不变）；**这是全书第七次在动笔之前把事前估写下来**，也是第 8 篇的第三次 | — | — | 事前估 **12,560**（前置段 900 ＋ 散文 6 节 × **1,011** ＝ 6,066 ＋ 接线节 1,200 ＋ 固定小段 2,744 ＋ 围栏 110 行 × 15 ＝ 1,650；**这一行原写 12,514，是五项相加时抄错了一个旧值——五项一个没动，改的是那个和，见 `LEDGER` 8.3 的真结论第 6 条**）；四项全部取**与本章同形**的实测——散文系数这一步是 8.2 那条修正（「取样本之前先看形状」）的**第一次执行**：本章每节是「一段机制 ＋ 一块读数 ＋ 3–6 行小表」，取 **8.1 实测的单节 1,011**，**不取 8.2 的 1,521、也不把两种形状混着取中位**（那正是 8.2 偏 70.8% 的错法）；前置段照 8.1／8.2 判据取 900、接线节照判据取 1,200、固定小段取最近六章实测中位 2,744；围栏**按块数 × 每块行数**估（8.2 那次的教训：它照抄了 8.1 的 90 行，而实测 138 行） |
| 8.4 | 多租户、配额与计费 | 13,000 | FastAPI Security · Milvus 多租户 | ✅ 13,382 字（达成 103%；**第六棵树** `zhizhou-v8/` 的第四段增量：三块模块 `tenant`／`quota`／`billing`（隔离落在哪一层、配额按什么量、一笔调用在三个时刻各说一遍账）；**事前估在动笔前落笔**：分项之和 12,788、**估低 4.5%**——八次事前估里最准的一次） |
| 8.4（写作前校正） | 计划值 6,000 → **13,000**（本章无素材可拆，判定不变）；**这是全书第八次在动笔之前把事前估写下来**，也是第 8 篇的第四次 | — | — | 事前估 **12,788**（前置段 1,000 ＋ 散文 6 节 × **1,031** ＝ 6,186 ＋ 接线节 1,300 ＋ 固定小段 2,652 ＋ 围栏 110 行 × 15 ＝ 1,650）；6 散文节 ＋ 1 接线节同样在动笔前定；**散文系数取的是「每节一组读数」那个同形样本**（8.1 实测 1,031），而不是「每节两组」的 8.3（1,504） |
| 8.5 | 线上运维：SLO、告警、故障演练与容量规划 | 15,000 | OpenTelemetry 官方文档 | ✅ 15,384 字（达成 102%；**第六棵树** `zhizhou-v8/` 的第五段增量：四块模块 `slo`／`alert`／`drill`／`capacity`（四个数与一条怎么花的规矩、燃尽率与三档窗口、演练的一张清单、排队与三种买法）＋ **七组读数**；**事前估在动笔前落笔**：分项之和 14,797、**估低 3.8%**——九次事前估里最准的一次） |
| 8.5（写作前校正） | 计划值 6,000 → **15,000**（本章无素材可拆，判定不变）；**这是全书第九次在动笔之前把事前估写下来**，也是第 8 篇的第五次（本篇收口） | — | — | 事前估 **14,797**（前置段 1,150 ＋ 散文 6 节 ＝ 4 × 1,500 ＋ 2 × 1,100 ＝ 7,800 ＋ 接线节 1,400 ＋ 固定小段 2,497 ＋ 围栏 130 行 × 15 ＝ 1,950）；**这是第一次真的按 8.4 那条新判据估**（先数「每节几块」再取系数），也是第一次把「学习目标超 8 条就往上取一档」执行到位 |

> `1.1` 只讲本地环境、`1.14` 只讲单元测试，都是为了给这一篇留位置，避免重复。

### 第 10 篇 · 求职冲刺（7 章 / 85,000 字，题库驱动）

| 章 | 标题 | 计划 | 素材来源 | 素材状态 |
| --- | --- | ---: | --- | --- |
| 10.1 | 简历：AI 应用开发岗怎么写才有回音 | 13,000 | — | ✅ 12,656 字（达成 97%；**完全原创、素材为零**；**事前估在动笔前落笔**：分项之和 13,088、**估高 3.5%**——十次事前估里第二准；**本篇不建第七棵树**，所以它是全书第一篇章内没有接线小节的章） |
| 10.1（写作前校正） | 计划值 6,000 → **13,000**（本篇无素材可拆，判定不变）；**这是全书第十次在动笔之前把事前估写下来**，也是**第 10 篇的第一次**，并带着一个本篇独有的形态决定：**不建第七棵树**（选题前问过用户，三个选项里选「不建树，改交一份可核对的题库索引」） | — | 事前估 **13,088**（前置段 1,250 ＋ 散文 6 节 ＝ 4 × 1,400 ＋ 2 × 1,000 ＝ 7,600 ＋ **接线节 0** ＋ 固定小段 2,438 ＋ 围栏 120 行 × 15 ＝ 1,800）；前置段取 8.1（900）与 8.5（1,286）两章的**高位一侧**——**新篇开篇的导读要多一段「这一篇管什么」**，而学习目标预计 14 条，8.4 那条「超 8 条往上取一档」这一次是在动笔之前就写进去的；散文**先数每节几块再取系数**（8.4 那条判据的第二次执行），固定小段取最近六章实测中位 2,437.5，围栏**按块数 × 每块行数**估（表 9 ＋ 非命令输出块 5 ＋ 命令块 1）；五项全部同形实测 → **不乘任何折扣**。三问（读数长成什么／围栏散不散／含不含容错）已逐条答完，分节计划 6 散文节、**无接线节**，均见 `tools/audit_coverage.py` 的 10.1 注释 |
| 10.2 | 项目讲述法：STAR、亮点提炼与追问防御 | 12,000 | — | ✅ 11,143 字（达成 93%；**完全原创、素材为零**；**事前估在动笔前落笔**：分项之和 11,816、**估高 6.1%**；**散文那一项第一次落进 ±5%**；仍无接线节） |
| 10.2（写作前校正） | 计划值 6,000 → **12,000**（本篇无素材可拆，判定不变）；**这是全书第十一次在动笔之前把事前估写下来**，也是第 10 篇的第二次 | — | 事前估 **11,816**（前置段 900 ＋ 散文 6 节 ＝ 5 × 1,400 ＋ 1 × 1,000 ＝ 8,000 ＋ **接线节 0** ＋ 固定小段 2,391 ＋ 围栏 35 行 × 15 ＝ 525）；前置段取 8.1 的 900（非开篇章，导读只一段）；固定小段取**最近六章**实测中位 2,390.5；围栏是 **10.1 那条新判据的第一次执行**（不问「几组读数」、只问「这一章有几块真的要围栏」）；**散文系数不动**（10.1 实测单节 1,517，比系数高 8.4%——把实测当系数用就是「用答案改题目」，所以估低这件事写进结论而不改系数）；三问已逐条答完，分节计划 6 散文节、**无接线节**，均见 `tools/audit_coverage.py` 的 10.2 注释 |
| 10.3 | **AI 系统设计面试** | 12,000 | — | ✅ 11,201 字（达成 93%；**完全原创、素材为零**；**事前估在动笔前落笔**：分项之和 11,782、**估高 5.0%**；仍无接线节） |
| 10.3（写作前校正） | 计划值 8,000 → **12,000**（本章无素材可拆，判定不变）；**这是全书第十二次在动笔之前把事前估写下来**，也是第 10 篇的第三次 | — | 事前估 **11,782**（前置段 900 ＋ 散文 6 节 ＝ 5 × 1,400 ＋ 1 × 1,000 ＝ 8,000 ＋ **接线节 0** ＋ 固定小段 2,387 ＋ 围栏 33 行 × 15 ＝ 495）；前置段取 8.1 的 900（非开篇章）；固定小段取**最近六章**实测中位 2,386.5，而这份样本**第一次含同篇两章**（10.1 与 10.2）；围栏是 **10.1 那条新判据的第二次执行**（不问「几组读数」、只问「这一章里有几块真的要围栏」，预计 3 块 33 行）；三问已逐条答完，分节计划 6 散文节、**无接线节**，均见 `tools/audit_coverage.py` 的 10.3 注释 |
| 10.4 | AI Agent 面试考点手册 | 15,000 | AI Agent 题库（1038 道） | ✅ 12,705 字（达成 85%；**题库仅作考点索引、答案须重写**；**事前估在动笔前落笔**：分项之和 14,857、**估高 17.0%**；仍无接线节；**本章的读数由 `tools/bank_index.py --check` 守**） |
| 10.4（写作前校正） | 计划值 **15,000 不变**（事前估 14,857 落在它的 1% 内）；**这是全书第十三次在动笔之前把事前估写下来**，也是**第 10 篇的第四次**，并且是**第一次「计划值与事前估互相印证」**（前十次都要么改计划值，要么差在 3% 以上） | — | 事前估 **14,857**（前置段 **850**（第 10.3 那条修正的第一次执行：900 → 850）＋ 散文 **8 节 ＝ 8 × 1,400 ＝ 11,200**（**节数由题库自身的形状定**：题库有九个专题）＋ **接线节 0** ＋ 固定小段 2,387 ＋ 围栏 **28 行 × 15 ＝ 420**（无树篇按 **块数 × 7 行** 估的第 10.3 修正））；三问已逐条答完，分节计划 8 散文节、**无接线节**，均见 `tools/audit_coverage.py` 的 10.4 注释 |
| 10.5 | 后端高频考点（Python / AI 岗位视角） | 13,000 | 后端题库（165 道） | ✅ 11,927 字（达成 92%；**题库仅作题面与难度标记、答案须换视角**；**事前估在动笔前落笔**：分项之和 13,020、**估高 9.3%**；仍无接线节；**本章的读数由 `tools/backend_index.py --check` 守**） |
| 10.5（写作前校正） | 计划值 15,000 → **13,000**（题库体系与岗位体系不一致，但那些题不参与写作，判定不变）；**这是全书第十四次在动笔之前把事前估写下来**，也是第 10 篇的第五次 | — | 事前估 **13,020**（前置段 **650 ＋ 学习目标 14 条 × 25 ＝ 1,000**（10.4 那条修正的第一次执行）＋ 散文 8 节 ＝ **3 节 × 1,400 ＋ 5 节 × 900 ＝ 8,700**（按「有没有可贴的读数」分两类）＋ **接线节 0** ＋ 固定小段 **2,900**（小结按「本节数 × 3 ＝ 24 条」估）＋ 围栏 28 行 × 15 ＝ 420）；八节分节计划、四块围栏与三问都在 `tools/audit_coverage.py` 的 10.5 注释里 |
| 10.6 | 移动端考点：Android 与 Flutter-Dart | 13,000 | 移动端两套题库（1850 道） | ✅ 11,332 字（达成 87%；**两份题库的格式不统一，解析要分别写**；**整理稿可用性量成三个读数**：模板答案 43 道（逐字相同 40 道）、OCR 未识别 6 道、题面含水印 37 道；**事前估 13,387 → 估高 15.4%**，四组读数由 `tools/mobile_index.py` 现算） |
| 10.6（写作前校正） | 计划值 12,000 → **13,000**（两份题库共 1,850 道，与岗位的错位比 10.5 更远，但处置判据把这件事接住了）；**这是全书第十五次在动笔之前把事前估写下来**，也是第 10 篇的第六次，且是**第一次同时面对两份格式不同的题库** | — | 事前估 **13,387**（前置段 **912**（导读 3 段 × 150 ＋ 21 条 × 22；10.5 那条修正的第一次执行）＋ 散文 8 节 ＝ **4 节 × 1,400 ＋ 4 节 × 900 ＝ 9,200**（按「有没有可贴的读数」分两类）＋ **接线节 0** ＋ 固定小段 **2,900**（小结按「本节数 × 3 ＝ 24 条」估）＋ 围栏 **25 行 × 15 ＝ 375**（5 块 × 5 行，按 10.5 那条修正））；八节分节计划、五块围栏与三问都在 `tools/audit_coverage.py` 的 10.6 注释里 |
| 10.7 | 模拟面试与复盘清单 | 7,000 | — | ✅ 6,318 字（达成 90%；**全书最后一章**）；四组读数全由 `tools/mock_round.py` 从写在脚本里的剧本现算：**时间账 45 → 46.5 分钟**、**追问穿透 7/7｜5/6｜1/3**、**四维评分 13 分（及格线 14）**、**四类失败模式 4｜2｜1｜1**；十日冲刺表收 10.4–10.6 三张反向索引的并集 |
| 10.7（写作前校正） | 计划值 5,000 → **7,000**（本章没有外部题源也没有外部读数，但交付物与四组读数要落下来）；**这是全书第十六次在动笔之前把事前估写下来**，也是第 10 篇的第七次：**收尾章第一次把「小结要收全书」算进估算**（不再按「本节数 × 3」） | — | 事前估 **7,188**（前置段 **608**（导读 2 段 × 150 ＋ 14 条 × 22）＋ 散文 5 节 ＝ **3 节 × 900 ＋ 2 节 × 700 ＝ 4,100**＋ **接线节 0** ＋ 固定小段 **2,180**（小结估 18 条）＋ 围栏 **20 行 × 15 ＝ 300**（4 块 × 5 行））；五节分节计划、四块围栏与三问都在 `tools/audit_coverage.py` 的 10.7 注释里 |

> 题库**不整本搬运**。做法是：高频考点精讲 + 其余以「速查问答表」收敛，
> 并把分散考点在第 3 篇各章「面试视角」中提前消化。
> `10.3` 是扩展新增章：知识点问答答得再好，也替代不了「给你一个需求，你给方案」。

### 第 9 篇 · 进阶方向（预留，暂不启用）

按 [`GAPS.md`](GAPS.md) 方案 B，共 4 章（本地推理与量化、微调 LoRA/SFT/DPO、
多模态与语音、Computer Use Agent）。**编号预留给它，但当前不排入写作批次**——
先把 P0/P1 交付完，作品集阶段再按需启用。

---

## 四、体系缺口与扩展提案

对照“一个能拿到 AI 应用开发岗 Offer 的人应该会什么”逐项检查后，发现原定 7 篇 53 章
**少了一整块**：从「能做出 demo」到「能交付一个有 SLO 的产品」之间的工程化与交付环节。
逐项对照表与优先级清单见 [`GAPS.md`](GAPS.md)。

### 4.1 缺口集中在三处

| 优先级 | 缺口 | 现状 |
| --- | --- | --- |
| **P0** | 模型接入与多厂商 API | 完全没讲“怎么调模型” |
| **P0** | 部署与交付（容器化、CI/CD、运维） | 学完只会在本地跑 `uvicorn` |
| **P0** | AI 系统设计面试 | 题库只覆盖知识点，不覆盖方案设计 |
| P1 | 评测流水线、可观测性、安全、成本工程、前端流式交互、数据合规 | 只有理论或完全没有 |
| P2 | 本地推理、微调、多模态与语音、多租户计费、GraphRAG | 差异化加分项 |

### 4.2 已采纳：方案 A（新增 3 篇 14 章）

扩展篇插在「RAG 与生产级系统」之后、「求职冲刺」之前——先会做系统，
再学怎么交付出去，最后带着可演示的项目去面试。求职冲刺因编号后移为**第 10 篇**，
第 9 篇预留给「进阶方向」（方案 B 的内容），当前不启用。

| 新篇 | 章数 | 字数 | 内容 |
| --- | ---: | ---: | --- |
| 第 6 篇 · 模型接入与成本工程 | 4 | 35,000 | 模型选型、多厂商接入、网关与路由降级、成本与性能 |
| 第 7 篇 · AI 应用工程化 | 5 | 56,500 | 评测流水线、可观测性、Prompt 版本化与灰度、安全、合规 |
| 第 8 篇 · 交付与产品化 | 5 | 37,000 | 容器化部署、CI/CD 与发布、前端流式交互、多租户与计费、线上运维 |
| 第 10 篇新增 1 章 | 1 | 8,000 | AI 系统设计面试（10.3） |

> 扩展后 **10 篇 68 章 / 约 628,000 字**（扩展前 7 篇 53 章 / 358,000 字）——这是 2026-09-11 定案时的估计，
> 此后第 2、3 篇按 STYLE 8.7 重设了两轮，再往后第 4–8 篇按同一口径逐章重设，**现行计划总量为 855,000 字**（见各篇小节与本文件首）。
> 另有两种范围可选：[`GAPS.md`](GAPS.md) 第四节的方案 B（再加进阶篇 4 章，共 72 章）
> 与方案 C（只补 P0，共 63 章）。

### 4.3 工作量的实话

新增篇章的**素材覆盖度全部为 0**——素材里一句相关的话都没有，只能依据官方文档原创撰写。
按每章约 7,000 字计，方案 A 新增约 **102,000 字纯原创**（含系统设计章 8,000 字）。
这意味着工作量比「整理素材」大得多，因此把它们整体排进**批次四**，
放在第 0–5 篇之后（那几篇或多或少还有素材可用，实测在 0.14–0.83 之间）。

### 4.4 对既有篇章的连带调整

| 既有位置 | 调整 |
| --- | --- |
| 1.1 环境搭建 | 只讲本地环境，Docker 统一归 8.1，避免重复 |
| 1.14 测试 | 保留单元测试；流水线与 CD 归 8.2 |
| 3.8 安全、3.9 可观测性 | **第 3 篇讲原理（为什么），第 7 篇讲落地（怎么做）**，两处互相交叉引用 |
| 5.6 RAG 评估 | 5.6 讲指标与实验方法，7.1 讲怎么接进 CI |
| 5.7 生产级工程 | 收敛到「RAG 服务本身」，成本归 6.4、运维归 8.5 |
| 第 4 篇 框架 | 全篇按 **LangChain v1** 重写（素材是旧版写法） |
| 求职篇 | 章号变为第 10 篇，新增 10.3 系统设计章，其余顺延 |

---

## 五、写作批次与里程碑

先做还有素材可用的篇（第 0–2、5 篇），把原创量最大的第 3、4 篇放在后面——
这样你可以**用前面几批的实际成稿判断质量**，再来决定第 3、4 篇是原创补写还是压缩。
注意「有素材」不等于「轻松」：按节实测后，第 2 篇只有 0.36，实际写起来以原创为主
（2.5、2.6 均已如此）。

| 批次 | 范围 | 章数 | 字数 | 工作量特征 | 里程碑 |
| --- | --- | ---: | ---: | --- | --- |
| **批次一** | 第 0 篇 + 第 1 篇 | 18 | 196,500 | 素材充裕，改写为主 | 能独立写出并测试一个 FastAPI 后端服务 |
| **批次二** | 第 2 篇 + 第 5 篇 | 15 | 225,500 | 素材相对集中（0.36 / 0.47），但两篇实际都以原创为主 | 会用 AI 工具开发，且能构建并评估一个 RAG 系统 |
| **批次三** | 第 3 篇 + 第 4 篇 | 15 | 193,500 | **几乎全是原创**，最耗时 | 能讲清 Agent 原理，能**手写**一个能跑的 Agent，再用 LangChain/LangGraph 搭一遍 |
| **批次四** | 第 6 篇 + 第 7 篇 + 第 8 篇 | 14 | 154,500 | **零素材，纯原创**，须逐条回到官方文档 | 能把系统部署上线，并接上评测、监控与安全 |
| **批次五** | 第 10 篇 + 全书统稿 | 7 | 85,000 | 题库筛选 + 综述 | 简历与面试闭环；术语表、索引、通读修订 |
| **合计** | | **69** | **855,000** | | |

每章执行节奏（防丢失闭环，规范见 [`STYLE.md`](STYLE.md) 第八节）：

1. 按覆盖度报告定位该章素材段落 → 2. 通读素材，把知识点逐条写进
   [`LEDGER.md`](LEDGER.md)（状态先标 `⬜`） → 3. 按十段式写正文
→ 4. 跑通全部代码示例 → 5. 回填「面试视角」「延伸阅读」与台账状态（`✅`/`🔀`/`⏭`）
→ 6. 跑 `python tools/lint_book.py --only <章号>`，零错误才算定稿。

---

## 六、风险清单

| 风险 | 说明 | 应对 |
| --- | --- | --- |
| 🔴 **第 4 篇素材近乎为零** | LangChain + LangGraph 合计 4,680 汉字，无规范代码块 | 压缩到 5 章；以官方文档为准原创；或直接砍掉第 4 篇 |
| 🔴 **第 3 篇素材仅 5,700 汉字** | 10 章里没有一章算「素材够用」（最高 3.3 的 0.16），且题库答案不可信 | 原创＋权威资料；已按决策项 9 把项目增量摊进各章，不靠素材量解决 |
| 🔴 **素材在关键处已经过时** | LangChain 已发布 **v1**（旧命名空间移入 `langchain-classic`）；OpenAI **Assistants API 已于 2026-08-26 关停**；`Claude Code SDK` 已更名为 **Claude Agent SDK**；Claude 文档换域；Pydantic 文档换域；MCP 规范按日期版本化 | 正文一律回到官方文档核对；[`REFERENCES.md`](REFERENCES.md) 第二节列出全部变动，并用 `tools/check_refs.py` 校验链接 |
| 🟠 **体系少一整块（工程化与交付）** | 模型接入、成本、评测流水线、安全落地、部署与运维全部缺位 | 已采纳方案 A：新增第 6–8 篇与系统设计章 |
| 🟠 **新增 14 章零素材** | 第 6–8 篇素材覆盖度为 0，只能依官方文档原创（约 10.2 万字） | 排在批次四；每章先把 `REFERENCES.md` 的出处立好再动笔 |
| 🟠 **AI Agent 题库质量低** | 通用套话 + 专题漂移，不能当真题用 | 降级为考点索引，答案依权威资料重写 |
| 🟠 **后端题库是 Java 体系** | JVM/Spring/垃圾回收占比高，网络仅 0 次 | 10.5 整体换成 Python/AI 岗位视角，剔除 Java 专有内容 |
| ✅ **Flutter 题库含模板废答案** | 至少 20 题零实质内容 | 10.6 逐题筛选；不达标者弃用——**已量化：43 道共用同一句开场模板（逐字相同 40 道），另 6 道 OCR 占位、37 道题面含水印；三类共 86 道的答案一律弃用、题面照用** |
| 🟡 **1.10 Pydantic 素材仅 1,356 字** | 第 1 篇最薄 | 原创补 Pydantic v2 完整内容 |
| 🟡 **Vibe Coding 原档以截图为主** | 文字仅 34.7k 字符 | 只取结论，示例重写 |
| ⚠️ **版权** | 素材多为第三方整理，含"仅供个人学习"声明 | 仅个人学习使用；出版前须确认授权并实质性重写 |

---

## 七、待你决策的事项

1. **书名与主线项目**：是否采用《AI 应用开发就业实战：从语法地基到 Offer》？
   贯穿项目是否沿用「知舟」？
2. **第 3、4 篇怎么办？**（最关键的决策）
   - **A. 接受原创**（推荐）：按现计划 14 章原创撰写，我依据权威资料（论文与官方文档）
     写，但正文不再是"整理你给的资料"；
   - **B. 压缩**：第 3 篇 9→6 章、第 4 篇并成 3 章，总章数降到 48 章；
   - **C. 补充素材**：你提供 LangChain/LangGraph 官方文档、ReAct 等论文或更好的笔记，
     我重新审计后再定；
   - **D. 砍掉第 4 篇**：只讲原理（第 3 篇）与系统（第 5 篇），框架细节交给官方文档。
3. **第 2 篇位置**：放在第 2 篇（紧随地基，工具提升后续效率，**当前方案**），
   还是移到最后（先打牢基础再谈工具）？
4. **移动端内容**：Android + Flutter 两套题库（1850 道，质量中/偏低）是否纳入？
   纳入则第 10 篇 7 章；不纳入则减为 6 章，教材聚焦 AI 应用 / 后端方向。
5. **批次顺序**：是否按上表先做「批次一（0+1 篇）」，并在看到成稿后再推进？
6. ~~是否先写一章样章？~~ **已定：不单独写样章，直接从批次一（第 0、1 篇）开始**
   （2026-09-11）。第 0 篇先交付，可据此确认文风；若要调整，改 [`STYLE.md`](STYLE.md)
   后再回改已完成章节。
7. **`raw/` 是否纳入 git**：当前经 `.gitignore` 排除（295MB，含 129MB 单文件 PDF）。
   若要版本管理原始资料，需改用 Git LFS。
8. **附加产出**：是否需要 PDF/EPUB 导出、MkDocs 在线站点、术语中英对照表、全书索引？
   —— **全书索引已交付**（2026-09-21）：[`INDEX.md`](INDEX.md) 由 `tools/index_book.py`
   从术语表与正文生成（术语 → 章节、六棵树与门脚本 → 章节、69 章小节目录），
   `--check` 进提交门与 CI；见 [`STYLE.md`](STYLE.md) 8.12。
   —— **读者附录已交付**（2026-09-22）：[`APPENDIX.md`](APPENDIX.md) 由 `tools/appendix.py`
   从盘上生成——附录 A 六棵树速查（目录 / 依赖 / 每条离线入口 / 该看到什么话）、
   附录 B 门脚本速查（每支管什么、有哪些开关、跑在提交门的哪一关）、
   附录 C 总量与它们的复算入口；`--check` 进提交门（4h/5）与 CI；见 [`STYLE.md`](STYLE.md) 8.14。
   仍待定：PDF/EPUB、MkDocs 站点（仓库里没有任何导出配置），
   以及术语中英对照表（`GLOSSARY.md` 的英文列已覆盖大半，独立成表的必要性待定）。
   —— **仍未做的附录候选**：常见坑总表（968 条压在 69 个小节里）、练习难度总表
   （301 条已按三级标签归一，按难度配题现在可机械做）、素材溯源与授权表（出版前必经一关）。
9. ~~第 3 篇的项目怎么落？~~ **已定：采纳方案 B——摊进各章 ＋ 收口章**（2026-09-13）。
    背景：STYLE 第五节把「智能问答与 Agent 工具调用」归给第 3、4 篇，而第 3 篇写到 3.4 时
    「知舟」出现 **0 次**、3.1–3.9 全是概念章，读者读完 10 万字手里没有交付物，
    也与 10.2 的「项目讲述」对不上。四个选项与取舍：

    | 选项 | 做法 | 代价 |
    | --- | --- | --- |
    | A（未采纳） | 只加一章「3.10 项目实战」 | 概念章仍无可跑的项目代码，学生感觉依旧 |
    | **B（已采纳）** | 3.1–3.10 每章一节「知舟接线」（可独立验收的增量）＋ 3.10 组装 | 每章 ＋1,500–3,000 字；需先定契约，否则七段拼接不起来 |
    | C（未采纳） | 把知舟做成独立代码仓库，正文只讲片段 | 要把第 1 篇的项目代码真正写出来并长期维护 |
    | D（未采纳） | 不建项目，只改 STYLE 的映射 | 学生仍无作品 |

    选择 B 的理由：它同时满足「看着项目长大」（STYLE 第五节的原意）、
    「每章有可验证的交付物」（与第 1 篇 1.15 同规格）、以及「不拖延现有节奏」；
    代价是要先写一份契约。契约已定稿：[`PROJECT.md`](PROJECT.md)（七段增量 ＋ 前置两段 ＋ 组装段，
    含文件路径、端点、表结构、验收标准与八条硬约定）；
    且与第 4 篇的分界明确：**第 3 篇手写，第 4 篇用框架重写同一件事并对照**。
    连带变更：第 3 篇 9 → 10 章、全书 68 → 69 章、644,500 → 678,000 字（详见第五节）；
    3.1–3.4 已定稿的正文需回填接线小节，台账在 PROJECT.md 第六节，lint 已接检查。
10. ~~是否扩展体系？~~ **已定：采纳方案 A**（2026-09-11）。新增第 6–8 篇共 14 章，
   加第 10 篇「AI 系统设计」1 章（即现在的 10.3），全书规模另见决策项 9。
   另两种范围（B 完整 72 章、C 最小 63 章）见 [`GAPS.md`](GAPS.md) 第四节；
   日后想扩或缩，只需重排第 6 篇之后的章号。
11. **【新增】过时内容怎么处理？** 素材里的旧写法（LangChain 旧版链、Assistants API）
    建议不作为正文依据，但可以在「常见坑」里当反例讲——
    这反而是很好的教学点。是否保留这类“旧写法对照”？
12. **【待办】第 4、5 篇的素材权重之和超 1.0**（`LangChain从入门到实战.md` 1.30、
    `RAG技术从入门到深入.md` 1.05、`生产级RAG系统构建实战.md` 1.35）。与第 2 篇
    同类的重复计入，但第 4、5 篇还没开写，两章的节级边界需要先通读那三份文件才能量。
    影响：4.1–4.5 与 5.1–5.8 的素材量与判定偏高，**开工前必须先改**。
    `python tools/audit_coverage.py` 会在开头报出这份名单。

---

## 八、进度看板

| 篇 | 章数 | 计划字数 | 素材比值 | 已完成 | 状态 |
| --- | ---: | ---: | ---: | ---: | --- |
| 0 导论 | 3 | 12,000 | — | 3 | ✅ 已完成（批次一） |
| 1 编程地基 | 15 | 184,500 | 0.61 | 15 | ✅ 已完成（批次一） |
| 2 AI 时代的开发方式 | 6 | 115,500 | 0.36 | 6 | ✅ 已完成（批次二） |
| 3 大模型与 Agent 原理 | 10 | 155,500 | 0.04 | 10 | ✅ 已完成（批次三） |
| 4 AI 应用开发框架 | 5 | 38,000 | 0.12 | 5 | ✅ 已完成（批次三） |
| 5 RAG 与生产级系统 | 9 | 110,000 | 0.47 | 9 | ✅ 已完成（批次二） |
| 6 模型接入与成本工程 | 4 | 35,000 | 0.00 | 4 | ✅ 已完成（批次四） |
| 7 AI 应用工程化 | 5 | 56,500 | 0.00 | 5 | ✅ 已完成（批次四） |
| 8 交付与产品化 | 5 | 63,000 | 0.00 | 5 | ✅ 已完成（批次四） |
| 10 求职冲刺 | 7 | 85,000 | 题库驱动 | 7 | ✅ 已完成（批次五） |
| **合计** | **69** | **855,000** | | **69** | ✅ 全部完成 |

前置工作进度：

| 事项 | 状态 |
| --- | --- |
| 资料归档到 `raw/`（295MB，未删改） | ✅ 已完成 |
| 文本素材库生成（`sources/`，216 个文件） | ✅ 已完成 |
| git 初始化与两次提交 | ✅ 已完成 |
| 写作体例与文风规范（[`STYLE.md`](STYLE.md)） | ✅ 已完成 |
| 素材覆盖度审计（[`COVERAGE.md`](COVERAGE.md)） | ✅ 已完成 |
| 官方文档出处与链接校验（[`REFERENCES.md`](REFERENCES.md)，483 个唯一链接全部有效） | ✅ 已完成 |
| 体系缺口审计（[`GAPS.md`](GAPS.md)） | ✅ 已完成 |
| 章节规划与篇幅校准（本文档） | ✅ 已定稿（采纳方案 A） |
| 第 0 篇正文（0.1–0.3） | ✅ 已完成 |
