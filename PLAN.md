# 编写计划（PLAN.md）

> **本版为素材审计校准 + 体系缺口审计后的定稿**：已采纳扩展**方案 A**（见第四节），
> 全书为 **10 篇 68 章 / 约 636,000 字**。第 0–1 篇正文**已完成**（18 章，实测 180,090 字），
> 第 2 篇正文**已完成**（6 章，实测 111,650 字），第 3 篇正文**已完成**（2 章，实测 24,758 字）
> ——即 3.1、3.2 已定稿（3.3–3.9 待写）；全书进度 **26 / 68 章**，实测 316,498 字。
> 素材覆盖度数据来自 `python tools/audit_coverage.py`，报表见 [`COVERAGE.md`](COVERAGE.md)；
> 体系缺口分析见 [`GAPS.md`](GAPS.md)；官方文档出处见 [`REFERENCES.md`](REFERENCES.md)。
> 状态标记：⬜ 未开始 ｜ 🟡 进行中 ｜ ✅ 已完成

---

## 一、总体设计

### 1.1 组织原则

1. **从浅到深**：地基（会写代码）→ 工具（会用 AI 写代码）→ 原理（懂为什么）→ 框架（会搭）→ 系统（能做）→ 求职（能讲）。
2. **一条主线**：全书围绕同一个项目「知舟」成长（见 [`STYLE.md`](STYLE.md) 第五节），不换 demo。
3. **知识点闭环**：每个技术点都回答三件事——*是什么 → 怎么用 → 面试怎么问*。
4. **题库不单独堆放**：题库按考点打散，嵌回各章「面试视角」，第 10 篇只做综合冲刺。
5. **素材是原料不是成品**：所有章节重写，保留溯源，不整段搬运。

### 1.2 规模

| 项目 | 数值 |
| --- | --- |
| 篇数 / 章数 | **10 篇 / 68 章**（含扩展篇 14 章；第 9 篇留作「进阶方向」，暂不启用） |
| 计划总字数 | **约 636,000 字** |
| 素材总量 | **1,245,172 汉字**（散文 249,610 ＋ 题库 995,562） |
| 需原创补写的章 | **58 / 68 章**（全书口径，含方案 A 扩展篇与题库驱动章），其中 28 章散文素材为零 |

> 与初版计划的差异：素材审计把第 4 篇由 8 章压到 5 章（56 → 53 章）；
> 体系缺口审计采纳方案 A，新增第 6–8 篇与「AI 系统设计」1 章（53 → 68 章）。
> 扩展篇编号后，**求职冲刺由第 6 篇改为第 10 篇**，章号相应变为 10.1–10.7。

### 1.3 难度曲线

```
浅 ────────────────────────────────────────────────────────────► 深
第1篇        第2篇        第3篇        第4篇        第5篇
Python     AI 工具链    原理与概念   框架与编排   系统工程
FastAPI    （生产力）    （为什么）   （怎么做）   （做多大）
素材 0.61   素材 0.36    素材 0.09    素材 0.14    素材 0.83

第6篇        第7篇        第8篇        第10篇
模型接入     应用工程化   交付与产品化  求职输出
（怎么调）    （怎么控）   （怎么上线）  （怎么讲）
新增·纯原创  新增·纯原创  新增·纯原创   题库驱动
```

---

## 二、素材审计结论（本版新增）

审计按**汉字量**而非页数计量，并区分两类素材。这一步改变了原计划的多处判断。

### 2.1 三个关键发现

**发现一：素材总量很大，但分布极度失衡。**

| 篇 | 章数 | 计划字数 | 散文素材池 | 题库池 | 散文比值 | 判定 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 0 导论 | 3 | 12,000 | 0 | 0 | 0.00 | 无素材 |
| 1 编程地基 | 15 | 184,500 | 112,192 | 0 | **0.61** | 偏薄 |
| 2 AI 时代的开发方式 | 6 | 115,500 | 41,855 | 0 | **0.36** | 严重不足 |
| 3 大模型与 Agent 原理 | 9 | 67,000 | 5,700 | 282,867 | **0.09** | 严重不足 |
| 4 AI 应用开发框架 | 5 | 33,000 | 4,529 | 0 | **0.14** | 严重不足 |
| 5 RAG 与生产级系统 | 9 | 63,000 | 52,172 | 0 | **0.83** | 够用 |
| 6 模型接入与成本工程（扩展） | 4 | 28,000 | 0 | 0 | **0.00** | 无素材 |
| 7 AI 应用工程化（扩展） | 5 | 33,000 | 0 | 0 | **0.00** | 无素材 |
| 8 交付与产品化（扩展） | 5 | 33,000 | 0 | 0 | **0.00** | 无素材 |
| 10 求职冲刺 | 7 | 67,000 | 0 | 201,627 | — | 题库驱动 |
| **合计** | **68** | **636,000** | | | | |

> 散文比值 = 散文素材汉字 ÷ 计划汉字。教材正文不是素材的压缩，一章 6,000 字
> 需要 1.2 倍以上素材才算充裕。分篇用**素材池**（各章计权相加，单份素材在一篇里最多算 1.0），因为同一段素材
> 会被多章共用，各章直接相加必然虚高。

**发现二：页数会骗人，关键词检索也会骗人。**

| 素材 | 页数 | 汉字 | 真相 |
| --- | ---: | ---: | --- |
| LangChain 从入门到实战 | 23 | **2,279** | 页数看着不少，实际只有 2 千多字，且全文**没有一个规范的代码块** |
| LangGraph 智能体实战 | 28 | **2,401** | 同上 |
| 大模型 AI Agent 从 0-1 笔记 | 113 | **12,774** | 113 页只有 1.3 万字，是稀疏的演示稿 |
| Python 核心语法手册 | 238 | **54,421** | 其中 **30,078 字（55%）藏在无编号标题的"进阶块"里**，覆盖异常/日志/装饰器/OOP 进阶/网络编程/并发编程；按关键词检索会误判为"没素材" |
| 生产级 RAG 系统构建实战 | 229 | **32,068** | 结构极佳：Milvus、RAGAS、流式通信、可观测性、企业级项目，几乎逐条对上第 5 篇 |

**发现三：题库质量分层严重，不能一律当作"真题答案"。**

| 题库 | 汉字 | 真实题量 | 质量 |
| --- | ---: | ---: | --- |
| AI-Agent 面试题合集 | 352,616 | 1038 道 | **低**：结构完整（考点/解答过程/答案），但答案是通用套话，存在专题漂移——"规划与决策"专题里混入强化学习的状态空间/动作空间题，"检索增强"专题抽到的是灰度发布流量分配 |
| Android 面试题合集 | 344,074 | 1136 道 | **中**：结构完整，抽样内容有实质（如 Intent 五要素讲得清楚），但仍需逐题筛选 |
| Flutter-Dart 面试题合集 | 214,864 | 714 道 | **偏低**：至少 20 题答案是模板占位（"先明确题目中的核心对象和使用场景……"），零实质内容 |
| 后端高频面试题大集合 | 84,008 | 165 道 | **高**：含考点分析与难度标注，但**体系是 Java**（JVM 107 次、Spring 57 次、垃圾回收 83 次，而网络 0 次、Python 2 次），须换视角 |

> 这条发现直接推翻了两处原计划：10.4 原本要"从 1038 道中精讲 120 道真题"——
> 这些题不是真题，答案也不可信，只能当**考点索引**用；10.5 原本假定是通用后端题库，
> 实际是 Java 体系，须整体换成 Python / AI 岗位视角重写。

### 2.2 逐章覆盖度

完整 68 行逐章报表见 [`COVERAGE.md`](COVERAGE.md) 第三节。判定为**偏薄及以下**的章节汇总：

| 判定 | 章节 | 章数 | 计划字数 |
| --- | --- | ---: | ---: |
| 严重不足 | 1.10、1.11、1.12、1.13、1.14、1.15、2.1、2.4、2.5、2.6、3.2、3.3、3.4、3.5、3.6、3.7、4.1、4.2、4.3、4.4、4.5、5.3、5.8 | 23 | 281,000 |
| 偏薄 | 1.1、1.5、1.6、2.2、2.3、5.1、5.7 | 7 | 78,000 |
| 无素材 | 0.1、0.2、0.3、5.9 | 4 | 16,000 |
| 题库驱动（散文为零） | 3.1、3.8、3.9 | 3 | 23,000 |
| **合计** | | **37** | **398,000** |

**原定第 1–5 篇中素材为零的 8 章**：`0.1` `0.2` `0.3`（导论，本就是综述性写作）、
`3.1` 大模型基础（Transformer/Token，素材零覆盖）、`3.7` `3.8` `3.9`（多 Agent／安全／
可观测性，只有不可信的题库）、`5.9` 项目复盘。

采纳方案 A 后，零素材章增至 **25 章**：再加上第 6–8 篇的 14 章与第 10 篇的 7 章
（求职篇属题库驱动，正文同样须重写）。全书口径的完整表见 [`COVERAGE.md`](COVERAGE.md) 第三节。

> **防丢检查（本轮新增）**：`audit_coverage.py` 现在会核对「每个素材单元是否都被分配」——
> 本轮就是靠它发现**手册第 12–17 章（if/for/while 控制流）没有被任何章用到**，
> 已补进 1.2 的素材池（该类比值从 1.39 升到 2.20）。

> `3.7`–`3.9` 虽然题库里有 300 多道同名题目，但审计抽样确认答案是通用套话
> （详见下文发现三），因此实质上仍需原创撰写，不能计入“有素材”。

### 2.3 由审计得出的三处计划修订

1. **第 4 篇由 8 章压到 5 章**（55,000 → 33,000 字）。LangChain + LangGraph 合计只有
   4,680 个汉字且无结构化代码，写 8 章等于 100% 原创；压到 5 章并把细节交给官方文档，
   反而是更诚实的做法。
2. **第 5 篇综合项目换主干**。原计划用《实战智能出行 Agent 助手》（仅 1,944 汉字）撑
   10,000 字的实战章，不可行；改用《生产级 RAG》自带的「企业级多格式知识库问答系统」
   （素材 10,169 汉字，含项目结构、评估、运维），智能出行项目降为补充案例。
3. **第 3 篇明确标注"以原创为主"**。9 章里 7 章判定严重不足或无素材，AI Agent 题库
   降级为考点索引，答案须依权威资料重写。
4. **采纳扩展方案 A**（第 6–8 篇 + 系统设计章）。工程化与交付这一整块在素材中
   **覆盖度为 0**，只能依官方文档原创；它们依次排在批次四，不与素材改写混在一起。

---

## 三、分篇章节规划

每章列出**计划字数**与**散文比值**（素材充裕度）。

> **字数口径（重要）**：表中的「计划」是**有效字数**，第 1 篇及以后各篇的
> 「状态」列一律填**有效字数**（口径由 `lint_book.py` 统一计算）。
> 技术章节的代码本身就是内容，只数汉字会把「代码密集、讲得清楚」的章误判成偷工减料：
> 1.4 的汉字只有 2,735，但它还有 205 行有效代码，换算后是 5,810。
> 折算公式：
>
> ```text
> 有效字数 = 汉字数 + 15 × 有效代码行数     # 一行有效代码 ≈ 15 个汉字的信息量
> ```
>
> 为什么不直接放宽容差：放宽容差会把**真问题**（某章确实没写够）一起放过；
> 折算成同一口径再比，才能既不冤枉代码密集的章、也不放过写得单薄的章。
> 偏差容忍 ±40%，由 `python tools/lint_book.py` 自动校验。
>
> 🔧 **本节口径修正（2026-09-13）**：上式里的「有效代码行数」原本把整块 `splitlines`
> 全数计入，于是开围栏（` ```python `）与闭围栏也各占去 15 个汉字的位置——
> 与上面那句「有内容、**不含空行与围栏**」自相矛盾。
> **全书 671 个围栏、虚增 20,130 有效字**（实测 311,870 → **291,740**）。
> 已修 `lint_book.py`，并重算本表每章的「✅ 字数」「达成%」与全部汇总；
> 换算关系很简单：**每章减去 30 × 代码块数**（2.6 减 1,230、1.11 减 2,250）。
>
> 修正时踩到的三处细节（都已进 `lint_book.py` 的注释）：围栏并不总是裸 ` ``` `——
> 引用块里是 `> ``` `（1.14）、列表项里是缩进后的 `   ``` `（2.5）；少了这三态判定会
> 静默少减一行（第一轮就漏了 2.5，靠「每章差值应恰为 30 × 块数」这一比对才揪出来）。
> **本文件里 2026-09-13 之前的叙述性快照**（各章「实写 X 字、达成 Y%」）记的是旧口径，
> 不逐条回改——它们记录的是当时的实测，与新表不一致时以新表为准。
> 教训与 8.5 同族：**口径写在一行注释里、实现却按另一种算法跑，两边从来没对过**；
> 而它同时是每章状态、达成% 与全书总分的单一输入，一旦漂了就全漂。
>
> **已完稿 8 章的实测规律**（用于后续章节估算，而不是事后调数）：
>
> | 章的形态 | 实测达成 | 实例 |
> | --- | --- | --- |
> | 单一主题 | 80%–105% | 1.2 语法 96%、1.3 数据结构 80%、1.4 装饰器 97% |
> | 概念密集 / 一章装三块以上内容 | 140%–157% | 1.5 面向对象 157%、1.6 异常+文件+模块 152%、1.7 并发 140% |
>
> 处理方式：**按实测校准计划值（改计划，不删已覆盖的内容），并在修订说明里写清原因**；
> 校准后的计划值必须同时改 `tools/audit_coverage.py` 与本文档，再用 `--out`/`--check` 对齐。

### 第 0 篇 · 导论（3 章 / 12,000 字，全部原创）

| 章 | 标题 | 计划 | 素材 | 状态 |
| --- | --- | ---: | --- | --- |
| 0.1 | 岗位地图：AI 应用开发岗到底要什么 | 4,000 | 无素材 | ✅ 3,945 字（达成 99%） |
| 0.2 | 能力模型与 180 天学习路线 | 5,000 | 无素材 | ✅ 3,597 字（达成 72%） |
| 0.3 | 本教材使用说明与素材索引 | 3,000 | 无素材 | ✅ 2,933 字（达成 98%） |

### 第 1 篇 · 编程地基：Python 与 FastAPI 后端工程（15 章 / 184,500 字，素材 0.61）

| 章 | 标题 | 计划 | 散文素材 | 比值 | 判定 | 状态 |
| --- | --- | ---: | ---: | ---: | --- | --- |
| 1.1 | 环境搭建：Python、依赖管理、IDE 与 Git | 4,000 | 2,115 | 0.53 | 偏薄 | ✅ 3,764 字（达成 94%） |
| 1.2 | Python 语法速通：变量、类型、控制流 | 6,000 | 13,177 | 2.20 | 充裕 | ✅ 5,120 字（达成 85%） |
| 1.3 | 常用数据结构与推导式 | 6,000 | 7,484 | 1.25 | 充裕 | ✅ 4,393 字（达成 73%） |
| 1.4 | 函数、装饰器与高阶用法 | 6,000 | 7,605 | 1.27 | 充裕 | ✅ 5,240 字（达成 87%） |
| 1.5 | 面向对象：类、继承、魔术方法 | 9,000 | 4,606 | 0.51 | 偏薄 | ✅ 8,729 字（达成 97%） |
| 1.6 | 异常处理、文件操作与模块化 | 7,500 | 3,599 | 0.48 | 偏薄 | ✅ 6,979 字（达成 93%） |
| 1.7 | 并发与异步：线程、进程与 asyncio | 8,000 | 6,688 | 0.84 | 够用 | ✅ 7,846 字（达成 98%） |
| 1.8 | Web 与 HTTP 基础（面试必考） | 8,000 | 8,827 | 1.10 | 够用 | ✅ 5,815 字（达成 73%） |
| 1.9 | FastAPI 入门：路由、请求与响应 | 15,000 | 20,619 | 1.37 | 充裕 | ✅ 14,275 字（达成 95%） |
| 1.10 | 数据校验：Pydantic 模型 | 11,500 | 1,356 | 0.12 | **严重不足** | ✅ 10,844 字（达成 94%） |
| 1.11 | 依赖注入与中间件 | 20,500 | 5,436 | 0.27 | **严重不足** | ✅ 18,831 字（达成 92%） |
| 1.12 | 数据库与缓存：SQLAlchemy + Redis | 17,500 | 7,407 | 0.42 | **严重不足** | ✅ 16,153 字（达成 92%） |
| 1.13 | 认证与授权：JWT、OAuth2 与权限设计 | 20,500 | 6,876 | 0.34 | **严重不足** | ✅ 19,428 字（达成 95%） |
| 1.14 | 三层架构、日志、异常与单元测试 | 24,000 | 10,535 | 0.44 | **严重不足** | ✅ 23,060 字（达成 96%） |
| 1.15 | 项目实战：知舟博客 API 从零到一 | 21,000 | 7,394 | 0.35 | **严重不足** | ✅ 19,138 字（达成 91%） |

> 本轮修正（素材归属）：`1.9` 原本整目录引用 `fa 02-13`，但 `02` 目录的 8 篇横跨三章
> （应用配置属 1.9，生命周期与生产实践属 1.14，中间件与依赖注入属 1.11），
> 且 `fa 04-05` 被 1.8 与 1.9 完整重复计入。审计已支持子文件级引用
> （写法 `02/01-05`）与共用素材按权重分记（1.8 记 0.45 / 1.9 记 1.0）。
> 修正后：`1.9` 26,625 → 25,346，`1.8` 10,702 → 8,827，`1.11` 6,752 → 7,245，
> `1.14` 7,777 → 8,563。判定档位变化：`1.8` 充裕 → 够用、第 1 篇 充裕 → 够用。
>
> 本轮修正（`1.11` 再次归位）：`1.11` 原本整目录引用 `fa 20`（十篇中间件），
> 等于把认证 / 日志 / 连接池也塞进一章——这正是 1.9 踩过的坑。已归位：
> `20/06`（JWT 认证中间件）→ 1.13、`20/08`（日志中间件）→ 1.14、
> `20/09`（连接池中间件）→ 1.12；`1.11` 只留 `20/01-05+07+10`。
> 归位后 1.11 素材 7,245 → 5,436（判定回落到「严重不足」，这是实情：
> 依赖注入与中间件必须按官方文档原创），1.12/1.13/1.14 分别升到
> 7,407 / 6,876 / 10,535，第 1 篇素材池总量不变（素材只是换了归属）。
>
> 本轮修正（`1.13` 的计划值本身就是错的）：原计划 6,000 是按「素材 6,876 汉字 × 0.9」
> 倒推出来的，这正是 [`STYLE.md`](STYLE.md) 8.7 明确禁止的估法——**按素材量估篇幅**。
> 1.10 已经演示过一次：素材为零不说明写得少。认证章的完整面貌（口令存储 → JWT 结构 →
> 签发与校验 → OAuth2 角色与授权方式 → 依赖注入认证 → scope 与 RBAC → 刷新与撤销 →
> Cookie/Session → 中间件 vs 依赖 → 综合示例）本身就是十几节的量，
> 且代码密集（认证的关键都在代码里）。已按 8.7 的公式（小节数 × 800–1,100 + 15 × 代码行）
> **写作前**改为 17,000；第 1 篇 147,000 → 158,000，全书 513,000 → 524,000。
>
> 本轮修正（`1.13` 事前估算再次偏低）：按 8.7 公式估 17,000，实写 20,739
> （汉字 11,953 + 代码 580 行），达成 121%。**错因与 1.11 同一条：代码行估少。**
> 这是第三次同因偏差（1.11 为 732 行、1.12 为 624 行），已把「带综合示例的章，
> 代码实际落在 550–750 行」这个先验区间写进 [`STYLE.md`](STYLE.md) 8.7。
> 按实测校准为 20,500；第 1 篇 158,000 → 161,500，全书 524,000 → 527,500。
>
> 本轮修正（`1.14` 同样是按素材量定的篇幅）：原始计划 10,000 是「素材 10,535 × 0.95」
> 倒推的，所以显示为「够用」。但这一章装了**六个主题**（统一响应格式、异常处理、日志、
> 三层架构、单元测试、生命周期与生产实践），其中后两个还是从 1.9 与 1.11 归位过来的。
> 已按 8.7 公式**事前**估 21,500（16 小节 × 800 + 代码按 600 行起估，用了本轮新增的
> 「带综合示例的章代码落 550–750 行」先验），实写 24,132（112%）——**第四次低估，
> 偏差仍在代码那头**：实得 813 行 × 15 = 12,195，高于预估的 9,000，
> 且实跑审阅后正文又长了一截（补了中间件异常边界的四组对照、`AsyncMock`、
> 脱敏深度上限等实测结论）。已按实测校准为 24,000，不留容差。
> 判定降为**严重不足**（10,535 / 24,000 = 0.44）——这是实情：
> 日志工程化、测试组织与统一响应格式的取舍均需按官方文档原创。
> 第 1 篇 161,500 → 175,500，全书 527,500 → 541,500。
>
> 另一次修正（`1.15` 的素材归属）：原计划 12,000 是按「素材 10,991 × 0.92」倒推的，
> 但 10,991 里有 **3,597 汉字的前端讲解**——第 1 篇是后端工程，这部分不属本篇。
> 修正后后端可用散文只有 **7,394**，比值降到 **0.35（严重不足）**。若不动手会把
> 前端素材算成后端的「充裕」底子，那一章的写作策略（以素材改写为主 vs 以官方文档原创为主）
> 就会选错。同时按 8.7 公式事前估 21,000（14 小节 × 800 + 表格/示例密集按 675 行代码）——
> 表单、上传、下载与断点续传、静态挂载、评论树这些技术点**只在这一章讲**，
> 篇幅本来就压不下去。第 1 篇 175,500 → 184,500，全书 541,500 → 550,500。
> `1.9` 的经历：写作前按「10 小节 × 约 900」估 12,000，实写 22,341（达成 186%）——
> 原因是把「统一响应格式」与「表单 / 文件上传」也塞进了同一章。处理不是放宽容差，
> 而是**把两摇内容归位**（fa 10 → 1.14，fa 11-12 → 1.15，Cookie 写入 → 1.13，
> 计 1.14 由 7,000 提到 10,000、1.15 由 10,000 提到 12,000），再删掉重复的平行示例，
> 实写 15,665（含实跑后的四处素材纠错），按实测校准为 15,000。
> 教训：**素材充裕不等于该堆在一章里**。

> 本轮校准（`1.11`）：估 13,000、实写 20,589（达成 158%）。原因与 1.9、1.10 都不同：
> **漏算了代码行项**。本章 732 行代码 × 15 = 10,980，占实测的一半；
> 而 `STYLE.md` 8.7 的公式本就含这一项，是估算时只数了小节、没看代码密度。
> 已校准为 20,500。教训：**代码密集的章（中间件、ORM、模板）必须先把代码行估出来**，
> 否则会系统性地把这类章判成「严重超标」。

> 本轮校准（`1.10`）：按素材量估的 6,000 与实际相差近一倍（实写 11,861）。
> 这不是内容超载——十节里没有一节是多余的，是**估法本身错了**：素材为零只说明
> 「要自己写」，不说明「写得少」。已按实测校准为 11,500，教训写进 [`STYLE.md`](STYLE.md) 8.7。

> 本轮修正：`1.2` 的素材池原本漏了手册第 12–17 章（if/for/while），补上后从 8,343 升到 13,177。

> - 本章投入最重（占全书 26%），因为后面所有篇章的代码质量都取决于这里。
> - **1.10 是全篇素材最薄的一章**（素材仅 1,356 字），但薄的是素材不是章：
>   它按 Pydantic v2 的完整面貌写成十节（数据契约 → 模型 → Field → 类型 → 配置 →
>   校验器 → 嵌套继承联合 → 别名序列化 → FastAPI 集成 → 综合示例），实写 11,861 字。
> - `1.1` 的 venv、依赖锁、Git 工作流在素材中零覆盖，须原创。
> - 意外的收获：素材手册的"进阶块"含**网络编程 8,580 字**（TCP/IP 五层协议）与
>   **并发编程 5,693 字**，让 `1.8` 与 `1.7` 从"缺素材"变成"充裕/够用"。
> - 注意 `1.11` 的判定一路回落：初判"充裕" → "够用" → 归位后"严重不足"。
>   两次回落是两个不同的原因：先是把每个讲义文件的溯源头部也算成了素材，
>   后是把不属于它的三篇（认证 / 日志 / 连接池）还给了对应的章。
>   它必须靠官方文档原创补齐，这与 `1.10` 同属"素材薄、章不薄"的一类。

> 本轮修正（`2.1` 同样按素材量定了篇幅）：原计划 5,000 是「素材 6,180 × 1.24」的
> 正向读法——素材充裕就该少写。但 6,180 只覆盖本章**三条主线里的一条**：
> Vibe Coding → Harness 的思维模型（外加五阶段成熟度）。另外两条——**能力边界**与
> **风险**——素材零覆盖，必须回到 OWASP GenAI LLM Top 10、METR 的对照实验、
> DORA 2025 与两篇安全论文原创。已按 8.7 公式事前估 8,000
> （9 小节 × 约 600 + 固定段约 2,000 + 实跑演示约 80 行代码），
> 判定由「充裕」改为**偏薄**（6,180 / 8,000 = 0.77）——这是实情，不是降级。
> 第 2 篇 38,000 → 41,000，全书 550,500 → 553,500；需原创补写的章 49 → 50。

> 本章事后校准（`2.1`）：事前估 8,000，实写 11,159（达成 139%）。原因不是内容超载，
> 而是**估法只数了小节、没估表格**：本章的主体是纯文字与技术结论，而 8.7 的公式
> 是按「小节约几段散文」推的，没给对比表、风险清单与出处表留位置
> ——而它们恰恰是本章最有价值的部分。已按实测校准为 **11,000**；
> 第 2 篇 41,000 → 44,000，全书 553,500 → 556,500；
> 第 2 篇散文比值 1.26 → 1.18（充盈 → 够用）。教训写进 [`STYLE.md`](STYLE.md) 8.7。

> **本轮修正（第 2 篇素材重复计入）：** 三份手册（Claude Code / Codex / Harness）
> 每份都被多章按「整份文件的百分比」记了一次，权重之和最高到 2.0：
>
> | 文件 | 原记 | 合计 | 改为 |
> | --- | --- | ---: | --- |
> | `Claude-Code从入门到实战.md` | 2.2（0.9）+ 2.5（0.5） | 1.40 | 2.2（0.84）+ 2.5（0.11）+ 2.6（0.05） |
> | `Codex从入门到实战.md` | 2.3（0.9）+ 2.5（0.4） | 1.30 | 2.3（0.81）+ 2.5（0.13）+ 2.6（0.05） |
> | `AI-Harness入门与实践.md` | 2.1（0.7）+ 2.5（0.5）+ 2.6（0.8） | 2.00 | 2.1（0.7）+ 2.5（0.15）+ 2.6（0.15） |
>
> 改为按**各章实际用到的节**实测拆分（例如 2.5 用两份手册里的
> 规则文件 / MCP / Skills / Hooks 节，而不是「切一半」）。后果不小：
> **`2.5` 由充裕（2.11）落到偏薄（0.57）、`2.6` 由够用（1.18）落到严重不足（0.45）**
> ——它们当初的「素材充裕」是重复计数造成的错觉。两章的篇幅**不减**
> （素材薄≠章该薄，参见 1.10 的教训），但原创量要重新排。
> 审计已新增这道检查（权重之和 > 1.0 就报）。
> 第 4、5 篇的三份素材（`LangChain` 1.30、`RAG技术` 1.05、`生产级RAG` 1.35）
> 同样超额，需逐份量出节级拆分后再改，已列入下方待定项。

> 本章事前校准（`2.2`）：原定 7,000 是「素材量 × 1.24」倒推的，而本章的代码块
> 不是算法而是**命令、工单模板与配置**，代码行项占比很高（8.7 公式里的
> `15 × 代码行` 在这一章是主导项）。按 11 节 × 约 800 + 固定段 2,200 + 约 150 行 × 15
> 事前估 **11,000**。第 2 篇 44,000 → 48,000，全书 556,500 → 560,500；
> `2.2` 判定由充裕（1.77）回落为够用（1.13）。

> 本章事前重估与事后校准（`2.3`）：原定 6,000 是「素材 10,325 汉字 × 比例」推的，方向错了
> （素材量不决定篇幅）。按 8.7 式子事前改为 **13,000**，实写 **16,856（达成 130%）**，
> 按实测校准为 **16,500**。归因：不是「装了两章」（归位已完成：闭环与工单归 2.2、
> 规则文件归 2.5、进阶能力归 2.5/2.6），而是两项低估——
> ① **固定段的规模**：决策表、工具对照表、五步综合示例各占一节，不是附录；
> ② **实跑带出的新内容**：`git apply` 与 `--3way` 的原子性差别、
> 「索引也被改写」、以及两种恢复来源的差别，这一整块照文档写不出来。
> 第 2 篇 58,000 → 68,500，全书 570,500 → 581,000；
> `2.3` 判定：充裕 → **偏薄**（0.62）。教训写进 [`STYLE.md`](STYLE.md) 8.7。

> 本章事前重估（`2.4`）：6,000 → 14,000，同时把素材权重从 0.8 改为 **0.67**。
> 原值同样是「素材量 × 比例」倒推的；而这份素材是**视频课件大纲**，当时的构成记录是
> 课程旁白 1,799 汉字（「简介：讲解…」「掌握 X」，不可作散文素材）、
> 文末团队规范 4,076、其余操作步骤与概念 9,694。按实际用到的部分量出 0.67 后，
> 散文素材 10,431，比值 2.07 → **0.75（偏薄）**。
> ⚠️ **这三行数字里的 4,076 已被证伪**（实测 739），0.67 / 10,431 / 0.75 随之作废——
> 详见本节的第三次校准。
> 第 2 篇 68,500 → 76,500，全书 581,000 → 589,000；
> 需原创补写的章 54 → 55（该行原记 52，已按实测校正；并新加一道对账：
> 这一行现在也由 `tools/audit_coverage.py --check` 按 `needs_original()` 口径核，
> 漂了会拦提交——此前它无人核对，实测写着 52 而实际已是 54）。

> 本章事后校准（`2.4`）：事前估 **14,000**，实写 **15,301（达成 109%）**，按实测校准为 **15,000**。
> 归因不是「写多了」，而是两项没算进去：
> ① **可执行清单的输入夹具也是正文**——不贴出跑的那三份文件，下面的输出就无法核对；
> ② **一手报告要给出处**：把 Veracode / CodeRabbit / GitClear 从「Wikipedia 转述」换成自家报告，
> 多出的是一条原则，不是一段文字。
> 第 2 篇 76,500 → 77,500，全书 589,000 → 590,000；判定与章数不变。

> **回补与二次校准（`2.2`、`2.3`，2026-09-12）**：读者指出这两章与 PDF 素材不符。
> 逐节对齐后确认：骨架对得上，但**「能照着做」的部分被压掉了**——
> IDE 五个动作的提示词、JetBrains 与桌面端实操、截图五轮的视觉分析提示词、
> §19 的七组提示词库、§20 的三段检查清单（2.2）；以及素材 §7 的编辑器三种模式与五个动作（2.3，
> 它在台账里**连一行都没有**）。已全部回补：2.2 新增 2.2.13「可直接复制的模板与检查清单」
> 与安装登录小节，2.3.4 新增编辑器小节。
> 篇幅按实测二次校准：`2.2` 21,000 → **26,000**（实写 26,091），`2.3` 16,500 → **17,500**（实写 17,657）。
> 第 2 篇 77,500 → 83,500，全书 590,000 → 596,000；素材比值 0.67 → 0.62（分母变大）。
> 一条教训已写进 [`STYLE.md`](STYLE.md) 8.5：**素材里的可复制段落（提示词、模板、清单）是正文资产，
> 压成一句概括就是丢内容**；台账的 `✅` 必须可核对，写「散落在各节」等于没写。

> **反向核对与三次校准（`2.4`，2026-09-12）**：按 `STYLE.md` 8.5 的新规矩，拿素材的 9 章 25 节反向扫一遍台账，
> 又发现 8 节没有对应行（合计约 4,457 汉字），已把其中五节补进正文
> （2.4.2 预设命令、2.4.1 插件形态、2.4.8 联调分工、2.4.10 交付前的仓库文档）。
> 同时**修掉一个从没被复算过的数字**：旧记「文末团队规范 4,076 汉字」，实测只有 **739**——
> 它在源文件里对不上任何一段连续区间（最接近的 4,123 是把第八、九章一起算了进去），
> 而它曾是权重计算的实际输入（0.4 × 4,076 ≈ 1,630）。改成逐节实测后：
> 素材权重 **0.67 → 0.40**、散文素材 10,431 → **6,195**、比值 0.75 → **0.36（严重不足）**。
> 判定变差不是坏消息，而是承认这一章的内容主要来自官方文档；
> 篇幅 15,000 → **17,000**（实写 17,646）。
> 第 2 篇 83,500 → 85,500，全书 596,000 → 598,000；素材比值 0.62 → 0.61。

> **`2.5` 篇幅事前估（2026-09-13）**：原计划 8,000 是按素材量给的，而这一章的正文主要来自官方文档
> （素材比值 0.57 只说明「素材少」，不说明「该写得少」）。按 [`STYLE.md`](STYLE.md) 8.7 先把模板
> **逐个**数出来：CLAUDE.md / AGENTS.md / SKILL.md / hooks `settings.json` / Codex `.rules` /
> `.mcp.json` 加综合示例，合计约 **340 行**（×15 ≈ 5,100），散文按 12 小节 × 约 850 估。
> 篇幅 8,000 → **15,000**，第 2 篇 85,500 → 92,500，全书 598,000 → **605,000**。
> 分母变大，两个数字跟着变：第 2 篇素材比值 **0.61 → 0.56**，
> 而 2.5 自己的判定由「偏薄」落到**严重不足**（4,594 / 15,000 = 0.31）。
> 这不是坏消息：判定说的是「素材够不够」，不是「这一章该不该写」——
> 规则文件、Skills、hooks 与 .rules 的写法本来就只能在官方文档里找。
> 这是 2.2 之后第二次「配置模板体」估幅（上次估 150 行、实 324 行），所以这次先数模板再估散文。

> **`2.5` 篇幅事后校准（2026-09-13，写后）**：事前估 15,000、实写 **19,352**（129%）。
> 两处低估：一是官方文档在这批机制上比素材走得更远（这一章的价值正在这里），
> 二是四张对照表（`.claude/rules` 与 Codex `.rules`、两份规则文件、三层机制、三级权限）各占掉半节。
> 按实测校准为 **19,500**；同时 Harness 的素材权重由「切一半」的 0.15 改成**按节实测的 0.07**
> （本章素材 4,594 → 3,888）。两个数字一升一降，2.5 的比值由 0.31 → **0.20**。
> 第 2 篇 92,500 → **97,000**，全书 605,000 → **609,500**；第 2 篇素材比值 0.56 → **0.53**。

### 第 2 篇 · AI 时代的开发方式（6 章 / 115,500 字，素材 0.36）

| 章 | 标题 | 计划 | 散文素材 | 比值 | 判定 | 状态 |
| --- | --- | ---: | ---: | ---: | --- | --- |
| 2.1 | 与 AI 协作编程：思维模型、能力边界与风险 | 11,000 | 3,090 | 0.28 | **严重不足** | ✅ 10,769 字（达成 98%） |
| 2.2 | Claude Code 实战：从对话到工程化 | 26,000 | 12,272 | 0.47 | 偏薄 | ✅ 24,621 字（达成 95%，含回补） |
| 2.3 | Codex 实战：云端与本地的工作流 | 17,500 | 10,248 | 0.59 | 偏薄 | ✅ 16,607 字（达成 95%，含回补） |
| 2.4 | AI 编辑器与 Vibe Coding 全栈开发 | 17,000 | 6,195 | 0.36 | **严重不足** | ✅ 17,106 字（达成 101%，含回补） |
| 2.5 | 上下文工程：规则文件、Skills、Hooks 与 MCP | 19,500 | 3,888 | 0.20 | **严重不足** | ✅ 18,662 字（达成 96%） |
| 2.6 | AI Harness：把 AI 编程接入工程流水线 | 24,500 | 6,160 | 0.25 | **严重不足** | ✅ 23,885 字（达成 97%） |

> **本轮修正（2026-09-13，开写 2.6 前量权重）**：给 2.6 量素材权重时，工具的
> 「同一份素材被多章重复计入」检查报出 Harness 三章合计 **1.28 > 1.0**，倒推出
> **2.1 的 0.7 从未与用到的节对过**——它说的是「整份文件的三分之二」，而 2.1 实际只用到
> §1 前言与词源、§2 HarnessAgent、§3 三层演进、§4 五个硬问题、§5 五阶段、§6 组件概览
> ＝ **3,051 汉字 → 0.35**。改后 2.1 由「偏薄」落到**严重不足（0.28）**，
> 与台账自己的说法一致：「素材只覆盖思维模型一条主线，能力边界与风险两条必须按官方出处原创」。
> 2.6 的三份权重也一并按节实测：Harness **0.54**（§6.1/6.4–6.9、§7–9、§12–25＝4,776）、
> Claude **0.06**（§15 ＋ §16.1–16.2＝878）、Codex **0.04**（§15.1–15.3＝460），合计散文素材 **6,160**。
> （Harness 那一项在反向核对时由 0.51 提到 0.54：§22「对产品经理的意义」起初被标成
> 「不在本章范围」，但 2.6.2 确实用了它。）
> 顺带修掉 0.7% 的口径不一致：三章切片相加曾到 1.007，因为 2.2 的余数按含文件头的分母算
> （14,882），而报表分母是 14,786；按同一口径重算 2.2 = **0.83**。
> 篇幅：2.6 由 6,000 提到 **18,000**（事前估，按 8.7 式子：11 小节 × 约 900
> ＋ 脚本/模板/配置约 340 行 × 15 ＋ 固定段约 1,500 ≈ 16,600，取 18,000）。
> 第 2 篇 97,000 → **109,000**，全书 609,500 → **621,500**；第 2 篇素材比值 0.53 → **0.47**。
> 这是同一类错的**第三次**（前两次：2.4 的 4,076、2.5 的 0.15），所以量法已写进 [`STYLE.md`](STYLE.md) 8.5。

> **`2.6` 篇幅事后校准（2026-09-13，写后）**：事前估 18,000、实写 **25,115**（140%）。
> 这是**第八次低估**，两个错向各又长了一层：
> ① 代码行项估 340、实际 **584 行**（占实测 35%）。多出来的这一章特有的东西是
> **不是散文的那些行**：console 围栏 82 行、JSON 42 行、YAML 29 行（实跑输出与配置），
> 规则与模板文件（`text`/`markdown`）262 行，四项合计 415 行＝**71%**；
> 真正自己写的 Python 只有 157 行。这与 2.4 的「输入夹具也是正文」同族：
> 只算了「自己写的代码」，漏了「跑出来的东西」和「贴在正文里的配置」。
> ② 散文项估约 10,000、实际 **15,361 汉字**（剔代码块）。这一章有 **27 张表、134 个数据行**，
> 表格行本身占 3,158 汉字，其余 12,203 汉字是 11 小节的讲解——平均每小节 **1,110 汉字**、
> 平均每张表带 **450 汉字**的配套说明，而「小节数 × 900」只给出 9,900。
> 对以概念对照为主体的章，**表格（及其讲解）比小节数更能预测篇幅**（估算式第三项是主项而非补丁）。
> 按实测校准为 **24,500**，2.6 比值 0.33 → **0.25**。
> 第 2 篇 109,000 → **115,500**，全书 621,500 → **628,000**；
> 分母变大后，第 2 篇的素材比值由 0.47 → **0.45**，判定由「偏薄」落到**严重不足**。
> 这不是坏消息，而是把话说清：这一篇的正文比素材多得多，因为真正难的部分
> （权限机制、钩子契约、CI 门禁、评测）官方文档才是唯一可靠的来源。

> 这是素材质量最好的一篇：Claude Code 素材有 468 个结构化条目，密度高、可塑性好。
> `2.4` 已写完：那份原档以截图为主，示例确实全部重写；它的技术结论主要来自各家官方文档，
> 2.4.5 的七个数字换成了**一手报告**（Veracode / CodeRabbit / GitClear，出处见 [`REFERENCES.md`](REFERENCES.md)）。

### 第 3 篇 · 大模型与 Agent 原理（9 章 / 67,000 字，素材 0.09 — **以原创为主**）

| 章 | 标题 | 计划 | 散文素材 | 比值 | 判定 | 状态 |
| --- | --- | ---: | ---: | ---: | --- | --- |
| 3.1 | 大模型基础：从 Transformer 到 Token | 10,000 | 0 | 0.00 | 题库驱动 | ✅ 9,642 字（达成 96%，含题库 9,539 字考点索引） |
| 3.2 | 提示工程与链式推理 | 12,000 | 306 | 0.03 | 严重不足 | ✅ 15,116 字（达成 126%，含题库 20,069 字考点索引） |
| 3.3 | Agent 是什么：LLM + 规划 + 工具 + 记忆 | 6,000 | 1,906 | 0.32 | 严重不足 | ⬜ |
| 3.4 | 规划与决策策略 | 7,000 | 386 | 0.06 | 严重不足 | ⬜ |
| 3.5 | 工具调用与函数编排 | 7,000 | 1,293 | 0.18 | 严重不足 | ⬜ |
| 3.6 | 记忆与上下文管理机制 | 6,000 | 842 | 0.14 | 严重不足 | ⬜ |
| 3.7 | 多 Agent 协作框架设计 | 6,000 | 966 | 0.16 | 严重不足 | ⬜ |
| 3.8 | 安全、对齐与防护 | 6,000 | 0 | 0.00 | 题库驱动 | ⬜ |
| 3.9 | 可观测性与评估 | 7,000 | 0 | 0.00 | 题库驱动 | ⬜ |

> **再再修正（2026-09-13：散文笔记的权重原本也是估的）**：那份笔记全文 12,852 汉字，
> 旧值按 0.15/0.45/0.10/0.20/0.10 分给 3.2–3.6（和恰好 1.00，看上去很「干净」），
> 但**每一个值都对不上**。开写 3.3 前按它自己的节标题逐节量，结果如上表：
> 3.3 由 5,748 降到 **1,906**、3.2 由 1,916 降到 **306**，而 `3.7` 原本被当成纯题库章，
> 实际上它的笔记素材有 **966**。第 3 篇散文池 12,773 → **5,700**，比值 0.19 → **0.09**；
> 题库驱动章由 4 章减为 **3 章**（3.1/3.8/3.9），需原创补写的章 57 → **58**。
> 逐节之和与文件实测逐个相等，已分配 5,699 与未分配的 7,153 都逐项记在
> `tools/audit_coverage.py` 的注释里。
>
> **这一篇没有哪一章算「素材够用」**：最高的是 `3.3`（0.32），因为那份笔记是**代码与
> 提纲为主**（汉字只 1,906，另有大量代码不参与散文计量）；`3.4` 最低（0.06）。
> 九章都须原创，且**不能引用题库答案**——1038 道题的答案是通用套话。
>
> **本轮修正（2026-09-13：题库权重改为按题库自己的分章实测）**：旧值给每章一律
> `0.05`，而 1038 道题自带九个分章，体量差到 1.7 倍（最小 28,778、最大 50,032 汉字），
> 一律 0.05 等于把「题多题少」抹平。实测口径：权重 ＝ 分章汉字 ÷ 全题库 352,616。
> 同时发现 **`3.1` 并非「素材零覆盖」**——题库的「大语言模型原理与应用（151道）」
> 就是它的地盘，其中注意力与结构 3,399 ＋ 训练范式 2,136 ＋ Scaling Law 2,647
> ＋ 分词四题 1,357 ＝ **9,539**，已计入（判定由「无素材」→「题库驱动」）。
> 第 3 篇题库池 123,415 → 288,276。
>
> **再修正（同日傍晚：分章整块 → 按考点分组）**：上一步仍把「一个分章整块归一章」
> 当作默认，而**题库没有哪个分章是整章只归一章的**。最突出的是
> 「提示工程与链式推理（112道）」：整章 38,156 汉字原先全记在 3.2 名下，实测只有
> 15,430 属它，其余按题拆给 3.1（3,083）｜3.4（2,170）｜3.5（2,519）｜3.6（2,873）
> ｜3.7（1,088）｜3.8（2,463）｜3.9（3,121），另有 5,409 有意不分配（RAG 架构待第 5 篇、
> Token 成本与路由待第 6 篇）。**增量之和 42,795 恰好等于旧值**——只换归属，没多没少。
> 第 3 篇题库池 288,276 → **282,867**（五章归还给别的篇的那部分不再计在本篇）；
> 有意不分配的合计 24,983 已逐项登记在 `tools/audit_coverage.py` 的注释里，
> 不让它静静消失。
> 三个选项见第六节决策项 2。

### 第 4 篇 · AI 应用开发框架（5 章 / 33,000 字，素材 0.14 — **缺口最严重**）

| 章 | 标题 | 计划 | 散文素材 | 比值 | 判定 |
| --- | --- | ---: | ---: | ---: | --- |
| 4.1 | LangChain 核心抽象与第一个应用 | 7,000 | 230 | 0.03 | 严重不足 |
| 4.2 | 提示模板、输出解析与 LCEL 编排 | 7,000 | 330 | 0.05 | 严重不足 |
| 4.3 | 工具与 Agent：让模型动手干活 | 7,000 | 2,287 | 0.33 | 严重不足 |
| 4.4 | LangGraph：StateGraph 与状态编排 | 7,000 | 1,200 | 0.17 | 严重不足 |
| 4.5 | 持久化、多智能体编排与实战 | 5,000 | 480 | 0.10 | 严重不足 |

> 两本素材合计 4,680 汉字，且**没有一个规范的代码块**（原 PDF 把代码排成了图片）。
> 这一篇实质上是原创写作，必须以官方文档为准。
> `4.1`/`4.2` 的比重看似比旧值更低，是因为旧值把整份文件切三块估的（三块之和 1.3），
> 而实测下来：§1–3 核心概念＝ 230 汉字、§4 Prompt 模板＝ 330、§5–8 工具与 Agent
> 与完整项目＝ 1,567。**LCEL 与输出解析素材里根本没有**，只能原创。

### 第 5 篇 · RAG 与生产级系统（9 章 / 63,000 字，素材 0.83）

| 章 | 标题 | 计划 | 散文素材 | 比值 | 判定 |
| --- | --- | ---: | ---: | ---: | --- |
| 5.1 | RAG 全景：为什么需要检索增强 | 6,000 | 3,962 | 0.66 | 偏薄 |
| 5.2 | 文档解析与切分策略 | 7,000 | 6,620 | 0.95 | 够用 |
| 5.3 | 向量化与向量数据库 | 7,000 | 648 | 0.09 | **严重不足** |
| 5.4 | 检索增强：混合检索、重排与查询改写 | 8,000 | 18,695 | 2.34 | 充裕 |
| 5.5 | 生成、引用与幻觉抑制 | 6,000 | 5,085 | 0.85 | 够用 |
| 5.6 | RAG 评估：指标、实验与迭代 | 7,000 | 7,574 | 1.08 | 够用 |
| 5.7 | 生产级工程：缓存、并发、版本与成本 | 8,000 | 6,273 | 0.78 | **偏薄** |
| 5.8 | 综合项目：企业级多格式知识库问答系统 | 10,000 | 3,312 | 0.33 | **严重不足** |
| 5.9 | 项目复盘：从能跑到能讲 | 4,000 | 0 | 0.00 | 无素材 |

> **按节实测后的真实分布是「极度集中在检索」，不是「整篇充裕」**：两份 RAG 素材的散文
> 有一多半落在一个地方——`5.4` 拿 **18,695**（查询改写、融合、重排、路由、多表示索引 /
> RAPTOR / ColBERT）；而基础环节几乎没素材：**《生产级 RAG》第 3 章（Milvus 实战）
> 只有 197 汉字**（正文以代码与表格为主），`5.3` 因此落到 **0.09**——全篇最低。
>
> 所以这一篇的写法要分开定：
>
> | 章 | 素材状况 | 写法 |
> | --- | --- | --- |
> | 5.4 | 充裕（2.34） | 以素材整理改写为主，但示例迁到官方文档的当前 API |
> | 5.2 / 5.5 / 5.6 | 够用（0.85–1.08） | 素材打底＋按官方文档补齐缺口 |
> | 5.1 / 5.7 | 偏薄（0.66 / 0.78） | 素材打底，缺口按官方文档原创（5.7 的成本与缓存素材里没有） |
> | 5.3 / 5.8 | **严重不足（0.09 / 0.33）** | 散文原创：Milvus / BGE / RAGAS 官方文档；自带项目（第 10 章 2,763 汉字）以代码为主体，须重写并补讲 |
>
> `5.6` 的素材里有 **2,265 汉字是文末重复的副本**（评估 / 实战 / 进阶三块各出现两次，
> 逐字比对 40 字窗口 22/22 重合），已剔除，只算首次出现。

> **本轮修正（2026-09-13：第 4、5 篇的素材权重全部改为按节实测）**：
> `audit_coverage.py --check` 一直报着三处「同一份素材被多章重复计入」（各章权重之和 > 1.0），
> 它们与 `2.4`/`2.5`/`2.6` 是同一个错——权重是「把整份文件切一块」估的，没人拿它和
> 实际用到的节对过。现在三份都逐节量过了（逐节汉字量写进 `tools/audit_coverage.py` 的注释，可复核）：
>
> | 素材 | 汉字 | 旧权重之和 | 实测定下的分布 |
> | --- | ---: | ---: | --- |
> | `LangChain从入门到实战.md` | 2,279 | 1.30（4.1–4.3） | 4.1 §1–3 ＝ 230 ｜ 4.2 §4 ＝ 330 ｜ 4.3 §5–8 ＝ 1,567 |
> | `RAG技术从入门到深入.md` | 25,444 | 1.05（5.1–5.5） | 主体在 5.4（14,053）；5.1/5.2/5.3 各仅数百字 |
> | `生产级RAG系统构建实战.md` | 32,068 | 1.35（5.2–5.8） | 解析 5.2 ＝ 5,988 ｜ 评估 5.6 ＝ 5,309 ｜ 运维 5.7 ＝ 6,273；第 3 章 Milvus 仅 197 |
>
> **权重的小数位是反算出来的**：取到「能让 `int(素材总量 × 权重)`（工具用 `int()`
> 截断）正好等于实测值」的那一位。于是**本表里的素材量与上表的实测数逐个相等**，
> 可以相乘反查；旧值 0.10/0.14/0.69 会把 230 算成 227、330 算成 319，同一件事在
> 注释与表格里写成两个数。反算后的权重（含 5 位小数者）记在
> `tools/audit_coverage.py` 的注释里。
>
> 顺带修掉一处**篇级口径**的毛病：分篇的「素材池」原是把引用到的**整份文件**相加，
> 于是归给别章的部分也算进了本篇（第 5 篇因此显示 0.94）。改为「各章计权相加，
> 每份素材在一篇里最多算 1.0」后，第 1/2/5 篇由 0.64 / 0.45 / 0.94 落到
> **0.61 / 0.36 / 0.83**，题库池由 995,562 落到 **201,627**（后者原是把整份题库充数）。
> 两个变化都让数字变小——**不是坏消息，而是把话说清**：第 4、5 篇要以官方文档为主来写。
> 需原创补写的章 55 → **57**，风险章节 34 → **36**（多出的一章是 `5.7`：6,273/8,000
> ＝ 0.78，从「够用」落到「偏薄」——旧值 0.80 正是权重估高带来的假边界）。
> 这是同一类错的**第四、五次**（前三次：2.4 的 4,076、2.5 的 0.15、2.6 的 0.15），
> 所以量法已写进 [`STYLE.md`](STYLE.md) 8.5，且工具的重复计入检查会在闸门里一直看着。
>
> 闸门也跟着补了一层：`--check` 原先只核**篇级小计**，章表里的逐章数字没人管，
> 而这两处是各自手写的——本轮 4.1 就在表里写 227、注释里写 230，两边都「看着对」。
> 现在逐章核「计划 / 素材 / 比值」三格（带夹具），所以上面这张表里的每个数字
> 都被锁住了。

### 第 6 篇 · 模型接入与成本工程（4 章 / 28,000 字，**素材为零，纯原创**）

| 章 | 标题 | 计划 | 官方出处 |
| --- | --- | ---: | --- |
| 6.1 | 模型选型：能力、价格与上下文窗口的三角权衡 | 7,000 | Claude Models · OpenAI API docs |
| 6.2 | 多厂商接入：Messages、Responses 与 OpenAI 兼容层 | 7,000 | platform.claude.com · developers.openai.com |
| 6.3 | 模型网关：统一接口、路由、重试与降级 | 7,000 | LiteLLM · vLLM · Ollama |
| 6.4 | 成本与性能工程：Token 计费、缓存与模型分流 | 7,000 | 官方定价页 · OpenAI 数据控制 |

> 这一篇回答「AI 应用到底怎么把模型接进自己的服务」。素材里一句相关的话都没有，
> 每章必须先立好 [`REFERENCES.md`](REFERENCES.md) 的出处再动笔。

### 第 7 篇 · AI 应用工程化（5 章 / 33,000 字，**素材为零，纯原创**）

| 章 | 标题 | 计划 | 官方出处 |
| --- | --- | ---: | --- |
| 7.1 | 评测流水线：数据集、指标与 CI 回归 | 7,000 | Ragas 官方文档 |
| 7.2 | 可观测性：链路追踪、指标与成本看板 | 7,000 | Langfuse · OpenTelemetry GenAI |
| 7.3 | Prompt 与配置版本化：灰度发布与 A/B 实验 | 6,000 | Langfuse · Git 工作流 |
| 7.4 | 安全落地：提示注入、越狱与工具权限治理 | 7,000 | OWASP LLM Top 10 · MCP 授权 · NIST AI RMF |
| 7.5 | 数据合规与脱敏：留存、隐私与审计 | 6,000 | OpenAI 数据控制 · NIST AI RMF |

> 与第 3 篇的分工：**第 3 篇讲原理（为什么危险、为什么要评估），本篇讲落地（怎么做）**。

### 第 8 篇 · 交付与产品化（5 章 / 33,000 字，**素材为零，纯原创**）

| 章 | 标题 | 计划 | 官方出处 |
| --- | --- | ---: | --- |
| 8.1 | 容器化部署：Docker、镜像瘦身与 Compose | 7,000 | Docker 官方文档 |
| 8.2 | CI/CD 与发布：自动化测试、构建与灰度上线 | 7,000 | GitHub Actions 文档 |
| 8.3 | 前端流式交互：SSE 与 AI SDK 对话式 UI | 7,000 | Vercel AI SDK · Next.js |
| 8.4 | 多租户、配额与计费 | 6,000 | FastAPI Security · Milvus 多租户 |
| 8.5 | 线上运维：SLO、告警、故障演练与容量规划 | 6,000 | OpenTelemetry 官方文档 |

> `1.1` 只讲本地环境、`1.14` 只讲单元测试，都是为了给这一篇留位置，避免重复。

### 第 10 篇 · 求职冲刺（7 章 / 67,000 字，题库驱动）

| 章 | 标题 | 计划 | 素材来源 | 素材状态 |
| --- | --- | ---: | --- | --- |
| 10.1 | 简历：AI 应用开发岗怎么写才有回音 | 6,000 | — | 无素材 |
| 10.2 | 项目讲述法：STAR、亮点提炼与追问防御 | 6,000 | — | 无素材 |
| 10.3 | **AI 系统设计面试** | 8,000 | — | 无素材（扩展新增） |
| 10.4 | AI Agent 面试考点手册 | 15,000 | AI Agent 题库（1038 道） | 题库驱动，**仅作考点索引** |
| 10.5 | 后端高频考点（Python / AI 岗位视角） | 15,000 | 后端题库（165 道） | 题库驱动，须换视角 |
| 10.6 | 移动端考点：Android 与 Flutter-Dart | 12,000 | 移动端两套题库（1850 道） | 题库驱动，须逐题筛选 |
| 10.7 | 模拟面试与复盘清单 | 5,000 | — | 无素材 |

> 题库**不整本搬运**。做法是：高频考点精讲 + 其余以「速查问答表」收敛，
> 并把分散考点在第 3 篇各章「面试视角」中提前消化。
> `10.3` 是扩展新增章：知识点问答答得再好，也替代不了「给你一个需求，你给方案」。

### 第 9 篇 · 进阶方向（预留，暂不启用）

按 [`GAPS.md`](GAPS.md) 方案 B，共 4 章（本地推理与量化、微调 LoRA/SFT/DPO、
多模态与语音、Computer Use Agent）。**编号预留给它，但当前不排入写作批次**——
先把 P0/P1 交付完，作品集阶段再按需启用。

---

## 四、体系缺口与扩展提案

对照“一个能拿到 AI 应用开发岗 Offer 的人应该会什么”逐项检查后，发现原定 7 篇 53 章
**少了一整块**：从「能做出 demo」到「能交付一个有 SLO 的产品」之间的工程化与交付环节。
逐项对照表与优先级清单见 [`GAPS.md`](GAPS.md)。

### 4.1 缺口集中在三处

| 优先级 | 缺口 | 现状 |
| --- | --- | --- |
| **P0** | 模型接入与多厂商 API | 完全没讲“怎么调模型” |
| **P0** | 部署与交付（容器化、CI/CD、运维） | 学完只会在本地跑 `uvicorn` |
| **P0** | AI 系统设计面试 | 题库只覆盖知识点，不覆盖方案设计 |
| P1 | 评测流水线、可观测性、安全、成本工程、前端流式交互、数据合规 | 只有理论或完全没有 |
| P2 | 本地推理、微调、多模态与语音、多租户计费、GraphRAG | 差异化加分项 |

### 4.2 已采纳：方案 A（新增 3 篇 14 章）

扩展篇插在「RAG 与生产级系统」之后、「求职冲刺」之前——先会做系统，
再学怎么交付出去，最后带着可演示的项目去面试。求职冲刺因编号后移为**第 10 篇**，
第 9 篇预留给「进阶方向」（方案 B 的内容），当前不启用。

| 新篇 | 章数 | 字数 | 内容 |
| --- | ---: | ---: | --- |
| 第 6 篇 · 模型接入与成本工程 | 4 | 28,000 | 模型选型、多厂商接入、网关与路由降级、成本与性能 |
| 第 7 篇 · AI 应用工程化 | 5 | 33,000 | 评测流水线、可观测性、Prompt 版本化与灰度、安全、合规 |
| 第 8 篇 · 交付与产品化 | 5 | 33,000 | 容器化部署、CI/CD 与发布、前端流式交互、多租户与计费、线上运维 |
| 第 10 篇新增 1 章 | 1 | 8,000 | AI 系统设计面试（10.3） |

> 扩展后 **10 篇 68 章 / 约 628,000 字**（扩展前 7 篇 53 章 / 358,000 字）。
> 另有两种范围可选：[`GAPS.md`](GAPS.md) 第四节的方案 B（再加进阶篇 4 章，共 72 章）
> 与方案 C（只补 P0，共 63 章）。

### 4.3 工作量的实话

新增篇章的**素材覆盖度全部为 0**——素材里一句相关的话都没有，只能依据官方文档原创撰写。
按每章约 7,000 字计，方案 A 新增约 **102,000 字纯原创**（含系统设计章 8,000 字）。
这意味着工作量比「整理素材」大得多，因此把它们整体排进**批次四**，
放在第 0–5 篇之后（那几篇或多或少还有素材可用，实测在 0.14–0.83 之间）。

### 4.4 对既有篇章的连带调整

| 既有位置 | 调整 |
| --- | --- |
| 1.1 环境搭建 | 只讲本地环境，Docker 统一归 8.1，避免重复 |
| 1.14 测试 | 保留单元测试；流水线与 CD 归 8.2 |
| 3.8 安全、3.9 可观测性 | **第 3 篇讲原理（为什么），第 7 篇讲落地（怎么做）**，两处互相交叉引用 |
| 5.6 RAG 评估 | 5.6 讲指标与实验方法，7.1 讲怎么接进 CI |
| 5.7 生产级工程 | 收敛到「RAG 服务本身」，成本归 6.4、运维归 8.5 |
| 第 4 篇 框架 | 全篇按 **LangChain v1** 重写（素材是旧版写法） |
| 求职篇 | 章号变为第 10 篇，新增 10.3 系统设计章，其余顺延 |

---

## 五、写作批次与里程碑

先做还有素材可用的篇（第 0–2、5 篇），把原创量最大的第 3、4 篇放在后面——
这样你可以**用前面几批的实际成稿判断质量**，再来决定第 3、4 篇是原创补写还是压缩。
注意「有素材」不等于「轻松」：按节实测后，第 2 篇只有 0.36，实际写起来以原创为主
（2.5、2.6 均已如此）。

| 批次 | 范围 | 章数 | 字数 | 工作量特征 | 里程碑 |
| --- | --- | ---: | ---: | --- | --- |
| **批次一** | 第 0 篇 + 第 1 篇 | 18 | 196,500 | 素材充裕，改写为主 | 能独立写出并测试一个 FastAPI 后端服务 |
| **批次二** | 第 2 篇 + 第 5 篇 | 15 | 178,500 | 素材相对多（0.36 / 0.83），但第 2 篇实际以原创为主 | 会用 AI 工具开发，且能构建并评估一个 RAG 系统 |
| **批次三** | 第 3 篇 + 第 4 篇 | 14 | 100,000 | **几乎全是原创**，最耗时 | 能讲清 Agent 原理，能用 LangChain/LangGraph 搭出智能体 |
| **批次四** | 第 6 篇 + 第 7 篇 + 第 8 篇 | 14 | 94,000 | **零素材，纯原创**，须逐条回到官方文档 | 能把系统部署上线，并接上评测、监控与安全 |
| **批次五** | 第 10 篇 + 全书统稿 | 7 | 67,000 | 题库筛选 + 综述 | 简历与面试闭环；术语表、索引、通读修订 |
| **合计** | | **68** | **636,000** | | |

每章执行节奏（防丢失闭环，规范见 [`STYLE.md`](STYLE.md) 第八节）：

1. 按覆盖度报告定位该章素材段落 → 2. 通读素材，把知识点逐条写进
   [`LEDGER.md`](LEDGER.md)（状态先标 `⬜`） → 3. 按十段式写正文
→ 4. 跑通全部代码示例 → 5. 回填「面试视角」「延伸阅读」与台账状态（`✅`/`🔀`/`⏭`）
→ 6. 跑 `python tools/lint_book.py --only <章号>`，零错误才算定稿。

---

## 六、风险清单

| 风险 | 说明 | 应对 |
| --- | --- | --- |
| 🔴 **第 4 篇素材近乎为零** | LangChain + LangGraph 合计 4,680 汉字，无规范代码块 | 压缩到 5 章；以官方文档为准原创；或直接砍掉第 4 篇 |
| 🔴 **第 3 篇素材仅 12,774 汉字** | 9 章里只有 1 章（3.3）有实质素材，且题库答案不可信 | 原创+权威资料；或压缩章数；见决策项 2 |
| 🔴 **素材在关键处已经过时** | LangChain 已发布 **v1**（旧命名空间移入 `langchain-classic`）；OpenAI **Assistants API 已于 2026-08-26 关停**；`Claude Code SDK` 已更名为 **Claude Agent SDK**；Claude 文档换域；Pydantic 文档换域；MCP 规范按日期版本化 | 正文一律回到官方文档核对；[`REFERENCES.md`](REFERENCES.md) 第二节列出全部变动，并用 `tools/check_refs.py` 校验链接 |
| 🟠 **体系少一整块（工程化与交付）** | 模型接入、成本、评测流水线、安全落地、部署与运维全部缺位 | 已采纳方案 A：新增第 6–8 篇与系统设计章 |
| 🟠 **新增 14 章零素材** | 第 6–8 篇素材覆盖度为 0，只能依官方文档原创（约 10.2 万字） | 排在批次四；每章先把 `REFERENCES.md` 的出处立好再动笔 |
| 🟠 **AI Agent 题库质量低** | 通用套话 + 专题漂移，不能当真题用 | 降级为考点索引，答案依权威资料重写 |
| 🟠 **后端题库是 Java 体系** | JVM/Spring/垃圾回收占比高，网络仅 0 次 | 10.5 整体换成 Python/AI 岗位视角，剔除 Java 专有内容 |
| 🟡 **Flutter 题库含模板废答案** | 至少 20 题零实质内容 | 10.6 逐题筛选；不达标者弃用 |
| 🟡 **1.10 Pydantic 素材仅 1,356 字** | 第 1 篇最薄 | 原创补 Pydantic v2 完整内容 |
| 🟡 **Vibe Coding 原档以截图为主** | 文字仅 34.7k 字符 | 只取结论，示例重写 |
| ⚠️ **版权** | 素材多为第三方整理，含"仅供个人学习"声明 | 仅个人学习使用；出版前须确认授权并实质性重写 |

---

## 七、待你决策的事项

1. **书名与主线项目**：是否采用《AI 应用开发就业实战：从语法地基到 Offer》？
   贯穿项目是否沿用「知舟」？
2. **第 3、4 篇怎么办？**（最关键的决策）
   - **A. 接受原创**（推荐）：按现计划 14 章原创撰写，我依据权威资料（论文与官方文档）
     写，但正文不再是"整理你给的资料"；
   - **B. 压缩**：第 3 篇 9→6 章、第 4 篇并成 3 章，总章数降到 48 章；
   - **C. 补充素材**：你提供 LangChain/LangGraph 官方文档、ReAct 等论文或更好的笔记，
     我重新审计后再定；
   - **D. 砍掉第 4 篇**：只讲原理（第 3 篇）与系统（第 5 篇），框架细节交给官方文档。
3. **第 2 篇位置**：放在第 2 篇（紧随地基，工具提升后续效率，**当前方案**），
   还是移到最后（先打牢基础再谈工具）？
4. **移动端内容**：Android + Flutter 两套题库（1850 道，质量中/偏低）是否纳入？
   纳入则第 10 篇 7 章；不纳入则减为 6 章，教材聚焦 AI 应用 / 后端方向。
5. **批次顺序**：是否按上表先做「批次一（0+1 篇）」，并在看到成稿后再推进？
6. ~~是否先写一章样章？~~ **已定：不单独写样章，直接从批次一（第 0、1 篇）开始**
   （2026-09-11）。第 0 篇先交付，可据此确认文风；若要调整，改 [`STYLE.md`](STYLE.md)
   后再回改已完成章节。
7. **`raw/` 是否纳入 git**：当前经 `.gitignore` 排除（295MB，含 129MB 单文件 PDF）。
   若要版本管理原始资料，需改用 Git LFS。
8. **附加产出**：是否需要 PDF/EPUB 导出、MkDocs 在线站点、术语中英对照表、全书索引？
9. ~~是否扩展体系？~~ **已定：采纳方案 A**（2026-09-11）。新增第 6–8 篇共 14 章，
   加第 10 篇「AI 系统设计」1 章，全书 **10 篇 68 章 / 约 628,000 字**。
   另两种范围（B 完整 72 章、C 最小 63 章）见 [`GAPS.md`](GAPS.md) 第四节；
   日后想扩或缩，只需重排第 6 篇之后的章号。
10. **【新增】过时内容怎么处理？** 素材里的旧写法（LangChain 旧版链、Assistants API）
    建议不作为正文依据，但可以在「常见坑」里当反例讲——
    这反而是很好的教学点。是否保留这类“旧写法对照”？
11. **【待办】第 4、5 篇的素材权重之和超 1.0**（`LangChain从入门到实战.md` 1.30、
    `RAG技术从入门到深入.md` 1.05、`生产级RAG系统构建实战.md` 1.35）。与第 2 篇
    同类的重复计入，但第 4、5 篇还没开写，两章的节级边界需要先通读那三份文件才能量。
    影响：4.1–4.5 与 5.1–5.8 的素材量与判定偏高，**开工前必须先改**。
    `python tools/audit_coverage.py` 会在开头报出这份名单。

---

## 八、进度看板

| 篇 | 章数 | 计划字数 | 素材比值 | 已完成 | 状态 |
| --- | ---: | ---: | ---: | ---: | --- |
| 0 导论 | 3 | 12,000 | — | 3 | ✅ 已完成 |
| 1 编程地基 | 15 | 184,500 | 0.61 | 15 | ✅ 已完成（批次一） |
| 2 AI 时代的开发方式 | 6 | 115,500 | 0.36 | 6 | ✅ 已完成（批次二） |
| 3 大模型与 Agent 原理 | 9 | 67,000 | 0.09 | 2 | 🟡 进行中（批次三） |
| 4 AI 应用开发框架 | 5 | 33,000 | 0.14 | 0 | ⬜ 未开始（批次三） |
| 5 RAG 与生产级系统 | 9 | 63,000 | 0.83 | 0 | ⬜ 未开始（批次二） |
| 6 模型接入与成本工程 | 4 | 28,000 | 0.00 | 0 | ⬜ 未开始（批次四） |
| 7 AI 应用工程化 | 5 | 33,000 | 0.00 | 0 | ⬜ 未开始（批次四） |
| 8 交付与产品化 | 5 | 33,000 | 0.00 | 0 | ⬜ 未开始（批次四） |
| 10 求职冲刺 | 7 | 67,000 | 题库驱动 | 0 | ⬜ 未开始（批次五） |
| **合计** | **68** | **636,000** | | **26** | 🟡 批次三进行中 |

前置工作进度：

| 事项 | 状态 |
| --- | --- |
| 资料归档到 `raw/`（295MB，未删改） | ✅ 已完成 |
| 文本素材库生成（`sources/`，216 个文件） | ✅ 已完成 |
| git 初始化与两次提交 | ✅ 已完成 |
| 写作体例与文风规范（[`STYLE.md`](STYLE.md)） | ✅ 已完成 |
| 素材覆盖度审计（[`COVERAGE.md`](COVERAGE.md)） | ✅ 已完成 |
| 官方文档出处与链接校验（[`REFERENCES.md`](REFERENCES.md)，82 链接全部有效） | ✅ 已完成 |
| 体系缺口审计（[`GAPS.md`](GAPS.md)） | ✅ 已完成 |
| 章节规划与篇幅校准（本文档） | ✅ 已定稿（采纳方案 A） |
| 第 0 篇正文（0.1–0.3） | ✅ 已完成 |
